/**
 * BACKWARD COMPATIBILITY SHIM
 * Canonical code now lives in server/ai-types/invoke.ts
 */
export {
  invokeCatalogEntry,
  resolveInvokeTarget,
  type CatalogInvokeInput,
  type CatalogInvokeResolution,
} from "../ai-types/invoke";
