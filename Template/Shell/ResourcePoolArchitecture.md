# Resource Pool Architecture

Defines abstract pools:

- CPU Pool
- GPU Pool
- Storage Pool
- Budget Pool

Each Resource Tier maps to pool allocations.

Enforcement occurs:
- API layer
- Agent execution layer
- Job queue layer
