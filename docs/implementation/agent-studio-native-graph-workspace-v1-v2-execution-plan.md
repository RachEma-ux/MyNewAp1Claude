# Agent Studio Native Graph Workspace — V1 / V1.5 / V2 Successor Execution Plan

**Status:** Draft (2026-05-13). Approved scope; implementation tracks ready to start.
**Predecessor:** `agent-studio-native-graph-workspace-execution-plan.md` (MVP 0–4, closed)
**Predecessor closure evidence:** `agent-studio-native-graph-workspace-status-check-2026-05-13.md`
**Owner agents (per AGENTS.md):** Planner (this doc) → Builder / Reviewer / Tester / Governance per phase
**Scope boundary:** This plan covers everything the MVP 0–4 plan explicitly deferred to V1+ — including Phase 13.5 Agentic GraphRAG, the standalone scopes of Phase 15–19, Track J production hardening, multi-region, CRDT, and offline-first.

---

## 0. Why this plan exists

The MVP 0–4 plan capped its scope at G10 closure (Graph Agent Lite live, golden questions passing). Everything beyond that — adaptive planning, full collaborative editing, multi-region deployment, plugin/extension framework, production hardening — was **explicitly deferred** to a successor plan. PR #723's closure addendum, this plan's predecessor, classified those items as *Deferred by plan, not failed* and named this doc as the return path.

Each phase below has:
- **Status today** (in repo)
- **Acceptance criteria** (what closes the phase)
- **First implementation PR** (concrete, actionable)
- **Dependencies / order**
- **Governance / hard-rule constraints**

The same Planner → Builder → Reviewer → Tester → Governance order from AGENTS.md applies.

---

## 1. Critical path

```
V1.0 — Production hardening + Agentic GraphRAG slice
    │
    ├─ Phase J-1   Production hardening minimum: ops runbooks + alerts
    ├─ Phase 13.5  Agentic GraphRAG — planner contract + bounded loop
    └─ Phase 19-α  Sync/publish strategy implementation slice 1
    ↓
V1.5 — Extension surface + standalone editor scopes
    │
    ├─ Phase 15-α  Templates / attachments standalone management
    ├─ Phase 16-α  Saved views extended scope
    ├─ Phase 17-α  Canvas data model + minimal UI
    └─ Phase 18-α  Extension framework registry + governance
    ↓
V2.0 — Multi-region + real-time collaboration
    │
    ├─ Phase MR-1  Multi-region graph deployment
    ├─ Phase CRDT  Real-time collaborative editing
    └─ Phase OL-1  Offline-first / local-first mode
    ↓
V2.x — Track J production hardening full surface (Aura / CRDT / multi-region together)
```

The path **does not** require linear execution — V1 phases can ship in parallel; V1.5 phases can begin once their V1 prerequisites land; V2.0 phases gate on V1.5 + production telemetry.

---

## 2. Phase-by-phase plan

### Phase 13.5 — Agentic GraphRAG (V1.0) — **FULLY IMPLEMENTED on main** (2026-05-13)

**MVP 0–4 deferral rationale:** plan §9 — "deferred to V1. Phase 13 is 'Graph Agent Lite' intentionally — overbuilding adaptive planning in MVP risks the G9 boundary verification."

**Status (post-merge):**
- Graph Agent Lite (`server/agent-studio/services/graph-agent/`) ships both a fixed plan→retrieve→reason→answer pipeline AND a bounded adaptive loop via `runAgentic()`.
- Phase 13.5 trio merged: PR #731 (contract) + PR #732 `ffb4eba9` (engine wiring + `RoundRobinPlanner`) + PR #737 `a8f5c634` (`createModelDrivenPlanner` for LLM-emitted plans).
- 62/62 tests green on main (boundary 27 + model-planner 20 + engine-agentic 9 + engine 6).
- MCP dispatcher boundary + OpenRouter Model Access boundary + no-mutation property all source-scan tested under the agentic surface.
- See ADR `docs/architecture/agent-studio-agentic-graphrag.md` (Status: ACTIVE) for the truth-claim table.

**Acceptance criteria:**
1. Planner contract — explicit ADR + TS interface for `AgenticPlanner`:
   `plan(query, prevSteps) → { action: "retrieve" | "answer" | "stop", arguments: ... }`
2. Bounded adaptive retrieval loop — `services/graph-agent/agentic-loop.ts`:
   max-N-iterations bound (configurable; default 4), short-circuits when answer-confidence ≥ threshold or budget exhausted.
3. No direct tool execution from the planner — all tool calls still route through `dispatchMcpToolCall()`. Source-scan tested.
4. No direct graph mutation — same Phase 11.5 proposal/approval surface as Graph Agent Lite.
5. Full trace emission — every iteration writes a `graphAgentDecisionTrace` step row (stepKind: `agentic_iteration_N`).
6. Governance guardrails — `agentic_eligibility` flag on skill packs; only packs explicitly marked agentic-eligible can be used by the loop.
7. Property-based tests — extend `graph-permission-visibility-property.test.ts` to cover multi-iteration paths (no leak across iterations).
8. First PR ships only the planner contract + tests + ADR — no engine wiring yet. Engine integration follows in PR #2.

**First implementation PR (Phase 13.5 / PR #1):**
- New file: `docs/architecture/agent-studio-agentic-graphrag.md` (ADR with boundary table)
- New file: `server/agent-studio/services/graph-agent/agentic-planner-contract.ts`
- New file: `server/agent-studio/services/graph-agent/agentic-loop.ts` (no-op stub returning `action: "stop"` to keep boundaries verifiable)
- New file: `tests/agent-studio/agentic-planner-boundary.test.ts` — source-scan: no `neo4j-driver` import, no direct `dispatchMcpToolCall` call (must route through engine), no direct graph mutation, max-iterations guard exists.

**Dependencies:** MVP 4 G9 closure (done). Production-hardening Phase J-1 ops runbook is parallel.

**Governance:** Touch with Reviewer + Governance both. Adaptive loops historically slip past dispatcher/governance boundaries; the ADR + source-scan tests are non-negotiable.

---

### Phase 15 — Templates / Attachments standalone scope (V1.5)

**MVP 0–4 deferral rationale:** plan §9 — Phase 15 standalone scope was bundled into MVP 1 Phase 4 + 5; standalone management surfaces deferred.

**Status today:**
- Templates: `services/vault/markdown-import-export.ts` handles import/export. No dedicated template-management UI surface.
- Attachments: `services/vault/attachments.ts` handles attachment binding. No dedicated attachment library / quota / scrubbing surface.

**Acceptance criteria:**
1. Template management UI page — operator can create / edit / delete / browse vault templates (route `/agent-studio/templates`).
2. Template-instantiation tracking — `agsVaultTemplateInstantiations` table records which note was created from which template (for refactor-safety + audit).
3. Attachment library UI — operator can browse attachments across vault, filter by mime, drop unused.
4. Attachment quota enforcement — per-workspace MB cap; configurable; surfaced in `RetrofitPage` operator dashboard alongside retention crons.
5. Source-scan + unit tests for the new surfaces.

**First implementation PR (Phase 15-α):**
- New file: `client/src/modules/agent-studio/pages/TemplatesPage.tsx`
- New file: `client/src/modules/agent-studio/pages/AttachmentLibraryPage.tsx`
- New file: `drizzle/tables/agent-studio-vault-template-instantiations.ts`
- New file: `tests/agent-studio/vault-template-instantiation.test.ts`

**Dependencies:** MVP 1 (done).
**Governance:** Touch the Vault module manifest; ensure publication boundaries preserved.

---

### Phase 16 — Saved views extended scope (V1.5)

**Status today:**
- MVP minimal saved views exist (`services/vault/saved-views.ts`). Per-user, per-workspace, JSON definition.

**Acceptance criteria:**
1. Sharing model — saved views can be marked `workspace_shared` (visible to all members) vs `personal`.
2. Versioning — saved-view definitions are immutably versioned (same shape as `agsNoteVersions`).
3. UI surface — saved-views browser, edit / share / version-history.
4. Permission-aware materialization — shared saved views must respect viewer's permissions (re-runs the visibility filter per viewer, not snapshot of author's results).

**First implementation PR (Phase 16-α):**
- Extend `services/vault/saved-views.ts` with sharing + versioning columns
- Migration: `agsSavedViews` adds `visibility`, `version`, `parentSavedViewId`
- New file: `client/src/modules/agent-studio/components/SavedViewsBrowser.tsx`
- Property-based test: visibility filter is re-applied per viewer

**Dependencies:** MVP 1 (done). Permission filter pushdown (G8, done).

---

### Phase 17 — Canvas data model + minimal UI (V1.5)

**MVP 0–4 deferral rationale:** plan §9 — Phase 17 lands as ADR-only in MVP 0; full implementation deferred.

**Status today:** ADR exists at `docs/architecture/agent-studio-canvas-strategy.md`.

**Acceptance criteria:**
1. Canvas data model — `agsCanvases` + `agsCanvasNodes` + `agsCanvasEdges` tables (additive; no overlap with `agsGraphNodes`/`agsGraphEdges`).
2. Canvas → Vault projection — when a Canvas references a note, the relationship projects into the graph backend as a typed edge (`CANVAS_REFERENCES_NOTE`).
3. Minimal UI — operator can create / open / arrange a Canvas; nodes are notes + free-text + embedded queries.
4. No plugin framework yet — Canvas is a first-class module, not a plugin host. (Phase 18 wires plugins in.)
5. CAG/RAC awareness — Canvas content can flow into CAG blocks via the existing source-note reference path (Phase 10).

**First implementation PR (Phase 17-α):**
- New tables in `drizzle/tables/agent-studio-canvas.ts`
- New file: `server/agent-studio/services/canvas/manifest.ts` + `public-api.ts` + `router.ts`
- Frontend: `client/src/modules/agent-studio/pages/CanvasPage.tsx`
- Source-scan boundary tests: Canvas data goes through GraphRepository for relationship queries; no direct neo4j-driver imports.

**Dependencies:** Phase 7 (typed graph), Phase 12 (retrieval).

---

### Phase 18 — Extension framework (V1.5)

**MVP 0–4 deferral rationale:** plan §9 — Phase 18 lands as ADR-only in MVP 0; full implementation deferred.

**Status today:** ADR exists at `docs/architecture/agent-studio-extension-framework-strategy.md`.

**Acceptance criteria:**
1. Extension registry — `agsExtensions` table tracks installed extensions per workspace.
2. Extension manifest contract — TS interface in `services/extensions/contracts.ts` defining: identity, capability declarations (which RACT lanes can the extension hook), required permissions, signing key.
3. Governance — extensions must pass the same approval scaffolding as new agents (`agsApprovalSteps` reused).
4. Capability-pinned dispatch — extensions cannot bypass the MCP dispatcher. Each extension declares which tools it intends to invoke; runtime asserts the declared set matches actual calls.
5. Source-scan blockers — no extension code can `import { dispatchMcpToolCall }` directly; must use the wrapper `services/extensions/runtime.ts:invokeFromExtension()`.
6. UI — extension management page (browse, install, approve, disable).

**First implementation PR (Phase 18-α):**
- New file: `drizzle/tables/agent-studio-extensions.ts`
- New file: `server/agent-studio/services/extensions/manifest.ts` + `contracts.ts` + `runtime.ts`
- New file: `tests/agent-studio/extension-dispatcher-boundary.test.ts` — source-scan + unit
- Frontend: `client/src/modules/agent-studio/pages/ExtensionsPage.tsx`

**Dependencies:** MCP dispatcher chokepoint (G7, done). Approval scaffolding (Cycle-4, done).

---

### Phase 19 — Sync / publish strategy (V1.0)

**MVP 0–4 deferral rationale:** plan §9 — Phase 19 lands as ADR-only in MVP 0; full implementation deferred.

**Status today:** Existing publish surface in `services/promotion/` covers note-version-pinned bindings. No multi-target sync (publish to staging-env, publish to remote vault, publish to external KB).

**Acceptance criteria:**
1. Publish target registry — `agsPublishTargets` table: target id + type (`staging_env` | `remote_vault` | `external_kb`) + endpoint + credentials reference (via `withProviderCredential` — Plan v3 D1, no env reads).
2. Publish-request workflow — operator creates a publish request, governance signs off (existing scaffolding), worker pushes to target with retry/idempotency.
3. Telemetry — publish results land in `agsPublishResults` and the workspace-observability bundle.
4. Rollback — every published artifact carries the source `notePromotionId` so rollback walks the existing promotion-rollback path.

**First implementation PR (Phase 19-α):**
- New table: `agsPublishTargets`
- Extend `services/promotion/` (do not greenfield) with target binding
- Source-scan: no direct `process.env.*_API_KEY` reads (D1)
- Integration test: publish request → target push → ledger row

**Dependencies:** Promotion governance (G7, done). Provider connections (Plan v3 D1, done).

---

### Track J / Phase 27 — Production hardening (V1.0 partial; V2.x full)

**MVP 0–4 deferral rationale:** CLAUDE.md Non-Build List + plan §13 Q4 — out-of-scope until Neo4j CE limits actually bite in production.

**Status today:**
- Upgrade path documented at `docs/architecture/agent-studio-neo4j-aura-upgrade-path.md`.
- Reference architecture at `docs/architecture/agent-studio-neo4j-aura-agent-reference-architecture.md`.
- No operator-actionable runbook for the upgrade itself.

**V1.0 acceptance criteria (Phase J-1 — operator runbook):**
1. Operator-actionable runbook at `docs/runbooks/agent-studio-neo4j-ce-to-aura-migration-runbook.md` with:
   - Trigger conditions (when CE limits bite — query throughput, write contention, fan-out depth)
   - Pre-flight inventory (active graph backend, projection queue state, fixture freshness)
   - Migration steps (Aura provision, projection-queue drain, dual-write window, cutover)
   - Verification — Aura + ASDB row-count parity, projection-queue idle
   - Rollback — restore CE bolt URI, replay queue
   - Evidence path
2. Alerting hooks — Phase 14 trace-graph cron checks Neo4j health latency + writes a `runtime_alert` on threshold breach.

**V2.x acceptance criteria (Track J full):**
1. Aura adoption (operator pull-trigger).
2. Multi-region (see Phase MR-1).
3. Production SLO budget for the graph stack (extends `agent-studio-runtime-slo.md`).

**First implementation PR (Phase J-1):**
- New file: `docs/runbooks/agent-studio-neo4j-ce-to-aura-migration-runbook.md`
- New file: `server/agent-studio/services/graph/health-alert.ts` (writes runtime_alert on threshold breach)
- New file: `tests/agent-studio/graph-health-alert.test.ts`

**Dependencies:** Phase 14 runtime trace graph (done). Phase 27 ADR (done).

---

### Phase MR-1 — Multi-region graph deployment (V2.0)

**Status today:** ADR exists at `docs/architecture/agent-studio-multi-region.md` (forward-looking, single-region operational baseline).

**Acceptance criteria:**
1. Region registry — `agsRegions` table: region id + canonical Postgres URI + canonical Neo4j URI + active flag.
2. Routing model — workspace → region pinning; queries route to the workspace's home region; cross-region reads are denied unless workspace is replicated.
3. Replication model — Postgres logical replication (Aurora-style or pg_basebackup-based); Neo4j projection re-built per region from the local Postgres source-of-truth.
4. Failover runbook — `docs/runbooks/agent-studio-multi-region-failover-runbook.md`.
5. Governance — cross-region promotion / publish requires explicit governance step.

**First implementation PR (Phase MR-1):**
- New file: `docs/runbooks/agent-studio-multi-region-failover-runbook.md`
- New table: `agsRegions`
- Source-scan: any `import { db }` resolves via the region-routed connection helper, never the global pool.

**Dependencies:** Track J operator runbook (Phase J-1).
**Risk:** Highest in V2.0 — touches the most boundaries. Single phase, multiple PRs.

---

### Phase CRDT — Real-time collaborative editing (V2.0)

**Status today:** No CRDT support; vault notes use last-writer-wins. No existing ADR.

**Acceptance criteria:**
1. ADR at `docs/architecture/agent-studio-realtime-collab-crdt.md` (this PR creates it) — pinned CRDT choice (Yjs leading candidate; comparison vs Automerge), source-of-truth model (Postgres remains canonical; CRDT layer is presence + cursor + transient edits), governance boundaries.
2. Presence + cursor surface — `services/vault/presence.ts` tracks who is in which note in near-real-time (no persistence beyond session).
3. CRDT document layer — `services/vault/realtime-doc.ts` wraps Yjs document; sync with backend uses the existing Vault repository at save boundaries.
4. Conflict policy — concurrent edits merge via Yjs; on save, the Vault repository writes the new canonical version; `agsNoteVersions` row is created.
5. Audit — every save resulting from a multi-author session is tagged in `agsRuntimeRuns` so audit can reconstruct collaborator list.

**First implementation PR (Phase CRDT-α):**
- New file: `docs/architecture/agent-studio-realtime-collab-crdt.md` (lands with THIS V1+ plan PR as a stub ADR; full content in dedicated PR)
- New file: `server/agent-studio/services/vault/presence.ts`
- Source-scan: no concurrent-write bypass of `agsNoteVersions`.

**Dependencies:** Vault editor (MVP 1, done).
**Risk:** UI complexity, fork-merge edge cases. Plan many small PRs.

---

### Phase OL-1 — Offline / local-first mode (V2.0)

**Status today:** No offline support. No existing ADR.

**Acceptance criteria:**
1. ADR at `docs/architecture/agent-studio-offline-local-first.md` (this PR creates it) — source-of-truth rules (server remains canonical; offline cache is a derived view), sync conflict policy (CRDT-aware when CRDT phase lands; otherwise field-level merge), graph projection behavior (stale until reconnect), security model (no graph mutation in offline mode; offline writes queue for governance review on reconnect).
2. Service-worker scaffold — client-side cache of recent notes + linked-graph subset.
3. Sync queue — offline writes accumulate in IndexedDB; on reconnect, queue replays through normal `services/vault/*` write paths (and approval/governance as needed).

**First implementation PR (Phase OL-1):**
- New file: `docs/architecture/agent-studio-offline-local-first.md` (this PR creates ADR stub)
- New file: `client/src/modules/agent-studio/services/offline-cache.ts`

**Dependencies:** CRDT phase (in particular, the merge semantics if CRDT is in scope).
**Risk:** Security model — offline graph mutations are a leak surface. Governance enforcement on reconnect is the load-bearing piece.

---

## 3. Cross-cutting hard rules (V1+)

These extend the CLAUDE.md Non-Build List. They MUST be source-scan tested in every PR that touches the relevant surface:

| Rule | Applies to |
|---|---|
| Provider credentials flow through `withProviderCredential` (Plan v3 D1) — never `process.env.*_API_KEY` reads in script bodies | Phase 19-α (sync/publish), Phase 13.5 (agentic engine eventually) |
| Postgres = source of truth; CRDT layer + offline cache are derived | Phase CRDT, Phase OL-1 |
| Region-router boundary — no direct `db` import in region-scoped code | Phase MR-1 |
| Extension dispatcher boundary — no direct `dispatchMcpToolCall` import outside `services/extensions/runtime.ts` | Phase 18 |
| Agentic loop boundary — bounded max-iterations + governance flag | Phase 13.5 |
| Graph mutations only through Phase 11.5 proposal/approval | All V1+ phases that touch the graph |

---

## 4. Testing layers added in V1+

The V1+ plan adds two new test layers on top of the existing 1–9:

| Layer | What it tests | Lands in |
|---|---|---|
| **Layer 10 — adaptive loop boundary** | Agentic GraphRAG max-iterations, no-mutation, governance-flag respected | Phase 13.5 |
| **Layer 11 — cross-region routing** | Workspace → region pinning, cross-region denial, failover replay | Phase MR-1 |

Layer 4 — e2e browser tests — also lands in V1.0 (covered separately in PR-C of the closure bundle: Playwright smoke + ADR + first executable test).

---

## 5. Risk register (V1+)

| Risk | Likelihood | Impact | Mitigation |
|---|---|---|---|
| Agentic loop slips past dispatcher | Medium | Critical | Source-scan + property-based tests + Reviewer + Governance both review |
| CRDT introduces silent data loss on merge | Medium | High | Property-based tests on merge semantics; every save creates an `agsNoteVersions` row |
| Offline writes leak ungoverned mutations | High | Critical | On-reconnect approval gate (no offline write skips governance) |
| Multi-region split-brain | Low | Critical | Cross-region read-deny default; explicit replication-aware promotion |
| Extension framework grows into a parallel runtime | High | Critical | Capability-pinned dispatch + Phase 18 source-scan tests |
| V1+ scope creep delaying V2 | Medium | Medium | Each phase has a "first implementation PR" identified — ship slim |

---

## 6. First-slice PR ledger (2026-05-13)

| PR | Phase | Scope | Merge SHA |
|---|---|---|---|
| #748 (PR-V1-1) | Phase J-1 | Graph health-alert evaluator + scanner + `ags_runtime_alerts` table; threshold-breach taxonomy for the Neo4j CE → Aura migration trigger conditions | `23e47643` |
| #749 (PR-V1-2) | Phase 19-α | `ags_publish_targets` + `ags_publish_target_executions` ledger + `services/publish-targets/` (Plan v3 D2-clean — pusher owns credential acquisition) | `892519b0` |
| #750 (PR-V1-3) | Phase 15-α | `ags_vault_template_instantiations` ledger + `services/vault/template-instantiations.ts` (sha256 digest + record/list helpers) + router wiring | `71c6889d` |
| #751 (PR-V1-4) | Phase 16-α | Saved views: `visibility` + `version` + `parentSavedViewId` columns + `saved-views-visibility.ts` (per-viewer materialization filter; load-bearing #4 acceptance) | `a6959a6d` |
| #752 (PR-V1-5) | Phase 17-α | Canvas data model (3 tables) + `services/canvas/` (CRUD + snapshot loader + note-reference projection helper) | `9fc503de` |
| #753 (PR-V1-6) | Phase 18-α | Extension framework: `ags_extensions` + `ags_extension_invocations` + `evaluateCapability` (pure) + `invokeFromExtension()` (only path to MCP dispatcher) | `9f019393` |
| #754 (PR-V2-1) | Phase MR-1 | `ags_regions` + `services/region/` (pure router with cross-region deny) + multi-region failover runbook | `1bf8e509` |
| #755 (PR-V2-2) | Phase CRDT-α | `ags_vault_note_presence` (5-min idle TTL) + `services/vault/presence.ts` (enter / heartbeat / leave / list with eager eviction) | `0b36c820` |
| #756 (PR-V2-3) | Phase OL-1 | `client/src/modules/agent-studio/services/offline-cache.ts` — `OfflineQueue` + load-bearing deny-list for graph + agent + provider operations | `3e6bae92` |

All 9 first-slice PRs landed on main on 2026-05-13. Each carries:
- Closed-taxonomy type unions where applicable
- Source-scan boundary tests preserving CLAUDE.md / Plan v3 D2 invariants
- ASDB-unavailable test fall-through patterns
- No raw `process.env.*_API_KEY` reads, no `credential-resolver` imports outside model-access, no `dispatchMcpToolCall` imports outside the MCP chokepoint / `services/extensions/runtime.ts` wrapper

**Status:** V1.0 + V1.5 + V2.0 first slices are now **FULLY IMPLEMENTED** at the data-model + service + boundary-test layer. UI surfaces and lane-specific runtime hooks land in subsequent β/γ slices per each phase's "out of scope for the α slice" section.

### 6.1 Second-slice (β/γ) PR ledger — opened 2026-05-13

| PR | Phase | Scope | Merge SHA |
|---|---|---|---|
| #758 (PR-V1-7) | Phase J-1-β | Health-alert cron (`services/graph/health-alert-cron.ts`) wrapping PR-V1-1 evaluator via `makeRetentionCron` factory at `*/5 * * * *`; boot Step 3.25; admin tRPC `agentStudio.graphHealth.getAlertCronStatus` + `listOpen` | `660303f6` |
| #759 (PR-V1-8) | Phase 19-β | Shared `makeHttpPusher` factory + `registerDefaultPublishPushers()` wiring the 3 target types (`staging_env` POST /promote / `remote_vault` PUT /vault/notes / `external_kb` POST /ingest); boot Step 3.26 | `525c5f8b` |
| #760 (PR-V1-9) | Phase 15-β | Attachment library service (`services/vault/attachment-library.ts`): browseAttachmentLibrary (vault-wide + mime-class filter + sort) + findUnusedAttachments (LEFT JOIN to vault_notes for trash detection) + computeAttachmentQuota + closed 6-value mime-class taxonomy | `ad7f9829` |
| #761 (PR-V1-10) | Phase 17-β | Canvas → Note projection edge (`services/canvas/projection.ts`): buildCanvasReferenceProjection emits the 4-write bundle (Canvas + CanvasNode + CONTAINS_CANVAS_NODE + CANVAS_REFERENCES_NOTE); ProjectionSyncWorker handles `canvas.note_reference_changed` / `_removed` event kinds | `e608cffe` |
| #762 (PR-V1-11) | Phase OL-2 | OfflineQueue durability (`client/.../offline-cache-store.ts`): closed `OfflineQueueStore` contract + `InMemoryOfflineQueueStore` (test default + non-browser fall-through) + `IndexedDbOfflineQueueStore` (production; lazy-open db `agent-studio-offline` / store `offline_queue`) + `createOfflineQueueStore()` factory; OfflineQueue gains optional `persistence` adapter + `hydrate()` to restore on reload; no breaking change to OL-1 API | `c67fab23` |
| #763 (PR-V1-12) | Phase MR-2 | Region-routed connection helper (`services/region/connection-helper.ts`): `getDbForRegion(region, opts)` with per-regionKey Drizzle pool cache + `getDbForWorkspace(input, opts)` composing the MR-1 pure router + `getNeo4jUriForRegion(region)` + `listWarmRegionPoolKeys()` operator inspection. Caller migration deferred to a follow-up sub-arc | `af8b1667` |
| #764 (PR-V1-13) | Phase CRDT-β | Realtime document layer (`services/vault/realtime-doc.ts`): pinned `RealtimeDocBackend` interface (encodeSnapshot/applyUpdate/getText/setText/pendingUpdateCount) + `InMemoryRealtimeDocBackend` reference impl (zero deps; JSON-encoded snapshots; last-writer-wins) + `RealtimeDocSession` wrapper + process-local session registry + `getCanonicalContentForSave` save-boundary helper. Yjs adapter + WebSocket transport + browser IDB persistence land in CRDT-γ | `183f0909` |
| #765 (PR-V1-14) | Phase CRDT-γ | Yjs adapter (`services/vault/realtime-doc-backend-yjs.ts`) implementing the CRDT-β `RealtimeDocBackend` contract via a typed-only `YjsNamespace` DI seam — production passes `Y` from `import * as Y from "yjs"` (declared in package.json under `dependencies`); unit tests use a typed fake and run locally without the yjs install. WebSocket transport + browser IDB persistence land in CRDT-γ-2 | `2a534c51` |
| #766 (PR-V1-15) | Phase 15-γ | Attachment quota write-gate (`services/vault/attachment-quota-guard.ts`): `assertWithinQuota({ vaultId, sizeBytesAdded, bytesLimit })` pure function (calls `computeAttachmentQuota` + projects usage + throws `AttachmentQuotaExceededError`) + `resolveDefaultAttachmentBytesLimit()` env-based config resolver (`AGS_VAULT_ATTACHMENT_BYTES_LIMIT`). Caller integration into `createAttachment` deferred to a follow-up sub-arc | `61e0c600` |
| #767 (PR-V1-16) | Phase 16-β | `listVisibleSavedViewsForUser` (`services/vault/saved-views.ts`): composes `listSavedViews` + the pure `filterVisibleSavedViews` predicate from `saved-views-visibility.ts` so per-viewer filtering happens in one call. `options.listImpl` DI seam keeps the composition testable without a live ASDB. Existing `listSavedViews` (operator/admin path) unchanged | `0c04a3a5` |
| #768 (PR-V1-17) | Phase 18-β | Extension lane hook registry (`services/extensions/lane-hooks.ts`): process-local `Map<ExtensionCapabilityLane, LaneHookFn>` + `registerLaneHook` (rejects `lane="tool"` — dispatcher-only invariant) + `getLaneHook` + `listRegisteredLaneHookLanes` + `__resetExtensionLaneHooksForTests`. `invokeFromExtension` calls the registered hook for non-tool lanes (capability check + hook + ledger), captures throws as `{ succeeded: false, errorMessage }`, falls back to α "assert + ledger" when no hook is registered. `options.getDb` test seam added to runtime for ASDB-unavailable test path | `09c67999` |
| #769 (PR-V1-18) | Phase 16-γ | Immutable saved-view version history: new `ags_vault_saved_view_versions` table (savedViewId + version unique; name/viewKind/filters/sort/columns/visibility/capturedByUserId/capturedAt) + `updateSavedView` captures prior row snapshot BEFORE applying the patch (not-found check moves earlier to the snapshot read) + `listSavedViewVersions(savedViewId)` + `getSavedViewVersionById(versionId)` + `UpdateSavedViewOptions.capturedByUserId` for audit trail | `200d4665` |
| #770 (PR-V1-19) | Phase 16 wire-up | Vault router gains 3 protected tRPC procedures wiring the 16-β + 16-γ service surfaces: `listVisibleSavedViews` threads `ctx.user.id` as `viewerUserId` into the service; `listSavedViewVersions(savedViewId)` + `getSavedViewVersion(versionId)` expose the immutable history. 11-test source-scan procedure-mount integrity suite at `tests/agent-studio/vault-saved-views-router-procedures.test.ts` | `3db12f93` |
| #771 (PR-V1-20) | Phase 15-γ wire-up | `createAttachment` router mutation enforces the quota: calls `assertWithinQuota({ vaultId, sizeBytesAdded, bytesLimit: resolveDefaultAttachmentBytesLimit() })` BEFORE the insert; maps `AttachmentQuotaExceededError` to `TRPCError code="FORBIDDEN"` (not the default INTERNAL_SERVER_ERROR); 8-test source-scan order-of-operations + error-code-mapping suite at `tests/agent-studio/vault-attachment-quota-router-wireup.test.ts` | `fdf7ba36` |
| #772 (PR-V1-21) | Phase 19-γ | Publish executor governance gate (`services/publish-targets/`): closed `GOVERNANCE_DECISIONS` 3-value taxonomy (`approved`/`pending`/`rejected`) + `GovernanceGateFn` + `isGovernanceDecision` narrowing in `types.ts`; `executePublish` branches: rejected → ledger row `status=failed` + `errorMessage="governance_rejected"` + `details.governanceDecision="rejected"`, pending → `status=pending` + `details.governanceDecision="pending"`, both short-circuit before the pusher runs; new `targetLoader` test seam in `ExecutePublishInput` for ASDB-unavailable unit-test path. 11-test suite at `tests/agent-studio/publish-targets-governance-gate.test.ts`. The gate is operator-supplied (DI); real `agsApprovalSteps` wiring lands in a follow-up adapter | `123de148` |
| #773 (PR-V1-22) | Phase OL-3 | OfflineQueue client bootstrap (`client/.../offline-cache-bootstrap.ts`): `getOfflineQueueInstance(opts)` module-singleton lazy-init with `createOfflineQueueStore()` (picks IDB in browser, InMemory elsewhere) + `__resetOfflineQueueInstanceForTests`; `hydrateOfflineQueueOnLoad(opts)` rehydrates persisted entries with `beforeHydrate/afterHydrate` telemetry hooks; `installOfflineDrainListener({ onDrain, eventTarget?, afterDrain? })` registers a `window.addEventListener("online", drainNow)` handler and returns an `OfflineDrainListenerHandle { drainNow, uninstall }`. SSR/CLI no-op-installer fall-through path. 17-test suite at `tests/agent-studio/offline-cache-bootstrap.test.ts` | `d5806bf8` |
| #774 (PR-V1-23) | Phase CRDT-γ-2 | Realtime-doc WebSocket transport scaffold (`services/vault/realtime-doc-transport.ts`): `RealtimeDocConnection` narrow interface (send/on(message)/on(close)/close) + `RealtimeDocTransport.attachConnection(conn, opts)` lifecycle: sends initial snapshot, wires `on("message")` → `session.handleUpdate` + broadcast to peers (originator-skip + per-peer error tolerance), `on("close")` → `detachConnection` + drop session when last client leaves AND `backend.pendingUpdateCount() === 0`. `shouldBroadcast` gate. Module-singleton accessor + test reset seam. 14-test suite at `tests/agent-studio/vault-realtime-doc-transport.test.ts`. Out of scope: y-protocols message framing + auth/authz upgrade + reconnect (CRDT-γ-3) | `5c69ae7b` |
| #775 (PR-V1-24) | MR×19 integration | Cross-region publish governance composition (`services/publish-targets/cross-region-governance.ts`): `requiresCrossRegionGovernance({ sourceRegionKey, targetRegionKey })` pure predicate (true iff both known + differ; null on either side treated as same-region) + `wrapWithCrossRegionGovernance(baseGate, opts)` adapter that composes any `GovernanceGateFn` with the cross-region check (same-region passes through to the base gate; cross-region downgrades to `pending` by default or `rejected` mode). Closes V1+ plan §MR-1 acceptance criterion #5 ("Governance — cross-region promotion / publish requires explicit governance step"). 16-test suite at `tests/agent-studio/publish-targets-cross-region-governance.test.ts` | `d02e3ae6` |
| #776 (PR-V1-25) | AS-1 ApprovalSteps gate adapter | Concrete `agsApprovalSteps` adapter for the 19-γ governance gate (`services/publish-targets/governance-gate-approval-steps.ts`): `decideGovernanceFromApprovalSteps(steps)` pure rule (any rejected → "rejected"; non-empty all-approved → "approved"; else "pending") + `createApprovalStepsGovernanceGate(opts)` factory returning a `GovernanceGateFn`. DI seams: `resolvePublishRequestId(input) → number \| null`, `getDb` (defaults to `getAsDb`), and `listApprovalSteps(id)` (test seam bypasses ASDB entirely). Read-only against `agsApprovalSteps`; defensive `"pending"` when the resolver returns null or ASDB is unavailable (never silent approve). 22-test suite at `tests/agent-studio/publish-targets-governance-gate-approval-steps.test.ts` | `1798c1bc` |
| #777 (PR-V1-26) | OL-4 offline drain dispatcher registry | Module-scoped registry + dispatcher composer that bridges OL-3's `installOfflineDrainListener({ onDrain })` and the per-kind tRPC drain implementations (`client/src/modules/agent-studio/services/offline-drain-dispatcher.ts`). Mirrors the server-side `publish-targets/registry.ts` pattern: `registerOfflineDrainImpl({ kind, impl })` / `getOfflineDrainImpl(kind)` / `listRegisteredOfflineDrainKinds()` / `__resetOfflineDrainRegistryForTests()` + `OfflineDrainImplNotRegisteredError` raised on unknown kind (no silent drop — Phase OL-3 keeps the entry queued and bumps `attempts`). `createOfflineDrainDispatcher({ lookup? })` returns an `OfflineDrainOne`-compatible function. The dispatcher does NOT re-validate `OFFLINE_DENIED_OPERATION_KINDS` — `OfflineQueue.enqueue()` is the gate. 18-test suite at `tests/agent-studio/offline-drain-dispatcher.test.ts` | `a16cf4f8` |
| #778 (PR-V1-27) | OL-5 default offline drain impls factory | Mirror of `publish-targets/defaults.ts` for the OL-4 registry (`client/src/modules/agent-studio/services/offline-drain-defaults.ts`). `registerDefaultOfflineDrainImpls({ updateNote, createNote, deleteNote })` wires the three `OFFLINE_OPERATION_KINDS` into the dispatcher registry: each closure receives the raw entry payload and resolves to void (return values discarded). Idempotent — re-registration overwrites prior closures per kind. The factory is tRPC-type-free (no `AppRouter` import) so the offline-cache module stays buildable without a tRPC namespace dependency; callers (App.tsx bootstrap) supply the trpc closures at call time. Returns the kinds in deterministic alphabetical order. 13-test suite at `tests/agent-studio/offline-drain-defaults.test.ts` | `8fc11b49` |
| #779 (PR-V1-28) | OL-6 offline cache full bootstrap composer | Single-call composer (`client/src/modules/agent-studio/services/offline-cache-full-bootstrap.ts`) that wraps the locked OL-5→OL-4→OL-3 chain: `bootstrapOfflineCache({ updateNote, createNote, deleteNote, beforeHydrate?, afterHydrate?, afterDrain?, eventTarget? })` runs `registerDefaultOfflineDrainImpls` → `createOfflineDrainDispatcher()` → `await hydrateOfflineQueueOnLoad(...)` → `installOfflineDrainListener({ onDrain: dispatcher })` in order, returning `{ hydratedCount, handle }`. App.tsx / main.tsx call sites become one-liners. tRPC-type-free (no `AppRouter`/`@/lib/trpc` imports — closures injected). With this, the OL-1→OL-6 chain is end-to-end composed at the service layer; remaining work is the real App.tsx call site with live trpc mutations. 13-test suite covering composition, online-event drain through plain `EventTarget`, hydratedCount, telemetry beacons, uninstall semantics, and source-scan boundaries | `4d9abb01` |
| #780 (PR-V1-29) | NV-1 server-side `vault.note.delete` procedure | Closes the contract gap surfaced during OL-5: `OFFLINE_OPERATION_KINDS` declares `vault.note.delete` but the vault router had no `deleteNote` procedure. Adds `NoteDeleteInput` Zod (noteId + optional `expectedVersion` for online optimistic-lock; optional for offline-drain replays that may not know current state) + `VaultRepository.deleteNote(input, userId)` interface method + `VaultRepositoryStub` semantics (idempotent on already-deleted, conflict on version-mismatch, notFound on missing id) + `AsdbVaultRepository` soft-delete impl (`UPDATE ags_vault_notes SET deletedAt = NOW()` with `deletedAt IS NULL` precondition + `getNoteById` left unfiltered for audit-recovery; `listNotesInVault` already filters `deletedAt IS NULL` so deleted notes drop out of listings) + `agentStudio.vault.deleteNote` tRPC mutation (NOT_FOUND TRPCError on missing, alreadyDeleted:true on idempotent second call, conflict+latestVersion on optimistic-lock mismatch). 21-test suite at `tests/agent-studio/vault-delete-note.test.ts` (contract parsing, stub semantics, router source-scan, ASDB source-scan) | `f26c3829` |
| #781 (PR-V1-30) | OL-7 main.tsx wire-up to vanilla tRPC client | New `client/src/modules/agent-studio/services/offline-cache-app-wireup.ts` builds a vanilla `createTRPCClient<AppRouter>` (sibling to the React-hooks client) and wires its three `agentStudio.vault.*Note` mutations into `bootstrapOfflineCache` (OL-6). `main.tsx` imports and `void`-invokes `wireOfflineCacheToTrpc()` once at boot — fire-and-forget so a failed bootstrap never blocks render. Hard-rule clean (no `credential-resolver` / `dispatchMcpToolCall` / `*_API_KEY` / `neo4j-driver`). 11-test source-scan suite (`tests/agent-studio/offline-cache-app-wireup.test.ts`) covering tRPC client shape, mutation bindings, `.catch` failure path, hard-rule absences, + `main.tsx` single-call invariant. Closes the OL-1→OL-7 end-to-end offline-cache compose chain at the live App.tsx boundary | `75313ad2` |
| #782 (PR-V1-31) | AS-2 default governance-gate registry | Module-singleton registry (`server/agent-studio/services/publish-targets/default-governance-gate.ts`) consulted by `executePublish` when callers do not supply an explicit `governanceGate`. Exposes `installDefaultGovernanceGate` / `getDefaultGovernanceGate` / `hasDefaultGovernanceGate` / `__resetDefaultGovernanceGateForTests` (parallels the existing `registry.ts` pattern). Executor falls through via `input.governanceGate ?? getDefaultGovernanceGate()` — per-call gates still win. With this, AS-1's `createApprovalStepsGovernanceGate` can be installed once at boot rather than threaded through every `executePublish` caller. 16-test suite at `tests/agent-studio/publish-targets-default-governance-gate.test.ts` (registry lifecycle + executor fall-through + per-call precedence + barrel re-export + source-scan boundary). Real `installDefaultGovernanceGate(createApprovalStepsGovernanceGate(...))` boot call defers to a follow-up PR that owns the `resolvePublishRequestId` mapping | `b0e906e3` |
| #783 (PR-V1-32) | OL-8 offline-cache enqueue helper | Consumer-side surface for the OL-1..OL-7 compose chain (`client/src/modules/agent-studio/services/offline-cache-enqueue.ts`). Single helper `enqueueOfflineVaultMutation({ kind, payload, id?, generateId?, queue? })` that (1) routes through the OL-3 module-singleton queue, (2) generates a UUIDv4 id when none supplied (default via `crypto.randomUUID`; `generateId` test seam), and (3) delegates `assertOfflineQueueable(kind)` to `OfflineQueue.enqueue()` so the OL-1 deny-list is still the only gate. Re-exports `OfflineMutationDeniedError` for callers wanting to catch + degrade. tRPC-type-free (no `AppRouter` import) so the offline-cache module stays buildable without a tRPC namespace dependency — payloads stay `Record<string, unknown>` and the server re-runs Zod on drain. 15-test suite at `tests/agent-studio/offline-cache-enqueue.test.ts` (happy path, caller-supplied id, generateId seam, default crypto.randomUUID shape, deny-list rejection, unknown-kind rejection, allowlist coverage, source-scan boundary) | `192dc42b` |
| #784 (PR-V1-33) | AS-3 composeGovernanceGates helper | Pure generic composer (`server/agent-studio/services/publish-targets/compose-governance-gates.ts`): runs N `GovernanceGateFn`s in order; returns the strictest decision (rejected > pending > approved); short-circuits on rejected so an expensive gate (DB lookup, remote call) is skipped when a cheap pre-check has already rejected; defensive snapshot so caller mutation of the input array after composition does not change behavior; empty list returns "approved" (no constraint, preserves executor's "no gate ⇒ approved" semantics). With AS-1 (`createApprovalStepsGovernanceGate`) + AS-2 default-gate registry + this composer, operators can install `installDefaultGovernanceGate(composeGovernanceGates([approvalStepsGate, wrapWithCrossRegionGovernance(...)]))` at boot to enforce BOTH approval-steps AND cross-region governance without hand-rolling a wrapper. 17-test suite at `tests/agent-studio/publish-targets-compose-governance-gates.test.ts` (decision matrix + short-circuit + order preservation + defensive snapshot + public-api re-export + source-scan boundary including no `drizzle-orm` import — pure composer) | `a15583c5` |
| #785 (PR-V1-34) | OL-9 offline-or-live mutation router | One-liner for UI call sites that need to decide between calling the live tRPC mutation (online) and enqueueing offline (`client/src/modules/agent-studio/services/offline-cache-router.ts`). `routeVaultMutation({ kind, payload, callLive, isOnline?, id?, generateId?, queue? })` defaults `isOnline` to `navigator.onLine` (true when navigator is unavailable — non-browser env); online path returns `{ mode: "live", result }` with `callLive` invoked exactly once; offline path returns `{ mode: "queued", entry }` via OL-8 with `callLive` NOT invoked. Live-mode errors propagate unchanged (caller decides retry semantics — no implicit fall-through to enqueue, which would mask transient server errors). Pure function (no React/JSDOM coupling); future React-hook layer is a separate PR if needed. 13-test suite at `tests/agent-studio/offline-cache-router.test.ts` (online happy path, live-error propagation, offline enqueue, deny-list rejection on offline path, generateId seam, navigator.onLine default both directions, source-scan boundary — no react/react-dom/AppRouter imports) | `912a8490` |
| #786 (PR-V1-35) | AS-4 payload-based resolver + env-flag boot install | Closes the AS-2 named follow-up. Two new files: `payload-publish-request-id-resolver.ts` (`parsePublishRequestIdFromBody` pure parsing + `resolvePublishRequestIdFromPayload` async wrapper — pulls `payload.body.publishRequestId` as a positive integer; null on missing / non-integer / non-positive — defensive null is load-bearing, NEVER silent-approves when linkage is unclear) + `install-default-governance-gate.ts` (`maybeInstallDefaultGovernanceGate({ env?, install?, makeGate? })` env-flag-gated installer: composes AS-1 `createApprovalStepsGovernanceGate` with the AS-4 resolver and registers via AS-2 `installDefaultGovernanceGate` when `AGS_PUBLISH_GOVERNANCE_GATE=approval_steps_payload_resolver`; any other value or unset → no-op). Boot Step 3.27 in `server/agent-studio/boot.ts` calls the installer + logs the decision. Default OFF — preserves existing "no gate ⇒ approved" behavior on first install; operators opt-in once their callers wire `payload.body.publishRequestId`. 35-test suite at `tests/agent-studio/publish-targets-install-default-governance-gate.test.ts` (parsing matrix incl. NaN/Infinity/float/zero/negative rejection, env-flag strict-equality matrix incl. wrong casing, composed gate end-to-end via AS-1's listApprovalSteps seam, public-api re-exports, source-scan boundary including no `drizzle-orm` import in the resolver) | `439c5335` |
| #787 (PR-V1-36) | CRDT-γ-3-auth realtime-doc authorization rule | Pure authorization helper (`server/agent-studio/services/vault/realtime-doc-authorize.ts`) that the future WS-upgrade handler will call BEFORE `RealtimeDocTransport.attachConnection` (#774). `authorizeRealtimeDocConnection({ sessionKey, userId, getVaultIdsForUser })` returns the closed 3-value taxonomy: `{ allow: true }` when `sessionKey.vaultId` ∈ `getVaultIdsForUser(userId)`, else `{ allow: false, reason: "missing_user_id" \| "not_a_vault_member" \| "vault_membership_lookup_failed" }`. Defensive deny on DI throw — NEVER silent-approves on lookup failure (a duplex realtime channel cannot afford a silent-approve hole). Missing-userId short-circuits BEFORE the lookup. tRPC-type-free + repository-import-free — `getVaultIdsForUser` is a closure the caller wires from `VaultRepository.listVaultsForUser`. 16-test suite at `tests/agent-studio/vault-realtime-doc-authorize.test.ts` (closed taxonomy, allow path, 4 deny paths, lookup invocation count, source-scan boundary). Out of scope: WS-upgrade handler that calls this (separate PR), y-protocols message framing (CRDT-γ-3-framing), reconnect (CRDT-γ-3-reconnect) | `329526de` |
| #788 (PR-V1-37) | CRDT-γ-3-upgrade-handler — composes γ-3-auth + γ-2 transport | `handleRealtimeDocUpgrade({ conn, sessionKey, userId, transport, authorize? \| getVaultIdsForUser? })` is the entry-point the future WebSocket-upgrade pipeline calls (`server/agent-studio/services/vault/realtime-doc-upgrade-handler.ts`). Allow → `transport.attachConnection(conn, { sessionKey })` and returns `{ attached: true, disposer }`. Deny → closes the connection with a stable WebSocket close code in the private-use 4xxx range + the deny reason as message; NEVER attaches. Closed close-code taxonomy: `missing_user_id → 4401`, `not_a_vault_member → 4403`, `vault_membership_lookup_failed → 4500` (HTTP-ish for client UX). Tolerates `conn.close` throwing (race against client-side abort). Two DI surfaces: `authorize` for tests + framework wrappers, `getVaultIdsForUser` for production (builds the default authorizer via closure). Throws when neither is supplied (no silent default). 17-test suite at `tests/agent-studio/vault-realtime-doc-upgrade-handler.test.ts` (close-code taxonomy, allow→attach, parameterized 3-reason deny→close, conn.close race tolerance, getVaultIdsForUser default-wiring both directions, missing-DI throw, authorize-override-wins precedence, source-scan boundary). Still out of scope: WebSocket framework choice + actual upgrade pipeline (TBD), y-protocols framing (CRDT-γ-3-framing), reconnect (CRDT-γ-3-reconnect) | `388a575a` |
| #789 (PR-V1-38) | WebSocket upgrade pipeline ADR + first slice | ADR at `docs/architecture/agent-studio-websocket-upgrade-pipeline.md` pins **`ws` ^8.18.3 with `noServer: true`** as the framework; rejects uWebSockets.js / native http upgrade / socket.io / framework-bundled with rationale. Bridge at `server/agent-studio/services/vault/realtime-doc-websocket-bridge.ts` exports `adaptWsToRealtimeDocConnection(ws)` (wraps a `ws.WebSocket`-like in the `RealtimeDocConnection` narrow interface; routes `error` events to the close handler with `code=1011` + `reason="ws_error:..."` so no error is silently dropped) + `installRealtimeDocUpgradeOnServer(server, opts)` (env-flag-gated dispatcher that subscribes once to `server.on("upgrade")`; right path `/api/agent-studio/vault/realtime` + valid `vaultId`+`noteId` query → `wss.handleUpgrade` → adapter → `handleRealtimeDocUpgrade`; wrong path / malformed query → `socket.destroy()` — no half-open sockets). Env flag `AGS_REALTIME_DOC_WEBSOCKET_ENABLED=true` enables; default OFF preserves boot behavior. Boot install in `server/_core/index.ts` between `createServer(app)` and `server.listen(...)` — async-import + try/catch so an install error never crashes the process. tRPC-type-free, no `credential-resolver` / `dispatchMcpToolCall` / `*_API_KEY` / `neo4j-driver` / `openrouter` imports — source-scan tested. 43-test suite at `tests/agent-studio/vault-realtime-doc-websocket-bridge.test.ts` (adapter forwarding, error→close routing, parsePathname / parseSessionKey matrices, env-flag strict-equality matrix, wrong-path / malformed-query destroy paths, dispatch routing, deny path closes the ws, missing-userId 4401 path, source-scan boundary, boot-install integrity). Out of scope: production auth-cookie resolver, `getVaultIdsForUser` production wiring (DI seams for now), CRDT-γ-3-framing dispatch, live integration test against a running server | `a40ab696` |
| #790 (PR-V1-39) | CRDT-γ-3-framing message framing primitives + dispatcher | Pure framing layer (`server/agent-studio/services/vault/realtime-doc-framing.ts`) the future transport-side integration will compose with. Closed 3-value taxonomy `REALTIME_DOC_FRAME_TYPES = ["sync", "awareness", "unknown"]`; matches y-protocols wire format (leading byte 0 → sync, 1 → awareness, else → unknown). `parseRealtimeDocFrame(data, opts?)` returns `{ type, leadingByte, payload, totalBytes }`; payload is a `subarray` view (no copy). Throws `RealtimeDocFrameEmptyError` on empty buffer + `RealtimeDocFrameTooLargeError` on oversize (default cap `DEFAULT_MAX_REALTIME_DOC_FRAME_BYTES = 2 MiB`; tunable via `opts.maxBytes`). `dispatchRealtimeDocFrame({ data, handlers, ctx, maxBytes? })` composes parse + handler routing: sync → `handlers.onSync`, awareness → `handlers.onAwareness`, unknown → `handlers.onUnknown` (no-op when handler omitted); awaits async handlers + propagates their rejections + propagates parse errors so the caller (future transport message handler) decides close-vs-drop. tRPC-type-free, drizzle-free, y-protocols-free (DI seam — engine plugs in later via the handler closures). Hard-rule clean (no `credential-resolver` / `dispatchMcpToolCall` / `*_API_KEY` / `neo4j-driver` / `openrouter` / `GraphRepository` imports). 32-test suite at `tests/agent-studio/vault-realtime-doc-framing.test.ts` (closed taxonomy, parseRealtimeDocFrameType matrix incl. bytes 2..255 → unknown, parse happy paths with subarray-view assertion, empty-buffer + oversize errors at default + custom limits + at-limit no-throw, dispatcher routing for sync/awareness/unknown incl. missing-handler no-op, parse-error propagation through dispatch, async-handler rejection propagation, source-scan boundary). Out of scope: transport-side wire-up (separate PR; the framing primitives must land first so dispatch is testable in isolation), real y-protocols sync engine, real y-protocols awareness engine | `e99fcbf5` |
| #791 (PR-V1-40) | CRDT-γ-3-framing transport wire-up | Composes PR-V1-39's `parseRealtimeDocFrame` + `dispatchRealtimeDocFrame` into the CRDT-γ-2 transport's per-connection `onMessage` handler (`server/agent-studio/services/vault/realtime-doc-transport.ts`). Three new `AttachConnectionOptions` fields: `frameHandlers?: RealtimeDocFrameHandlers` (opt-in — when set, every inbound message routes through the dispatcher with the leading type byte stripped before handler invocation), `maxFrameBytes?: number` (frame-size cap override; ignored when frameHandlers unset), `closeOnFrameError?: boolean` (default `true` — empty / oversize / handler-throw closes the connection with a stable code). Closed 3-value close-code taxonomy `REALTIME_DOC_FRAME_CLOSE_CODES = { empty_frame: 4400, frame_too_large: 4413, handler_error: 4500 }` in the WebSocket private-use range. When `frameHandlers` is omitted, the existing CRDT-γ-2 behavior is preserved bit-for-bit (raw `data` → `session.handleUpdate`; raw frame broadcast to peers) — no breaking change to existing call sites. shouldBroadcast=false still suppresses both dispatch and broadcast (defense-in-depth). Tolerates `conn.close` throwing during error-handling (already-closed race). 15-test suite at `tests/agent-studio/vault-realtime-doc-transport-framing.test.ts` (default-behavior preservation, sync/awareness/unknown dispatch routing, payload-stripping assertion, peer-broadcast assertion, 3 close-code error paths, closeOnFrameError=false, maxFrameBytes override, shouldBroadcast=false interplay, taxonomy assertion, source-scan that transport now imports the framing dispatcher). Existing 14-test transport regression + 122-test full realtime-doc regression suite still green | `337753e6` |
| #792 (PR-V1-41) | 17-γ canvas → graph projection events sink + caller hook | ADR `docs/architecture/agent-studio-canvas-projection-events.md` accepts the **sink registry + caller hook** pattern (mirrors AS-2 default-governance-gate registry). Rejects: extending `agsRuntimeGraphEvents` (runtimeRunId notNull constraint), boot-installed `ProjectionSyncWorker` singleton (tight coupling), EventEmitter (process-local), tRPC-triggered worker (no operational benefit), cron polling (misses events). New `server/agent-studio/services/canvas/projection-events-sink.ts`: module-singleton sink (`registerCanvasProjectionEventSink` / `getCanvasProjectionEventSink` / `hasCanvasProjectionEventSink` / `__resetCanvasProjectionEventSinkForTests`) + `recordCanvasProjectionEvent(event)` convenience entry-point. Closed `CanvasProjectionEvent` union (`canvas.note_reference_changed` + `canvas.note_reference_removed`) declared structurally so the sink module stays isolated from `services/graph/projection/sync-worker.ts` (whose `GraphRepository` import would pull forbidden surfaces through a transitive scan). Default sink is no-op — installing this code does NOT change canvas-service behavior on first install. Sink errors caught + `console.warn` logged — mutation path never fails on sink failure. Caller wired in `createCanvasNode`: when `node.referencedNoteId != null`, emits `canvas.note_reference_changed`. 21-test suite at `tests/agent-studio/canvas-projection-events-sink.test.ts` (registry semantics, no-op default both kinds, sink forwarding both kinds, async-sink await, sync + async sink-throw catch, public-api re-export of 5 names + 2 types, source-scan boundary including no-ProjectionSyncWorker import in either sink or canvas-service). Out of scope: persistence sink + new `ags_canvas_projection_events` table (schema migration its own PR), drain wiring boot step, update/delete caller-side hooks (canvas-service has no updateCanvasNode/deleteCanvasNode yet), cross-region propagation via MR-2 | `0540c826` |
| #793 (PR-V1-42) | 18-γ-contracts extension lane hook contracts (typed surface) | ADR `docs/architecture/agent-studio-extension-lane-hook-contracts.md` pins the **typed per-lane contract surface** every concrete hook must satisfy. Closed 3-value `CONTRACTED_EXTENSION_LANES = ["retrieve", "assemble", "compose"]` — `tool` lane is excluded by construction (dispatcher-only invariant). Per-lane outcome types in `server/agent-studio/services/extensions/lane-hook-contracts.ts`: `ContractedRetrieveOutcome` (`supplementalPlanItems?` + `supplementalChunks?` with required `provenance.sourceType` + `sourceId` so the existing `retrieval-filter.ts` governance + permission filter applies without special-casing), `ContractedAssembleOutcome` (`supplementalChunks?` + `tokensAttributed?` — `RAC_MAX_RETRIEVAL_EVIDENCE_TOKENS` cap enforced at the assembler), `ContractedComposeOutcome` (`supplementalBlocks?` in CAG `BuildPackInput.userBlocks` shape — hash invariant preserved; a different contribution produces a different pack id). Discriminated `ContractedLaneHookFn<Lane>` that the compiler uses to enforce per-lane outcome shape at registrant boundary. **Hard rules (load-bearing per CLAUDE.md):** no RAC bypass (all contributions are INPUTS to existing pipeline; planner/filter/assembler still run); no raw context injection (contributions flow through the same filter); citation + freshness preserved (provenance required at type level); CAG hash invariant preserved; tool lane stays dispatcher-only (excluded from contract surface). Type-only imports of RAC + CAG canonical types — runtime boundary preserved; source-scan tests catch a future contributor accidentally promoting a type-only import to a value import. 18-test suite at `tests/agent-studio/extension-lane-hook-contracts.test.ts` (closed taxonomy, retrieve / assemble / compose outcome shape construction, contract→registry composition assertion for all 3 lanes via `ContractedLaneHookFn` up-cast to `LaneHookFn`, discriminated union test, public-api re-export of 9 names, source-scan boundary + type-only-import boundary). Out of scope: concrete retrieve / assemble / compose hook implementations (each its own PR with RAC + CAG pipeline integration), extension-install UI | `0d8bb0fc` |
| #794 (PR-V1-43) | MR-3 getAsDb caller inventory + first batch migration | Inventory doc `docs/implementation/agent-studio-mr-3-getasdb-inventory.md` classifies all 151 `getAsDb()` call sites across 44 files into 5 categories: A (should migrate now — workspaceId in scope), B (requires plumbing), C (already acceptable — infrastructure / boot / seed), D (tRPC routers — dedicated sub-arc), E (requires ADR — multi-workspace iterators). New Phase-1 shim `getAsDbForWorkspace(workspaceId)` in `server/agent-studio/db/connection.ts` accepts `workspaceId` and delegates to `getAsDb()` — preserves single-region operational baseline bit-for-bit; Phase-2 follow-up wires real region routing via the existing `getDbForWorkspace` helper without caller changes. First-batch migration: `extensions/manifest.ts::installExtension(input)` → `getAsDbForWorkspace(input.workspaceId)`; `extensions/manifest.ts::listExtensionsByWorkspace(workspaceId, ...)` → `getAsDbForWorkspace(workspaceId)`. The other 3 functions in the same file (`approveExtension` / `setExtensionStatus` / `getExtensionById`) are Category B (need workspaceId plumbed through callers) — left on `getAsDb()` intentionally; source-scan tests assert this. 9-test suite at `tests/agent-studio/mr-3-getasdb-for-workspace-shim.test.ts` (shim cache-identity check, function-body source-scan with comment-stripping, asserts both migrated functions call the shim, asserts the 3 unmigrated functions still call `getAsDb()`, inventory-doc presence check). Out of scope: Phase-2 of the shim (real region routing), Category B migrations (one per caller-chain), Category D router sub-arc, Category E ADRs | `d7297ad6` |
| #795 (PR-V1-44) | 16-δ SavedViewVersionHistoryPanel first UI surface | First Phase G UI slice: small focused component (`client/src/modules/agent-studio/components/SavedViewVersionHistoryPanel.tsx`) backed by the existing `agentStudio.vault.listSavedViewVersions` tRPC procedure (#770). Renders 4 states (loading / error / empty / populated) with one `<li>` per version showing `v{N} — {name}`, relative capturedAt, and optional `Captured by user #{id}` line. Single-responsibility extracted component pattern per the playbook's "Avoid large RetrofitPage-style growth"; matches the retention-panel arc reference. 4-test JSDOM-environment suite at `SavedViewVersionHistoryPanel.test.tsx` covering loading branch, error branch with message echo, empty branch, populated branch with version/name/user-id rendering + null-userId omits the "Captured by" line. Out of scope: route/page that embeds the panel, edit/restore actions (those need additional tRPC procedures), saved-view detail page composition | `370308dd` |
| #796 (PR-V1-45) | 15-δ AttachmentListPanel second focused UI surface | Sibling to PR-V1-44's pattern. Component (`client/src/modules/agent-studio/components/AttachmentListPanel.tsx`) backed by the existing `agentStudio.vault.listAttachments` tRPC procedure. Accepts `vaultId` and/or `noteId` (router enforces "one of"; panel renders a guard-error card when both are missing rather than firing an invalid query). 5 states (input-guard / loading / error / empty / populated). Populated rows render filename, mimeType, formatted size via local `formatBytes` (B / KiB / MiB / GiB), relative `createdAt`, and an amber "source artifact" badge when `isSourceArtifact=true`. 6-test JSDOM-environment suite at `AttachmentListPanel.test.tsx` (input-guard branch, 3 query-state branches, populated multi-row rendering with size formatting + badge selector assertion, sub-1KiB byte-formatting branch). Out of scope: route/page composition, attachment-quota dashboard, attachment-library deletion / link / source-artifact-toggle UI | `81fbe761` |
| #797 (PR-V1-46) | MR-3 second batch — `vault/repository-asdb.ts::createVault` | Second migration batch off the MR-3 Phase-1 shim (#794). Widens `getAsDbForWorkspace`'s signature from `(workspaceId: number)` to `(workspaceId: number \| null \| undefined)` to accommodate optional / nullable workspaceId at call sites — Phase-1 shim still delegates to `getAsDb()` regardless, so single-region behavior is preserved bit-for-bit; Phase-2 will treat null/undefined as "use the default region" rather than throwing. `createVault(input, createdByUserId)` migrates to `getAsDbForWorkspace(input.workspaceId)` because `VaultCreateInput.workspaceId` is on the input schema (optional). The other 8 methods in the same file (`addMember` / `listVaultsForUser` / `createNote` / `getNoteById` / `getNoteVersion` / `updateNote` / `listNotesInVault` / `deleteNote`) remain on `getAsDb()` — they're inventory Category B (require workspaceId plumbed through callers via a separate per-method sub-arc). 12-test source-scan suite at `tests/agent-studio/mr-3-second-batch-vault-repository.test.ts` (shim regression for `undefined` / `null`, import-shape check, `createVault` body asserts `getAsDbForWorkspace(input.workspaceId)` + no `getAsDb()`, parameterized assertion that the 8 unmigrated methods still call `getAsDb()`). Comment-stripping method-body extractor reused from #794. Out of scope: Phase-2 of the shim (real region routing), Category B migrations (one per caller-chain), Category D router sub-arc | `c6839045` |
| #798 (PR-V1-47) | MR-3 third batch — `ingestion/ingestion-job-service.ts::startJob` | Third migration batch off the MR-3 Phase-1 shim (#794). `startJob(input)` in `server/agent-studio/services/ingestion/ingestion-job-service.ts` migrates to `getAsDbForWorkspace(input.workspaceId)` (StartJobInput.workspaceId is required `number`). The other 2 functions in the same file (`completeJob` / `getJob`) remain on `getAsDb()` — they take only `jobId` and would need workspaceId plumbed through callers (Category B). 4-test source-scan suite at `tests/agent-studio/mr-3-third-batch-ingestion-job-service.test.ts` (import-shape check, `startJob` body assertion, parameterized assertion that the 2 unmigrated functions still call `getAsDb()`). Function-body extractor variant of #794's method extractor — handles top-level `export async function` shape instead of class methods. Out of scope: Phase-2 of the shim, Category B migrations for `completeJob` / `getJob`, the other 3 ingestion files still on `getAsDb()` (separate per-file sub-arcs) | `176befbb` |
| #799 (PR-V1-48) | MR-3 fourth batch — `ingestion/knowledge-unit-service.ts::insertUnit` | Fourth migration batch off the MR-3 Phase-1 shim (#794). `insertUnit(input)` in `server/agent-studio/services/ingestion/knowledge-unit-service.ts` migrates to `getAsDbForWorkspace(input.workspaceId)` (`InsertUnitInput.workspaceId` is required via `NormalizedKnowledgeUnitInput`). The other function in the same file (`archiveUnit(unitId)`) remains on `getAsDb()` — it takes only `unitId` and would need workspaceId plumbed through callers (Category B). 3-test source-scan suite at `tests/agent-studio/mr-3-fourth-batch-knowledge-unit-service.test.ts` (import-shape check, `insertUnit` body assertion, `archiveUnit` Category-B assertion). Out of scope: Phase-2 of the shim, Category B migration for `archiveUnit`, the other 2 ingestion files still on `getAsDb()` (`provenance-service.ts`, `universal-ingestion-service.ts` — each a separate sub-arc) | `688ad711` |
| #800 (PR-V1-49) | MR-3 fifth batch — `ingestion/provenance-service.ts::recordProvenance` | Fifth migration batch off the MR-3 Phase-1 shim (#794). `recordProvenance(input)` in `server/agent-studio/services/ingestion/provenance-service.ts` migrates to `getAsDbForWorkspace(input.workspaceId)` (`RecordProvenanceInput.workspaceId` is required `number`). The other function in the same file (`getProvenance(provenanceId)`) remains on `getAsDb()` — it takes only `provenanceId` and would need workspaceId plumbed through callers (Category B). 3-test source-scan suite at `tests/agent-studio/mr-3-fifth-batch-provenance-service.test.ts` (import-shape check, `recordProvenance` body assertion, `getProvenance` Category-B assertion). Out of scope: Phase-2 of the shim, Category B migration for `getProvenance`, the last remaining ingestion file (`universal-ingestion-service.ts`, sixth batch) | `bb1a1dc0` |
| #801 (PR-V1-50) | MR-3 sixth batch — `ingestion/universal-ingestion-service.ts` (FULL file migration) | Sixth and final ingestion migration off the MR-3 Phase-1 shim (#794). Both call sites in this file have `workspaceId` in scope (Category A): the Layer-4 extractor loop in `runIngestion(request, ...)` uses `request.workspaceId`; the `persistArtifact(input)` helper uses `input.workspaceId`. Both migrate to `getAsDbForWorkspace(workspaceId)`. Because there are **no remaining `getAsDb()` call sites** in this file, the import line drops `getAsDb` and only imports `getAsDbForWorkspace`. This is the FIRST ingestion file in MR-3 to be 100% migrated; closes the 4-of-4 ingestion sub-arc (with #798 / #799 / #800 covering the other three). 6-test source-scan suite at `tests/agent-studio/mr-3-sixth-batch-universal-ingestion-service.test.ts` (import-shape check, no-getAsDb-import assertion, no-getAsDb-call-sites assertion, ≥2 getAsDbForWorkspace call-sites count, both Layer-4 and persistArtifact body assertions). Out of scope: Phase-2 of the shim, Category B migrations across other files | `6dc03385` |
| #802 (PR-V1-51) | CRDT-γ-3 production `getVaultIdsForUser` wiring | Closes the V1+ named follow-up surfaced by PR-V1-37 (#787) / PR-V1-38 (#789): the realtime-doc WebSocket bridge accepts `installRealtimeDocUpgradeOnServer(server, { getVaultIdsForUser })` but the first slice shipped with no production default — every upgrade landed `not_a_vault_member` (4403). New `server/agent-studio/services/vault/realtime-doc-default-getvaultids.ts` exports `createDefaultGetVaultIdsForUser({ getRepo? })` — returns a `GetVaultIdsForUserFn` closure that consults `VaultRepository.listVaultsForUser(userId)` and projects row ids. Production lazy-imports `AsdbVaultRepository`; test seam injects a stub. Repo cached after first invocation (one construction per closure). Lookup throw propagates unchanged so the upgrade handler can map to `vault_membership_lookup_failed` → close code 4500 (defensive deny). Boot at `server/_core/index.ts` now passes `{ getVaultIdsForUser: createDefaultGetVaultIdsForUser() }` to the existing install call — env-flag gating unchanged (`AGS_REALTIME_DOC_WEBSOCKET_ENABLED=true`). 11-test JSDOM-environment suite at `tests/agent-studio/vault-realtime-doc-default-getvaultids.test.ts` (happy path with multi-row projection, empty rows → empty array, repo invoked with userId, repo cached across invocations, throw propagation, source-scan boundaries with comment-stripped code). Out of scope: production auth-cookie resolver for `getUserIdFromUpgradeRequest` (separate PR; still defaults to null) | `e91423f1` |
| #803 (PR-V1-52) | CRDT-γ-3 production auth-cookie resolver | Closes the second V1+ named follow-up from PR-V1-38 (#789): the realtime-doc WebSocket bridge accepts `installRealtimeDocUpgradeOnServer(server, { getUserIdFromUpgradeRequest })` but the first slice shipped a null-returning default — every upgrade landed `missing_user_id` (4401). The bridge's signature for `getUserIdFromUpgradeRequest` is widened from `(req) => number \| null` to `(req) => number \| null \| Promise<number \| null>` so async DB-backed resolvers fit (the dispatcher now awaits the return). New `server/agent-studio/services/vault/realtime-doc-default-getuserid.ts` exports `createDefaultGetUserIdFromUpgradeRequest({ authenticate? })`: returns a closure that calls the OAuth SDK's `authenticateRequest(req)` (cast-safe because the SDK only reads `req.headers.cookie`, which `IncomingMessage` provides), and returns `user.id` on success. ANY throw from the authenticator (verification failure, DB unavailability) is caught and translated to `null` — defensive deny is load-bearing because a duplex realtime channel must NEVER silently allow on auth error. DI seam (`authenticate`) lets unit tests bypass the SDK entirely. Boot wires the production closure alongside `getVaultIdsForUser` (#802). 11-test JSDOM-environment suite at `tests/agent-studio/vault-realtime-doc-default-getuserid.test.ts` covering authenticated path, null-from-authenticator, async + sync throw both → null, return-type widening assertion, source-scan boundaries. Out of scope: CRDT-γ-3-reconnect (server session-resume), y-protocols sync/awareness engines | `6a466d5c` |
| #804 (PR-V1-53) | 17-γ `ags_canvas_projection_events` table + seed wiring | First half of the 17-γ persistence sink: the schema migration. New table `agsCanvasProjectionEvents` declared in `drizzle/tables/agent-studio-canvas.ts` (alongside the existing canvas tables from #752) — columns: `id`, `kind` (closed taxonomy mirroring `CanvasProjectionEvent` in `projection-events-sink.ts`), `workspaceId` (for retention + multi-region routing), `canvasId`, `canvasNodeId`, `referencedNoteId`, `metadata`, `createdAt`. Indexes on `(workspaceId, createdAt)`, `canvasId`, `canvasNodeId`, and `kind`. Append-only by sink-contract — no UPDATE/DELETE paths declared. Seed script (`server/agent-studio/db/seed.ts`) now imports the canvas schema barrel and spreads it into `combinedSchema` so the introspection loop auto-provisions the new table (and the existing 3 canvas tables that were previously missed by the seed — recovered as a side-benefit). 18-test source-scan suite at `tests/agent-studio/canvas-projection-events-table.test.ts` (table declared, column shape, index names, type exports, seed-script import + spread, kind taxonomy lockstep with sink, hard-rule boundaries). Out of scope: ASDB-backed sink implementation that writes to this table (follow-up PR composes on this schema); drain wiring boot step; update/delete caller-side hooks; cross-region propagation via MR-2 | `1dc655c0` |
| #805 (PR-V1-54) | 17-γ ASDB-backed canvas projection events sink | Second half of the 17-γ persistence sink: the writer. New `server/agent-studio/services/canvas/projection-events-sink-asdb.ts` exports `createAsdbCanvasProjectionEventSink({ getDb?, resolveWorkspaceId? })` returning a `CanvasProjectionEventSink` that writes one row per event to `agsCanvasProjectionEvents`. Default `resolveWorkspaceId` queries `agsCanvases.vaultId` → `agsVaults.workspaceId` for the event's `canvasId`. The `CanvasProjectionEvent.note_reference_removed` payload is extended (additively) to carry `canvasId` + `priorReferencedNoteId` so persistence has the same shape on both kinds — no existing caller emits `_removed` yet (canvas-service has no `updateCanvasNode`/`deleteCanvasNode`), so the extension is safe. Defensive posture: null-workspaceId → WARN + drop (`agsCanvasProjectionEvents.workspaceId` is notNull, can't fabricate); resolver throw → WARN + drop; `getDb()` returns null → WARN + drop. Append-only — `.update`/`.delete` on the events table are source-scan-forbidden. 13-test JSDOM suite at `tests/agent-studio/canvas-projection-events-sink-asdb.test.ts` (happy path both kinds, resolver invocation, 3 defensive paths, source-scan boundaries including no-GraphRepository import + write-only assertion). Out of scope: boot install (next PR registers the sink via `registerCanvasProjectionEventSink`); update/delete caller hooks (canvas-service surface not yet present); drain consumer worker (separate sub-arc) | `8faaf57c` |
| #806 (PR-V1-55) | 17-γ env-flag-gated default canvas projection events sink boot install | Closes the 17-γ persistence sink wire-up trio: schema (#804) + writer (#805) + this install. New `server/agent-studio/services/canvas/install-default-projection-events-sink.ts` exports `maybeInstallDefaultCanvasProjectionEventsSink({ env?, install?, makeSink? })` — mirrors AS-4 (`install-default-governance-gate.ts` from #786) one-for-one. Env flag `AGS_CANVAS_PROJECTION_EVENTS_SINK=asdb` → installs the ASDB-backed sink via `registerCanvasProjectionEventSink(createAsdbCanvasProjectionEventSink())`. Any other value / unset → no-op (preserves the silent-swallow default sink). Boot Step 3.28 in `server/agent-studio/boot.ts` calls the installer + logs the decision. Default OFF — preserves existing no-op behavior on first install so operators can flip the flag once `ags_canvas_projection_events` is confirmed provisioned. 19-test JSDOM suite at `tests/agent-studio/canvas-install-default-projection-events-sink.test.ts` (env-flag strict-equality matrix incl. uppercase/whitespace/sentinel rejection, install+makeSink seam composition, process.env default, source-scan boundaries, boot.ts wiring assertion for Step 3.28). Out of scope: drain consumer worker (separate sub-arc); update/delete caller hooks; cross-region propagation via MR-2 | `e92cc517` |
| #807 (PR-V1-56) | 17-γ canvas projection events reader (drain helper) | Pure read helper for the future drain consumer (`server/agent-studio/services/canvas/projection-events-reader.ts`). `readCanvasProjectionEventsAfter({ workspaceId, lastSeenId, limit }, { getDb? })` returns up to `limit` rows from `ags_canvas_projection_events` where `id > lastSeenId`, scoped to `workspaceId`, ordered by `id` ASC (keyset pagination — strict ordering, no `createdAt`-tie issues). Defensive clamps: `limit` is truncated and capped at `READ_CANVAS_PROJECTION_EVENTS_HARD_LIMIT = 1000`; floor of 1. ASDB-unavailable → `[]` (no throw, so the future cron-style consumer can treat it as "no progress; retry next tick"). DI seam (`getDb`) for unit testing. 14-test JSDOM suite at `tests/agent-studio/canvas-projection-events-reader.test.ts` (happy-path returns, limit application, orderBy ASC count, hard-limit clamping, below-1 clamping, float truncation, ASDB-unavailable safety, source-scan boundaries including write-only-forbidden assertion). Out of scope: cron/queue/worker loop that calls this reader, cursor persistence (separate sub-arc — operational shape TBD: process-local cursor vs. dedicated table vs. piggyback on existing workspace observability) | `f05ccd7b` |
| #808 (PR-V1-57) | 17-γ canvas projection events drain orchestrator | Stateless per-workspace drain (`server/agent-studio/services/canvas/projection-events-drain.ts`). `drainCanvasProjectionEventsForWorkspace({ workspaceId, lastSeenId, forward, limit?, maxBatches? }, { getDb? })` composes #807's reader with a caller-supplied `forward(event)` closure. Multi-batch loop: keeps reading until either the reader returns fewer than `limit` rows (genuine catch-up) OR `maxBatches` fires (defensive cap so a runaway workspace can't monopolise a cron tick). Per-event forward throw → halts the drain immediately, cursor stays at the LAST SUCCESSFUL row id (next tick retries). Returns `{ workspaceId, nextLastSeenId, processed, failed, batchesRun, hitMaxBatches }` so the caller can wire telemetry + cursor persistence (kept out of scope intentionally — the orchestrator is stateless and operator-supplied storage decides cron / queue / table / observability piggyback). Defaults: `limit=100`, `maxBatches=10`. 12-test JSDOM suite at `tests/agent-studio/canvas-projection-events-drain.test.ts` (single-batch catch-up, empty-page no-op, multi-batch full drain, maxBatches ceiling fires, forwarder throw halts at the failing row + cursor stays + WARN logged, default-limit assertion, sane DEFAULT_DRAIN_MAX_BATCHES, source-scan boundaries including no-write assertion). Out of scope: cron scheduler that invokes this drain, cursor persistence, multi-workspace fan-out | `8309e292` |
| #809 (PR-V1-58) | 17-γ canvas projection events drain fan-out (multi-workspace) | Multi-workspace orchestrator (`server/agent-studio/services/canvas/projection-events-drain-fanout.ts`). `drainCanvasProjectionEventsAllWorkspaces({ cursorMap, forward, getWorkspaceIds?, limit?, maxBatchesPerWorkspace? }, { getDb? })` iterates workspaces and calls #808's per-workspace drain for each. `cursorMap: Map<workspaceId, lastSeenId>` is mutated in place (caller retains the reference across ticks; process-local persistence). Workspace discovery: caller-supplied `getWorkspaceIds()` overrides the default; the default `defaultListActiveProjectionWorkspaces` queries `SELECT DISTINCT workspaceId FROM agsCanvasProjectionEvents` so new workspaces show up automatically. Per-workspace isolation: a workspace drain throwing does NOT block others — the cursor map stays unchanged for the failing workspace (next tick retries from the same point). Workspace-discovery failure → no-op tick + WARN (operator's cron stays alive across ASDB blips). Aggregate result exposes `{ workspacesScanned, totalProcessed, totalFailed, anyHitMaxBatches, perWorkspace }`. 11-test JSDOM suite at `tests/agent-studio/canvas-projection-events-drain-fanout.test.ts` (multi-workspace happy path with cursor mutation, empty-workspace-list result, existing-cursor as starting point, missing-key default to 0, per-workspace failure isolation, workspace discovery throw → no-op, source-scan boundaries). Out of scope: cron scheduler tick wiring (next PR), cursor persistence across process restarts | `a7131036` |
| #810 (PR-V1-59) | 17-γ canvas projection events drain — module-singleton tick wrapper | Module-local cursor + forwarder install wrapping #809's fan-out so a cron / setInterval / admin trigger can invoke a nullary `tickCanvasProjectionEventsDrain()` (`server/agent-studio/services/canvas/projection-events-drain-tick.ts`). Exports: `installCanvasProjectionEventsDrainForwarder(forwarder)`, `hasCanvasProjectionEventsDrainForwarder()`, `tickCanvasProjectionEventsDrain(options?)`, `getCanvasProjectionEventsDrainLastTickResult()` (observability — ticksRun, lastTickResult, lastTickError, cursorMapSize), `__resetCanvasProjectionEventsDrainForTests()`. Cursor scope is process-local Map — survives ticks within the same process, resets on boot. First tick after boot scans from `lastSeenId=0` per workspace; forwarder MUST be idempotent (call out documented in module docstring). Tick before install → no-op + WARN. Unexpected fan-out throw → captured to `lastTickError` + WARN; module-singleton state preserved. 11-test JSDOM suite at `tests/agent-studio/canvas-projection-events-drain-tick.test.ts` (no-forwarder no-op, hasForwarder reflects install, invoke-after-install delivers events, cursor persists across ticks, observability state on success + error capture path, source-scan boundaries). Out of scope: actual setInterval-based scheduler that calls the tick (separate PR composes with `setInterval` or `makeRetentionCron`-style envelope) | `60fc0429` |
| #811 (PR-V1-60) | 17-γ canvas projection events drain — env-flag-gated setInterval scheduler installer | Closes the 17-γ persistence sink END-TO-END loop: schema (#804) → writer (#805) → boot-install of writer (#806) → drain helpers (#807, #808, #809) → tick wrapper (#810) → this scheduler. New `server/agent-studio/services/canvas/install-default-projection-events-drain-scheduler.ts` exports `maybeInstallDefaultCanvasProjectionEventsDrainScheduler({ env?, setInterval?, tick? })`. Env flags: `AGS_CANVAS_PROJECTION_EVENTS_DRAIN=interval` → installs setInterval that calls #810's `tickCanvasProjectionEventsDrain()` every `intervalMs`. `AGS_CANVAS_PROJECTION_EVENTS_DRAIN_INTERVAL_MS=<ms>` overrides the default 60_000ms. Invalid override → falls back to default. Default OFF — operator opt-in. Forwarder is OPERATOR-supplied (not installed by this scheduler) so the integration boundary stays explicit: scheduler decides WHEN to drain, operator decides WHERE events go. Fire-and-forget tick callback never throws out of `setInterval`; rejections logged via WARN; the tick wrapper already captures errors to `lastTickError`. Best-effort `handle.unref()` so the interval doesn't block process shutdown when the handle exposes it. Returns `{ mode, installed, intervalMs, handle }` for boot-step logging + shutdown via `clearInterval(handle)`. 17-test JSDOM suite at `tests/agent-studio/canvas-install-default-projection-events-drain-scheduler.test.ts` (env strict-equality matrix, default interval, valid override, invalid override fallback, interval-callback tick invocation, fire-and-forget rejection safety + WARN, unref invocation, numeric-handle tolerance, source-scan boundaries). Out of scope: boot.ts wire-up (next PR) | `3020862e` |
| #812 (PR-V1-61) | 17-γ canvas projection events drain — boot.ts wire-up Step 3.29 | Closes the operator-facing surface: `server/agent-studio/boot.ts` now calls `maybeInstallDefaultCanvasProjectionEventsDrainScheduler()` in a new Step 3.29 immediately after the #806 sink-install (Step 3.28). Logs the installed-mode case with both `mode` and `intervalMs` so operators can verify the cadence the scheduler picked; logs the skipped case when `AGS_CANVAS_PROJECTION_EVENTS_DRAIN` is unset. Wrapped in try/catch so a malformed env / install throw never crashes boot. 6-test source-scan suite at `tests/agent-studio/canvas-projection-events-drain-scheduler-boot.test.ts` (import name, file-path reference, installed-mode log shape, skipped-mode log, Step 3.29 label, try/catch structural assertion). With this, the 17-γ persistence sink loop is **operator-ready** end-to-end: schema is provisioned (#804 seed), the writer auto-installs on `AGS_CANVAS_PROJECTION_EVENTS_SINK=asdb` (#806), the drain auto-installs on `AGS_CANVAS_PROJECTION_EVENTS_DRAIN=interval`; the operator's only remaining task is to install a forwarder (e.g. into `ProjectionSyncWorker` or an external queue) | `57d7fdf6` |
| #813 (PR-V1-62) | 17-γ canvas projection events drain — admin tRPC status router | Operator observability surface for the 17-γ drain scheduler. New `server/agent-studio/services/canvas/projection-events-drain-router.ts` exports `canvasProjectionEventsDrainRouter` with one `adminProcedure` `getDrainStatus` that returns the module-singleton state from #810 (`{ ticksRun, lastTickResult, lastTickError, cursorMapSize }`). Mounted at `agentStudio.canvasProjectionEventsDrain.*` (mirrors the J-1-β `agentStudio.graphHealth.*` shape). Operator questions answered: is the scheduler running (`ticksRun > 0`)? Did the last tick succeed (`lastTickError == null`)? How many workspaces are tracked (`cursorMapSize`)? Per-workspace aggregates from the last tick (`lastTickResult.perWorkspace`). Read-only — no `.mutation` procedures by source-scan. 11-test source-scan suite at `tests/agent-studio/canvas-projection-events-drain-router.test.ts` (router shape, adminProcedure declaration, delegates to #810's getter, import from tick module, main router imports + mounts, source-scan boundaries including no-mutation assertion). Out of scope: client-side panel that consumes this procedure | `46384931` |
| #814 (PR-V1-63) | 17-γ `CanvasProjectionEventsDrainStatusPanel` operator UI | Client-side panel consuming the #813 `getDrainStatus` admin procedure (`client/src/modules/agent-studio/components/CanvasProjectionEventsDrainStatusPanel.tsx`). Renders 5 branches: loading / error (with `error.message` echo) / not-yet-ticked (`ticksRun=0` advisory with cursorMapSize) / populated (ticksRun, workspaces tracked, last-tick processed/failed counts with singular-vs-plural pluralisation, optional last-tick-error in destructive style, optional hit-max-batches amber advisory) / null-status fallback. Reuses `formatRelative` for the lastTickError timestamp. Pattern matches #795 / #796 single-responsibility extracted-component shape; mounts into an admin dashboard via the next composition PR. 9-test JSDOM-environment suite at `CanvasProjectionEventsDrainStatusPanel.test.tsx` (5 branch states + singular pluralization + failure-suffix + lastTickError row + hit-max-batches advisory). Hard-rule clean (no credential-resolver / dispatchMcpToolCall / *_API_KEY / neo4j-driver / openrouter / GraphRepository imports — verified by the existing client-side boundary tests on the modules/agent-studio path). Out of scope: route/page that embeds the panel | `5e6d4d32` |
| #815 (PR-V1-64) | 15-δ vault `getAttachmentQuota` tRPC procedure | New `agentStudio.vault.getAttachmentQuota` protectedProcedure (`server/agent-studio/services/vault/router.ts`) that wraps `computeAttachmentQuota` from #760's attachment-library service. Takes `{ vaultId }`; threads `resolveDefaultAttachmentBytesLimit()` from #771's quota-guard so the panel's displayed "X% used" matches the actual `createAttachment` rejection threshold bit-for-bit (single source of truth for the limit). Error path routes through the existing `throwTrpcAndCapture` observability hook used by all vault procedures (#491/#492/#493 pattern). 6-test source-scan suite at `tests/agent-studio/vault-quota-router-procedure.test.ts` (computeAttachmentQuota import, resolveDefaultAttachmentBytesLimit import, protectedProcedure + vaultId input shape, delegation assertion, bytesLimit threading assertion, throwTrpcAndCapture in the error path). Unblocks the next slice — a Phase G UI panel that calls this procedure. Out of scope: the UI panel itself; mutation procedures for adjusting the quota (operator action via env var) | merged @ 02790061 |
| #816 (PR-V1-65) | 15-δ AttachmentQuotaPanel Phase G UI surface | New `client/src/modules/agent-studio/components/AttachmentQuotaPanel.tsx` consuming `trpc.agentStudio.vault.getAttachmentQuota.useQuery({vaultId})` from #815. Mirrors AttachmentListPanel (#796) and CanvasProjectionEventsDrainStatusPanel (#814): single-responsibility extracted component with explicit query-state branches. Six rendering branches: loading, error, null-data fallback, null-bytesLimit ("No quota limit configured"), overQuota=true (destructive style, "new uploads will be rejected"), utilization > 0.8 (amber "Approaching quota" warning), utilization ≤ 0.8 (standard "Used: X/Y (Z%)"). Singular/plural pluralization on `attachmentCount`. Byte formatting reuses the AttachmentListPanel helper shape (B/KiB/MiB/GiB). 9-test JSDOM suite at `client/src/modules/agent-studio/components/AttachmentQuotaPanel.test.tsx` with `vi.hoisted`+`vi.mock("@/lib/trpc")` covering all branches plus singular/plural variants. Closes the 15-δ vault attachment quota slice end-to-end (service `computeAttachmentQuota` → router `getAttachmentQuota` → panel `AttachmentQuotaPanel`). Out of scope: the operator route/page that composes AttachmentListPanel + AttachmentQuotaPanel into a single Vault Attachments admin surface (next PR). | pending |
| #817 (PR-V1-66) | 15-δ VaultAttachmentsAdminSurface composition slice | New `client/src/modules/agent-studio/components/VaultAttachmentsAdminSurface.tsx` composing the two atomic 15-δ panels (`AttachmentQuotaPanel` #816 on top, `AttachmentListPanel` #796 below) into a single per-vault admin surface. Single-responsibility composition shell — no business logic, no extra fetches, no provider knowledge: each child panel owns its own tRPC query. Renders the surface inside a `data-testid="vault-attachments-admin-surface"` wrapper with a `data-vault-id` attribute for test/operator visibility, separated by a Tailwind `space-y-4` stack. 5-test JSDOM suite at `VaultAttachmentsAdminSurface.test.tsx` mocking both useQuery hooks via `vi.hoisted` + `vi.fn` (with `beforeEach` mockReset so call-history assertions are per-test): wrapper render, both child section labels appearing, vaultId forwarding to the quota useQuery, vaultId + listLimit forwarding to the list useQuery, and combined over-quota + empty-list rendering. Drops the eventual route/page into a single `<VaultAttachmentsAdminSurface vaultId={…} />` call. Out of scope: AgentStudioShell view wiring + URL routing (next PR); mutation/upload UX. | pending |
| #818 (PR-V1-67) | 17-γ drain row→event projector + projecting forwarder | Closes the operator-facing integration boundary for the 17-γ persistence sink loop. The #808-#811 drain hands rows from `ags_canvas_projection_events` to a caller-supplied forwarder; this PR provides the reusable row→event reconstruction so operators don't re-implement DB-row de-serialization at each downstream binding. New `server/agent-studio/services/canvas/projection-events-drain-row-projector.ts` exports `projectAgsCanvasProjectionEventRow(row): CanvasProjectionEvent \| null` — pure function with closed `CANVAS_PROJECTION_EVENT_ROW_KINDS` taxonomy (`note_reference_changed` / `note_reference_removed`); maps the row-kind suffix back to the canonical canvas-service event kind (`canvas.note_reference_changed` / `canvas.note_reference_removed`); reads `referencedNoteId` as `priorReferencedNoteId` for the removed kind (matches #805's writer convention). Defensive null on unknown kinds + null `referencedNoteId` (sink writer guarantees non-null; defensive drop on schema drift). New `server/agent-studio/services/canvas/projection-events-drain-projecting-forwarder.ts` exports `createProjectingCanvasProjectionEventForwarder({dispatch})` returning a `CanvasProjectionEventForwarder` that projects then awaits the operator-supplied `dispatch(event)` closure. Unprojectable rows → WARN + advance cursor (do not block the drain). `dispatch` throws propagate so #808's "halt at failing row, cursor stays at last successful" semantics still apply. Strict hard-rule boundary: no `ProjectionSyncWorker` / `GraphRepository` / `sync-worker` imports — operators bind their worker at boot by passing the `dispatch` closure (preserves the `services/graph/repository/**` import locality rule per CLAUDE.md). 14-test JSDOM suite at `tests/agent-studio/canvas-projection-events-drain-row-projector.test.ts` (closed taxonomy assertion, 2 happy-path projections, 3 defensive null branches, 2 forwarder dispatch-shape assertions, 2 unprojectable-row no-dispatch branches with WARN spy, dispatch throw propagation, async dispatch await, comment-stripped source-scan asserting `ProjectionSyncWorker` + `sync-worker` + `GraphRepository` imports stay out). Out of scope: a default `installProjectingCanvasProjectionEventDrainForwarder` boot step (separate PR — operator's worker reference must be available at install time); a separate ASDB-direct sink that bypasses the drain (already shipped in #805, this PR is for downstream consumers of the drain). | pending |
| #819 (PR-V1-68) | MR-3 seventh batch — `services/cag/events.ts` | Seventh migration batch off the MR-3 Phase-1 shim (#794). Two Category A functions migrate to `getAsDbForWorkspace`: `appendPackEvent(input)` → `getAsDbForWorkspace(input.workspaceId)` (AppendEventInput.workspaceId is required); `listEventsByPack(workspaceId, packId, limit)` → `getAsDbForWorkspace(workspaceId)` (workspaceId is the first parameter). The third function `listPackEvents(agentDraftId, limit)` remains on `getAsDb()` — Category B (agentDraftId-scoped; workspaceId would need to be plumbed through callers via a separate sub-arc). 4-test source-scan suite at `tests/agent-studio/mr-3-seventh-batch-cag-events.test.ts` (import-shape check; `appendPackEvent` + `listEventsByPack` body assertions; `listPackEvents` Category B assertion). Comment-stripping function-body extractor reused from #797 / #800. Out of scope: Phase-2 of the shim (real region routing), Category B migration for `listPackEvents`, remaining `services/cag/store.ts` migrations (separate sub-arc — 8 call sites). | pending |
| #820 (PR-V1-69) | MR-3 eighth batch — `services/cag/store.ts` | Eighth migration batch off the MR-3 Phase-1 shim (#794). Three Category A functions in `services/cag/store.ts` migrate: `createPack(input)` → `getAsDbForWorkspace(input.workspaceId)` (CreatePackInput.workspaceId required); `getPackById(workspaceId, packId)` → `getAsDbForWorkspace(workspaceId)`; `listPacksForAgent(workspaceId, agentId)` → `getAsDbForWorkspace(workspaceId)`. Five Category B functions stay on `getAsDb()` — `getLatestPack(agentDraftId)`, `listPacks(agentDraftId)`, `markPackStale(packId, …)`, `markPackUsed(packId)`, `recordPackTokenActual(packId, …)` — all packId/agentDraftId-scoped (workspaceId would need to be plumbed through callers via a separate per-method sub-arc, or read from the loaded row for the workspaceId-discovering helpers). 9-test source-scan suite at `tests/agent-studio/mr-3-eighth-batch-cag-store.test.ts` (import-shape check; parameterized Category A body assertions for the 3 migrated functions; parameterized Category B assertions for the 5 unmigrated functions). Out of scope: Phase-2 of the shim (real region routing); Category B migrations for the 5 remaining functions; remaining `services/runtime/trace-writer.ts` migrations (separate sub-arc — 2 call sites). | pending |
| #821 (PR-V1-70) | MR-3 ninth batch — `services/runtime/trace-writer.ts` | Ninth migration batch off the MR-3 Phase-1 shim (#794). `recordToolCallTrace(input)` in `services/runtime/trace-writer.ts` migrates to `getAsDbForWorkspace(input.workspaceId)` (ToolCallTraceInput.workspaceId required). The second function `patchRacRuntimeTrace(traceId, patch)` remains on `getAsDb()` — Category B (traceId-scoped; workspaceId would need to be plumbed through callers via a separate sub-arc). 3-test source-scan suite at `tests/agent-studio/mr-3-ninth-batch-trace-writer.test.ts` (import-shape check; `recordToolCallTrace` body assertion; `patchRacRuntimeTrace` Category B assertion). Out of scope: Phase-2 of the shim (real region routing); Category B migration for `patchRacRuntimeTrace`; remaining `services/mcp/tool-knowledge-sync.ts` + `services/mcp/auto-sync-resolver.ts` migrations (separate sub-arc — 2 call sites). | pending |
| #822 (PR-V1-71) | MR-3 tenth batch — `services/mcp/*` (FULL 2-file migration) | Tenth migration batch off the MR-3 Phase-1 shim (#794). Two MCP service files each migrate their sole `getAsDb()` call site: `tool-knowledge-sync.ts::syncToolKnowledge(input)` → `getAsDbForWorkspace(input.workspaceId)` (SyncToolKnowledgeInput.workspaceId required); `auto-sync-resolver.ts::defaultAutoSyncResolver(serverId)` → `getAsDbForWorkspace(fallbackWorkspaceId)` where `fallbackWorkspaceId` is env-derived via `readWorkspaceFallback()` and validated non-null before the DB call. Because each file has no remaining Category B `getAsDb()` call sites, both DROP the bare `getAsDb` import — the import becomes pure `getAsDbForWorkspace`. This is the SECOND 100% migration in MR-3 (joining #801 `universal-ingestion-service.ts`); each file is now done with the Phase-F migration arc. 6-test source-scan suite at `tests/agent-studio/mr-3-tenth-batch-mcp.test.ts` (parameterized per-file: import-shape check, no-getAsDb-import assertion, body call-site assertion). Function-body extractor variant of #800's extractor + a fall-through to file-body match for the `export const … = async (…)` arrow shape in `auto-sync-resolver.ts`. Out of scope: Phase-2 of the shim (real region routing); remaining `services/rac/sources/store.ts` (10 call sites) + `services/runtime/trace-writer.ts::patchRacRuntimeTrace` + ASDB-direct seeds + boot helpers (each a separate sub-arc). | pending |
| #823 (PR-V1-72) | MR-3 eleventh batch — `services/rac/sources/store.ts` (6 Cat A migrations) | Eleventh migration batch off the MR-3 Phase-1 shim (#794). `services/rac/sources/store.ts` has 14 functions; 6 Category A migrations land in this batch: `createProfile(input)` → `getAsDbForWorkspace(input.workspaceId)`; `createSource(input)` → `getAsDbForWorkspace(input.workspaceId)`; `listSourcesForWorkspace(workspaceId, …)` → `getAsDbForWorkspace(workspaceId)`; `upsertPolicy(input)` → `getAsDbForWorkspace(input.workspaceId)`; `getWorkspaceEmbeddingDefault(workspaceId)` → `getAsDbForWorkspace(workspaceId)`; `upsertWorkspaceEmbeddingDefault(input)` → `getAsDbForWorkspace(input.workspaceId)`. 8 Category B functions stay on `getAsDb()` — `getProfile(profileId)`, `listProfilesForDraft(agentDraftId, …)`, `updateProfile(input)` (input.profileId only), `getSource(sourceId)`, `listSourcesForProfile(profileId, …)`, `updateSource(input)` (input.sourceId only), `deleteSource(sourceId)`, `getPolicyForProfile(profileId)` — all scoped by id-only (workspaceId would need to be plumbed through callers via a separate per-method sub-arc, or read from the loaded row for the discovering helpers). 15-test source-scan suite at `tests/agent-studio/mr-3-eleventh-batch-rac-sources-store.test.ts` (import-shape check; parameterized Category A for the 6 migrated; parameterized Category B for the 8 unmigrated). Out of scope: Phase-2 of the shim (real region routing); Category B migrations for the 8 remaining functions; ASDB-direct seeds + boot helpers (separate sub-arc). | pending |
| #824 (PR-V1-73) | MR-3 twelfth batch — `services/rac/trace/store.ts` (6 Cat A migrations) | Twelfth migration batch off the MR-3 Phase-1 shim (#794). `services/rac/trace/store.ts` has 8 DB-touching functions; 6 Category A migrations land in this batch: `writeTrace(input)` → `getAsDbForWorkspace(input.workspaceId)`; `updateTraceScores(input)` → `getAsDbForWorkspace(input.workspaceId)`; `getTraceById(workspaceId, traceId)` → `getAsDbForWorkspace(workspaceId)`; `getTraceForMessage(workspaceId, messageId)` → `getAsDbForWorkspace(workspaceId)`; `recordFeedback(input)` → `getAsDbForWorkspace(input.workspaceId)`; `getFeedbackForMessage(workspaceId, messageId)` → `getAsDbForWorkspace(workspaceId)`. 2 Category B functions stay on `getAsDb()` — `writeContextBlocks(rows[])` (workspaceId is in each row but the array-of-rows shape makes single-region routing ambiguous; defer to rows[0].workspaceId Phase-2 or a separate Category B sub-arc) and `listContextBlocks(traceId)` (traceId-scoped). 9-test source-scan suite at `tests/agent-studio/mr-3-twelfth-batch-rac-trace-store.test.ts` (import-shape check; parameterized Category A for the 6 migrated; parameterized Category B for the 2 unmigrated). Out of scope: Phase-2 of the shim (real region routing); Category B migrations for `writeContextBlocks`/`listContextBlocks`; remaining `services/rac/ingestion/knowledge-unit-adapter.ts` + `services/export-catalog-lookups.ts` migrations (separate sub-arc). | pending |
| #825 (PR-V1-74) | MR-3 thirteenth batch — `services/rac/ingestion/knowledge-unit-adapter.ts` | Thirteenth migration batch off the MR-3 Phase-1 shim (#794). The file is an object-literal `RacIngestionAdapter` export with two methods; the Cat-A `search(input)` method migrates to `getAsDbForWorkspace(input.workspaceId)` (RacRetrievalRequest.workspaceId required). The `health()` method stays on `getAsDb()` — Category B-ish: a workspace-agnostic ASDB availability check whose signature cannot accept workspaceId without changing the `RacIngestionAdapter` interface (broader contract change deferred). 3-test object-literal-aware source-scan at `tests/agent-studio/mr-3-thirteenth-batch-knowledge-unit-adapter.test.ts` (import-shape check; `search()` body slice asserting `getAsDbForWorkspace(input.workspaceId)`; `health()` body slice still on `getAsDb()`). Object-literal slicer uses `async METHOD(` header positions instead of the top-level `extractFunctionBody` regex. Out of scope: Phase-2 of the shim (real region routing); broadening the `RacIngestionAdapter.health()` signature to accept workspaceId; remaining `services/export-catalog-lookups.ts` migrations (separate sub-arc — 4 call sites). | pending |
| #826 (PR-V1-75) | MR-3 fourteenth batch — `api/kb-router.ts` (FIRST Category D router migration) | First Category D (tRPC router) batch off the MR-3 Phase-1 shim (#794). `api/kb-router.ts` uses a private `asdb()` helper to centralize the ASDB lookup + ASDB-unavailable TRPCError. This batch widens the helper signature from `asdb()` to `asdb(workspaceId: number)` so every kb-router procedure routes through `getAsDbForWorkspace(workspaceId)`. Every procedure already takes `input.workspaceId` via `workspaceRefSchema` — no caller-shape changes; each `const db = asdb()` becomes `const db = asdb(input.workspaceId)` (6 procedures: `listUnits`, `getUnit`, `listProvenance`, `getValidation`, `archiveUnit`, plus one more). Drops the bare `getAsDb` import — the file is 100% migrated. Phase-1 shim still delegates to `getAsDb()` so single-region behavior is preserved bit-for-bit. 6-test source-scan suite at `tests/agent-studio/mr-3-fourteenth-batch-kb-router.test.ts` (import-shape check + bare-`getAsDb`-absent assertion; helper signature; helper body `getAsDbForWorkspace(workspaceId)`; no zero-arg `asdb()` call sites; ≥6 `asdb(input.workspaceId)` call sites). Out of scope: Phase-2 of the shim (real region routing); other tRPC routers — `api/router.ts` (1 call site), `api/tool-approvals-router.ts` (1 call site), `api/tool-knowledge-router.ts` (1 call site), `services/graph-quality/router.ts` (variable) — each its own follow-up sub-arc. | pending |
| #827 (PR-V1-76) | MR-3 fifteenth batch — `api/tool-knowledge-router.ts` (second Cat D) | Second Category D (tRPC router) batch off the MR-3 Phase-1 shim (#794). Same shape as the #826 kb-router migration: `api/tool-knowledge-router.ts` already takes `workspaceId` on every procedure via `workspaceRefSchema`. Widens the private `asdb()` helper to `asdb(workspaceId: number)` calling `getAsDbForWorkspace(workspaceId)`. Both procedures (`listTools` + `getTool`) call `asdb(input.workspaceId)`. Drops the bare `getAsDb` import — file is 100% migrated. Phase-1 shim still delegates to `getAsDb()` so single-region behavior is preserved bit-for-bit. 6-test source-scan suite at `tests/agent-studio/mr-3-fifteenth-batch-tool-knowledge-router.test.ts` (parallel structure to #826's). Out of scope: Phase-2 of the shim; remaining Cat D routers — `api/router.ts`, `api/tool-approvals-router.ts` (needs workspaceId plumbing via lookup helpers; deferred), `services/graph-quality/router.ts`. | pending |
| #828 (PR-V1-77) | MR-3 sixteenth batch — canvas lazy-loader migrations (reader + sink-asdb) | Lazy-loader shape (third Cat-D-shaped migration off the MR-3 Phase-1 shim #794): both `services/canvas/projection-events-reader.ts` (#807) and `services/canvas/projection-events-sink-asdb.ts` (#805) carry a `loadDefaultGetDb()` async helper that the call site invokes once per drain/sink invocation (instead of a top-level `getAsDb()` call site). Both are renamed to `loadDefaultGetDbForWorkspace()` and now resolve `mod.getAsDbForWorkspace`. Call sites pass the workspaceId — reader threads `input.workspaceId` directly; sink threads the locally-resolved `workspaceId` (the resolver helper that LOOKS UP workspaceId from `agsCanvases`/`agsVaults` legitimately stays on `getAsDb()` — that's the bootstrapping lookup before workspaceId is known; intentional Category C exception, documented in the renamed helper's comment). Both files preserve the nullary `options.getDb` test seam so the existing 14-test reader suite + 13-test sink-asdb suite stay green (all 27 tests verified locally). Phase-1 shim still delegates to `getAsDb()` so single-region behavior is preserved bit-for-bit. 8-test source-scan at `tests/agent-studio/mr-3-sixteenth-batch-projection-events-reader.test.ts` (5 reader + 3 sink-asdb + Category C resolver-helper exception assertion). Out of scope: Phase-2 of the shim; broadening the resolver-helper Category C exception into a fully-routed lookup (would require a registry of `vaultId → workspaceId` cache in single-region); remaining canvas-service migrations (Cat B). | pending |
| #829 (PR-V1-78) | MR-3 seventeenth batch — `writeContextBlocks` Cat B→Cat A via rows[0].workspaceId | First "discovering-helper" pattern migration off the MR-3 Phase-1 shim (#794). `writeContextBlocks(rows[])` in `services/rac/trace/store.ts` previously stayed on `getAsDb()` (#824 deferred it as Cat B because the array-of-rows shape made single-region routing ambiguous). This batch promotes it to Cat A by routing through `rows[0].workspaceId` — relying on the documented invariant: every row in a single `writeContextBlocks` call shares a parent trace, and traces are workspace-scoped, so all rows in one call go to the same workspace. The Phase-1 shim still delegates to `getAsDb()` so behavior is bit-for-bit identical in single-region; Phase-2 routes correctly because the invariant holds. The earlier-shipping #824 ledger asserted `writeContextBlocks` was Cat B; the #824 source-scan test is amended to drop the `writeContextBlocks` Cat B assertion (note added pointing at the new test). Sibling `listContextBlocks(traceId)` stays Cat B. 3-test source-scan at `tests/agent-studio/mr-3-seventeenth-batch-context-blocks.test.ts` (rows[0].workspaceId routing assertion + early-return-precedes-DB-call ordering invariant + listContextBlocks Cat B assertion). Out of scope: Phase-2 of the shim; Cat B migration for `listContextBlocks` (would need workspaceId plumbed via the trace lookup). | pending |
| #830 (PR-V1-79) | 17-γ env-flag-gated default drain forwarder installer (log mode) + boot Step 3.30 | Closes the "operator-supplied forwarder" gap surfaced in #812 + #818. The #811 scheduler ticks the drain on a fixed cadence, but a tick with no installed forwarder is a no-op + WARN — operators had to wire their own forwarder. New `server/agent-studio/services/canvas/install-default-projection-events-drain-forwarder.ts` exports `maybeInstallDefaultCanvasProjectionEventsDrainForwarder({env?, install?})` with a closed mode taxonomy `CANVAS_PROJECTION_EVENTS_DRAIN_FORWARDER_MODES = ["log"]`. `AGS_CANVAS_PROJECTION_EVENTS_DRAIN_FORWARDER=log` installs a log-only forwarder that emits one structured `[ags-canvas-projection-events-drain] event kind=… payload=…` line per projected event — useful for smoke-tests + phased rollouts: operators flip the flag, observe events flow end-to-end (sink → DB → drain → forwarder), and graduate to a real downstream consumer once verified. Anything else / unset → no-op (default OFF — preserves existing operator-supplied semantics + WARN). Composes with #818's `createProjectingCanvasProjectionEventForwarder({dispatch})` factory; no `ProjectionSyncWorker` / `GraphRepository` imports (future `sync-worker` mode lands in a sibling installer under `services/graph/projection/` to keep the canvas/** boundary clean). New boot Step 3.30 in `server/agent-studio/boot.ts` calls the installer + logs the decision with `mode` in the installed-case message; wrapped in try/catch so a malformed env never crashes boot. 15-test JSDOM suite at `tests/agent-studio/canvas-install-default-projection-events-drain-forwarder.test.ts` (closed taxonomy + strict-equality env matrix incl. `LOG`/whitespace/`sync-worker` rejections + log-mode forwarder dispatch behavior with `console.log` spy asserting the kind + canvasNodeId fields appear + boot Step 3.30 source-scan + try/catch structural assertion + installed-mode log shape). Out of scope: `sync-worker` mode (separate sub-arc — requires Phase-7 graph backend decision factory in scope); cron'd ProjectionSyncWorker handoff. | pending |
| #831 (PR-V1-80) | MR-3 eighteenth batch — `cag/store.ts::markPackStale` discovering-helper (split-handle Cat B→A) | Second discovering-helper pattern after #829's `writeContextBlocks` — but with a NEW shape: the **split-handle** pattern for SELECT-then-UPDATE mutations where workspaceId isn't on the input. `markPackStale(packId, reason, actorId)` in `services/cag/store.ts` was Cat B per #820 (packId-scoped). This batch promotes it to Cat A by splitting the DB access into two handles: (1) `lookupDb = getAsDb()` for the SELECT-by-id (workspaceId isn't known yet — intentional Category C bootstrap lookup, documented inline); (2) `db = getAsDbForWorkspace(row.workspaceId)` for the UPDATE AFTER the row is loaded. In single-region Phase-1 both handles delegate to the same `getAsDb()` (bit-for-bit identical); in Phase-2 the UPDATE correctly routes to the workspace-region pool. The #820 ledger asserted `markPackStale` was Cat B; the #820 source-scan test is amended to drop the `markPackStale` Cat B assertion (note added pointing at the new test). Read-only siblings (`getLatestPack`, `listPacks`, `markPackUsed`, `recordPackTokenActual`) stay on the existing pattern — read DB is single-region anyway, and the cost of an extra lookup-then-route pass isn't worth it for read-only paths. 4-test source-scan at `tests/agent-studio/mr-3-eighteenth-batch-cag-discovering-helpers.test.ts` (two-handle assertion + SELECT precedes routed handle + UPDATE uses routed handle + read-only siblings still single-handle). Out of scope: Phase-2 of the shim; same-pattern promotion for the 4 read-only siblings (deferred — marginal value); Cat B migrations for other packId-scoped mutations across the codebase (separate sub-arc per function). | pending |
| #832 (PR-V1-81) | 18-γ observability-only lane hooks (FIRST concrete contracted impl) | First concrete implementation of the typed contract surface pinned in PR-V1-42 (#793). New `server/agent-studio/services/extensions/lane-hooks-observability.ts` exports `createObservabilityLaneHooks({log?})` returning `{retrieve, assemble, compose}` typed as `ContractedLaneHookFn<lane>`. Each hook returns `{ succeeded: true }` WITHOUT contributing supplemental data — zero RAC pipeline impact — and emits one structured `[ags-extensions-lane-observability] lane=… extension=#… extensionKey=… workspaceId=… toolName=…` line per invocation. Best-effort log: a throwing log sink is caught and never propagated to the wrapper (a misbehaving log line MUST NOT break the `invokeFromExtension` path). Sibling `installObservabilityLaneHooks({register?, log?})` iterates the closed `CONTRACTED_EXTENSION_LANES` taxonomy in declaration order and calls `registerLaneHook` for each non-tool lane (the `tool` lane is intentionally excluded — `dispatchMcpToolCall` is the only tool execution path per CLAUDE.md). The `register` + `log` test seams keep the install path testable without touching production state. Smoke-tests the wire-up end-to-end: `invokeFromExtension` → lane-hooks registry → hook executes → ledger row written. Establishes the contract-conformance pattern for future retrieval / assembly / compose contributors. 11-test JSDOM suite at `tests/agent-studio/extension-lane-hooks-observability.test.ts` (3 zero-contribution outcomes + log-sink shape with all 5 fields + toolName="(none)" fallback + throwing-log swallow + default `console.log` fallback + installer-call-order + each-registered-hook-produces-contract-conforming-outcome + source-scan boundary asserting no credential-resolver / *_API_KEY / dispatchMcpToolCall / neo4j-driver / openrouter / GraphRepository imports + type-only contracts.ts import assertion). Out of scope: actual retrieve / assemble / compose contributors with non-empty supplemental data (each its own follow-up PR with RAC + CAG pipeline integration); boot-time installer wire-up (separate PR — operators currently call `installObservabilityLaneHooks()` from their own bootstrap if they want observability). | pending |
| #833 (PR-V1-82) | 18-γ env-flag-gated observability lane hooks boot install (Step 3.31) | Closes the boot wire-up gap from #832. New `server/agent-studio/services/extensions/install-default-observability-lane-hooks.ts` exports `maybeInstallDefaultObservabilityLaneHooks({env?, log?, install?})` that strict-equality-checks `AGS_EXTENSIONS_LANE_HOOKS_OBSERVABILITY === "on"` and calls `installObservabilityLaneHooks({log})` from #832 when matched. Anything else / unset → no-op (default OFF; the α "assert + ledger" fallback runs unchanged). Test seams: `env` (test-injected map), `log` (forwarded to the underlying installer), `install` (override the installer hook so tests can capture call shape without touching the real registry). New boot Step 3.31 in `server/agent-studio/boot.ts` calls the installer + logs the decision with `lanes=…` joined from `result.installedLanes` in the installed-case message; wrapped in try/catch so a malformed env never crashes boot. 15-test source-scan + behavior suite at `tests/agent-studio/extension-install-default-observability-lane-hooks.test.ts` (closed env-equality matrix incl. `ON`/whitespace/`true`/`1`/`yes`/`off` rejections + installed-case lane forwarding + log-option forwarding + boot Step 3.31 source-scan with try/catch structural assertion + installed-message shape assertion). Out of scope: same env-flag wiring for the future non-trivial retrieve / assemble / compose contributors (each will be its own PR with its own env flag, or share `AGS_EXTENSIONS_LANE_HOOKS=…` if a taxonomy emerges). | pending |
| #834 (PR-V1-83) | 15-δ VaultAttachmentsPage + AgentStudioShell URL wiring | Closes the named "AgentStudioShell view wiring for VaultAttachmentsAdminSurface" follow-up — completes the 15-δ slice end-to-end at the operator URL boundary. New `client/src/modules/agent-studio/pages/VaultAttachmentsPage.tsx` is a global Agent Studio page (no agentId context, mirrors `GraphWorkspacePage`) reachable at `/agent-studio/vault-attachments`. The page calls `trpc.agentStudio.vault.listMyVaults.useQuery()` to populate a `<select>` vault picker (4 query-state branches: loading / error / empty / populated), auto-selects the first vault on load, then renders `<VaultAttachmentsAdminSurface vaultId>` from #817 below the picker. `AgentStudioShell.tsx` is wired in three places: (1) lazy import `VaultAttachmentsPage`; (2) URL parser path-prefix match for `/agent-studio/vault-attachments` → `view="vault-attachments"`; (3) global-view switch case mounting `<VaultAttachmentsPage />`. 9-test JSDOM suite at `client/src/modules/agent-studio/pages/VaultAttachmentsPage.test.tsx` (3 picker query-states + auto-select-first-vault + picker-change-updates-surface + dropdown-label-shape) + 3-test Shell wiring source-scan at `tests/agent-studio/vault-attachments-shell-wiring.test.ts` (lazy import + URL-parser path-prefix + global-switch case-mount). Out of scope: a sidebar entry under a "Vaults" group (sidebar visibility for the page can land in a follow-up; `graph-workspace` ships URL-only too); pre-selected-vault deep linking (`/agent-studio/vault-attachments/:vaultId`); mutation/upload UX. | pending |
| #835 (PR-V1-84) | 15-δ VaultAttachments sidebar entry + navigation handler routing | Closes the "sidebar visibility" follow-up surfaced in #834. Adds `"vault-attachments"` to the `AgentStudioView` enum and declares a new `Vaults` `SectionGroup` in `HOME_GROUPS` with a `{key: "vault-attachments", label: "Attachments", icon: Folder}` entry. The Shell's `handleNavigate` adds a routing case so clicking the sidebar entry navigates to `/agent-studio/vault-attachments` regardless of the current agent context (mirrors `mcp-manager` / `catalog-skills` / `marketplace` patterns). 4-test source-scan at `tests/agent-studio/vault-attachments-sidebar-wiring.test.ts` (view-union extension assertion + Vaults group + Folder icon import + handleNavigate routing). With #834 + this PR, the 15-δ slice is fully reachable end-to-end via both URL bar and sidebar click. Out of scope: deep-linking via URL params, sidebar grouping under existing Studio/Catalog groups (kept separate so the "Vaults" surface can expand with more entries — saved views, links, etc.). | pending |
| #836 (PR-V1-85) | 16-δ VaultSavedViewsPage + Shell + Sidebar wiring | Mirror of the 15-δ #834+#835 pattern for the 16-δ slice — completes the SavedViewVersionHistoryPanel (#795) end-to-end at the operator URL boundary. New `client/src/modules/agent-studio/pages/VaultSavedViewsPage.tsx` is a global Agent Studio page reachable at `/agent-studio/vault-saved-views`. Two-picker shape: vault picker (driven by `listMyVaults`) → saved-view picker (driven by `listVisibleSavedViews` for the selected vault) → renders `<SavedViewVersionHistoryPanel savedViewId={…} />` from #795. The two pickers auto-default to first row each; changing the vault resets the saved-view selection. `AgentStudioShell` wired in three places (lazy import + URL parser + global-view case) + `handleNavigate` routing case (parallels #834). `AgentStudioSidebar` extends the existing `Vaults` group with a `{key: "vault-saved-views", label: "Saved Views", icon: GitBranch}` entry (joins #835's `Attachments` entry under the same group). 9-test JSDOM page suite at `VaultSavedViewsPage.test.tsx` (3 vault-picker states + 3 saved-view picker states + 3 populated end-to-end variants: auto-select-both + saved-view-change-reroutes-query + vault-change-resets-selection) + 7-test source-scan at `tests/agent-studio/vault-saved-views-shell-sidebar-wiring.test.ts` (Shell lazy import + URL prefix + global-view case + handleNavigate routing; Sidebar view-union extension + Vaults group entry + GitBranch icon). With this PR the 16-δ slice is fully reachable end-to-end via both URL bar and sidebar click. Out of scope: pre-selected-vault/saved-view deep linking via URL params; restore-version / compare-versions actions (would need additional tRPC procedures). | pending |
| #837 (PR-V1-86) | 17-γ CanvasProjectionEventsDrainPage + Shell + Sidebar wiring | Same shape as #834+#836 for the 17-γ slice — completes the CanvasProjectionEventsDrainStatusPanel (#814) end-to-end at the operator URL boundary. New `client/src/modules/agent-studio/pages/CanvasProjectionEventsDrainPage.tsx` is a global Agent Studio page reachable at `/agent-studio/canvas-projection-events-drain`. Single panel mount (no picker — the drain is process-local and cross-workspace). `AgentStudioShell` wired in 4 places (lazy import + URL parser + global-view case + handleNavigate routing). `AgentStudioSidebar` adds a new `Canvas` `SectionGroup` with a `{key: "canvas-projection-events-drain", label: "Projection Drain", icon: Activity}` entry. 3-test JSDOM page suite at `CanvasProjectionEventsDrainPage.test.tsx` (populated panel mount + loading branch + error branch) + 6-test source-scan at `tests/agent-studio/canvas-projection-events-drain-shell-sidebar-wiring.test.ts` (Shell lazy import + URL prefix + global-view case + handleNavigate routing; Sidebar view-union extension + Canvas group entry + Activity icon). With this PR the 17-γ persistence loop is fully observable via both URL bar and sidebar click. Out of scope: cron-status panel composition (separate sub-arc); operator-trigger UI to manually invoke `tickCanvasProjectionEventsDrain()`; histogram of drain durations. | pending |
| #838 (PR-V1-87) | MR-3 nineteenth batch — `cag/store.ts` update-only helpers Cat B→A via split-handle | Extends the #831 split-handle pattern to UPDATE-only mutations that don't already have a SELECT in the body. `markPackUsed(packId)` and `recordPackTokenActual(packId, tokensActual)` were Cat B per #820 (packId-scoped). This batch promotes both to Cat A by adding a SELECT-by-id round-trip purely to discover the workspaceId, then routing the UPDATE via `getAsDbForWorkspace(lookup[0].workspaceId)`. Same Cat C bootstrap exception: the discovery SELECT legitimately uses `getAsDb()`. Trade-off: one extra round-trip per call vs. correct Phase-2 region routing — acceptable for these observability writeback paths (useCount + token-actuals) where multi-region correctness matters more than nanosecond latency. The #820 source-scan test is amended to drop both names from the Cat B array (note added pointing at this batch's test). After this PR the only remaining Cat B helpers in cag/store.ts are the read-only `getLatestPack` + `listPacks` siblings — intentionally kept on the single-handle pattern (no UPDATE/DELETE means no routing benefit from a split). 4-test source-scan at `tests/agent-studio/mr-3-nineteenth-batch-cag-update-only-helpers.test.ts` (markPackUsed two-handle assertion + SELECT-precedes-UPDATE ordering + recordPackTokenActual two-handle assertion + #820 CATEGORY_B array body source-scan asserting the two names are absent from the array). Out of scope: Phase-2 of the shim; widening to Cat B helpers in other files (separate per-function sub-arcs). | pending |
| #839 (PR-V1-88) | MR-3 twentieth batch — `rac/sources/store.ts` mutation split-handles (3 fns Cat B→A) | Applies the #831 / #838 split-handle pattern to three id-scoped mutations in `services/rac/sources/store.ts`: `updateProfile(input)` (input.profileId only), `updateSource(input)` (input.sourceId only), `deleteSource(sourceId)`. Each adds a SELECT-by-id round-trip that pulls just the `workspaceId` column, then routes the UPDATE/DELETE through `getAsDbForWorkspace(lookup[0].workspaceId)`. Cat C bootstrap on the discovery SELECT (workspaceId not yet known). The #823 source-scan test is amended to drop the three names from its Cat B array (note added pointing at this batch's test). After this PR the remaining Cat B helpers in rac/sources/store.ts are: `getProfile`, `listProfilesForDraft`, `getSource`, `listSourcesForProfile`, `getPolicyForProfile` — all read-only (intentionally kept on the single-handle pattern). 7-test source-scan at `tests/agent-studio/mr-3-twentieth-batch-rac-sources-mutations.test.ts` (parameterized two-handle + SELECT-precedes-mutation ordering for the 3 mutations + #823 CATEGORY_B array body source-scan). Out of scope: Phase-2 of the shim; Cat B promotion for the 5 remaining read-only helpers (no routing benefit; keep simple). | pending |
| #840 (PR-V1-89) | MR-3 twenty-first batch — `extensions/manifest.ts` mutation split-handles (2 fns Cat B→A) | Applies the split-handle pattern to two id-scoped mutations in `services/extensions/manifest.ts`: `approveExtension(input)` (input.extensionId only) + `setExtensionStatus(extensionId, status, actorUserId?)`. Each adds a SELECT-by-id pulling just `workspaceId`, then routes the UPDATE through `getAsDbForWorkspace(lookup[0].workspaceId)`. Throws the same `ExtensionNotFoundError` if the lookup returns no row (preserves existing error contract). `getExtensionById(extensionId)` stays Cat B — read-only; marginal discovery-SELECT cost outweighs routing benefit. 5-test source-scan at `tests/agent-studio/mr-3-twentyfirst-batch-extensions-mutations.test.ts` (parameterized two-handle + SELECT-precedes-UPDATE ordering for the 2 mutations + getExtensionById Cat B assertion). After this PR the remaining Cat B in extensions/manifest.ts is just `getExtensionById` (read-only, intentional). Out of scope: Phase-2 of the shim. | pending |
| #841 (PR-V1-90) | MR-3 twenty-second batch — `runtime/trace-writer.ts::patchRacRuntimeTrace` Cat B→A via split-handle | Promotes the final non-trivial Cat B mutation in `services/runtime/trace-writer.ts` to Cat A via the split-handle pattern. The H4-c7 empty-patch early-return (`if (Object.keys(norm).length === 0) return`) is preserved BEFORE the discovery SELECT — so no-op patches do not pay the round-trip cost. After the early-return, a SELECT-by-id pulls just `workspaceId` from `agsRacRuntimeTraces`, and the UPDATE is routed via `getAsDbForWorkspace(lookup[0].workspaceId)`. A `lookup.length === 0` fast-return preserves the prior soft-fail behavior for vanished traceIds. Cat C bootstrap exception applies to the discovery SELECT (workspaceId not yet known). Amends the #821 ninth-batch assertion (previously "patchRacRuntimeTrace is Category B") — weakened to "still references getAsDb()" (true via the discovery SELECT) with a comment block pointing at this batch's test for the up-to-date two-handle assertions. 3-test source-scan at `tests/agent-studio/mr-3-twentysecond-batch-trace-writer-patch.test.ts` (two-handle assertion + H4-c7-precedes-SELECT ordering invariant + #821 amendment bookkeeping assertion). After this PR the only remaining Cat B in trace-writer.ts is `recordToolCallTrace` (already Cat A per #821) and read-only fetch helpers. Out of scope: Phase-2 of the shim. | pending |
| #842 (PR-V1-91) | MR-3 twenty-third batch — `ingestion/ingestion-job-service.ts::completeJob` Cat B→A via split-handle | Promotes the Cat B `completeJob(input)` UPDATE in `services/ingestion/ingestion-job-service.ts` (jobId-scoped per #806) to Cat A via split-handle. SELECT-by-id pulls `workspaceId` from `agsIngestionJobs`, UPDATE routes through `getAsDbForWorkspace(lookup[0].workspaceId)`. Soft-fail on vanished jobIds is preserved via a `lookup.length === 0` early return (`return;`, not `throw`) — completion is observability, not source of truth, so a no-op for a vanished job row matches the pre-existing semantics. Amends the #806 third-batch test: the `it.each(["completeJob", "getJob"])` Cat B assertion is split into a weakened `completeJob still references getAsDb()` assertion (true via discovery SELECT) + a standalone `getJob` Cat B assertion (`getJob` stays read-only Cat B; intentional). 4-test source-scan at `tests/agent-studio/mr-3-twentythird-batch-ingestion-job-complete.test.ts` (two-handle assertion + SELECT-precedes-UPDATE ordering + soft-return-on-empty-lookup invariant + #806 amendment bookkeeping assertion). Out of scope: Phase-2 of the shim. | pending |
| #843 (PR-V1-92) | MR-3 twenty-fourth batch — `ingestion/knowledge-unit-service.ts::archiveUnit` Cat B→A via split-handle | Promotes the Cat B `archiveUnit(unitId)` UPDATE in `services/ingestion/knowledge-unit-service.ts` (unitId-scoped per #807) to Cat A via split-handle. SELECT-by-id pulls `workspaceId` from `agsKnowledgeUnits`, UPDATE routes through `getAsDbForWorkspace(lookup[0].workspaceId)`. Soft-return on vanished unitIds preserved via `lookup.length === 0` early return — archival is idempotent, no point throwing if the unit row is gone. Amends the #807 fourth-batch test (weakened to "still references getAsDb()") with a comment block pointing at this batch's test. 4-test source-scan at `tests/agent-studio/mr-3-twentyfourth-batch-knowledge-unit-archive.test.ts` (two-handle assertion + SELECT-precedes-UPDATE ordering + soft-return-on-empty-lookup invariant + #807 amendment bookkeeping assertion). After this PR the only remaining call to `getAsDb()` in knowledge-unit-service.ts is the read-only fetch path — intentional Cat B. Out of scope: Phase-2 of the shim. | pending |
| #844 (PR-V1-93) | MR-3 twenty-fifth batch — `canvas/canvas-service.ts::createCanvas` Cat B→A via vault→workspace split-handle | Promotes the Cat B `createCanvas(input)` INSERT in `services/canvas/canvas-service.ts` (vaultId-scoped) to Cat A via a cross-table split-handle pattern. The Canvas table itself doesn't carry workspaceId, but the parent vault does (`agsVaults.workspaceId`). Discovery SELECT pulls vault.workspaceId for `input.vaultId`, then INSERT routes through `getAsDbForWorkspace(workspaceId)`. When the vault row is missing OR its `workspaceId` column is null (legacy rows pre-backfill), the routed handle falls back to the default `lookupDb` (`getAsDb()`) so the FK constraint on the INSERT surfaces the missing-vault error to the caller — preserves the pre-migration behavior for the unhappy path. Adds the `agsVaults` import alongside the existing canvas table imports. Sibling mutations (`createCanvasNode`, `createCanvasEdge` — both canvasId-scoped requiring a deeper canvas→vault→workspace JOIN) stay Cat B for a separate per-function follow-up batch. Read helpers (`getCanvasById`, `listCanvasesByVault`, `getCanvasSnapshot`, `listNoteReferencesForCanvas`) stay on the existing single-handle pattern — intentional Cat B for read-only paths. 5-test source-scan at `tests/agent-studio/mr-3-twentyfifth-batch-canvas-create.test.ts` (import-shape check + agsVaults import + select-then-route-via-getAsDbForWorkspace + SELECT-precedes-INSERT ordering + `workspaceId != null ? routed : lookupDb` fallback assertion). The 14-test behavior suite at `tests/agent-studio/canvas-service.test.ts` continues to pass — the existing "ASDB unavailable" early-throw fires before the new lookup logic. Out of scope: Phase-2 of the shim; Cat B→A promotion for `createCanvasNode` + `createCanvasEdge` (canvas→vault→workspace JOIN — separate batch). | pending |
| #845 (PR-V1-94) | MR-3 twenty-sixth batch — `canvas/canvas-service.ts::createCanvasNode` + `createCanvasEdge` Cat B→A via canvas→vault→workspace JOIN | Promotes the two remaining Cat B INSERT mutations in `services/canvas/canvas-service.ts` to Cat A via a **deeper cross-table** split-handle. Both `createCanvasNode(input)` + `createCanvasEdge(input)` take only `input.canvasId`; to discover workspaceId we JOIN agsCanvases × agsVaults on `vaultId`, then read `agsVaults.workspaceId`. The JOIN is factored into a private `resolveWorkspaceIdForCanvas(lookupDb, canvasId)` helper so both mutations share the same shape — mirrors the existing `defaultResolveWorkspaceId` pattern in `projection-events-sink-asdb.ts` (#828). When the chain returns null (canvas missing OR vault.workspaceId IS NULL), the caller falls back to the default `lookupDb` so the FK constraint surfaces the missing-canvas error. With this batch, ALL mutations in `canvas-service.ts` are Cat A — only the read helpers (`getCanvasById`, `listCanvasesByVault`, `getCanvasSnapshot`, `listNoteReferencesForCanvas`) remain on the single-handle pattern, intentionally. 6-test source-scan at `tests/agent-studio/mr-3-twentysixth-batch-canvas-node-edge.test.ts` (helper-exists + JOIN-shape assertion + parameterized resolver-usage + parameterized SELECT-precedes-INSERT ordering for both mutations). The 14-test behavior suite at `tests/agent-studio/canvas-service.test.ts` continues to pass (early-throw on ASDB-unavailable preserved). Out of scope: Phase-2 of the shim; pulling the resolver helper out into a shared `services/canvas/workspace-resolver.ts` (cross-module DRY is deferred — duplication is currently 2 instances, both in canvas/**, threshold for extraction not yet hit). | pending |
| #846 (PR-V1-95) | MR-3 twenty-seventh batch — `workspace-default-bindings.ts` 4 fns Cat B→A (clean Cat A migration) | All four functions in `server/agent-studio/workspace-default-bindings.ts` take `workspaceId` directly on the input (the file's entire purpose is workspace-scoped bindings — no Cat B ambiguity). `resolveWorkspaceDefaultBinding(input)`, `upsertWorkspaceDefaultBinding(input)`, `deleteWorkspaceDefaultBinding(input)`, `listWorkspaceDefaultBindings(workspaceId)` — each `getAsDb()` site rewrites to `getAsDbForWorkspace(workspaceId)`. The bare `getAsDb` import is dropped — the file is 100% migrated. Phase-1 shim still delegates to `getAsDb()` so single-region behavior is bit-for-bit identical. This file was not enumerated in any prior MR-3 batch — it pre-dates the inventory split. 6-test source-scan at `tests/agent-studio/mr-3-twentyseventh-batch-workspace-default-bindings.test.ts` (import-shape check with bare-`getAsDb`-absent assertion + parameterized `getAsDbForWorkspace(<input>)` for each of the 4 functions). Out of scope: Phase-2 of the shim. | pending |
| #847 (PR-V1-96) | MR-3 twenty-eighth batch — `bindings.ts::upsertAgentProviderBinding` Cat B→A | Promotes the Cat B `upsertAgentProviderBinding(input)` mutation in `server/agent-studio/bindings.ts` to Cat A. Input already carries `workspaceId` (mandatory via `UpsertAgentProviderBindingInput`); migration is a clean call-site rewrite from `getAsDb()` to `getAsDbForWorkspace(input.workspaceId)`. The eligibility-gate path BEFORE the DB handle is unchanged (preserves the "must check eligibility before persisting `binding_v1`" Phase 11 contract from the file's top-of-file doc-block). Sibling read+mutation helpers stay Cat B by intent — `getAgentProviderBinding(draftId, role)`, `listBindingsForAgent(agentId)`, `removeAgentProviderBinding(draftId, role)`, `validateBindingPolicy(draftId, role, ...)`, `refreshBindingValidation(draftId, role)` — all draftId/agentId-scoped (would need a JOIN to `agsAgentDrafts`→`agsAgents.workspaceId` for routing benefit; deferred to a separate per-function sub-arc). 5-test source-scan at `tests/agent-studio/mr-3-twentyeighth-batch-upsert-agent-binding.test.ts` (import-shape check + upsertAgentProviderBinding two-handle-via-routed-only assertion + parameterized Cat B assertion for the 3 read helpers). Out of scope: Phase-2 of the shim; Cat B→A promotion for the read helpers / `validateBindingPolicy` UPDATE / draft-scoped mutations (separate per-function sub-arc with draftId→workspaceId resolver). | pending |
| #848 (PR-V1-97) | MR-3 twenty-ninth batch — `bindings.ts::validateBindingPolicy` UPDATE-on-refresh Cat B→A | Promotes the refresh-branch UPDATE in `bindings.ts::validateBindingPolicy` to Cat A using the already-loaded `binding.workspaceId`. The function calls `getAgentProviderBinding(draftId, role)` early, which returns the binding row including `workspaceId` (a non-null column on `agsAgentProviderBindings`). Routes the subsequent UPDATE through `getAsDbForWorkspace(binding.workspaceId)` — no extra round-trip, no Cat C bootstrap SELECT, because workspaceId was already loaded. The function's earlier read path through `getAgentProviderBinding` still uses bare `getAsDb()` (separate sub-arc, draftId-scoped Cat B per #847). 3-test source-scan at `tests/agent-studio/mr-3-twentyninth-batch-validate-binding-update.test.ts` (import-shape check + refresh-branch UPDATE routes via `getAsDbForWorkspace(binding.workspaceId)` + SELECT-precedes-UPDATE ordering). Out of scope: Phase-2 of the shim; Cat B→A for the read-only `getAgentProviderBinding` call site (separate sub-arc). | pending |
| #849 (PR-V1-98) | MR-3 state-of-the-union doc (29-batch progress snapshot) | Ships `docs/implementation/agent-studio-mr-3-state-of-the-union-2026-05-14.md` — captures the 29 batches landed since the original first-batch plan (`agent-studio-mr-3-getasdb-inventory.md`, 2026-05-13). Sections cover: (1) chronological index of all 29 PRs (#794 → #848) with file + pattern columns, (2) files now 100% Cat A (`api/kb-router.ts`, `api/tool-knowledge-router.ts`, `workspace-default-bindings.ts`, plus 5 partially-migrated files where all mutations Cat A but read helpers stay Cat B by intent), (3) read-only Cat B paths kept intentionally (11 files listed), (4) deferred cross-table JOIN sub-arcs (4 chains: approval-gate, tool-approvals-router, export-catalog-lookups, api/router.ts:explainRetentionEligibility), (5) Cat C kept intentionally, (6) Cat D status, (7) Cat E ADR-blocked items, (8) Phase-1 shim acceptance state, (9) test coverage shape (29 source-scan suites), (10) next batches numbered 30th–34th+, (11) reference cross-links. Cross-linked from the original inventory doc's §4 reference list. 8-test structural guard at `tests/agent-studio/mr-3-state-of-the-union-doc-exists.test.ts` asserts the doc exists + references the predecessor inventory + V1+ plan + MR-1 ADR + brackets the chronological table (#794 + #831 + #848) + names the Cat A/B/C/D/E taxonomy + flags the approval-gate JOIN deferral + names Phase-2 as the work behind the call surface. Out of scope: refreshing the first-batch plan itself (the SOTU is a sibling progress snapshot; the plan stays the *contract*). | pending |
| #850 (PR-V1-99) | MR-3 thirtieth batch — `bindings.ts::removeAgentProviderBinding` Cat B→A via split-handle | Promotes the Cat B `removeAgentProviderBinding(draftId, role)` DELETE in `server/agent-studio/bindings.ts` to Cat A via split-handle. The `agsAgentProviderBindings` table carries `workspaceId` (non-null) on every row, so a discovery SELECT picks up the binding's workspaceId and the DELETE routes through `getAsDbForWorkspace(lookup[0].workspaceId)`. If no row matches `(draftId, role)`, the function returns `false` (preserves the idempotent contract — no row to delete, no row deleted; no DELETE round-trip wasted). 3-test source-scan at `tests/agent-studio/mr-3-thirtieth-batch-remove-agent-binding.test.ts` (two-handle assertion + SELECT-precedes-DELETE ordering + idempotent `return false` on empty lookup). Out of scope: Phase-2 of the shim; Cat B→A for the remaining read helpers `getAgentProviderBinding` / `listBindingsForAgent` (sibling per-function sub-arcs). | pending |
| #851 (PR-V1-100) | MR-3 thirty-first batch — `extensions/runtime.ts::recordInvocation` Cat B→A via extension→workspace lookup | Promotes the Cat B `recordInvocation` helper in `services/extensions/runtime.ts` to Cat A via split-handle. The `agsExtensionInvocations` table does NOT carry workspaceId, but the parent `agsExtensions` row does (non-null). Discovery SELECT pulls vault-parent workspaceId via the existing `getDb` test seam, then INSERT routes via `getAsDbForWorkspace(lookup[0].workspaceId)`. Preserves the observability-path soft-fall-through: when the bootstrap lookup is null (test seam `() => null`) OR the extension row vanished (`lookup.length === 0`), the function no-ops without throwing. Test seam contract preserved — tests passing `getDb: () => null` still no-op via the existing `if (!lookupDb) return;` guard at the bootstrap. 5-test source-scan at `tests/agent-studio/mr-3-thirtyfirst-batch-extensions-record-invocation.test.ts` (import-shape + two-handle assertion + SELECT-precedes-INSERT ordering + soft-return-on-empty-lookup + test-seam-preserved). Existing 31-test extension suite (`extension-lane-hooks.test.ts` + `extension-dispatcher-boundary.test.ts`) continues to pass — the test-seam fall-through semantics are preserved. Out of scope: Phase-2 of the shim; widening the test-seam getDb option to return a routed handle (current pattern keeps the bootstrap testable). | pending |
| #852 (PR-V1-101) | MR-3 thirty-second batch — `vault/repository-asdb.ts::AsdbVaultRepository.addMember` Cat B→A via vault→workspace split-handle | Promotes `AsdbVaultRepository.addMember` to Cat A via the same vault→workspace lookup pattern as #844 createCanvas. `agsVaultMembers` does not carry workspaceId; the parent `agsVaults.workspaceId` does. Discovery SELECT pulls workspaceId, INSERT routes via `getAsDbForWorkspace(workspaceId)`. Falls back to the bootstrap handle when the vault row is missing OR workspaceId IS NULL (legacy rows) so the FK constraint on the INSERT surfaces the missing-vault error to the caller. 4-test source-scan at `tests/agent-studio/mr-3-thirtysecond-batch-vault-add-member.test.ts` (import-shape + SELECT-then-route + SELECT-precedes-INSERT ordering + `workspaceId != null ? routed : lookupConn` fallback). Other vault repository methods (`createNote`, `updateNote`, `getNoteById`, etc.) stay Cat B for separate per-method follow-ups — they require the same shape but each is its own batch to keep PR scope tight. Out of scope: Phase-2 of the shim; Cat B→A for the remaining vault repository methods. | pending |
| #853 (PR-V1-102) | MR-3 thirty-third batch — `vault/repository-asdb.ts::AsdbVaultRepository.createNote` Cat B→A via vault→workspace split-handle | Promotes `AsdbVaultRepository.createNote` to Cat A. createNote does **three sequential writes** (INSERT into `agsVaultNotes`, INSERT into `agsVaultNoteVersions`, UPDATE on `agsVaultNotes.currentVersionId` to backfill the FK). All three writes route through a single `conn` handle resolved **once at the top of the function** via a single vault.workspaceId lookup — preserves atomicity in Phase-2 (no cross-region writes within the function). Falls back to bootstrap handle when vault missing or workspaceId IS NULL. 4-test source-scan at `tests/agent-studio/mr-3-thirtythird-batch-vault-create-note.test.ts` (SELECT-at-top + SELECT-precedes-INSERT + single-conn-used-for-all-three-writes + fallback). Out of scope: Phase-2 of the shim; remaining vault repository mutations (`updateNote`, `deleteNote` — each its own batch). | pending |
| #854 (PR-V1-103) | MR-3 thirty-fourth batch — `vault/repository-asdb.ts::AsdbVaultRepository.updateNote` Cat B→A via note→vault→workspace JOIN | Promotes `AsdbVaultRepository.updateNote` to Cat A via a **deeper cross-table** split-handle (note→vault→workspace). updateNote takes `input.noteId` only; workspaceId resolution JOINs `agsVaultNotes × agsVaults` on `vaultId`, then reads `agsVaults.workspaceId`. The function performs up to three writes (conflict ledger row, version INSERT, currentVersionId UPDATE) — all routed through the single `conn` resolved once at the top. The read-only `getLatestNoteVersion` call still uses bare `getAsDb()` internally (its own Cat B path; deferred). Falls back to bootstrap when note missing OR workspaceId IS NULL. 3-test source-scan at `tests/agent-studio/mr-3-thirtyfourth-batch-vault-update-note.test.ts` (JOIN-shape + resolve-precedes-write + fallback). Out of scope: Phase-2 of the shim; Cat B→A for `getLatestNoteVersion` and other read-only note helpers. | pending |
| #855 (PR-V1-104) | MR-3 thirty-fifth batch — `vault/repository-asdb.ts::AsdbVaultRepository.deleteNote` Cat B→A via widened SELECT + note→vault→workspace JOIN | Promotes `AsdbVaultRepository.deleteNote` to Cat A by **widening the pre-existing SELECT** (it already pulled `id` + `deletedAt`) to also pull `agsVaults.workspaceId` via an `innerJoin` on `agsVaultNotes.vaultId`. Zero additional round-trips vs. pre-migration — the discovery SELECT is the same SELECT that already runs for the notFound/alreadyDeleted check. The subsequent soft-delete UPDATE routes via `getAsDbForWorkspace(row.workspaceId)`. Falls back to bootstrap handle when workspaceId IS NULL. 3-test source-scan at `tests/agent-studio/mr-3-thirtyfifth-batch-vault-delete-note.test.ts` (SELECT-widening + UPDATE-routing-via-row.workspaceId + SELECT-precedes-UPDATE). Out of scope: Phase-2 of the shim; Cat B→A for `getNoteById` / `listNotesInVault` / `getNoteVersion` (read-only paths). | pending |
| #856 (PR-V1-105) | MR-3 state-of-the-union rev 2 — batches 30–35 + schema-level workspace-scoping gap diagnosis | Refreshes the SOTU doc (`agent-studio-mr-3-state-of-the-union-2026-05-14.md`) to capture the 6 batches landed 2026-05-15 (#850 → #855: removeAgentProviderBinding, recordInvocation, addMember, createNote, updateNote, deleteNote). Adds a new **§11 Schema-level workspace-scoping gap** section diagnosing why approval-gate / repository.ts / tool-approvals-router cannot Cat A migrate via Phase-1 alone — `agsAgents`, `agsAgentDrafts`, `agsRuntimeRuns`, `agsPendingPermissionRequests`, `agsRuntimePolicyEvents` all predate workspace scoping at the schema level (no `workspaceId` column). Lists which tables HAVE workspaceId (the "escape hatches" used in #844/#845/#852/#853/#854/#855 vault chains, #851 extensions chain, #831/#838 cag chains, #821/#841 trace-writer chains, #843 knowledge-unit chain, #842 ingestion-job chain) vs. which don't (the agent-tier blocked surface). Names three Phase-2 unblock paths — **Path A** (backfill workspaceId to agsAgents + migration), **Path B** (indirect resolver via `agsAgentProviderBindings.workspaceId`), **Path C** (tRPC-context plumbing) — with trade-offs but no pre-commit to one. Confirms Phase-1 caller-migration surface is >95% saturated; remaining ~5% requires the architectural decision. Doc-existence test continues to pass (8 assertions still hold). Out of scope: making the Path A/B/C decision; running the Path A migration. | pending |
| #857 (PR-V1-106) | MR-3 thirty-sixth batch — Path B primitive `services/region/draft-workspace-resolver.ts` | Ships the **Path B primitive** named in SOTU rev 2 §11. New file `server/agent-studio/services/region/draft-workspace-resolver.ts` exports `resolveWorkspaceIdForDraft(db, draftId)` — a reusable helper that derives a draft's workspaceId via the `agsAgentProviderBindings` escape hatch (the binding table carries `workspaceId` per Phase-11 contract; the agent-tier tables don't). Returns `null` for unbound drafts (legacy pre-Phase-11 or binding-pending state) — caller must fall back to the bootstrap handle. The helper takes the `db` handle as a parameter (no `getAsDb` import) — keeps it Drizzle-shape-typed without coupling to the shim and enables unit-level testing without booting ASDB. 4-test suite at `tests/agent-studio/mr-3-thirtysixth-batch-draft-workspace-resolver.test.ts` (returns-workspaceId-for-bound-draft, returns-null-for-unbound, returns-null-on-defensive-null-column, limit(1) invariant). The helper is **not yet a caller of any approval-gate / repository.ts / tool-approvals-router code path** — that consumer migration is a separate per-function follow-up batch. Shipping the primitive in advance lets the consumer batches stay tight (one-file scope each) instead of bundling helper + consumer in the same PR. Out of scope: actually using the helper to migrate approval-gate / repository.ts / tool-approvals-router (Path B consumer batches — separate). | pending |
| #858 (PR-V1-107) | MR-3 thirty-seventh batch — `services/approval/approval-gate.ts::evaluateApprovalGate` Path-B consumer Cat B→A | **First consumer** of the Path B primitive shipped in #857. The `lastUsedAt`-bump UPDATE in `evaluateApprovalGate` is the routing-worthy site (every permit re-use writes a row through the M1-c6 staleness guard). The SELECT-by-(draftId,hash) stays on the bootstrap handle (Cat C bootstrap for the resolver itself, same pattern as the split-handle batches). When `resolveWorkspaceIdForDraft` returns null (legacy draft / binding-pending), the UPDATE falls back to the bootstrap handle — preserves pre-migration semantics for unbound drafts. Dynamic import of the resolver from the function body keeps the static import block stable and avoids module-load-time cycles between approval/ and region/. 4-test source-scan at `tests/agent-studio/mr-3-thirtyseventh-batch-evaluate-approval-gate.test.ts` (import-shape + resolver-call + workspaceId-fallback + dynamic-import-shape). Existing 13-test `approval-gate.test.ts` behavior suite continues to pass — the staleness guard fast-path still runs unchanged on cold paths. Out of scope: `createApprovalRequest` / `decideApprovalRequest` Path-B consumers (next sub-arc batches); promoting `agsRuntimePolicyEvents` writes (separate Cat C decision — those rows are intentionally cross-workspace observability). | pending |
| #859 (PR-V1-108) | MR-3 thirty-eighth batch — `services/approval/approval-gate.ts::createApprovalRequest` Path-B consumer Cat B→A | Second Path B consumer (after #858). The pending-row INSERT in `createApprovalRequest` routes via the resolved workspaceId; the dedup-SELECT and the post-INSERT `agsRuntimePolicyEvents` write **stay on the bootstrap handle** (the event-row table is intentionally Cat C — cross-workspace observability — and the dedup SELECT only reads row existence). Dedup-SELECT precedes the resolver call so the idempotent fast-path (re-create returns existing row id) skips the resolver round-trip when no new row will be inserted. Falls back to bootstrap when the draft has no binding. 4-test source-scan at `tests/agent-studio/mr-3-thirtyeighth-batch-create-approval-request.test.ts` (resolver-call + insertDb-routes-INSERT + dedup-SELECT-precedes-resolver + event-INSERT-stays-on-db Cat C assertion). Existing 13-test `approval-gate.test.ts` behavior suite continues to pass. Out of scope: `decideApprovalRequest` Path-B consumer (next batch). | pending |
| #860 (PR-V1-109) | MR-3 thirty-ninth batch — `services/approval/approval-gate.ts::decideApprovalRequest` Path-B consumer Cat B→A | Third Path B consumer. `decideApprovalRequest` takes `approvalRequestId` (not `draftId`), so it needs a **two-step lookup**: short pre-UPDATE SELECT pulls `agentDraftId` from the approval row, then Path B's resolver derives workspaceId from that draftId. If the approval row doesn't exist (draftId is null) OR the draft has no binding (resolver returns null), the UPDATE falls back to the bootstrap handle — preserves pre-migration semantics. The post-UPDATE event INSERTs into `agsRuntimePolicyEvents` (both the success path AND the "already-decided" rejection-audit path) stay on bootstrap (Cat C cross-workspace observability). With this batch the approval-gate trio (#858 + #859 + #860) is **fully Path-B-migrated** — only the read-only `evaluateApprovalGate` SELECT and the audit-event INSERTs remain on the bootstrap handle, both by intent. 4-test source-scan at `tests/agent-studio/mr-3-thirtyninth-batch-decide-approval-request.test.ts` (pre-UPDATE-SELECT-shape + resolver-call-on-discovered-draftId + UPDATE-routes-via-updateDb + SELECT-precedes-UPDATE). Existing 13-test `approval-gate.test.ts` behavior suite continues to pass. Out of scope: Path B promotion for `tool-approvals-router` procedures (sibling sub-arc); promoting `agsRuntimePolicyEvents` writes (intentional Cat C). | pending |
| #861 (PR-V1-110) | MR-3 fortieth batch — `repository.ts::updateDraft` Path-B consumer Cat B→A | First Path B consumer outside `services/approval/`. The existing `getCurrentDraft(agentId)` call already returns a draft row with `id` — we reuse that draftId to feed Path B's resolver, then route the UPDATE via `getAsDbForWorkspace`. **Zero extra round-trips** vs. pre-migration: the draft lookup was already happening for the sanitization + schema-version stamping logic. Falls back to bootstrap for unbound drafts. Dynamic imports for both the Path B primitive and `getAsDbForWorkspace` (no static imports — keeps the repository's import block stable). 4-test source-scan at `tests/agent-studio/mr-3-fortieth-batch-update-draft.test.ts` (resolver-call-on-draft.id + routed-handle-with-fallback + draft-lookup-precedes-UPDATE + dynamic-import-shape). Existing 21-test `config-schema-version.test.ts` behavior suite continues to pass. Out of scope: Path B for other repository.ts mutations (`archiveAgent`, `updateAgentLifecycleState`, `attachToolBinding`, `replaceToolBindings`, etc. — each its own batch since each takes a different identifier and would route through a different resolver chain). | pending |
| #862 (PR-V1-111) | MR-3 forty-first batch — `repository.ts::attachToolBinding` Path-B consumer Cat B→A | Same Path B consumer shape as #861 but `attachToolBinding(input)` already takes `input.draftId` directly — no pre-lookup needed. INSERT into `agsDraftToolBindings` routes via `getAsDbForWorkspace(workspaceId)`. Falls back to bootstrap for unbound drafts. Dynamic imports preserved. 3-test source-scan at `tests/agent-studio/mr-3-fortyfirst-batch-attach-tool-binding.test.ts` (resolver-call + routed-INSERT + dynamic-import-shape). Out of scope: `updateToolBinding`, `removeToolBinding`, `getToolBindingById`, `replaceToolBindings`, `replaceKnowledgeBindings`, `replaceMemoryConfigs`, `replaceWorkflowGraph` (each its own batch — different identifier shapes, e.g. `bindingId` requires a 2-step lookup like #860). | pending |
| #863 (PR-V1-112) | MR-3 forty-second batch — `repository.ts::updateToolBinding` + `removeToolBinding` Path-B consumers via shared `resolveBindingRoutedConn` helper | Both mutations take `bindingId` only (not `draftId`). Introduces a private `resolveBindingRoutedConn(lookupConn, bindingId)` helper that 2-step-resolves: (1) SELECTs draftId from `agsDraftToolBindings.id = bindingId` via the inner `resolveToolBindingDraftId` helper; (2) feeds Path B's `resolveWorkspaceIdForDraft`; (3) calls `getAsDbForWorkspace(workspaceId)`. Falls back to bootstrap on any null in the chain (missing binding row OR unbound draft). Both `updateToolBinding` and `removeToolBinding` now share the same routed-conn pattern — extraction prevents drift and keeps each consumer body small. 6-test source-scan at `tests/agent-studio/mr-3-fortysecond-batch-tool-binding-mutations.test.ts` (helper exists + helper shape + parameterized routed-conn-call + UPDATE-via-conn + DELETE-via-conn). The shared helper can be reused for future bindingId-scoped paths (`getToolBindingById` read promotion if needed). Out of scope: `getToolBindingById` (read-only Cat B by intent); `replaceToolBindings` (draftId-scoped — would use #861's pattern directly). | pending |
| #864 (PR-V1-113) | MR-3 forty-third batch — `repository.ts` draftId-scoped `replace*` mutations Path-B consumers via shared `resolveDraftRoutedConn` helper | Promotes the four draftId-scoped replace* mutations: `replaceToolBindings`, `replaceKnowledgeBindings`, `replaceMemoryConfigs`, `replaceWorkflowGraph`. All four share the DELETE-all + bulk-INSERT shape and take `draftId` as their first arg. Introduces a private `resolveDraftRoutedConn(lookupConn, draftId)` helper (sister to #863's `resolveBindingRoutedConn`, but skips the binding→draft step). The helper: (1) Calls Path B's `resolveWorkspaceIdForDraft(lookupConn, draftId)`; (2) routes via `getAsDbForWorkspace(workspaceId)`; (3) falls back to lookupConn on any null. **Atomicity invariant**: each function resolves once at the top and shares a single `conn` across all writes — no cross-region writes within one function body (matters most for `replaceWorkflowGraph` which does 2 DELETEs + up to 2 INSERTs). 10-test source-scan at `tests/agent-studio/mr-3-fortythird-batch-replace-bindings.test.ts` (helper exists + helper shape + parameterized routed-conn-call for all four + per-table DELETE+INSERT-via-conn + single-resolve-per-function for the workflow case). With this batch the four draftId-scoped replace* mutations are fully Path-B-migrated. Out of scope: `getToolBindingById` (read-only Cat B by intent); remaining `repository.ts` mutations (`archiveAgent`, `updateAgentLifecycleState`, `updateAgentCore`, etc. — each its own batch). | pending |
| #865 (PR-V1-114) | MR-3 forty-fourth batch — `repository.ts` agentId-scoped `agsAgents` mutations Path-B consumers via shared `resolveAgentRoutedConn` helper | Promotes the three agentId-scoped mutations on `agsAgents`: `updateAgentLifecycleState`, `updateAgentCore`, `archiveAgent`. Adds a private `resolveAgentRoutedConn(lookupConn, agentId)` helper (sister to `resolveDraftRoutedConn` and `resolveBindingRoutedConn`) that 2-step-resolves: (1) SELECTs `id` from `agsAgentDrafts.agentId = agentId` (any draft for the agent); (2) chains into Path B's `resolveWorkspaceIdForDraft`; (3) calls `getAsDbForWorkspace(workspaceId)`. Falls back to `lookupConn` on any null in the chain (agent has no drafts OR drafts have no binding). Each consumer body remains tight — single resolve at top, single UPDATE on `conn`. 8-test source-scan at `tests/agent-studio/mr-3-fortyfourth-batch-agent-mutations.test.ts` (helper exists + helper shape + parameterized routed-conn-call for all three + per-fn UPDATE-via-conn). Three sister helpers (`resolveAgentRoutedConn` agentId-scoped, `resolveDraftRoutedConn` draftId-scoped, `resolveBindingRoutedConn` bindingId-scoped) now cover the three identifier shapes used across `repository.ts` mutations. Out of scope: `createAgent` (INSERT path — workspaceId-aware Cat A already); read-only agent SELECTs (Cat B by intent); `createVersion`/`createPublishRequest` (different identifier shapes — own batches). | pending |
| #866 (PR-V1-115) | MR-3 forty-fifth batch — `repository.ts::createVersion` + `createPublishRequest` Path-B consumers Cat B→A | Both functions take `input.agentId` so they reuse the existing `resolveAgentRoutedConn` helper introduced in #865. `createVersion` is the first agentId-scoped consumer where the **same routed conn is shared across a SELECT (max versionNumber) and an INSERT** — preserves atomicity (no cross-region writes within one function body). `createPublishRequest` is a pure INSERT but also routes via the same helper. 6-test source-scan at `tests/agent-studio/mr-3-fortyfifth-batch-create-version-publish-request.test.ts` (parameterized routed-conn-call for both + per-fn DB-call-via-conn + single-resolve-per-function for createVersion). With this batch, the agentId-scoped `repository.ts` Cat B→A surface comprises 5 consumers across 2 helpers (3 `agsAgents` mutations + 2 INSERT mutations). Out of scope: `updatePublishRequestState` (publishRequestId-scoped — would need a `resolvePublishRequestRoutedConn` helper that joins publishRequest→agent→draft→binding); `decideApprovalStep` (stepId-scoped — even deeper join); `publishRelease` (versionId+agentId mixed shape — own batch). | pending |
| #867 (PR-V1-116) | MR-3 forty-sixth batch — `repository.ts::publishRelease` + `createSimulationRun` Path-B consumers Cat B→A | Both functions take `input.agentId` so they reuse `resolveAgentRoutedConn`. `publishRelease` is the second agentId-scoped consumer with **multiple writes on a single routed conn** (after #866 createVersion): an INSERT into `agsAgentReleases` AND an UPDATE on `agsAgents` share the same `conn` — without that share, Phase-2 multi-region could split the writes across regions and lose atomicity. `createSimulationRun` is a pure INSERT but also routes via the same helper. 6-test source-scan at `tests/agent-studio/mr-3-fortysixth-batch-publish-release-create-simulation-run.test.ts` (parameterized routed-conn-call for both + per-table DB-call-via-conn for publishRelease + single-resolve-per-function for publishRelease + INSERT-via-conn for createSimulationRun). With this batch, the agentId-scoped `repository.ts` Cat B→A surface comprises **7 consumers across 2 helpers**. Out of scope: `saveSimulationScenario` (UPDATE branch is scenarioId-scoped — needs its own resolver, would split the function across two routed conns or factor the UPDATE out); `updatePublishRequestState`/`decideApprovalStep` (own batches). | pending |
| #868 (PR-V1-117) | MR-3 forty-seventh batch — `repository.ts::saveTestSuite` + `createTestRun` Path-B consumers Cat B→A via new `resolveSuiteRoutedConn` helper | `createTestRun(input)` takes both suiteId AND agentId — we route by agentId since that's the more direct (one-hop) lookup. `saveTestSuite` dispatches on `input.suiteId`: UPDATE branch routes via the new `resolveSuiteRoutedConn(lookupConn, suiteId)` helper (3-step: SELECTs agentId from `agsTestSuites.id = suiteId`, then chains into resolveAgentRoutedConn → resolveWorkspaceIdForDraft); INSERT branch routes via `resolveAgentRoutedConn(input.agentId)`. Each branch routes once at top, then performs its single write. Introduces the **fourth sister helper** (`resolveSuiteRoutedConn`) — completing the resolver family that maps from the four primary identifier shapes used across `repository.ts` (agentId/draftId/bindingId/suiteId). 6-test source-scan at `tests/agent-studio/mr-3-fortyseventh-batch-test-suite-test-run.test.ts` (helper exists + helper shape + createTestRun routing + per-branch routing for saveTestSuite). Out of scope: `saveTestCase` (caseId UPDATE / suiteId INSERT — would chain through resolveSuiteRoutedConn); `recordTestResult`/`updateTestRun`/`removeTestCase` (runId/caseId-scoped — own batch chaining through agsTestRuns or agsTestCases); `appendSimulationStep`/`updateSimulationRun` (runId-scoped — own batch chaining through agsSimulationRuns). | pending |
| #869 (PR-V1-118) | MR-3 forty-eighth batch — `repository.ts::saveTestCase` + `removeTestCase` Path-B consumers Cat B→A via new `resolveCaseRoutedConn` helper | `saveTestCase` dispatches on `input.caseId`: UPDATE branch routes via new `resolveCaseRoutedConn` (4-step: caseId→suiteId via agsTestCases→agentId via agsTestSuites→draft→workspaceId); INSERT branch routes via the existing `resolveSuiteRoutedConn(input.suiteId)`. `removeTestCase(caseId)` routes via `resolveCaseRoutedConn`. The new caseId resolver is the **fifth sister helper** (after agentId/draftId/bindingId/suiteId) — demonstrates the resolver family chains naturally: each new identifier shape composes a single SELECT on top of the next-deeper resolver. 5-test source-scan at `tests/agent-studio/mr-3-fortyeighth-batch-test-case-mutations.test.ts`. Out of scope: `recordTestResult`/`updateTestRun`/`appendSimulationStep`/`updateSimulationRun` (runId-scoped — own batch); `saveSimulationScenario` (scenarioId/agentId mixed — own batch); `updatePublishRequestState`/`decideApprovalStep` (publishRequestId/stepId-scoped — own batches). | pending |
| #870 (PR-V1-119) | MR-3 forty-ninth batch — `repository.ts::updateTestRun` + `recordTestResult` Path-B consumers Cat B→A via new `resolveTestRunRoutedConn` helper | `agsTestRuns.agentId` is a direct FK — single SELECT, then chain into resolveAgentRoutedConn. `updateTestRun(runId, patch)` UPDATE on agsTestRuns; `recordTestResult(input)` INSERT on agsTestRunResults — both route via the new helper. **Sixth sister helper** in the resolver family (after agentId/draftId/bindingId/suiteId/caseId). 4-test source-scan at `tests/agent-studio/mr-3-fortyninth-batch-test-run-mutations.test.ts`. Out of scope: `appendSimulationStep`/`updateSimulationRun` (runId on agsSimulationRuns — own batch); `saveSimulationScenario` (scenarioId/agentId mixed — own batch); `updatePublishRequestState`/`decideApprovalStep` (publishRequestId/stepId — own batches); `appendRuntimeRun*` family (agentId/runId — own batch). | pending |
| #871 (PR-V1-120) | MR-3 fiftieth batch — `repository.ts::appendSimulationStep` + `updateSimulationRun` Path-B consumers Cat B→A via new `resolveSimulationRunRoutedConn` helper | `agsSimulationRuns.agentId` is a direct FK — single SELECT, then chain into resolveAgentRoutedConn. `appendSimulationStep(input)` INSERT on `agsSimulationRunSteps`; `updateSimulationRun(runId, patch)` UPDATE on `agsSimulationRuns` — both route via the new helper. **Seventh sister helper** (after agentId/draftId/bindingId/suiteId/caseId/testRunId). With this batch and #870 the runId-on-direct-FK pattern is consistent across the testing + simulation tiers. 4-test source-scan at `tests/agent-studio/mr-3-fiftieth-batch-simulation-run-mutations.test.ts`. Out of scope: `saveSimulationScenario` (scenarioId/agentId mixed); `updatePublishRequestState`/`decideApprovalStep`; `appendRuntimeRun*` family. | pending |
| #872 (PR-V1-121) | MR-3 fifty-first batch — `repository.ts::updatePublishRequestState` + `decideApprovalStep` Path-B consumers Cat B→A via two new helpers (`resolvePublishRequestRoutedConn` + `resolveApprovalStepRoutedConn`) | Introduces the deepest-chain resolvers so far: `resolvePublishRequestRoutedConn` (publishRequestId→agentId via agsPublishRequests, single SELECT) and `resolveApprovalStepRoutedConn` (stepId→publishRequestId via agsApprovalSteps, then chains into the publishRequest resolver — 5 hops total to workspaceId). `updatePublishRequestState(input.publishRequestId)` UPDATE on agsPublishRequests; `decideApprovalStep(input.stepId)` UPDATE on agsApprovalSteps. **Eighth + ninth sister helpers** — completing the publish/approval tier. With this batch the publish-tier is fully Path-B-migrated (4 consumers: createPublishRequest #866, publishRelease #867, updatePublishRequestState + decideApprovalStep #872). 6-test source-scan at `tests/agent-studio/mr-3-fiftyfirst-batch-publish-request-approval-step.test.ts`. Out of scope: `saveSimulationScenario` (scenarioId/agentId mixed); `createApprovalStep` (already-known publishRequestId in input but routes via #872's resolver — separate trivial batch if needed); `appendRuntimeRun*` family. | pending |
| #873 (PR-V1-122) | MR-3 fifty-second batch — `repository.ts::createApprovalStep` + `saveSimulationScenario` Path-B consumers Cat B→A via new `resolveSimulationScenarioRoutedConn` helper | `createApprovalStep(input)` reuses the existing `resolvePublishRequestRoutedConn` (#872) — input.publishRequestId routes INSERT into agsApprovalSteps. `saveSimulationScenario` dispatches on input.scenarioId: UPDATE branch routes via new `resolveSimulationScenarioRoutedConn` (scenarioId→agentId via agsSimulationScenarios, single SELECT, then chains into resolveAgentRoutedConn); INSERT branch routes via `resolveAgentRoutedConn(input.agentId)`. **Tenth sister helper**: resolveSimulationScenarioRoutedConn. With this batch every `repository.ts` agent/draft/binding/suite/case/testRun/simulationRun/publishRequest/approvalStep/scenario mutation is Path-B-migrated except the agsRuntime* family (next batch). 5-test source-scan at `tests/agent-studio/mr-3-fiftysecond-batch-approval-step-simulation-scenario.test.ts`. Out of scope: `appendRuntimeRun`, `appendRuntimeRunStep`, `appendRuntimeToolCall`, `updateRuntimeRun`, runtime memory/policy/hook events — own batch grouping. | pending |
| #874 (PR-V1-123) | MR-3 fifty-third batch — `repository.ts` agsRuntime* mutations Path-B consumers Cat B→A via new `resolveRuntimeRunRoutedConn` helper | Promotes the entire agsRuntime* family in one batch (7 mutations): `appendRuntimeRun(input.agentId)` routes via `resolveAgentRoutedConn`; `updateRuntimeRun(runId)`, `appendRuntimeRunStep`, `appendRuntimeToolCall`, `appendRuntimeMemoryEvent`, `appendRuntimePolicyEvent`, `appendRuntimeHookExecution` all route via new `resolveRuntimeRunRoutedConn` (runId→agentId via agsRuntimeRuns, single SELECT, then chains into resolveAgentRoutedConn). **Eleventh sister helper**. With this batch **every `repository.ts` mutation is Path-B-migrated** — 18 mutations across 11 helpers. 9-test source-scan at `tests/agent-studio/mr-3-fiftythird-batch-runtime-run-mutations.test.ts`. Out of scope: `repository.ts` read-only paths (Cat B by intent); read-only SELECTs in other modules (separate audit). | pending |
| #875 (PR-V1-124) | MR-3 fifty-fourth batch — `repository.ts` agsDraftHooks mutations Path-B consumers Cat B→A via new `resolveHookRoutedConn` helper | Starts the Phase-0b draft-scoped child-table migration arc. `saveHook` dispatches on input.hookId: UPDATE branch routes via new `resolveHookRoutedConn` (hookId→draftId via agsDraftHooks, then chains into `resolveDraftRoutedConn`); INSERT branch routes via `resolveDraftRoutedConn(input.draftId)`. `removeHook(hookId)` routes via `resolveHookRoutedConn`. `replaceHooks(draftId)` routes via `resolveDraftRoutedConn` (DELETE-all + bulk-INSERT share single routed conn). **Twelfth sister helper**: resolveHookRoutedConn. 6-test source-scan at `tests/agent-studio/mr-3-fiftyfourth-batch-hook-mutations.test.ts`. Out of scope: McpServer/Skill/Subagent/Plugin/PermissionRule mutations — each follows the same shape with its own xxxId resolver (own batch each). | pending |
| #876 (PR-V1-125) | MR-3 fifty-fifth batch — `repository.ts` agsDraftMcpServers mutations Path-B consumers Cat B→A via new `resolveMcpServerRoutedConn` helper | 6 mutations: `saveMcpServer` (UPDATE: serverId, INSERT: draftId), `removeMcpServer` (serverId), `updateMcpServerStatus` (serverId), `setMcpServerEnabled` (serverId), `updateMcpServerOAuth` (serverId), `replaceMcpServers` (draftId). The serverId-scoped mutations use the new `resolveMcpServerRoutedConn` (single SELECT, chains into resolveDraftRoutedConn); the draftId-scoped INSERT branch + replaceMcpServers use resolveDraftRoutedConn directly. **Thirteenth sister helper**: resolveMcpServerRoutedConn. 9-test source-scan at `tests/agent-studio/mr-3-fiftyfifth-batch-mcp-server-mutations.test.ts`. Out of scope: `recordMcpTransition` (writes to agsMcpTransitions with serverId + trim DELETE — own batch with its own resolver). | pending |
| #877 (PR-V1-126) | MR-3 fifty-sixth batch — `repository.ts` agsDraftSkills mutations Path-B consumers Cat B→A via new `resolveSkillRoutedConn` helper | 3 mutations: `attachSkill(input.draftId)` → resolveDraftRoutedConn; `removeSkill(skillId)` → resolveSkillRoutedConn (skillId→draftId, single SELECT, chains into resolveDraftRoutedConn); `replaceSkills(draftId)` → resolveDraftRoutedConn. **Fourteenth sister helper**: resolveSkillRoutedConn. 5-test source-scan at `tests/agent-studio/mr-3-fiftysixth-batch-skill-mutations.test.ts`. Out of scope: Subagent/Plugin/PermissionRule (own batches). | pending |
| #878 (PR-V1-127) | MR-3 fifty-seventh batch — `repository.ts` agsDraftSubagents mutations Path-B consumers Cat B→A via new `resolveSubagentRoutedConn` helper | 3 mutations: `saveSubagent` (UPDATE: subagentId, INSERT: draftId), `removeSubagent` (subagentId), `replaceSubagents` (draftId). The subagentId-scoped mutations use the new `resolveSubagentRoutedConn` (single SELECT, chains into resolveDraftRoutedConn); the draftId-scoped INSERT branch + replaceSubagents use resolveDraftRoutedConn directly. **Fifteenth sister helper**: resolveSubagentRoutedConn. 6-test source-scan at `tests/agent-studio/mr-3-fiftyseventh-batch-subagent-mutations.test.ts`. Out of scope: Plugin/PermissionRule (own batches). | pending |
| #879 (PR-V1-128) | MR-3 fifty-eighth batch — `repository.ts` agsDraftPlugins + agsDraftPermissionRules mutations Path-B consumers Cat B→A via two new helpers (`resolvePluginRoutedConn` + `resolvePermissionRuleRoutedConn`) | 6 mutations across two parallel triples: `savePlugin` (UPDATE: pluginId, INSERT: draftId), `removePlugin` (pluginId), `replacePlugins` (draftId), `savePermissionRule` (UPDATE: ruleId, INSERT: draftId), `removePermissionRule` (ruleId), `replacePermissionRules` (draftId). The xxxId-scoped mutations use the two new resolvers (each single SELECT + chain into resolveDraftRoutedConn); the draftId-scoped paths use resolveDraftRoutedConn directly. **Sixteenth + seventeenth sister helpers**. With this batch the Phase-0b child-table migration arc concludes — all 6 draft-scoped child tables (Hooks #875, McpServers #876, Skills #877, Subagents #878, Plugins + PermissionRules #879) are Path-B-migrated. 9-test source-scan at `tests/agent-studio/mr-3-fiftyeighth-batch-plugin-permission-rule-mutations.test.ts`. Out of scope: `updateRuntimeConfig` (agentId-scoped, no current draft id known until lookup); `updateScheduleConfig` family; catalog mutations; runtime hook executions trim; MCP transitions. | pending |
| #880 (PR-V1-129) | MR-3 fifty-ninth batch — `repository.ts::updateRuntimeConfig` + `updateScheduleConfig` + `updateScheduleConfigByDraftId` + `createPendingPermissionRequest` Path-B consumers Cat B→A | 4 mutations promoted without introducing new resolvers — all reuse existing helpers. `updateRuntimeConfig(agentId)` + `updateScheduleConfig(agentId)` already call `getCurrentDraft(agentId)` to discover draft.id for the WHERE clause; this batch wires that same draft.id into `resolveDraftRoutedConn` for routing. `updateScheduleConfigByDraftId(draftId)` routes via `resolveDraftRoutedConn(draftId)`. `createPendingPermissionRequest(input.runtimeRunId)` routes via `resolveRuntimeRunRoutedConn` (introduced #874). 4-test source-scan at `tests/agent-studio/mr-3-fiftyninth-batch-runtime-schedule-config.test.ts`. Out of scope: `recordMcpTransition` (serverId-scoped agsMcpTransitions writes); Catalog mutations (catalogTool/catalogSkill); Marketplace mutations. | pending |
| #881 (PR-V1-130) | MR-3 sixtieth batch — `repository.ts::recordMcpTransition` Path-B consumer Cat B→A | Reuses existing `resolveMcpServerRoutedConn` (#876) — agsMcpTransitions has a serverId FK to agsDraftMcpServers, so the same resolver applies. Both the INSERT and the trim DELETE (raw SQL `conn.execute`) share a single routed conn — preserves atomicity (no cross-region writes within one fn). 4-test source-scan at `tests/agent-studio/mr-3-sixtieth-batch-mcp-transition.test.ts`. With this batch every MCP-tier mutation (saveMcpServer/removeMcpServer/updateMcpServerStatus/setMcpServerEnabled/updateMcpServerOAuth/replaceMcpServers + recordMcpTransition) is Path-B-migrated. Out of scope: Catalog mutations; Marketplace mutations. | pending |
| #882 (PR-V1-131) | MR-3 sixty-first batch — `services/vault/template-instantiations.ts::recordTemplateInstantiation` Path-A discovering-helper migration Cat B→A | `agsVaultTemplateInstantiations` has no workspaceId column, but `noteId → agsVaultNotes.vaultId → agsVaults.workspaceId` is a clean two-table JOIN. The migration adds a Notes × Vaults `innerJoin` keyed on `input.noteId` to pull workspaceId in one round-trip, then routes the INSERT via `getAsDbForWorkspace`. Falls back to bootstrap when the note's vault is missing OR `vault.workspaceId IS NULL` (legacy pre-Phase-1 rows); FK constraint catches orphan noteId at the DB layer. Reads (`listInstantiationsByTemplate`/`listInstantiationsByNote`/`countDistinctDigestsForTemplate`) remain on bootstrap as intentional Cat B operator helpers. 4-test source-scan at `tests/agent-studio/mr-3-sixtyfirst-batch-vault-template-instantiation.test.ts`. Out of scope: vault/presence.ts writes (own batch — same chain shape but presence rows already carry vaultId, not noteId); vault/repository-asdb.ts already migrated. | pending |
| #883 (PR-V1-132) | MR-3 sixty-second batch — `services/vault/presence.ts` mutations Path-A discovering-helper migration Cat B→A | 3 mutations migrated: `enterPresence` (SELECT existing + UPDATE OR INSERT), `heartbeat` (UPDATE), `leavePresence` (DELETE). Introduces a module-local `resolveNoteRoutedConn(lookupDb, noteId)` helper that walks noteId → agsVaultNotes.vaultId → agsVaults.workspaceId via a single two-table JOIN and returns the workspace-routed handle. enterPresence shares the routed db across SELECT + UPDATE + INSERT — preserves atomicity (resolve-once per call). 6-test source-scan at `tests/agent-studio/mr-3-sixtysecond-batch-vault-presence.test.ts`. listPresence (read-path) intentionally remains on bootstrap as Cat B. Out of scope: presence.ts's listPresence eviction-DELETE side-effect; vault/template-instantiations.ts already migrated in #882. | pending |
| #884 (PR-V1-133) | MR-3 sixty-third batch — `services/cag/store.ts::getLatestPack` + `listPacks` read paths Path-B consumers Cat B-read → A-read | First read-path Path-B promotion. Both reads previously used the bootstrap `getAsDb()` handle; under Phase-1 (single-region shim) behavior is bit-for-bit identical, but Phase-2 multi-region requires reads of workspace-scoped tables (`agsCagCapabilityPacks` IS workspace-scoped) to route to the home region. Each function now dynamically imports `resolveWorkspaceIdForDraft` (Path B primitive), passes the lookup handle in, and routes via `getAsDbForWorkspace(workspaceId)` with bootstrap fallback for legacy drafts pre-Phase-11 (no binding). 2-test source-scan at `tests/agent-studio/mr-3-sixtythird-batch-cag-store-reads.test.ts`. The mutations on `agsCagCapabilityPacks` (`markPackStale`/`markPackUsed`/`recordPackTokenActual`) already use discovering-helper from prior batches. Out of scope: other Cat B reads across rac/cag/vault — each their own dedicated batch. | pending |
| #885 (PR-V1-134) | MR-3 sixty-fourth batch — `services/cag/events.ts::listPackEvents` Path-B read consumer Cat B-read → A-read | Second read-path Path-B promotion (after #884 cag/store reads). Same shape: dynamic import of Path B resolver, route via `getAsDbForWorkspace` with bootstrap fallback. agsCagPackEvents.workspaceId is non-null (workspace-scoped), so the read now targets the home region under Phase-2. 1-test source-scan at `tests/agent-studio/mr-3-sixtyfourth-batch-cag-events-reads.test.ts`. Out of scope: `appendPackEvent` already routes via caller-supplied workspaceId; `listEventsByPack` already accepts workspaceId as input. | pending |
| #886 (PR-V1-135) | MR-3 sixty-fifth batch — `services/rac/sources/store.ts::listProfilesForDraft` Path-B read consumer Cat B-read → A-read | Third read-path Path-B promotion. agsRacProfiles.workspaceId is non-null (workspace-scoped); the read now targets the home region under Phase-2 via dynamic import of `resolveWorkspaceIdForDraft` (relative path `../../region/...`). Same bootstrap fallback for legacy drafts pre-Phase-11. 1-test source-scan at `tests/agent-studio/mr-3-sixtyfifth-batch-rac-profiles-reads.test.ts`. Out of scope: `getProfile(profileId)`/`getSource(sourceId)`/`listSourcesForProfile(profileId)` — id-keyed single-row reads; need discovering-helper (own batches). | pending |
| #887 (PR-V1-136) | MR-3 sixty-sixth batch — `services/rac/sources/store.ts::getSource` + `listSourcesForProfile` Path-A discovering-helper reads | Fourth read-path Path-X promotion. Different from the Path-B draftId reads (#884–#886): these are id-keyed reads where the row carries workspaceId directly (getSource: agsRacSources.workspaceId) or via the parent profile (listSourcesForProfile: agsRacProfiles.workspaceId). A pre-projection SELECT pulls workspaceId up; the full-row SELECT then routes via `getAsDbForWorkspace` with bootstrap fallback. Soft-fall-through when row doesn't exist. 2-test source-scan at `tests/agent-studio/mr-3-sixtysixth-batch-rac-sources-reads.test.ts`. Out of scope: `getProfile(profileId)` — same shape but its own batch; ingestion-job-service / knowledge-unit-service / provenance-service already migrated. | pending |
| #888 (PR-V1-137) | MR-3 sixty-seventh batch — `services/rac/sources/store.ts::getProfile` + `getPolicyForProfile` Path-A discovering-helper reads | Fifth read-path Path-X promotion. `getProfile(profileId)`: agsRacProfiles.workspaceId is non-null; pull up, route the full SELECT. `getPolicyForProfile(profileId)`: agsRacPolicies has no workspaceId column; resolve via parent agsRacProfiles, route the SELECT on agsRacPolicies. With this batch the rac/sources/store.ts read-path Path-X promotions are complete (5 reads: listProfilesForDraft #886, getSource + listSourcesForProfile #887, getProfile + getPolicyForProfile #888). 2-test source-scan at `tests/agent-studio/mr-3-sixtyseventh-batch-rac-profile-policy-reads.test.ts`. | pending |
| #889 (PR-V1-138) | MR-3 sixty-eighth batch — `services/canvas/canvas-service.ts` read paths Path-A migration | Sixth read-path Path-X promotion. 4 reads migrated, all reusing the existing `resolveWorkspaceIdForCanvas` cross-table JOIN helper (introduced in #94): `getCanvasById(canvasId)`, `getCanvasSnapshot(canvasId)`, `listNoteReferencesForCanvas(canvasId)`. `listCanvasesByVault(vaultId)` uses a sibling pre-projection (SELECT workspaceId FROM agsVaults WHERE id=vaultId), then routes the SELECT on agsCanvases. With this batch every read in canvas-service.ts routes via Phase-2-ready handle. 4-test source-scan at `tests/agent-studio/mr-3-sixtyeighth-batch-canvas-reads.test.ts`. Mutations (createCanvas, createCanvasNode, createCanvasEdge) were already migrated in #94, #844, #845. | pending |
| #890 (PR-V1-139) | MR-3 sixty-ninth batch — ingestion service reads Path-A discovering-helper migration | Seventh read-path Path-X promotion. 2 reads migrated: `ingestion-job-service.ts::getJob(jobId)` (agsIngestionJobs.workspaceId is non-null FK) and `provenance-service.ts::getProvenance(provenanceId)` (agsProvenanceRecords.workspaceId is non-null FK). Each: pre-projection SELECT pulls workspaceId from the same table on the bootstrap handle, then the full-row SELECT routes via `getAsDbForWorkspace`. 2-test source-scan at `tests/agent-studio/mr-3-sixtyninth-batch-ingestion-reads.test.ts`. Mutations (`startJob`, `completeJob`, `recordProvenance`, `insertUnit`, `archiveUnit`) were already migrated in earlier batches. | pending |
| #891 (PR-V1-140) | MR-3 seventieth batch — `services/vault/repository-asdb.ts` read paths Path-A migration | Eighth read-path Path-X promotion. 3 reads migrated, all using the Notes × Vaults JOIN keyed on noteId to pull workspaceId up: `getNoteById(noteId)`, `getNoteVersion(noteId, version)`, `getLatestNoteVersion(noteId)`. The full-row SELECT then routes via `getAsDbForWorkspace`. Falls back to bootstrap when the note is missing OR vault.workspaceId IS NULL. 3-test source-scan at `tests/agent-studio/mr-3-seventieth-batch-vault-note-reads.test.ts`. Mutations (createNote, updateNote, deleteNote) already use the same JOIN pattern from prior batches. | pending |
| #892 (PR-V1-141) | MR-3 seventy-first batch — `services/vault/repository-asdb.ts::listNotesInVault` Path-A read consumer | Ninth read-path Path-X promotion. vaultId → agsVaults.workspaceId in a single pre-projection SELECT, then route the SELECT on agsVaultNotes to the home region. Falls back to bootstrap when the vault row is missing OR `vault.workspaceId IS NULL`. 1-test source-scan at `tests/agent-studio/mr-3-seventyfirst-batch-vault-list-notes.test.ts`. With this batch the vault repo's primary reads are Phase-2-ready (note-by-id + note version + latest version + list-in-vault). | pending |
| #893 (PR-V1-142) | MR-3 seventy-second batch — `bindings.ts::getAgentProviderBinding` Path-A read consumer | Tenth read-path Path-X promotion. agsAgentProviderBindings is the Path B primitive's source-of-truth (workspaceId column is non-null); the read now targets the home region under Phase-2 via a pre-projection SELECT keyed by (draftId, role) and then a full-row SELECT on the routed handle. 1-test source-scan at `tests/agent-studio/mr-3-seventysecond-batch-bindings-read.test.ts`. Out of scope: `listBindingsForAgent(agentId)` is intentionally cross-workspace (Cat C) — fan-out semantics are a Phase-2 ADR decision. | pending |
| #894 (PR-V1-143) | MR-3 seventy-third batch — `services/extensions/manifest.ts::getExtensionById` Path-A read consumer | Eleventh read-path Path-X promotion. agsExtensions.workspaceId is non-null FK; pre-projection pulls it up, then the full-row SELECT routes via `getAsDbForWorkspace`. 1-test source-scan at `tests/agent-studio/mr-3-seventythird-batch-extensions-read.test.ts`. Mutations (installExtension, approveExtension, setExtensionStatus) already use the same discovering-helper pattern. | pending |
| #895 (PR-V1-144) | MR-3 seventy-fourth batch — `services/rac/trace/store.ts::listContextBlocks` Path-A read consumer | Twelfth read-path Path-X promotion. agsRacContextBlocks has no workspaceId column directly; the parent agsRacRuntimeTraces row carries it. Pre-projection SELECT pulls workspaceId from the parent trace, then the SELECT on agsRacContextBlocks routes via `getAsDbForWorkspace`. 1-test source-scan at `tests/agent-studio/mr-3-seventyfourth-batch-rac-trace-context-blocks.test.ts`. Other rac/trace/store.ts paths (writeTrace, writeContextBlocks, updateTraceScores, getTraceById, getTraceForMessage, recordFeedback, getFeedbackForMessage) already use workspaceId-routed handles from prior batches. | pending |
| #897 (PR-V1-146) | MR-3 seventy-sixth batch — `services/extensions/runtime.ts::loadExtensionById` Path-A read consumer | Fourteenth read-path Path-X promotion. Mirrors `services/extensions/manifest.ts::getExtensionById` (#894) — same `agsExtensions.workspaceId` pre-projection + routed SELECT pattern, but on the internal `loadExtensionById` helper used by `invokeFromExtension`. 1-test source-scan at `tests/agent-studio/mr-3-seventysixth-batch-extensions-runtime-read.test.ts`. | pending |
| #896 (PR-V1-145) | MR-3 seventy-fifth batch — `services/approval/approval-gate.ts::evaluateApprovalGate` Path-B read promotion (hoist) | Thirteenth read-path Path-X promotion. Hoists the existing Path B resolver (`#107` migrated the lastUsedAt UPDATE only) up to the top of the function so the **SELECT-by-(agentDraftId, hash) also routes to the home region**. The routed `db` handle is reused for both the SELECT and the stale-row UPDATE — single resolver call replaces the prior pair of calls (was inside if-isStale branch). Net effect: SELECT now Phase-2-routed without adding a round-trip. 1-test source-scan at `tests/agent-studio/mr-3-seventyfifth-batch-approval-gate-read.test.ts`. PR-V1-107's existing test (`mr-3-thirtyseventh-batch-evaluate-approval-gate.test.ts`) updated lockstep for the new hoisted shape — variable rename `db`→`lookupDb` for the bootstrap handle, routed handle now named `db`. Existing 13-test approval-gate behavior suite + 5-test M1-c6 lastUsedAt guard suite continue to pass. | pending |
| #898 (PR-V1-147) | MR-3 state-of-the-union rev 3 — 76 batches landed (substantively complete) | Documentation-only PR. Updates `docs/implementation/agent-studio-mr-3-state-of-the-union-2026-05-14.md` to rev 3 covering the 41 PRs landed in this stretch on top of rev 2. New §12 documents the **17 sister resolvers** in `repository.ts` (every mutation Path-B-routed). New §13 documents the **14 read-path Path-X promotions** sub-arc. Net status: every mutation in agent-studio that targets a workspace-scoped table now routes via `getAsDbForWorkspace` under Phase-2; every read of a workspace-scoped table does the same. Remaining `getAsDb()` callers are intentional Cat C (cross-workspace observability, global registries, polymorphic tables, boot wiring, health probes, intentional cross-workspace fan-out). Phase MR-1 Phase-2 cutover can proceed without further MR-3 work. | pending |
| #899 (PR-V1-148) | MR-1 Phase-2 first slice — `ags_workspace_region_pins` table + pin service | First concrete Phase-2 step after the MR-3 substantive close-out. Introduces the missing data-source for workspace→region routing: new ASDB-resident table `ags_workspace_region_pins` (workspaceId unique, regionKey, isReplicated flag, notes) + `services/region/workspace-region-pin-service.ts` (set / get / list / remove). `setWorkspaceRegionPin` validates the regionKey via `getRegionByKey` before insert/update — no dangling pins. Single-region operational baseline preserved: empty table = every workspace falls back to the primary region (existing `resolveWorkspaceRegion` semantics). Public-api barrel re-exports the new helpers + types. Drizzle schema barrel re-exports the new table. 8-test source-scan at `tests/agent-studio/workspace-region-pin-service.test.ts` (table shape + service exports + barrel re-export + upsert path + idempotent delete + null-DB fallback + hard-rule compliance). Out of scope: wiring the lookup into `getAsDbForWorkspace` — that requires a synchronous cache (subsequent PR). | pending |
| #900 (PR-V1-149) | MR-1 Phase-2 second slice — synchronous routing cache | Second concrete Phase-2 step on top of #899. The MR-3 shim `getAsDbForWorkspace(workspaceId)` is synchronous; routing it via the async pin service would break every call site. This PR introduces `services/region/workspace-region-cache.ts` — an in-process cache populated asynchronously by `warmRegionRoutingCache()` (loads `listActiveRegions` + `getPrimaryRegion` + `listAllWorkspaceRegionPins` in parallel) and read synchronously by `getCachedRegionForWorkspace(workspaceId)` / `getCachedRegionKeyForWorkspace` / `getCachedPrimaryRegion` / `getCachedActiveRegions`. Resolution: pinned → full region record; no pin → primary; pin to inactive/missing region → null (caller surfaces config error, matches pure-router semantics). `invalidateRegionRoutingCache()` returns to cold state. `getRegionRoutingCacheStatus()` exposes isWarm + lastWarmedAt + pin/region counts for the operator dashboard. Public-api barrel re-exports the new symbols. 8-test behavioral suite at `tests/agent-studio/workspace-region-cache.test.ts` (cold→empty fallbacks; warm→snapshot; pin resolution; primary fallback; inactive-pin → null; invalidate → cold; hard-rule source-scan + barrel re-export). Single-region operational baseline preserved: cold cache = every reader returns null/[]; callers fall back to bootstrap `getAsDb()`. Out of scope: actually plugging the cache into `getAsDbForWorkspace` — that's the next slice. | pending |
| #901 (PR-V1-150) | MR-1 Phase-2 third slice — shim cutover via late-binding bridge | Third concrete Phase-2 step on top of #899 + #900. The shim `getAsDbForWorkspace` (previously: observe + delegate) now consults a region router; the router is wired in by a separate bridge module after boot. Late-binding pattern avoids the circular import that would arise from `db/connection.ts` importing `services/region/*` (those modules import `getAsDb` from connection.ts). Adds `configureRegionRouter(router \| null)` to `db/connection.ts` — the shim's late-bound slot. Adds `services/region/region-routing-bridge.ts` exposing `installRegionRouter()` / `uninstallRegionRouter()` — the closure that combines `getCachedRegionForWorkspace` + `getDbForRegion`. Public-api barrel re-exports the install/uninstall pair. Single-region operational baseline preserved: until `installRegionRouter` is called, behavior is unchanged; cold cache → null → fallback; null workspaceId → bypass router entirely. 7-test behavioral suite at `tests/agent-studio/region-routing-bridge.test.ts` (no-router fallback, cold-cache fallback, pinned-workspace routing, pool-init-failure fallback, uninstall-reverts, null-workspaceId bypass, hard-rule source-scan). Out of scope: boot wiring (`installRegionRouter` call at startup + initial `warmRegionRoutingCache`) — separate PR so multi-region operators control the cutover. | pending |
| #902 (PR-V1-151) | MR-1 Phase-2 fourth slice — env-flag-gated boot wiring | Fourth concrete Phase-2 step on top of #899 + #900 + #901. Adds `services/region/install-region-routing.ts` exposing `maybeInstallRegionRouting()` — an env-flag-gated wrapper that warms the cache + installs the router in one call, mirroring the existing pattern (Step 3.27..3.31 lane hooks / sink installers). Env flag: `AGS_REGION_ROUTING=on` (default OFF). Boot Step 3.32 imports + invokes the helper, logs `[ags-region-routing] installed — activeRegions=N pins=M primary=X` on success or `not installed — <reason>` otherwise. Returns `{ installed, reason?, activeRegionCount?, pinCount?, primaryRegionKey? }` for observability. Public-api barrel re-exports the helper + result type. 6-test behavioral suite at `tests/agent-studio/install-region-routing.test.ts` (flag-unset / wrong-value / on+warm-succeeds / on+warm-throws / boot.ts source-scan / hard-rule compliance + barrel re-export). Single-region operational baseline preserved: flag unset = behavior unchanged. Multi-region operators populate ags_regions + ags_workspace_region_pins then flip the flag. With this slice the Phase MR-1 Phase-2 cutover is **operationally complete** end-to-end: data-source (#899) → cache (#900) → bridge (#901) → boot (#902). | pending |
| #903 (PR-V1-152) | MR-1 Phase-2 fifth slice — pin-change invalidation hook | Pin writes invalidate the routing cache and trigger a fire-and-forget re-warm, so operator pin changes are visible without a process restart. Adds a late-bound `setPinChangeHook(hook \| null)` to `workspace-region-pin-service.ts`; `setWorkspaceRegionPin` (both INSERT + UPDATE branches) and `removeWorkspaceRegionPin` call `firePinChanged()` after every successful write. Hook errors are swallowed (try/catch around the fire) so a broken hook doesn't break the pin write. `installRegionRouter()` now also registers a closure that invalidates the cache + re-warms asynchronously; `uninstallRegionRouter()` clears both the shim slot AND the pin hook. Public-api barrel re-exports `setPinChangeHook`. 5-test suite at `tests/agent-studio/pin-change-invalidation.test.ts` (hook plumbing + service source-scan asserting 3 fire sites + bridge wires both install hooks + hard-rule compliance + install/uninstall sequence with mocked downstream modules). Single-region operational baseline preserved: without `installRegionRouter`, the hook slot stays null and pin writes are no-ops on the cache. | pending |
| #904 (PR-V1-153) | MR-1 Phase-2 sixth slice — region-change invalidation hook (symmetric) | Symmetric with PR-V1-152: region-registry writes now invalidate + re-warm the cache. Adds a late-bound `setRegionChangeHook(hook \| null)` to `region-service.ts`; `registerRegion` calls `fireRegionChanged()` after every successful insert. Bridge `installRegionRouter()` now wires THREE slots (shim router + pin hook + region hook); `uninstallRegionRouter()` clears all three. Bridge introduces a small DRY helper `reloadCacheAfterChange(label)` reused by both hook closures. Public-api barrel re-exports `setRegionChangeHook`. 5-test suite at `tests/agent-studio/region-change-invalidation.test.ts` (hook plumbing + service source-scan asserting fireRegionChanged after insert + bridge wires both hooks + uninstall clears all three slots + hard-rule compliance + barrel re-export). Region writes are rarer than pin writes (operator onboarding), but new region propagation matters: an already-pinned workspace whose region row arrives later still routes correctly after the cache re-warm. | pending |
| #905 (PR-V1-154) | MR-1 Phase-2 state-of-the-union (cutover operationally complete) | Documentation-only PR. New doc `docs/implementation/agent-studio-mr-1-phase-2-cutover-state-of-the-union-2026-05-15.md` consolidates the 6-PR cutover arc (#899–#904) with: post-cutover architecture diagram; six-PR ledger; operational baseline (single-region no-op + multi-region cutover sequence + rollback); hard-rule compliance scan results; deferred-to-successor items (multi-process invalidation / replication catch-up / admin UI / periodic re-warm / cross-region middleware); connection back to MR-3. Names the milestone — both MR-3 invariant ("every workspace-scoped DB call observes workspaceId") and MR-1 Phase-2 invariant ("every observed workspaceId routes to the pinned region") now compose end-to-end. Multi-region deployments are now a config-only change. | pending |
| #906 (PR-V1-155) | MR-1 Phase-2 seventh slice — periodic cache re-warm cron | First post-SOU follow-up; closes one of the SOU §5 deferred items. New `services/region/region-cache-rewarm-cron.ts` wraps `warmRegionRoutingCache` via the existing `makeRetentionCron` factory (Step 3.21..3.23 + Step 3.25 pattern). Default cadence `*/10 * * * *`. Env-flag opt-out: `AGS_REGION_CACHE_REWARM_CRON_DISABLED`. Default ON when boot completes. Boot Step 3.33 starts the cron. Catches cross-replica drift (no pub-sub yet, SOU §5) and the edge case where a hook closure threw and the cache stayed stale. Public-api barrel re-exports `tickRegionCacheRewarmCron` / `getRegionCacheRewarmCronStatus` / `ensureRegionCacheRewarmCronStarted` / `DEFAULT_REGION_CACHE_REWARM_CRON_EXPR` + 3 types. 5-test source-scan at `tests/agent-studio/region-cache-rewarm-cron.test.ts` (cron config + standard cron API + barrel re-export + boot wiring + hard-rule compliance). | pending |
| #907 (PR-V1-156) | MR-1 Phase-2 eighth slice — region admin tRPC router | Second post-SOU follow-up; closes the "Operator admin UI for pins" deferred item at the tRPC layer. New `services/region/region-admin-router.ts` mounted at `agentStudio.region.*` with 8 admin-only procedures: `listActiveRegions` (query), `registerRegion` (mutation), `listPins` (query), `getPin` (query), `setPin` (mutation, upsert), `removePin` (mutation, idempotent), `getCacheStatus` (query), `getRewarmCronStatus` (query). All inputs Zod-validated; `registerRegion` + `setPin` fire the invalidation hooks (#903/#904); cache + cron status mirror the operator dashboard panel shape used by graphHealth + graphProjection. Public-api barrel re-exports `regionAdminRouter`. 6-test source-scan at `tests/agent-studio/region-admin-router-mounted.test.ts` (router-mount check + 8-procedure declaration + verb correctness + Zod schemas + barrel re-export + hard-rule compliance). The React surface is a separate slice. | pending |
| #908 (PR-V1-157) | MR-1 Phase-2 ninth slice — region admin React page | Closes the React half of the "Operator admin UI for pins" deferred item. New `client/src/modules/agent-studio/components/RegionAdminPanel.tsx` queries the 4 region tRPC reads (`getCacheStatus`, `getRewarmCronStatus`, `listActiveRegions`, `listPins`) and renders four panels: routing cache status (isWarm + lastWarmedAt + counts + primary), re-warm cron status, active regions table, workspace pins table. Postgres URIs are credential-masked (`:****@`) before display, matching the region-service log masking pattern. New `client/src/modules/agent-studio/pages/RegionAdminPage.tsx` is a slim wrapper mounting the panel with PageHeader (Globe icon). Wired into `AgentStudioShell.tsx` via lazy-import + path matcher (`/agent-studio/region-admin`) + view dispatch. Pin add/edit/remove forms intentionally out of scope — pin onboarding is rare + sensitive; CLI / direct tRPC remain the supported write path. 5-test source-scan at `tests/agent-studio/region-admin-page-mounted.test.ts` (page + panel + 4-query check + URI masking + shell lazy-import + route + dispatch). | pending |
| #909 (PR-V1-158) | MR-1 Phase-2 tenth slice — cross-region access guard | Closes the "Cross-region read denial middleware" SOU §5 deferred item at the primitive layer. New `services/region/cross-region-guard.ts` exposes `guardCrossRegionAccess(workspaceId, ctx?)` — a pure function that throws `CrossRegionAccessDeniedError` when a workspace pinned to a different region than the process accesses local data AND is not flagged `isReplicated`. Process regionKey read from `AGS_PROCESS_REGION_KEY` env var (cached after first read; reset helper for tests). Permissive defaults preserve single-region baseline: env unset → no-op; workspace unpinned → no-op (primary fallback); pin matches process → no-op; pin matches but replicated → no-op. Public-api barrel re-exports `guardCrossRegionAccess` / `getProcessRegionKey` / `CrossRegionGuardContext`. 8-test behavioral suite at `tests/agent-studio/cross-region-guard.test.ts` covering every branch + error shape + hard-rule compliance + barrel re-export. Wiring into specific procedures (per-procedure manual call OR tRPC middleware factory) is deferred to a downstream slice — operators get the primitive now and can opt-in to specific procedures incrementally. | pending |
| #910 (PR-V1-159) | MR-1 Phase-2 eleventh slice — cross-region tRPC middleware factory | Composes the #909 guard primitive into a reusable tRPC middleware. New `services/region/cross-region-trpc-middleware.ts` exposes `createCrossRegionMiddleware<TInput>(extractWorkspaceId, options?)` — operators call once per procedure shape with a workspaceId extractor; the returned middleware extracts the id from input + calls `guardCrossRegionAccess`. CrossRegionAccessDeniedError is wrapped in `TRPCError({ code: "FORBIDDEN", cause: err })` at the boundary. Permissive defaults: extractor null/undefined → middleware no-op + next() runs; extractor throws → next() runs (let handler surface its own validation error); unrelated guard exceptions re-throw unchanged. Test seam: `options.guard` lets unit tests swap the guard. Public-api barrel re-exports `createCrossRegionMiddleware` + the options type. 8-test behavioral suite at `tests/agent-studio/cross-region-trpc-middleware.test.ts` covering happy path + null/undefined extractor + extractor-throw + FORBIDDEN wrap + unrelated re-throw + hard-rule compliance + barrel re-export. With this slice the cross-region read denial primitive AND the boundary wiring shape are both shipped; per-procedure application is operator-controlled and incremental. | pending |
| #911 (PR-V1-160) | MR-1 Phase-2 twelfth slice — cross-process pub-sub via Postgres LISTEN/NOTIFY | Closes the "Multi-process cache invalidation" SOU §5 deferred item. No new infra — uses Postgres LISTEN/NOTIFY on channel `ags_region_routing_invalidate`. New `services/region/region-cache-pubsub.ts` exposes `notifyRegionCacheInvalidation(reason)` (fire-and-forget; quote-escape + 200-char cap), `subscribeRegionCacheInvalidations(handler)` (dedicated `pg.Client` LISTEN with linear-backoff reconnect 10s→60s), `unsubscribeRegionCacheInvalidations()`, and `maybeSubscribeRegionCachePubsub(handler)` (env-flag-gated via `AGS_REGION_PUBSUB=on`). Bridge `reloadCacheAfterChange(label)` (#904) now also fires the NOTIFY after the in-process invalidate. Boot helper `maybeInstallRegionRouting` (#902) subscribes when the flag is on; subscriber's handler invalidates + re-warms the local cache. Result includes `pubsubSubscribed: boolean` for observability; boot log line includes `pubsub=on|off`. Public-api barrel re-exports the 4 pubsub helpers. 8-test source-scan at `tests/agent-studio/region-cache-pubsub.test.ts` (channel name + helper exports + NOTIFY escape + reconnect backoff + bridge wire-up + install wire-up + barrel re-export + boot log + hard-rule compliance). Pattern mirrors the planned `approval-event-bus.ts` D-RESUME-5 LISTEN/NOTIFY upgrade path — reusable for any future cross-process bus need. | pending |
| #912 (PR-V1-161) | MR-1 Phase-2 SOU rev 2 — 5 of 6 follow-ups closed | Documentation-only PR. Updates `docs/implementation/agent-studio-mr-1-phase-2-cutover-state-of-the-union-2026-05-15.md` to rev 2. Adds §8 "Post-SOU follow-ups (2026-05-15)" cataloging the 6-PR follow-up burst (#906–#911) that closed 5 of 6 SOU §5 deferreds (periodic re-warm cron, admin tRPC router, admin React page, cross-region guard primitive, cross-region tRPC middleware factory, multi-process invalidation via PG LISTEN/NOTIFY). Names the single remaining carry-forward (Cross-region replication catch-up) as correctly deferred to Phase 2.5/3 of the multi-region ADR — a multi-quarter engineering arc, not a single slice. Adds operational-summary table contrasting single-region default vs multi-region opt-in across 5 capability axes + 3 env flags. Names invariant: single-region operational baseline preserved across all 14 PRs. | pending |
| #913 (PR-V1-162) | Phase 18 follow-up — extensions admin tRPC router | Post-MR-1 pivot to next-priority open V1+ surface. Phase 18-α through 18-γ shipped the extension framework primitives (`agsExtensions` table, manifest/contracts/runtime, capability-pinned dispatch, lane hook registry, typed contracted lanes); no admin tRPC surface existed. New `services/extensions/extensions-admin-router.ts` mounted at `agentStudio.extensions.*` with 5 admin procedures: `list` (workspaceId + optional status filter), `get` (extensionId), `install` (mutation), `approve` (mutation), `setStatus` (mutation). Inputs Zod-validated against closed taxonomies (`EXTENSION_GOVERNANCE_STATUSES` 5-value + `EXTENSION_CAPABILITY_LANES` 4-value). Mutations route through the existing `installExtension` / `approveExtension` / `setExtensionStatus` manifest helpers — governance scaffolding contract preserved (no direct agsExtensions writes from the router). Public-api barrel re-exports `extensionsAdminRouter`. 7-test source-scan at `tests/agent-studio/extensions-admin-router-mounted.test.ts` (router-mount + 5-procedure declaration + verb correctness + Zod taxonomy enforcement + governance-contract preservation + barrel re-export + hard-rule compliance). React surface is a separate slice. | pending |
| #914 (PR-V1-163) | Phase 18 follow-up — extensions admin React page | Closes the React half of the Phase 18 admin surface. New `client/src/modules/agent-studio/components/ExtensionsAdminPanel.tsx` queries `agentStudio.extensions.list` for a single workspace and renders the result as a table with columns: key / name / version / status (color-coded badge) / declared lanes / declared tool count / approved-at timestamp. Read-only — install/approve/setStatus mutations are exposed via the tRPC router (#913) but a dedicated wizard UI is its own slice. Status badge color mapping is closed-taxonomy: approved=green, pending_approval=amber, rejected/revoked=destructive, disabled=muted. New `client/src/modules/agent-studio/pages/ExtensionsAdminPage.tsx` is a slim wrapper accepting optional `workspaceId` (default 1, matches the existing RetrofitPage default). Wired into `AgentStudioShell.tsx` via lazy-import + path matcher (`/agent-studio/extensions-admin`) + view dispatch. 5-test source-scan at `tests/agent-studio/extensions-admin-page-mounted.test.ts` (page + panel + query check + status-badge taxonomy mapping + shell lazy-import + route + dispatch). | pending |
| #915 (PR-V1-164) | Phase 15-δ follow-up — expose template-instantiation query helpers via tRPC | The 3 query helpers in `services/vault/template-instantiations.ts` (`listInstantiationsByTemplate`, `listInstantiationsByNote`, `countDistinctDigestsForTemplate`) have shipped since Phase 15-α (#750) but no router exposure existed — operators couldn't ask "which notes were created from template X?" or "how many distinct digests has this template churned through?" without direct DB queries. Mount all 3 on `vaultRouter` as `protectedProcedure` queries with Zod-validated positive-integer inputs + bounded limit (max 500 on `listInstantiationsByTemplate`). `countDistinctDigestsForTemplate` returns a structured `{ templateId, distinctDigests }` shape for client convenience. 4-test source-scan at `tests/agent-studio/vault-template-instantiation-queries-mounted.test.ts` (imports + procedure declarations + Zod input shapes + response shape). Behavioral tests for the underlying helpers already exist. | pending |
| #916 (PR-V1-165) | D-RESUME-5 closure — approval event bus cross-process pubsub | Closes the LISTEN/NOTIFY upgrade path documented in `approval-event-bus.ts` D-RESUME-4 → D-RESUME-5. Pattern mirrors `region-cache-pubsub.ts` (#911) — same Postgres LISTEN/NOTIFY infrastructure, no new dependencies. Before this slice (single-process baseline), an approval decision on process P2 was invisible to chat-stream waiting on process P1 → waitFor times out → user re-prompts. After: emit on P2 also fires `NOTIFY ags_approval_decided` with `{approvalRequestId, status, expiresAt?}` JSON payload; P1's subscriber LISTENs, parses, re-emits on P1's local bus, chat-stream resumes. New `services/runtime/approval-event-bus-pubsub.ts` (4 helpers + `__test` payload round-trip seam). `approval-event-bus.ts` gains `setApprovalEventEmitHook` late-binding slot + `fireEmitHook(event)` call in `emit()`. New `services/runtime/install-approval-bus-pubsub.ts` wires both directions with a `_inRelayedEmit` guard preventing fan-out loops (inbound re-emit doesn't trigger outbound NOTIFY). Boot Step 3.34 env-flag-gated via `AGS_APPROVAL_BUS_PUBSUB=on` (default OFF). 9-test suite at `tests/agent-studio/approval-bus-pubsub.test.ts` (helper exports + payload round-trip + null-expiresAt + malformed-input rejection + bus emit-hook wiring + guard mechanism + boot wire-up + hard-rule compliance). Single-process operational baseline preserved: flag unset = behavior identical. | pending |
| #917 (PR-V1-166) | Generic Postgres LISTEN/NOTIFY pubsub primitive (extracted) | Refactor-prep: extract the shared connect/reconnect/dispatch machinery from the two existing cross-process buses (#911 region + #916 approval) into a generic primitive at `services/pubsub/pg-listen-notify-bus.ts`. `createPgListenNotifyBus<TEvent>({channel, encode, decode, url?, logPrefix})` returns a fresh bus instance with `notify` / `subscribe` / `unsubscribe` / `maybeSubscribe` (env-flag-gated opt-in). Two ready-to-use codecs: `STRING_CODEC` (identity passthrough — for label-payload buses like region) + `jsonCodec<TEvent>(validate)` (round-trips JSON with caller-supplied validator — for structured events like approval). Quote-escape + 200-char cap + linear-backoff reconnect (10s→60s) preserved from the existing buses. New `services/pubsub/public-api.ts` barrel. 9-test suite at `tests/agent-studio/pg-listen-notify-bus.test.ts` covers codec round-trips + malformed-JSON rejection + bus shape + env-flag no-op + hard-rule compliance + NOTIFY-defense + reconnect-cap source-scans. **Not a refactor of the existing two implementations** — they keep working as standalone modules; migrating them onto this primitive is its own incremental slice once the API has soaked. | pending |
| #918 (PR-V1-167) | Region pubsub observability — `getPubsubStatus` admin surface | Closes an operator gap left by #911: there was no way to verify multi-process pubsub was actually wired without checking process logs. Adds lifetime counters to `region-cache-pubsub.ts` (`connectedAt`, `lastMessageAt`, `lastMessageReason`, `messagesReceived`, `reconnectAttempts`); set on success/error/notification paths. New `getRegionCachePubsubStatus()` returns `RegionCachePubsubStatus` snapshot. Exposed via `agentStudio.region.getPubsubStatus` (admin tRPC). `RegionAdminPanel.tsx` renders a new "Cross-process pubsub" card with 6 fields. Lifetime counters survive `unsubscribe` (only `subscribed`+`connectedAt` clear); fresh process restart resets them via module reload. Public-api barrel re-exports the new symbols. 8-test suite covering cold state + unsubscribe semantics + barrel + router exposure + UI fields + source-scan of state-tracking paths. | pending |
| #919 (PR-V1-168) | Approval bus pubsub observability — symmetric `getPubsubStatus` admin surface | Symmetric with PR-V1-167 for the approval-event-bus pubsub (#916). Adds lifetime counters to `approval-event-bus-pubsub.ts` (7 fields including `lastApprovalRequestId` + new `malformedReceived` counter for bad-payload tracking). `getApprovalBusPubsubStatus()` returns the snapshot; exposed via a new `services/runtime/approval-bus-admin-router.ts` mounted at `agentStudio.approvalBus.getPubsubStatus`. Single-procedure router today; the namespace exists so future approval-bus observability (e.g. waitFor cancel-rate / timeout-rate) lands here rather than scattering. 6-test suite covering cold state + 7-field shape + counter wiring source-scan + router exposure + mount + hard-rule compliance. Single-process operational baseline preserved: when flag unset, all counters stay at zero. | pending |
| #920 (PR-V1-169) | Phase 17-γ follow-up — canvas node update + delete + projection event emission | The 17-γ events sink/persistence/drain chain (#792 → #812) shipped the `canvas.note_reference_removed` event kind but no caller emitted it — `canvas-service.ts` had only `createCanvasNode`. This PR adds `updateCanvasNode` + `deleteCanvasNode` to the service, plus `UpdateCanvasNodeInput` and `CanvasNodeNotFoundError` to the types module. Update covers all 4 referencedNoteId diff cases (null→X, X→Y, X→null, X→X) with the correct event emission; delete emits `_removed` when the deleted node carried a referencedNoteId. Routes via the same `resolveWorkspaceIdForCanvas` split-handle that createCanvasNode uses. `updateCanvasNode` no-op short-circuit when the input patch is empty (no round-trip). `deleteCanvasNode` idempotent on missing rows (returns `false`). Public-api barrel re-exports the new symbols. 10-test source-scan at `tests/agent-studio/canvas-node-update-delete.test.ts` covers each diff branch + routing + idempotency + barrel + hard-rule. Out of scope: tRPC router exposure (canvas-service has no tRPC mount today; that's its own slice). | pending |
| #921 (PR-V1-170) | Phase 17 closure — canvas tRPC router (first external consumer of canvas-service) | Canvas-service has been hermetic since Phase 17-α (#752): no external callers. This PR mounts `agentStudio.canvas.*` exposing 9 procedures: 4 queries (`get`, `listByVault`, `getSnapshot`, `listNoteReferences`) + 5 mutations (`create`, `createNode`, `updateNode`, `deleteNode`, `createEdge`). All inputs Zod-validated against the closed `CANVAS_NODE_KINDS` 4-value taxonomy. Mutations delegate to canvas-service helpers — no direct `agsCanvases` / `agsCanvasNodes` / `agsCanvasEdges` writes from the router; the service owns the canvas→vault→workspace split-handle + 17-γ projection event emission. `protectedProcedure` across the board (any logged-in workspace member can read/write); a future slice can tighten mutations to `governedProcedure` if Canvas needs approval gates. `deleteNode` returns the structured `{ removed: boolean }` shape for client convenience. Public-api barrel re-exports `canvasRouter`. 8-test source-scan at `tests/agent-studio/canvas-router-mounted.test.ts` (router mount + 9-procedure declaration + verb correctness + closed-taxonomy Zod enforcement + service-delegation invariant + barrel + hard-rule + `removed` shape). With this slice the Phase 17 stack — schema (#752) → service (#752 + #844-#845 + #920) → events sink + persistence + drain (#792 → #812) → tRPC mount (this) — is operator-reachable end-to-end. | pending |
| #922 (PR-V1-171) | Phase 17 UI — canvas operator browser React page | First UI consumer of the canvas tRPC router from #921. New `client/src/modules/agent-studio/components/CanvasOperatorPanel.tsx` is a read-only operator browser: vault selector (default 1) → list canvases via `agentStudio.canvas.listByVault` → click "inspect" on a row to drill into a canvas → snapshot card shows node/edge counts + vault id; note-references card lists each canvas node's referenced note. Three tRPC reads (`listByVault`, `getSnapshot`, `listNoteReferences`) with `enabled` guards on the canvas-id-conditional pair. Scope intentionally minimal — an interactive editor (drag/drop nodes, draw edges) is a separate UX slice. New `client/src/modules/agent-studio/pages/CanvasOperatorPage.tsx` slim wrapper accepting optional `initialVaultId`. Wired into `AgentStudioShell.tsx` via lazy-import + path matcher (`/agent-studio/canvas-operator`) + view dispatch. 6-test source-scan at `tests/agent-studio/canvas-operator-page-mounted.test.ts` covers page + panel + 3-query check + `enabled` guard + inspect-handler + shell lazy-import + route + dispatch. With this slice the Phase 17 stack reaches the React UI end-to-end. | pending |
| #923 (PR-V1-172) | Phase MR-1 admin UI — pin mutation UI | Adds operator setPin / removePin mutation UIs to the existing `RegionAdminPanel` (#908). New "Set / update workspace pin" form: workspaceId (number) + regionKey (text with inline datalist of known region keys) + isReplicated (checkbox) + notes (text). Button disabled while pending or required fields empty; client-side validation that workspaceId is a positive integer; surface tRPC errors via a `mutationError` state. Pins table grows a per-row "remove" button calling `removePin`. Both mutations invalidate `listPins` + `getCacheStatus` + `getPubsubStatus` on success (the existing pin-change hook + pubsub fan-out also fire, but the UI invalidation avoids a stale-render race). 8-test source-scan at `tests/agent-studio/region-admin-mutation-ui.test.ts` covers import shape + mutation wiring + cache invalidations + error surface + workspaceId validation + datalist + disabled-state + remove-button. The operator install wizard for `registerRegion` (more substantial — needs postgres-URI + credential-binding-id UX) stays in carry-forward. | pending |
| #924 (PR-V1-173) | Phase 18 admin UI — extensions approve + setStatus mutation UI | Symmetric with #923: adds per-row mutation actions to `ExtensionsAdminPanel` (#914). Approve button appears only on `pending_approval` rows; requires a positive-integer Actor user ID entered at the top of the panel. Status dropdown exposes the closed 5-value `EXTENSION_GOVERNANCE_STATUSES` taxonomy (pending_approval / approved / rejected / disabled / revoked); no-op skip when the selected value equals the current status. Both mutations invalidate the workspace's list query on success. Error surface via shared `mutationError` state. 9-test source-scan at `tests/agent-studio/extensions-admin-mutation-ui.test.ts` covers approve-only-on-pending + actor-user-id validation + closed-taxonomy select + no-op-skip + invalidation + error surface + Actions column. The install wizard (`installExtension` with full manifest input UX) stays in carry-forward — it needs schema-validated multi-field input handling for capabilityLanes + declaredToolNames which is its own UX slice. | pending |
| #925 (PR-V1-174) | Phase 17-γ admin — canvas drain `forceTick` mutation | Operators can now trigger a manual catch-up tick of the canvas projection events drain via `agentStudio.canvasProjectionEventsDrain.forceTick`. Useful for (a) smoke-testing forwarder install in dev without waiting for the scheduler's default 60s interval, (b) force-draining after a manual catch-up. Short-circuits with `{ ran: false, reason: "no_forwarder_installed" }` when no forwarder is wired (mirrors the existing tick-wrapper's no-op + WARN behavior). Happy path returns `{ ran: true, result: <drain aggregate> }`. adminProcedure-gated. 5-test source-scan at `tests/agent-studio/canvas-drain-force-tick.test.ts` covers imports + verb + short-circuit + happy-path shape + hard-rule. The existing 17-γ tick wrapper's behavior tests (#810) cover the underlying mechanics; this slice adds only the operator-facing exposure. | pending |
| #926 (PR-V1-175) | Phase 17-γ UI — drain `forceTick` button on status panel | Adds the matching operator UI button + feedback line to `CanvasProjectionEventsDrainStatusPanel`. Wired to the #925 mutation; status query invalidated on success so the card refreshes with the new ticksRun/cursorMapSize. Feedback line surfaces `ran=true` aggregate (`processed N events across M workspace(s)`) or `ran=false` reason verbatim. Button disabled while pending. Test IDs (`drain-force-tick-button`, `drain-force-tick-feedback`) added for downstream Playwright. 7-test source-scan at `tests/agent-studio/canvas-drain-force-tick-ui.test.ts` covers wiring + invalidation + feedback formatting + disabled-state + testids + error path. | pending |
| #927 (PR-V1-176) | Phase MR-1 admin — region cache `forceRewarm` mutation + UI button | Symmetric with #925/#926. Adds `agentStudio.region.forceRewarm` admin mutation that runs `warmRegionRoutingCache()` synchronously and returns the new summary (activeRegionCount / pinCount / primaryRegionKey / lastWarmedAt). Matching "Force re-warm now" button on the routing-cache card of `RegionAdminPanel`; on success invalidates `getCacheStatus` + `listActiveRegions` + `listPins`; feedback line surfaces the new counts. Test IDs added (`region-cache-force-rewarm-button`, `region-cache-force-rewarm-feedback`). 6-test source-scan covers router wire-up + 4-field summary + UI mutation wiring + counts surface + testid + error prefix. Closes the "operator-triggered re-warm" gap without changing the 10-min cron cadence. | pending |
| #928 (PR-V1-177) | Sidebar nav — region / extensions / canvas-operator entries | The 3 admin pages shipped this session (region admin #908, extensions admin #914, canvas operator #922) all had routes registered in `AgentStudioShell` but no sidebar entry. Operators had to type the URL by hand. Adds Extensions / Multi-region / expanded Canvas groups to `HOME_GROUPS` in `AgentStudioSidebar`, with appropriate Lucide icons (`Plug` / `Globe` / `LayoutGrid`). Extends the `AgentStudioView` union with `region-admin` / `extensions-admin` / `canvas-operator` keys. Shell's `onItemClick` handler maps each new key to its absolute admin URL. 6-test source-scan covers union extension + icon imports + group declarations + key membership + shell click-mapping. | pending |
| #929 (PR-V1-178) | Manifest + routes — route inventory sync (admin pages) | Several admin pages (`vault-attachments`, `vault-saved-views`, `canvas-projection-events-drain`, `canvas-operator`, `region-admin`, `extensions-admin`, `mcp-manager`) dispatched in the Shell without a corresponding declaration in `manifest.routeInventory` or the `agentStudioRoutes` array. Adds all 7 to both files in lockstep so `check:module-route-inventory` tooling stays consistent and operator nav surfaces match the capsule's mount manifest. 14-test source-scan (7 paths × 2 files) at `tests/agent-studio/manifest-route-inventory-sync.test.ts`. | pending |
| #930 (PR-V1-179) | Phase 18 admin — extensions hard uninstall mutation + UI button | New `uninstallExtension(extensionId)` in `manifest.ts` distinct from `setExtensionStatus(..., "revoked")` — actually DELETEs the row (revoked preserves it for audit). Looks up workspaceId via `getAsDb()` then deletes via `getAsDbForWorkspace(workspaceId)` so the multi-region routing shim applies. Exposed via `agentStudio.extensions.uninstall` admin mutation returning `{ removed: boolean }`. Idempotent — returns `false` when no row matched. `ExtensionsAdminPanel` gets a per-row "Uninstall" button with `window.confirm()` gate explaining the difference vs. revoked status; button disabled while the mutation is pending; `onSuccess` invalidates `list`. 6-test source-scan at `tests/agent-studio/extensions-uninstall.test.ts` covers DELETE path + idempotency + barrel re-export + router shape + UI mutation hook + confirm() gate + disabled-state + hard-rule compliance. | pending |
| #931 (PR-V1-180) | Phase 18 admin — uninstall cascade-delete `ags_extension_invocations` | Correctness fix on #930. `ags_extension_invocations.extension_id` is a foreign key reference to `ags_extensions.id`; any extension that has ever been invoked has telemetry rows, and the parent DELETE in `uninstallExtension` would fail with an FK violation, defeating the uninstall path in production. Adds a leading `db.delete(agsExtensionInvocations).where(eq(..., extensionId))` step inside the same routing-shim-aware DB handle before the parent DELETE. Telemetry cascade is intentional — invocation log is per-extension and not the audit-of-record (that lives in `ags_approval_steps`); operators who want to preserve the telemetry should `setStatus(..., "revoked")` instead. New 7th test asserts the cascade exists, references the right column, and runs BEFORE the parent delete. | pending |
| #932 (PR-V1-181) | Phase 18 admin — extensions invocation telemetry surface | New `getInvocationSummariesByWorkspace(workspaceId)` in `manifest.ts` aggregates `ags_extension_invocations` per extension with `count(*) filter (where capability_check = 'allowed')` for allowed / denied split + `max(invoked_at)` for last-seen. Zero-fills extensions with no telemetry so the UI doesn't merge client-side. Exposed via `agentStudio.extensions.workspaceInvocationSummaries` admin query; `ExtensionsAdminPanel` joins the summary into the existing table by `extensionId` and renders two new columns: `Invocations` (`total (allowed/denied)`) + `Last invoked`. Refresh path extended so mutations invalidate the summary in lockstep with `list`. 6-test source-scan at `tests/agent-studio/extensions-invocation-summary.test.ts` covers function shape + interface + filter aggregation + barrel + router query wiring + UI consumption + invalidation + hard-rule compliance. | pending |
| #933 (PR-V1-182) | Phase 18 admin — recent-invocations operator activity feed | Follow-up to the summary surface (#932) that lets operators drill into the denied / failed invocations behind the `total (allowed/denied)` count. New `listRecentInvocationsByWorkspace({workspaceId, extensionId?, limit?})` returns the N (default 50, capped at 500) most-recent rows from `ags_extension_invocations`, ORDER BY `invokedAt DESC`. Workspace-bounded via an explicit pre-fetch of installed `extensionIds` + `inArray` intersect so cross-workspace ids can't leak even when an operator passes a stray `extensionId`. Exposed via `agentStudio.extensions.recentInvocations` admin query; `ExtensionsAdminPanel` adds a second Card "Recent invocations" below the table with a per-extension filter dropdown + 7-column log table (when / extension / lane / tool / capability / OK? / error), with non-"allowed" capability values rendered in `text-destructive`. Refresh path extended so mutations invalidate the log in lockstep with `list` + `workspaceInvocationSummaries`. 6-test source-scan at `tests/agent-studio/extensions-recent-invocations.test.ts` covers function + interface + workspace-bound + ORDER BY + limit clamp + barrel + router cap-500 + UI testid + filter dropdown + invalidation + hard-rule. | pending |
| #934 (PR-V1-183) | Phase MR-1 admin — workspace-location lookup tool | New `resolveWorkspaceRegionLookup(workspaceId)` in `workspace-region-cache.ts` answers the most-common operator question: "Where does workspace X live and how was that decided?" Pure read of the in-process cache (no DB roundtrip). Returns `{ workspaceId, regionKey, pinSource, pinExists, cacheIsWarm, lastWarmedAt }` with `pinSource` ∈ `"explicit-pin"` / `"primary-fallback"` / `"no-active-region"`. The misconfiguration case — pin row exists but the named region is inactive — surfaces as `pinSource="no-active-region"` with `pinExists=true` so the operator can tell apart "no pin → fell back to primary" from "pin pointed at a dead region". Exposed via `agentStudio.region.lookupWorkspaceRegion` admin query; `RegionAdminPanel` gets a new `WorkspaceLocationLookupCard` with a lazy-enabled query (only fires on `Look up` button press) + 5-field result block, with `pinSource="no-active-region"` rendered in `text-destructive`. 6-test source-scan at `tests/agent-studio/region-workspace-lookup.test.ts` covers cache export + closed taxonomy + 6 result fields + misconfiguration branch + cold-cache branch + barrel + router wiring + UI lazy enablement + testids + destructive-highlight + hard-rule. | pending |
| #935 (PR-V1-184) | D-RESUME-5 admin — approval-bus force-reconnect mutation + UI page | The `agentStudio.approvalBus.getPubsubStatus` surface shipped in #168 had no UI consumer and no remediation for the "subscribed=true but connectedAt=null" failure mode (LISTEN failed at boot or backoff stuck at 60s cap). Adds `forceReconnectApprovalBusSubscriber()` which preserves the existing handler, cancels the pending reconnect timer, ends the current `pg.Client`, and re-subscribes (no-op when nothing subscribed, returning `{reconnected:false, reason:"not-subscribed"}`). Exposed via `agentStudio.approvalBus.forceReconnect` admin mutation. Adds first UI consumer: new `ApprovalBusAdminPanel` (status grid + force-reconnect button with feedback, stale-connection highlight via `subscribed && connectedAt == null`, malformedReceived highlight in `text-destructive` when > 0) and `ApprovalBusAdminPage` reachable at `/agent-studio/approval-bus-admin`. Wired through Shell (URL parser + dispatch case), sidebar (new `Approval bus` group with `Radio` icon), manifest `routeInventory`, and `agentStudioRoutes`. 5-test source-scan at `tests/agent-studio/approval-bus-force-reconnect.test.ts` covers both branches of force-reconnect + router shape + UI testids + stale-connection highlight + invalidation + sidebar/manifest/routes inventory + hard-rule. | pending |
| #936 (PR-V1-185) | Phase MR-1 admin — region cache pubsub force-reconnect (symmetric with #935) | Symmetric `forceReconnectRegionCachePubsubSubscriber()` on the region cache pubsub. Same shape as the approval-bus version — preserves handler, cancels pending timer, ends current `pg.Client`, re-subscribes; idempotent no-op + `{reconnected:false, reason:"not-subscribed"}` when nothing subscribed. Exposed via `agentStudio.region.forceReconnectPubsub` admin mutation. `RegionAdminPanel` gets a Force reconnect now button + feedback line on the existing Cross-process pubsub card (no new page — wired inline), with `getPubsubStatus` invalidated on success. 5-test source-scan at `tests/agent-studio/region-pubsub-force-reconnect.test.ts` covers function + both branches + barrel + router + UI testids + invalidation + feedback shape + hard-rule. Closes the LISTEN-stuck-at-boot operator-recovery gap symmetrically across both LISTEN/NOTIFY channels. | pending |
| #937 (PR-V1-186) | Phase 19 admin — publish-targets registry + executions ledger (read-only) | The publish-targets subsystem (#749 et al.) had no operator observability — registry + executions ledger existed but operators had to write SQL to inspect them. Adds new `services/publish-targets/admin-queries.ts` with `listPublishTargets()` (no filters; targets are dozens-not-thousands) + `listRecentPublishExecutions({targetId?, status?, limit?})` (ORDER BY createdAt DESC, default 50, capped at 500). New `admin-router.ts` mounts `agentStudio.publishTargets.{listTargets, listRecentExecutions}` — both `adminProcedure.query`, no mutations (read-only first slice). New `PublishTargetsAdminPanel` + `PublishTargetsAdminPage` reachable at `/agent-studio/publish-targets-admin`: two Cards (registry table + recent-executions log) with `target` + `status` filter dropdowns and `succeeded`/`pending`/`failed` color coding. Wired Shell URL parser + dispatch, sidebar (new `Publish` group with `Send` icon), manifest `routeInventory`, `agentStudioRoutes`. 7-test source-scan at `tests/agent-studio/publish-targets-admin.test.ts` covers function shape + read-only assertion + ORDER BY + limit clamp + router shape + cap-500 + no-mutations + barrel + main-router mount + UI testids + filter ids + Shell/sidebar/manifest/routes inventory + hard-rule compliance. | pending |
| #938 (PR-V1-187) | Phase 19 admin — publish-targets enable/disable toggle | First mutation on the publish-targets admin surface (#937). `setPublishTargetEnabled(targetId, enabled)` uses the standard lookup-then-update split (SELECT existing → UPDATE → SELECT post-update for return shape) with `PublishTargetNotFoundForToggleError` on no-match. Disabled targets refuse dispatch via the existing `PublishTargetDisabledError` in `executePublish` — flipping this bit is the standard incident-response action for an unhealthy upstream. Exposed via `agentStudio.publishTargets.setEnabled` admin mutation. `PublishTargetsAdminPanel` gets a per-row `enable`/`disable` button with label flipping by current state; success invalidates `listTargets`. 5-test source-scan at `tests/agent-studio/publish-targets-set-enabled.test.ts` covers lookup-then-update + not-found error + router shape + boolean input + barrel re-export + UI testids + invalidation + label-flip + hard-rule. | pending |
| #939 (PR-V1-188) | Phase 19 admin — publish-targets per-target execution summary | Inline health summary for each target — symmetric with the extensions invocation-summary surface (#932). New `getPublishTargetExecutionSummaries()` aggregates `ags_publish_target_executions` per `targetId` with `count(*) filter (where status = '<status>')` for each of the closed `succeeded` / `pending` / `failed` taxonomy + `max(createdAt)` for last-seen, zero-filled against the registry so every target row in the UI has a corresponding summary entry. Exposed via `agentStudio.publishTargets.executionSummaries` admin query; `PublishTargetsAdminPanel` joins the summary into the registry table and renders two new columns: `Executions` (`total (s/p/f)`) + `Last execution`. `setEnabled` success invalidates the summary alongside `listTargets` so the operator sees the toggle's effect reflected immediately. 6-test source-scan at `tests/agent-studio/publish-targets-execution-summary.test.ts` covers function + interface + count+filter aggregation + groupBy + zero-fill + router shape + barrel + UI joining + display shape + invalidation + hard-rule. | pending |
| #940 (PR-V1-189) | Phase 19 admin — publish-targets per-execution detail drill-down | The recent-executions log (#937) showed status + error but not the `details` JSON blob that pushers populate with response bodies / http headers / latency etc. Adding `details` to the list query would balloon payload size; instead this slice adds an on-demand single-row fetch. New `getPublishExecutionById(executionId): Promise<PublishExecutionDetail | null>` returns the full row + parsed `details` JSON. `PublishExecutionDetail` extends `PublishExecutionRow` with `details: Record<string, unknown> | null`. Exposed via `agentStudio.publishTargets.getExecutionById` admin query. `PublishTargetsAdminPanel` adds a `view`/`hide` toggle column per row (8th column); clicking flips `expandedExecutionId` state, the query is lazy-enabled so no fetch happens unless an operator clicks. Expanded row renders `JSON.stringify(details, null, 2)` in a `<pre>` with `bg-muted/30`. 5-test source-scan at `tests/agent-studio/publish-targets-execution-details.test.ts` covers function + extends-pattern + null-on-no-match + router shape + barrel + UI lazy enablement + testids + label flip + JSON.stringify render + hard-rule. | pending |
| #941 (PR-V1-190) | Phase J-1-β admin — graph-health UI (first consumer of the J-1-β surface) | The `graphHealth` router (J-1-β, #758) had two admin queries (`getAlertCronStatus` + `listOpen`) with no UI consumer since shipping. This slice closes that gap end-to-end — same pattern as the ApprovalBusAdminPage (#935) and PublishTargetsAdminPage (#937). New `GraphHealthAdminPanel` with two Cards: cron status grid (`lastRunAt` / `lastError.message` highlighted in `text-destructive`, plus `scanned`/`raised`/`resolved` counters from `lastResult`) and an open-alerts table (`Raised` / `Key` / `Severity` color-coded by `critical`/`warning`/`info` / `Observed` / `Threshold` JSON-stringified). New `GraphHealthAdminPage` at `/agent-studio/graph-health-admin`. Wired through Shell URL parser + dispatch + onItemClick, sidebar (new `Graph Health` group with `HeartPulse` icon), manifest `routeInventory`, `agentStudioRoutes`. Read-only — alert resolution stays on the server-side `resolveHealthAlert` helper. 3-test source-scan at `tests/agent-studio/graph-health-admin-ui.test.ts` covers query wiring + testids + severity color coding + no-mutations assertion + Shell/sidebar/manifest/routes inventory. | pending |
| #942 (PR-V1-191) | Phase J-1-β admin — projection drift cron UI (first consumer) | Pair-mounted alongside the alert cron card on the GraphHealth panel (#941). `agentStudio.graphProjection.getDriftCronStatus` (strict-audit item #9 partial closure, 2026-05-13) had no UI consumer since shipping. Same `{ lastRunAt, lastResult, lastError }` shape — operators check both crons at the same time, so a separate page would have been friction. Slots a third Card between the alert cron and open-alerts cards on `GraphHealthAdminPanel`, with the same `lastError`-highlight pattern. 1-test source-scan at `tests/agent-studio/graph-projection-drift-cron-ui.test.ts` covers query wiring + testid + lastError highlight + card-label. | pending |
| #943 (PR-V1-192) | Phase 17-γ admin — canvas drain per-workspace breakdown UI | The drain status panel surfaced `totalProcessed` + `totalFailed` aggregates but not the `lastTickResult.perWorkspace[]` breakdown — so when `totalFailed > 0` the operator had no way to find the culprit workspace without writing SQL. Adds a 4-column breakdown table (workspaceId / processed / failed / cursor) rendered when `perWorkspace.length > 0`. Failed > 0 cells get `text-destructive`; idle rows (processed=0 + failed=0) get `text-muted-foreground` for de-emphasis. No new tRPC — purely a richer rendering of data already returned by `getDrainStatus`. 1-test source-scan at `tests/agent-studio/canvas-drain-per-workspace-ui.test.ts` covers testid + 4 column labels + failed/idle highlight branches + conditional render guard. | pending |
| #944 (PR-V1-193) | Phase 19 admin — publish-targets per-target config drill-down | Symmetric with the per-execution `details` drill-down (#940) — `agsPublishTargets.config` JSON holds per-target operator metadata (signing key id, max payload size, dry-run flag) that operators sometimes need to verify but adding it to the registry list would balloon the payload. New `getPublishTargetById(targetId): Promise<PublishTargetWithConfig | null>` returns the full row with `config: Record<string, unknown> | null`; `PublishTargetWithConfig extends PublishTargetSummary` with the additional field. Exposed via `agentStudio.publishTargets.getTargetById` admin query. `PublishTargetsAdminPanel` adds a second per-row button (`view config` / `hide config`) inside the Actions cell; clicking flips `expandedTargetId`, the query is lazy-enabled, and the expanded row renders `JSON.stringify(config, null, 2)` in a `<pre>` with `bg-muted/30`. Refactors the row render to wrap in `<React.Fragment>` so the per-target expansion can sit between the row and the next without breaking React's keyed-list invariant. 5-test source-scan at `tests/agent-studio/publish-targets-config-view.test.ts` covers function + extends-pattern + null-on-no-match + router + barrel + UI lazy enablement + testids + label flip + JSON.stringify render + hard-rule. | pending |
| #945 (PR-V1-194) | Phase 19 admin — publish executions duration column | `startedAt` + `completedAt` were already in the execution row payload but unrendered. Adds a derived `Duration` column to the executions table — operator-friendly format: `—` when either side is null (pending row), `<1s` for sub-second, `Xs` for seconds-only, `Xm Ys` otherwise. Operators can now see at a glance which pushes are slow without computing the diff themselves. The per-row testid `publish-execution-duration-<id>` keeps automation hooks stable. Bumps the expanded-detail-row colSpan from 8 → 9 to match the new column count. 2-test source-scan at `tests/agent-studio/publish-execution-duration-column.test.ts` covers Duration column label + fmtDuration helper (all four format branches) + cell wiring + colSpan-stays-in-sync + no stale colSpan=8 left behind. | pending |
| #946 (PR-V1-195) | Phase 18 admin — extensions actor-audit columns | Same shape as the publish-execution duration column (#945) — data already written to the DB by `installExtension` / `approveExtension` / `setExtensionStatus` (`installedByUserId` / `approvedByUserId` / `disabledByUserId`) but never read out on the `ExtensionRecord` shape. Adds the three fields to `ExtensionRecord`, populates them in `rowToExtension`, and renders an `Actors` column on `ExtensionsAdminPanel` showing the three user IDs in `installed / approved / disabled` shape with `—` placeholders for null slots and a `title` tooltip spelling out which is which. No new tRPC — pure read-shape extension. 3-test source-scan at `tests/agent-studio/extensions-actor-audit-columns.test.ts` covers contract fields + rowToExtension population + UI Actors column label + testid + display shape. | pending |
| #947 (PR-V1-196) | Phase 17-γ admin — canvas drain per-workspace batches column | Extends the per-workspace breakdown table (#943) with a `Batches` column showing `batchesRun` + a `!` suffix when `hitMaxBatches=true`, with `text-amber-600` highlight and a tooltip explaining what the ceiling-hit means. Closes the "which workspace hit the ceiling?" gap — the global panel-level amber warning (`drain-hit-max-batches`) said "one or more workspaces" but didn't say which; this column makes it visible per-row. No new tRPC — pure rendering extension of data already in `perWorkspace[]`. 1-test source-scan at `tests/agent-studio/canvas-drain-batches-column.test.ts` covers column header + testid + amber highlight + `!` suffix + cell wiring. | pending |
| #948 (PR-V1-197) | Phase 18 admin — extensions signing indicator | The `signingKeyId` field (already on `ExtensionRecord`) was unrendered, so operators couldn't tell at a glance whether an extension was a signed bundle or a dev-mode / unsigned install. Adds an inline indicator next to the extension name: `[unsigned]` in `text-destructive` when `signingKeyId == null`, or a muted `(<key-id>)` chip otherwise. Per-row testids (`extension-row-unsigned-<id>` / `extension-row-signing-<id>`) keep automation hooks stable. No new column — operator can spot unsigned bundles while scanning the name field. No tRPC changes. 2-test source-scan at `tests/agent-studio/extensions-signing-badge.test.ts` covers unsigned-branch + signed-branch + testids + tooltip wording. | pending |
| #949 (PR-V1-198) | Phase 18 admin — extensions lifecycle cell (3 timestamps in 1 column) | The Approved column displayed only `approvedAt`; the schema also carries `installedAt` + `disabledAt` but both were unrendered. Renames the column to `Lifecycle` and updates the inline cell to show the most-recently-relevant timestamp — `disabledAt` when status is `disabled` or `revoked`, `approvedAt` otherwise. The cell's `title` carries the full three-timestamp lifecycle (`installed X / approved Y / disabled Z`) so operators can hover for the complete audit without sacrificing column width. Per-row testid `extension-row-approved-<id>` (kept stable from prior PR for automation). No new tRPC — pure rendering extension. 2-test source-scan at `tests/agent-studio/extensions-lifecycle-cell.test.ts` covers header rename + inline-cell closed-taxonomy branch + tooltip carrying all three timestamps. | pending |
| #950 (PR-V1-199) | Phase 19 admin — publish executions Digest column | The `payloadDigest` field was already in the executions row payload (used by `executePublish` for idempotency + drift detection) but unrendered. Adds a Digest column showing the first 12 chars of the hash, with the full digest in the cell `title` for copy-paste. Operators can now spot duplicate pushes or verify idempotency at-a-glance. `—` em-dash when null. Bumps the expanded execution-details `colSpan` from 9 → 10 to match the new column count (the per-target config expansion at colSpan=9 is on the registry table and unaffected). Per-row testid `publish-execution-digest-<id>`. No tRPC changes. 2-test source-scan at `tests/agent-studio/publish-execution-digest-column.test.ts` covers header + truncated cell + null branch + tooltip + colSpan bump. | pending |
| #951 (PR-V1-200) | Phase J-1-β admin — graph-health cron field correctness fix | Bug fix on #941: the new GraphHealthAdminPanel referenced `lastResult.scanned`, but the actual `HealthAlertCronResult` server-side field is `decisionCount`. The grid silently rendered `undefined` for that count field. Renames the panel reference to `decisionCount`, plus surfaces the previously-unrendered `scope` / `scannedAt` / `status` / `latencyMs` fields that were also on the result type. Adds a contract-anchor test that asserts the server-side type still carries the 7 field names the UI now references — so if a server-side rename happens, this test catches it before the UI silently breaks. 2-test source-scan at `tests/agent-studio/graph-health-cron-fields.test.ts` covers panel field references + absence of the wrong `.scanned` field + server-side contract integrity. | pending |
| #952 (PR-V1-201) | Phase J-1-β admin — projection drift cron result fields | Symmetric expansion on the drift cron card (#942) — the card showed only `lastRunAt` + `lastError` but the `DriftCronResult` type carries 6 additional fields (`totalScanned` / `driftCount` / `permissionLeakCount` / `persistedEvents` / `scope` / `scannedAt`). Surfaces all 6 so operators see what the cron actually scanned, with `driftCount` + `permissionLeakCount` getting `text-destructive` when `> 0` since they represent integrity violations the operator should notice. Includes the same contract-anchor server-side type assertion pattern as #951 (PR-V1-200) so a future rename catches at test time rather than silently breaking the UI. 2-test source-scan at `tests/agent-studio/projection-drift-result-fields.test.ts` covers panel field references + destructive-when->0 highlight branches + server-side `DriftCronResult` contract integrity. | pending |
| #953 (PR-V1-202) | Phase MR-1 admin — region rewarm cron field correctness fix | Same bug class as #951. `RegionAdminPanel`'s `Re-warm cron` card referenced `cronQ.data.cronExpr` but `RetentionCronStatus` has no `cronExpr` field — the cell silently rendered the `??` fallback `"(default)"` no matter what the actual cron expression was. Drops the broken row, replaces the missing context with the previously-unrendered `lastResult` aggregate fields (`RegionCacheRewarmCronResult`: `scope` / `warmedAt` / `activeRegionCount` / `pinCount` / `primaryRegionKey`), and adds `text-destructive` to `lastError` when non-null. Also adds the two-level contract-anchor test pattern: assert both `RegionCacheRewarmCronResult` carries the 5 specific fields AND `RetentionCronStatus<TResult>` carries the 3 outer fields the UI relies on — so a future server-side rename trips a test at either level. 4-test source-scan at `tests/agent-studio/region-rewarm-cron-fields.test.ts` covers cronExpr-removed + lastResult field renders + RegionCacheRewarmCronResult contract integrity + RetentionCronStatus contract integrity. | pending |
| #954 (PR-V1-203) | Phase 18 admin — extensions per-row config drill-down | Symmetric with publish-targets config drill-down (#944). `agsExtensions.config` JSON holds per-extension operator-tunable metadata (rate limits, dry-run flag, etc.) — already on the `ExtensionRecord` shape returned by `listExtensionsByWorkspace` but unrendered. Adds a second per-row button (`view config` / `hide config`) next to the uninstall action. Clicking flips `expandedConfigId` state; the expanded row renders `JSON.stringify(ext.config ?? {}, null, 2)` in a `<pre>` with `bg-muted/30`. No new tRPC — the field is already on the list payload, so no extra fetch. Refactors the row render to wrap in `<React.Fragment>` so the expansion can sit between rows without breaking React's keyed-list invariant. `colSpan=11` matches the current column count. 3-test source-scan at `tests/agent-studio/extensions-config-drilldown.test.ts` covers toggle wiring + label flip + inline JSON render + colSpan + Fragment wrapping. | pending |
| #955 (PR-V1-204) | Phase 18 admin — extensions per-invocation details drill-down | Symmetric with publish-execution detail drill-down (#940). The recent invocations log payload doesn't include `details` JSON (could be large per row — forwarder response payloads, capability-check context, etc.). New `getExtensionInvocationById(invocationId): Promise<ExtensionInvocationDetail | null>` returns the full row including `details`. `ExtensionInvocationDetail extends ExtensionInvocationLogRow` with the additional `details: Record<string, unknown> \| null` field. Exposed via `agentStudio.extensions.getInvocationById` admin query. Log table gets a `view`/`hide` toggle column (8th column) per row; lazy-enabled query, expanded row renders `JSON.stringify(details, null, 2)` in a `<pre>` with `bg-muted/30`. 4-test source-scan at `tests/agent-studio/extensions-invocation-details.test.ts` covers service function + extends-pattern + null-on-no-match + router shape + barrel + UI lazy enablement + testids + label flip + JSON.stringify render + colSpan. | pending |
| #956 (PR-V1-205) | Phase MR-1 admin — region settings drill-down + credentialBindingId column | Symmetric with extensions config drill-down (#954) and publish-targets config drill-down (#944). `RegionRecord.settings` JSON is already on the `listActiveRegions` payload but unrendered. Adds (a) a new `Cred binding` column showing `credentialBindingId` (Plan v3 D1 anchor — null when region uses local-only credentials), and (b) a `view settings` / `hide settings` toggle in a new `Actions` cell that expands inline into `JSON.stringify(r.settings ?? {}, null, 2)`. Refactors the row render to wrap in `<React.Fragment>` so the expansion preserves React's keyed-list invariant. `colSpan=7` matches the new column count (Key / Name / Primary / Postgres URI / Neo4j URI / Cred binding / Actions). No new tRPC — fields already on the list payload, no extra fetch. 3-test source-scan at `tests/agent-studio/region-settings-drilldown.test.ts` covers credentialBindingId column + view/hide toggle + JSON inline render + colSpan + Fragment wrapping. | pending |
| #957 (PR-V1-206) | Phase 19 admin — publish executions workspace aggregate summary | Adds a one-line workspace-aggregate health summary above the recent executions filter row, derived by summing the per-target `executionSummaries` data. Surfaces total + per-status breakdown (`X executions — Y succeeded / Z pending / W failed`) with color coding: emerald when succeeded > 0, amber when pending > 0, `text-destructive` when failed > 0. Operators see at-a-glance whether anything is wrong without scrolling through the recent log. Only renders when there's at least one target — no `0/0/0/0` row clutter on empty workspaces. No new tRPC — derived from the existing `executionSummaries` query. 3-test source-scan at `tests/agent-studio/publish-executions-aggregate-summary.test.ts` covers aggregate computation + 3-branch color coding + render guard. | pending |
| #958 (PR-V1-207) | Phase 18 admin — extensions invocations workspace aggregate summary | Symmetric with the publish-executions aggregate (#957). Sums the per-extension `workspaceInvocationSummaries` data into a single workspace-aggregate line above the recent invocations filter row — `X invocations — Y allowed / Z denied`. Color coding: emerald when `allowed > 0`, `text-destructive` when `denied > 0`. Render-gated on `summariesQ.data.length > 0` so empty workspaces don't see `0/0/0`. No new tRPC — derived from existing query. 3-test source-scan at `tests/agent-studio/extensions-invocations-aggregate-summary.test.ts` covers aggregate computation + color-coding branches + render guard. | pending |
| #959 (PR-V1-208) | Phase J-1-β admin — graph health open alerts severity aggregate | Third in the aggregate-summary mini-arc (#957/#958/this). Adds a severity breakdown line above the open-alerts table — `N total — X critical / Y warning / Z info`. critical > 0 → `text-destructive`, warning > 0 → amber. Render-gated on `openQ.data.length > 0`. No new tRPC — derived from existing query. 3-test source-scan at `tests/agent-studio/graph-health-alerts-aggregate.test.ts` covers severity computation + color-coding + render guard. | pending |
| #960 (PR-V1-209) | Phase MR-1 admin — region pins replicated/non-replicated aggregate | Fourth in the aggregate-summary mini-arc (#957/#958/#959/this). The cache status grid surfaces the total pin count but not the replicated vs non-replicated split. Adds a one-line summary above the pins table — `N total — X replicated / Y non-replicated`. Render-gated on `pinsQ.data.length > 0`. No new tRPC — derived from existing `listPins` query. 2-test source-scan at `tests/agent-studio/region-pins-aggregate.test.ts` covers split computation + render guard. | pending |
| #961 (PR-V1-210) | Phase 18 admin — extensions list status aggregate | Fifth in the aggregate-summary mini-arc (#957–#960/this). Adds a status breakdown line above the installed-extensions table — `N total — X approved / Y pending / Z rejected / W revoked / V disabled` — covering the full closed-taxonomy. Color coding: `pending > 0` → amber, `rejected + revoked > 0` → `text-destructive`. Render-gated on `listQ.data.length > 0`. No new tRPC. 3-test source-scan at `tests/agent-studio/extensions-list-aggregate.test.ts` covers closed-taxonomy switch + color-coding + render guard. | pending |
| #962 (PR-V1-211) | Phase 19 admin — publish-targets registry enabled/disabled aggregate | Sixth in the aggregate-summary mini-arc (#957-#961/this). Adds an enabled/disabled split line above the registry table — `N total — X enabled / Y disabled`. `disabled > 0` → `text-destructive` so quiesced targets are immediately visible. Render-gated on `targetsQ.data.length > 0`. No new tRPC. 3-test source-scan at `tests/agent-studio/publish-targets-list-aggregate.test.ts` covers counter logic + destructive highlight + render guard. | pending |
| #963 (PR-V1-212) | Phase 17-γ admin — canvas drain distinct-workspaces-with-failures count | The existing "Last tick processed" line shows total events + total failed but doesn't distinguish "how many events broke" from "how many tenants are blocked". Adds a destructive-highlighted suffix when any per-workspace `failed > 0`: `(K failed) across N workspaces`. Computed in-render as `perWorkspace.filter(w => w.failed > 0).length`. Returns `null` (no render) when zero so healthy ticks stay clean. Per-row testid `drain-workspaces-with-failures` for automation. No new tRPC. 2-test source-scan at `tests/agent-studio/canvas-drain-workspaces-with-failures.test.ts` covers filter computation + singular/plural + destructive class + zero-guard. | pending |
| #964 (PR-V1-213) | Phase 19 admin — publish executions count label | The executions table renders the most-recent 50 rows by default but the panel never said so — operators didn't know they were looking at a windowed view rather than full history. Adds a small `Showing N most-recent execution(s) (capped at 50)` label above the table, with the "(capped at 50)" suffix only appearing when `recentQ.data.length >= 50` to make the window's edge visible. Singular/plural handled. Testid `publish-executions-count-label`. No new tRPC. 1-test source-scan at `tests/agent-studio/publish-executions-count-label.test.ts` covers label render + singular/plural + capped-at-50 hint. | pending |
| #965 (PR-V1-214) | Phase 18 admin — extensions recent invocations count label | Symmetric with publish executions count label (#964). Adds `Showing N most-recent invocation(s) (capped at 50)` above the extensions invocation log, with the capped suffix only appearing when the window is at the limit. Testid `extensions-recent-invocations-count-label`. No new tRPC. 1-test source-scan at `tests/agent-studio/extensions-recent-count-label.test.ts` covers label + singular/plural + capped hint. | pending |
| #966 (PR-V1-215) | Phase MR-1 admin — active regions topology aggregate | Adds an aggregate line above the active regions table — `N total — X primary / Y with neo4j / Z pg-only`. Primary count gets a 3-branch misconfiguration-aware highlight: `1` = healthy (no class), `0` = `text-destructive` (no primary anchored), `>1` = `text-amber-600` (multi-primary; usually a config error in single-primary deployments). Operators can spot topology misconfigurations at a glance without scanning the Primary column. Render-gated on `regionsQ.data.length > 0`. No new tRPC. 3-test source-scan at `tests/agent-studio/region-active-aggregate.test.ts` covers derived-count computation + 3-branch highlight + render guard. | pending |
| #967 (PR-V1-216) | Phase 17 admin — canvas snapshot per-kind node breakdown | The snapshot card shows total nodes + edges + vault but doesn't break down node kinds. Adds a one-line breakdown covering the closed `CANVAS_NODE_KINDS` taxonomy (`note_ref` / `embedded_query` / `free_text` / `image_ref`). Total tells "how heavy is the canvas"; kind breakdown tells "what type of heavy". `if (n.kind in counts)` guard prevents future kinds from silently being missed. Render-gated on `nodes.length > 0`. Includes a contract-anchor test for the server-side taxonomy so a server-side rename surfaces in tests rather than silently breaking the UI. 3-test source-scan at `tests/agent-studio/canvas-snapshot-kind-breakdown.test.ts` covers initialization + in-taxonomy guard + render gate + contract anchor. | pending |
| #968 (PR-V1-217) | Phase 18 admin — extensions per-lane capability breakdown | Symmetric with the canvas-snapshot kind breakdown (#967). Each extension hooks one-or-more capability lanes, so the per-lane counts can sum to more than the total extension count. Adds a second aggregate line under the status-aggregate (#961) showing the closed `EXTENSION_CAPABILITY_LANES` taxonomy split: `retrieve / assemble / compose / tool`. Nested loop with `if (lane in laneCounts)` guard prevents future server-side additions from silently being missed. Render-gated on `listQ.data.length > 0`. Contract-anchor test for the lane taxonomy. 2-test source-scan at `tests/agent-studio/extensions-lanes-aggregate.test.ts` covers nested-loop computation + 4-lane counts + in-taxonomy guard + contract anchor. | pending |
| #969 (PR-V1-218) | Phase MR-1 admin — region pins distribution by region key | Multi-region deployments care about how pins are spread across active regions; an unbalanced map signals a missed rebalance. Adds a second aggregate line under the replicated/non-replicated summary showing per-region-key counts, sorted by count descending so the heaviest region appears first: `By region: 30 us-east-1 / 20 eu-west-1`. Render-gated on `pinsQ.data.length > 0`. No new tRPC — derived from existing `listPins` query in-render. 2-test source-scan at `tests/agent-studio/region-pins-by-region-aggregate.test.ts` covers Map building + sort-desc-by-count + slash-separator-after-first + render gate. | pending |
| #970 (PR-V1-219) | Phase 19 admin — publish-targets per-targetType breakdown | Registry card showed total/enabled/disabled (#962) but not the type-axis distribution. Adds a one-line breakdown beneath the enabled/disabled aggregate covering the closed `PUBLISH_TARGET_TYPES` taxonomy: `staging_env` / `remote_vault` / `external_kb`. `if (t.targetType in counts)` guard mirrors canvas snapshot kind breakdown (#967). Render-gated on `targetsQ.data.length > 0`. 3-test source-scan at `tests/agent-studio/publish-targets-type-breakdown.test.ts`. | merged |
| #971 (PR-V1-220) | Phase 19 admin — publish-executions version column | `PublishExecutionRow.sourceVersionId` is the frozen source-note version id at dispatch time and acts as the rollback anchor — exposed on the server-side row shape but never rendered. Adds a Version column between Promotion and Attempt with first-12-char monospaced rendering and full-id tooltip. `—` when sourceVersionId is null. 3-test source-scan with contract anchor on `sourceVersionId: string \| null`. | merged |
| #972 (PR-V1-221) | Phase 19 admin — publish-targets never-published count | Enabled/disabled aggregate (#962) and type breakdown (#970) describe registry shape but not registry use. Adds `Never published: N of M` line beneath the type breakdown using the existing executionSummaries join — amber (not destructive) when > 0 since unused isn't broken. Render-gated on `targetsQ` AND `summariesQ` both loaded. 4-test source-scan with contract anchor on `totalExecutions: number`. | merged |
| #973 (PR-V1-222) | Phase 18 admin — extensions never-invoked count | Symmetric with publish-targets never-published (#972). An approved extension that has never been invoked is misconfigured / capability-gated-out / freshly installed. `Never invoked: N of M` line between status aggregate (#961) and lanes breakdown (#968). Amber when > 0. Render-gated on `listQ.data.length > 0` AND `summariesQ.data`. 4-test source-scan. | merged |
| #974 (PR-V1-223) | Phase 19 admin — publish-executions settled success rate | Aggregate health summary (#957) shows total / succeeded / pending / failed but no rate. Appends `X% success rate (settled)` using `succeeded / (succeeded + failed)` — pending excluded so a backlog doesn't suppress the rate. 1-decimal precision. Null-guard when `settled === 0`. Color tier: ≥99% emerald, ≥90% amber, else destructive. Testid `publish-executions-success-rate`. 4-test source-scan. | merged |
| #975 (PR-V1-224) | Phase 18 admin — extensions invocations allow rate | Symmetric with publish-executions success rate (#974). Every invocation is settled at capability-check time (`allowedCount + deniedCount = totalInvocations`), so rate is `allowed / total` with no pending denominator. Same ≥99/≥90/else heuristic. Testid `extensions-invocations-allow-rate`. 4-test source-scan. | merged |
| #976 (PR-V1-225) | Phase 17 admin — graph-health alert Age column | Open-alerts table renders raisedAt timestamp but not time-since-raised. Adds an Age column rendering `<1m / Xm / Xh Ym / Xd Yh` between Raised and Key columns. `fmtAge` helper with parameterized `now: number = Date.now()` so future operator-derived gauges can reuse without `Date.now()` coupling. Defense-in-depth em-dash for null / NaN / negative-ms. Testid `graph-health-alert-age-<id>`. 4-test source-scan. | merged |
| #977 (PR-V1-226) | Phase 17 admin — graph-health oldest-alert summary | Age column (#976) lets operators sort within rows; this slice surfaces "how stale is the longest-open alert?" as a headline. Adds one-line summary above the severity breakdown with oldest alert's key + severity (via existing `severityClass`) + age (via `fmtAge`). Min-by-raisedAt with NaN-safe guards on both compare sides. Testid `graph-health-oldest-alert-summary`. 3-test source-scan. | merged |
| #978 (PR-V1-227) | Phase 17 admin — canvas note-references distinct count | References table renders every `(nodeId, noteId)` pair but doesn't surface unique-note shape. A 10-node canvas referencing only 3 distinct notes is a high-duplication pattern useful for canvas-hygiene audits. Adds `Distinct notes: N across M reference(s)` line above the references table. `Set<number>` for uniqueness. Singular/plural suffix. Render-gated on `refsQ.data` presence. Testid `canvas-note-references-distinct-count`. 3-test source-scan. | merged |
| #979 (PR-V1-228) | Phase 17 admin — drain tick success rate | Third in the success-rate mini-arc (#974/#975/this). Drain status shows "Last tick processed: N events" + "(X failed)" but no rate. `processed / (processed + failed)` — settled at drain time, no pending denominator. Render-gated on `totalProcessed + totalFailed > 0` (don't show 0% for idle tick). Same ≥99/≥90/else color tier. Testid `drain-tick-success-rate`. 4-test source-scan. | merged |
| #980 (PR-V1-229) | Phase 18 admin — approval-bus malformed rate | Status grid shows `messagesReceived` + `malformedReceived` as raw counters but operators care about the ratio. Inline `malformed rate: X%` line below the grid. Render-gated on `messagesReceived > 0`. Destructive when > 1% — matches other high-watermark thresholds in workspace-observability. Testid `approval-bus-malformed-rate`. 4-test source-scan. | merged |
| #981 (PR-V1-230) | Phase 12.5 admin — graph-skill-usage distinct keys gauge | Usage panel renders pack-version granular rows but doesn't surface skill-key uniqueness. A workspace serving 12 calls from 3 distinct skill keys (and 9 pack versions) has a very different shape from 12 calls from 12 distinct keys. Adds `new Set(rows.map(r => r.skillKey)).size` gauge next to "Total calls" in page header. Title attribute names the duplicate-pack-version interpretation. Testid `graph-skill-usage-distinct-keys`. 3-test source-scan. | merged |
| #982 (PR-V1-231) | Phase 13 admin — graph-agent explain slowest step gauge | Why-This-Answer header shows total duration and step count. For perf investigations the follow-up is "which step ate the most time" — already in the steps array. Inline `Slowest: #i kind (Xms)` gauge in the header. Max-by-durationMs reduction in-render. Null durationMs treated as -1 in compare; all-null detected via `slowest.durationMs == null` → render nothing (engine ran without timing adapter). Reuses existing `formatDuration` helper. Testid `graph-agent-explain-slowest-step`. 4-test source-scan. | merged |
| #983 (PR-V1-232) | Phase 13 admin — graph-agent explain step-kind distribution | Per-step ledger shows each step's `stepKind` row-by-row but no aggregated shape. A 50-step run that's 90% one kind hints at a loop or fallback chain; a balanced distribution is healthy. Adds `By kind: N kind / N kind ...` distribution line above the step ledger. `Map<string, number>` + sort-by-count desc (reuses the pin-distribution shape from #969). Render-gated on `steps.length > 0`. Testid `graph-agent-explain-step-kind-distribution`. 3-test source-scan. | merged |

### 6.1.bis Post-#983 burst summary (2026-05-15 to 2026-05-16)

The first-slice ledger above stops at #983 (the closing slice of the admin-saturation arc). The remaining-execution-plan at `/sdcard/Download/agent-studio-native-graph-workspace-remaining-execution-plan.md` then opened tracks T-A through T-I. Continuous autonomous execution under the standing /goal mandate has shipped #984–#1171 (~188 PRs) across 4 sub-arcs. Per-PR detail is preserved in the auto-memory file `~/.claude/projects/-root/memory/project_v1_plus_session_2026_05_15.md` (lessons 1–28); this section is the doc-side summary so future readers of this ledger don't need to read the memory file to understand what's on main.

| PR range | Arc | Count | Notes |
|---|---|---|---|
| #984–#1011 | Remaining-plan track openings (T-A / T-C / T-D / T-E / T-F / T-G / T-I first slices) + validator-extension mini-arc | 28 | All 7 autonomous tracks of the remaining-execution-plan have ≥1 PR landed. Lens-stack boot-wired @ #1006 = `4d36b89`. PR #1000 milestone. |
| #1012–#1090 | Phase 22 closed-taxonomy emission rollout (T-I.4 audit + bridge + 11 emitter wirings) + bridge coverage guard + sub-arc closures | 79 | `recordFailureStateEvent` bridge live; 11/25 failure-state kinds emitted via the closed-taxonomy contract; 14 retention mini-arcs / dashboard panels / docs SOUs. |
| #1091–#1135 | Per-kind UI metadata canonization — first wave | 45 | 44 metadata tables (T-G.30–T-G.36, T-D.12, T-E.1–4, T-X.1–3, T-V.1–3, T-B.1–2, T-C.1–2, T-R.1, T-I.33, T-L.1–3, T-S.1–18, T-A.6–10, T-H.1). Pattern canonized: per-kind `X_METADATA: Readonly<Record<XKind, XMetadata>>` with `label` + `description` + 1–3 closed-classification fields + lockstep tests + ancillary catalog row + docs §7 table row. |
| #1136–#1171 | Per-kind UI metadata canonization — post-compaction continuation (reached 100-table milestone at #1169 / T-G.56) | 36 | T-S.19–22, T-E.5–6, T-A.11–14, T-D.13–15, T-X.4, T-G.37–G.58. Coverage saturation: every closed-taxonomy `as const` array AND every 2+-member literal union in `server/agent-studio/services/` and `shared/` now has an operator-facing metadata table or is documented as having no reasonable UI mapping. |
| #1172–#1175 | Remaining-plan T-A doc-drift sweep (closes T-A acceptance criteria) | 4 | T-A.1 V1+ exec plan §6.1.bis (this section, added by #1172 itself); T-A.2 continuation-state "Next material work" refresh; T-A.3 chatgpt tracker §1 latest-pointer refresh; T-A.4 chatgpt tracker §7 post-#983 summary. After this row T-A is fully closed and the 3 main planning docs are aligned with main. |
| #1176–#1205 | T-A doc self-reference closure + T-B.4 lane-hook factory trio + lesson-30 aggregator saturation across 24 surfaces | 30 | T-A.5/T-A.6 self-reference fixes (the §6.1.bis row added by #1172 itself didn't mention #1172, etc.); T-B.6 ViewKindBlueprint.defaultLensKind saved-views Lens preview wiring; T-B.4-retrieve/-assemble/-compose makeStaticFactRetrieveHook / makeStaticChunkAssembleHook / makeStaticBlockComposeHook factories with concrete chunk + block contributions (closes T-B.4 trio); 24 lesson-30 aggregator-pair instantiations (T-X.5 lane-hook registry / T-X.6 extensions / T-D.16 quality scanners / T-G.59 CAG packs / T-G.60 command registry / T-G.61 proposed tool calls / T-X.7 publish targets / T-E.7 publish executions / T-G.62 promotions / T-L.4 approval steps / T-L.5 publish requests / T-L.6 note promotions / T-D.17 graph quality agent runs / T-G.63 tool call traces / T-C.2 canvas nodes / T-G.64 runtime runs / T-G.65 ingestion jobs / T-B.7 saved views / T-G.66 knowledge units / T-I.34 error events / T-I.35 background jobs / T-I.36 pending permission requests / T-G.67 projection drift events / T-A.15 RAC sources). 15 structural variants surfaced across the burst: contract-refused axis / derived-closed-taxonomy / NULL-partition / bidirectional-drift-detector / generic-accessor / outcome-collapse / revertRate gauge / shape-typed-input / two-axis-closed-taxonomy / complementary-axes / named-accessor-for-subset / 4-parallel-counter-stats / closed-plus-open-string-axis / top-N truncation / symmetric-two-axis. PR #1200 milestone reached on T-G.66. |
| #1206–#1224 | Aggregator saturation finish-out + lesson-30 milestone (30 surfaces) + Phase 22 emission wiring pivot + projection-staleness 5-PR new-detection sub-arc | 19 | T-A.7 §6.1.bis doc-drift extension self-mentioning the prior row (#1206); 6 aggregators reaching the lesson-30 saturation milestone at 30 surfaces / 15 variants stable (T-I.37 user notifications / T-B.8 attachments / T-D.18 graph-correction proposals / T-D.19 graph-correction audit events / T-L.7 lifecycle holds / T-B.9 CRDT presence — first to expose operator-tunable `staleThresholdMs`). **Pivot to T-I emission wiring** with 3 corrected-premise wirings (T-I.38 `golden_question_failed` / T-I.40 `graph_correction_rejected` / T-I.41 `note_conflict`) hitting the Phase 22 batch-B 15/25 (60%) target at #1216. **5-PR new-detection sub-arc for kind #7 `neo4j_projection_stale`** (T-I.43-T-I.47 #1218-#1222): pure detector → orchestrator → DB fetcher → admin tRPC → scheduled cron + T-I.48 audit promotion + T-I.49 boot wiring. The 5-PR ladder shape is now the canonical template for closing detection-gap kinds. **Phase 22 emission coverage**: 11/25 pre-burst → 16/25 (64%) post-#1224. **Lesson 30 final state**: 30 surfaces, 15 structural variants saturated. |
| #1225–#1226 | Doc-drift catch-up + graph-health admin UI projection-staleness card | 2 | T-A.8 V1+ exec plan §6.1.bis post-#1205 burst summary refresh; T-I.50 GraphHealthAdminPanel projection-staleness cron card (first React surface for `getStalenessCronStatus` admin tRPC). |
| **#1227–#1229** | **T-I.51 kind #10 closure + 2 CI-recovery slices** | **3** | **T-I.51 (#1227)**: wires the existing `executeRetrieval` parallel-fan-out timeout branch to the closed-taxonomy bridge; one event per executor call (NOT per source) with `timedOutSourceIds` metadata for drill-down; closes kind #10 `graph_query_timeout` via the **1-PR sibling-emit pattern** (detection already existed at `runWithTimeout` → `errorReason: "timeout"`, only emission was missing — smaller than the 5-PR ladder kind #7 required). **CI-recovery slices (#1228, #1229)** restore honest CI signal: #1228 fixes `workspace-default-bindings.test.ts` mock (vi.hoisted + `getAsDbForWorkspace` re-export so the impl's V1+ MR-3 phase-1 workspace-aware caller resolves); #1229 bundles all 12 pre-existing typecheck errors — 5 sourceId number→string coercions matching the T-I.51 shape, 5 `z.record(z.string(), z.unknown())` zod-4 migrations, 1 `decision.alertKey` swap on health-alert errorMessage, 1 `totalKinds: number` literal-widening on lens runner-contract. **Phase 22 emission coverage**: 16/25 → 17/25 (68%) post-#1227. **Remaining detection-gap**: 1 🟡 (kind #6 `neo4j_query_timeout` only; needs per-Cypher-query timeout enforcement in `services/graph/repository/**` first, so still requires the 5-PR ladder pattern). |
| **#1230–#1233** | **T-A.9 ledger refresh + T-I.52 guard extension + T-I.53 burst summary addendum + T-I.54 kind #6 reclassification** | **4** | T-A.9 (#1230): V1+ plan §6.1.bis + tracker §1/§7 refresh for #1225–#1229. T-I.52 (#1231): bridge coverage guard `EXPECTED_WIRINGS` 13 → 17 rows (kinds #2/#7/#22/#23 added); distinct-kind floors 11 → 17; envelope regex relaxed to accept the test-seam indirected-emitter shape used by `staleness-detector.ts` + `live-evaluator.ts`. T-I.53 (#1232): burst summary §7 Addendum named **two closure patterns** (P-LADDER 5-PR new-detection ladder; P-SIBLING 1-PR sibling-emit) so the kind→pattern choice is documented for future kinds. **T-I.54 (#1233): code-verified reclassification of kind #6 `neo4j_query_timeout` from 🟡 detection-gap to ❌ phase-gated on Phase 7.5.** Inspection showed both `Neo4jCommunityGraphRepository.executeTemplate` (line 98) AND `Neo4jKgiaAdapter.runQuery` (line 60+) are skeletons — no `session.run()` actually executes today, so `GraphTimeoutError` cannot fire from any path. Kind #6 closure now co-requires the Phase 7.5 production Neo4j adapter, not a standalone P-LADDER slice. **Phase 22 live-coverage ceiling at current code state: 17/25 (68%)** — the previous T-I.53 §7.4 claim of "18/25 one slice away" is corrected. Bridge coverage guard is in lockstep at 17/17. |

| **#1234–#1238** | **T-A.10 ledger refresh + GraphHealthAdminPanel read-only → fully operator-actionable arc (T-I.55/56/57/58)** | **5** | T-A.10 (#1234): V1+ §6.1.bis + tracker refresh for #1230–#1233. **Panel-saturation arc** — closes the read-only restriction on `GraphHealthAdminPanel` across 4 vertical paths: T-I.55 (#1235) **Run-now button for projection-staleness check** (admin tRPC `graphProjection.runStalenessCheck` was shipped at T-I.46 / #1221; UI button missing); T-I.56 (#1236) **resolve-alert flow** — new `resolveHealthAlertById(id, scope)` server helper + `graphHealth.resolveAlert` admin tRPC + per-row Resolve button (300-line slice; fills the docblock TODO that pointed at a server helper that didn't exist); T-I.57 (#1237) **Run-now button for the health-alert scan itself** (`graphHealth.runAlertScan` calls the already-exported `runHealthAlertScan`, invalidates BOTH cron status AND listOpen since the scan can auto-resolve); T-I.58 (#1238) **Run-now button for the projection-drift scan** — required factoring the inline `runSweep` body into a standalone `runProjectionDriftScan()` so the operator path bypasses the cron-expression + minute-dedupe gate that `tickProjectionDriftCron` enforces. **After this 4-PR arc, all 3 cron cards on GraphHealthAdminPanel have operator Run-now buttons + the alert-cron card has the per-row Resolve button.** Closes the standing "(c) UI follow-up" candidate from the T-A.10 next-slice menu. |

**Main head post-#1238**: `0fc9bbcd` (T-I.58 squash-merge). **Phase 22 LIVE coverage ceiling holds at 17/25 (68%)** — no new emission this mini-arc (the work was operator-action UI, not closing more kinds). GraphHealthAdminPanel is the second admin panel after RegionAdminPanel (forceRewarm + forceReconnectPubsub) to reach **multi-action saturation** (read surface + ≥2 operator actions). **New precedent named:** **(h) panel-saturation pattern** — when a panel has multiple cron-status cards backed by admin tRPC procedures, ship one Run-now button per card AND ANY per-row resolution mutations the underlying server-side helpers expose. Each slice is ~150-300 LOC + a focused 12-15-case source-scan test. The runSweep refactor (T-I.58) demonstrates the standing fix for crons whose runner is inline in `makeRetentionCron({...})`: extract to a standalone function so the operator path doesn't trip the schedule + dedupe gate. **Standing-pattern menu after this mini-arc:** narrow (a) remains empty; (b) new territory T-D/T-F/T-G remains large; (c) UI follow-up has saturated GraphHealthAdminPanel — the next candidate panels are PublishTargets (already partly actionable since #938) and Extensions (already partly actionable since #930-#933).

| **#1240–#1243** | **Phase 22 events-card mini-arc — operator-read complement to the emission surface** | **4** | T-I.59 (#1240) **`listRecentFailureStateEvents` admin tRPC** — wraps `listErrorEvents` with a prebaked 25-value `errorClass IN` filter over `failure_state:<kind>` namespace + returns `{ rows, summary }` where summary is `summarizeFailureStateEventList(rows)`. `adminProcedure` because rows span workspaces. T-I.60 (#1241) **First UI consumer** — new "Recent failure-state events" Card on GraphHealthAdminPanel rendering summary rollup + by-kind breakdown (gated on `closedTaxonomyEvents > 0`, filter drops zero-count kinds, descending-count sort). T-I.61 (#1242) **Per-source-kind breakdown** — sibling table aggregated client-side from `rows` via a single-pass `Map<string, number>` reducer (no server change). T-I.62 (#1243) **Latest-rows table** — third sibling table rendering 4 columns (Created / Kind / Source kind / Message) truncated to `.slice(0, 15)` with `failure_state:` prefix stripped for readability. **After this 4-PR mini-arc the events card answers all 4 operator questions on one screen: how many / which kinds / which emitters / what specifically just happened.** |

**Main head post-#1243**: `da346ad5` (T-I.62 squash-merge). **Phase 22 LIVE coverage ceiling holds at 17/25 (68%)** — still ceiling-bound. The events-card mini-arc was the operator-read complement to the emission surface: the bridge has been writing rows since T-I.5 (#1014), but until now nothing surfaced them to operators. **New precedent named:** **(i) operator-dashboard tRPC-then-UI mini-arc** — when a server-side write surface has been live for many PRs without a first-class operator read, ship a focused 4-PR mini-arc: (1) admin tRPC wrapping the existing read with prebaked filter + summary; (2) first UI consumer rendering summary + primary breakdown; (3) client-side secondary aggregation on the raw rows (no server change); (4) raw-rows-as-table for individual-event investigation. Each slice is ~150-200 LOC + focused source-scan test. The mini-arc is naturally bounded — once the card answers (how many / which kinds / which emitters / what specifically) it's saturated. **Standing-pattern menu unchanged:** (a) narrow empty; (b) T-D/T-F/T-G remain large; (c) UI follow-up — GraphHealthAdminPanel now has 4 cards + 4 mutations + recent events drilldown — has reached the saturation diminishing-return point. The next candidate area is either pivoting to (b) new territory T-F production lens runners, OR repeating mini-arc (i) on another emission surface (e.g. canvas-projection-drain telemetry has emitter wirings but no operator-read drilldown).

| **#1245–#1247** | **Phase 22 events-card kind-filter mini-arc — tRPC → dropdown → clickable rollup** | **3** | T-I.63 (#1245) **`kind?: FailureState[]` + `sourceKind?: string \| string[]` filters on `listRecentFailureStateEvents`** — pre-filters the `errorClass IN` list at the server boundary; fallback to `FAILURE_STATES` preserves T-I.60's default. Solves the incident pattern where one loud kind (e.g. `graph_query_timeout`) dominates the 50-row buffer and a quieter kind (e.g. `text2cypher_rejected`) loses tail visibility. T-I.64 (#1246) **UI consumer** — `<select>` dropdown above the events-card content + `useState<string \| null>` + per-`kind` option rendered from a local stable `FAILURE_STATE_KIND_OPTIONS` constant (closed-taxonomy mirror — server `z.enum(FAILURE_STATES)` validator catches drift). T-I.65 (#1247) **Clickable kind cells on the by-kind rollup table** — fuses T-I.61 ("which kind is loudest") with T-I.64 ("narrow to one kind"): each Kind cell becomes a `<button>` that toggles `setKindFilter(kindFilter === kind ? null : kind)`; active row gets a `bg-muted/50` highlight. **After this 3-PR mini-arc the events card has a focus → drill-in → cleared loop end-to-end: see the rollup → click a kind → buffer narrows → click the same kind again or "Clear" to widen.** |

**Main head post-#1247**: `43103be7` (T-I.65 squash-merge). **Phase 22 LIVE coverage ceiling holds at 17/25 (68%)** — kind-filter work was operator-ergonomic, not new emission. **New precedent named:** **(j) drill-in extension on an operator dashboard mini-arc (i)** — once a card answers the 4 operator questions (how many / which kinds / which emitters / what), the natural next axis is narrowing the view to one of those answers. Mini-arc (j) shape is **3 slices**: (1) server-side narrower on the same admin tRPC (additive optional filter; fallback preserves existing default); (2) UI control that drives the filter (dropdown + clear); (3) clickable rollup fusion so the existing rollup table doubles as a filter selector. Each slice is ~100-200 LOC + focused source-scan test. **Standing-pattern menu after this mini-arc:** (a) narrow empty; (b) T-D/T-F/T-G remain large; (c) UI follow-up — events card now has filter-via-dropdown AND filter-via-rollup-click, so the operator-read surface is saturated for the closed-taxonomy stream. Next candidate area is either pivoting to (b) new territory T-F production lens runners, OR a NEW operator-read mini-arc (i) on a different emission surface (note: canvas-projection-drain was confirmed to NOT emit closed-taxonomy events, so it's NOT a mini-arc (i) candidate — a fresh emitter audit would be needed to find one).

| **#1249–#1251** | **Events-card drill-in saturation arc — sourceKind + time-window axes + empty-state messaging** | **3** | T-I.66 (#1249) **sourceKind axis drill-in** — clickable cells on the by-source-kind table (mirrors T-I.65 on the by-kind table) + filter chip + Clear button above the events card. Open-ended axis (no fixed enum), so UI affords selection via clicks rather than a dropdown. T-I.67 (#1250) **time-window axis drill-in** — 3 preset buttons (1h / 24h / 7d) driving `createdSince` derived from `Date.now() - window` so the window slides on each refetch. T-I.68 (#1251) **empty-state messaging** — small banner above the summary grid that distinguishes "filter narrowed to empty" from "workspace genuinely empty"; offers a "Clear all filters" button that resets all 3 setters in one click. **After this 3-PR arc the events-card drill-in is saturated across all 3 available axes (closed-taxonomy kind, open-ended sourceKind, time window) AND the empty-state has explanatory copy.** All 3 filters compose at the same useQuery — operator can ask "show me all `text2cypher_rejected` from `rac-retrieval-executor` in the last 24h" with three clicks. |

**Main head post-#1251**: `b29dc301` (T-I.68 squash-merge). **Phase 22 LIVE coverage ceiling holds at 17/25 (68%)** — all 3 slices were operator-ergonomic UI extensions; backend was already capable (T-I.59 inherited `createdSince` from `listErrorEvents` since #1240; T-I.63 added `sourceKind` pass-through). **New precedents named:** **(j₂) single-PR drill-in extension** — when the server already accepts the filter as input (either inherited from the underlying read API, or added in a prior server slice), the (j) mini-arc collapses to UI-only: state + useQuery wire-up + UI control + source-scan test. Each axis is ~100-200 LOC. Apply when extending an operator-dashboard mini-arc (i) with another filter axis whose server-side type is already supported. **(k) saturation completion via empty-state messaging** — once a card has multiple drill-in axes, the natural UX gap is "what does empty mean here?". Ship a small banner that distinguishes filter-narrowed-empty from genuinely-empty; offer a one-click "Clear all filters" reset. ~100 LOC + 8-9-case source-scan. The empty-state slice is the **natural close** of a drill-in saturation arc — after it, all 3 axes have UI, all 3 compose, and the operator can recover from over-narrowing. **Standing-pattern menu after this arc:** (a) narrow empty; (b) T-D/T-F/T-G remain large; (c) events card is **genuinely saturated** — kind dropdown + 3 clickable rollup axes + time-window buttons + empty-state messaging. Next candidate area is firmly (b) new territory.

| **#1252** | **T-A.14 ledger refresh for drill-in saturation arc (#1249–#1251)** | **1** | Doc-only — V1+ §6.1.bis row above + tracker §1/§7 refresh; named the (j₂) + (k) precedents for future arcs. Closes the events-card drill-in saturation arc bookkeeping. |

| **#1253–#1256** | **T-F lens-stack end-to-end mini-arc — first real lens runner reachable from operator UI** | **4** | First territory-(b) slice after the events-card saturated. **Pivots from saturation to new runtime phase per the T-A.14 menu.** Ships the lens-stack from runner internals to a clickable browser page in four bounded PRs. T-F.59 (#1253) **first real `LensRunnerFn`** for `kind="runtime"` — pure `buildRuntimeLensSnapshot` (parent / resumed-from / compacted-from lineage edges; no dangling), permission post-filter matching the impact-analysis contract (#993) with property-style permission-leak test sweeping the (triggeredBy × ownerId × visibility) cross, envflag installer `AGS_GRAPH_LENS_RUNTIME_RUNNER_INSTALL`. Runner file's source-scan asserts NO Drizzle / `getAsDb*` imports — DB I/O is forced into a sibling slice. T-F.60 (#1254) **ASDB read-seam adapter** — `createRuntimeLensAsdbReader({ getDbForWorkspace })` Drizzle-backed implementation of `RuntimeLensReadFn`; fail-soft when ASDB lookup returns null; selects `limit + 1` for truncation detection; batched single `agsAgents` query over the unique-agentId set; `maybeInstallRuntimeLensRunnerWithAsdb` composer turns production boot wiring into one call. T-F.61 (#1255) **`agentStudio.graphLens.*` tRPC router** — three `adminProcedure` endpoints: `list` joins definitions with `hasRunner`; `summary` exposes registry coverage gauge + kindLabels; `render` returns a **discriminated envelope** (`ok` / `no_runner_for_kind` / `not_found`) so the client renders empty-states without a pre-check round-trip. `viewer.userId` plumbs from `ctx.user.id` so the permission post-filter from #1253 holds end-to-end; source-scan locks `adminProcedure` on every procedure. T-F.62 (#1256) **operator UI page** at `/agent-studio/graph-lens-browser` — coverage gauge + lenses table with `live`/`placeholder` badges + render preview Card. The `ok` envelope renders counts grid + by-typeKey rollup + first 15 nodes + first 15 edges as table previews (full graph viz is a future slice). Canonical 4-site shell wiring (lazy import + path parse + nav case + render case) + sidebar entry + routes registry. |

**Main head post-#1256**: `fe43ffec` (T-F.62 squash-merge). **First territory-(b) burst — closes the saturation→new-territory pivot.** With both `AGS_GRAPH_LENS_DEFAULTS_INSTALL=on` + `AGS_GRAPH_LENS_RUNTIME_RUNNER_INSTALL=on` (plus an operator using `maybeInstallRuntimeLensRunnerWithAsdb`), the runtime lens is reachable end-to-end: registry → real runner → DB-backed read → admin tRPC → React panel. **New precedent named:** **(l) lens-stack end-to-end mini-arc — naturally bounded 4-PR shape**. Selection rule: when a lens-stack-like graph (registry → runner → tRPC → UI) is at "registry only / stubs only" state, ship a 4-PR mini-arc replacing one kind's stub with a real implementation end-to-end:
- (1) **Pure builder + envflag installer** for one kind. Source-scan locks "no DB imports in the runner file" so the read-seam stays cleanly separable. Property-test the permission-leak invariant.
- (2) **DB-backed read-seam adapter** (Drizzle / pg / whatever). Workspace scoping handled at connection level (region-routing) if available; truncation detection via `limit + 1`. Source-scan locks "no graph-driver / dispatcher / OpenRouter imports".
- (3) **tRPC layer** — `list` + `summary` + discriminated `render` envelope. `adminProcedure` for the whole router; source-scan locks the discipline. Permission-aware viewer context plumbed from `ctx.user.id`.
- (4) **Operator UI page** — coverage gauge + entity table + render preview Card with table-primitive previews. Canonical 4-site shell wiring. Client calls only through tRPC; source-scan locks no server-side imports.

Each slice is ~150-300 LOC + focused source-scan. **Other lens kinds (the 7 still-placeholder) can replicate the same shape** — pick a kind, ship (1) + (2) → kind goes from `placeholder` to `live` in the existing UI (since `list` already returns `hasRunner` and `render` already routes to the registered runner). Steps (3) + (4) are paid once for the whole stack.

**Standing-pattern menu after this arc:** (a) narrow empty; (b) other lens kinds — pick one (governance / mcp / runtime-error overlay / etc.) and apply precedent (l); (c) other arc territory — T-D Quality Agent runtime / T-F Bases MVP / T-G institutional/code/security/recommendation. Continuing precedent (l) replication is the cheapest next-slice category until lens-kind coverage approaches 100%.

| **#1258–#1264** | **T-F lens runner coverage saturation arc — 7 PRs reach 8/8 coverage of `GRAPH_LENS_KINDS`** | **7** | Continues precedent (l) steps (1)+(2) replication across the remaining 7 lens kinds. **All 7 follow the same shape**: pure builder + envflag installer + ASDB adapter + composer + ~20-test suite + 3 source-scan files. T-F.63 (#1258) **governance** — `agsPublishRequests` + `agsApprovalSteps`; nodes `publish_request` / `approval_step` / `approver` (synthesized from `decidedBy`); edges `has_step` (suppressed when parent out of scope) + `decided_by`. T-F.64 (#1259) **mcp** — `agsRuntimeToolCalls`; nodes `runtime_run` / `tool_call` / `tool`; edges `dispatched` + `target_tool`. T-F.65 (#1260) **cag** — `agsCagCapabilityPacks`; nodes `cag_pack` / `cag_agent` (synthesized from `agentId`); edges `owns`. **First column-level workspaceId fence** among lens runners (`agsCagCapabilityPacks` carries the column). T-F.66 (#1261) **rag** — `agsRacSources`; nodes `rag_source` / `rag_profile` (synthesized from `profileId`); edges `belongs_to_profile`. Column-level workspaceId fence. T-F.67 (#1262) **rac** — `agsRacRuntimeTraces`; nodes `rac_trace` / `rac_agent` / `rac_cag_pack` (cross-lens shape alignment with cag's `cag_pack`); edges `trace_for_agent` + `uses_cag_pack` (suppressed when cagPackId null). Column-level workspaceId fence. T-F.68 (#1263) **graph_skill** — `agsGraphSkillPacks` (global registry, no `workspaceId` column); nodes `graph_skill` / `graph_skill_domain`; edges `belongs_to_domain`. T-F.69 (#1264) **institutional_memory** — `agsAgents` (global registry); nodes `inst_agent` / `inst_owner` (synthesized from `ownerId`) / `inst_domain` (synthesized from `domain`); edges `owned_by` + `belongs_to_domain`. **8 / 8 lens-kind coverage milestone reached at #1264** — every entry in the closed `GRAPH_LENS_KINDS` taxonomy now has a real runner. Existing tRPC layer (#1255) + UI page (#1256) require zero changes per added kind — `list` already returns `hasRunner=true` and `render` already routes to the registered runner once the boot installer fires. Precedent (l) replication closes for this taxonomy. |

**Main head post-#1264**: `5fb3a2dc` (T-F.69 squash-merge). **Lens runner coverage saturation milestone — 8 / 8 (100%).** The arc validates precedent (l) as a reusable shape — 7 PRs of mechanical replication after the 4-PR end-to-end mini-arc, no novel pattern needed per kind. Cross-runner shape patterns observed across the arc:

- **Read-seam separation as a hard discipline** — every runner file has a source-scan asserting NO Drizzle / `getAsDb*` imports. The DB-touching adapter sits in a sibling file. This pays off when test cases need to swap in fake data: the builder is a pure function from a `*ReadResult` shape to `LensSnapshot`. No DB mocking infrastructure needed.
- **`limit + 1` for truncation detection** — every adapter uses the same pattern. If `raw.length > limit`, drop the extra row and set `truncated=true`. No off-by-one bugs across 8 adapters.
- **Permission gate is a single pure predicate** — `is*NodeVisibleToViewer(viewer)`. Today's policy is uniformly "anonymous → all redacted; authenticated → all visible". Per-row tightening (e.g. governance "approver or requester only") can land later without changing the shape contract.
- **Permission-leak invariant** — when `visible=false`, omit both `label` AND `meta` entirely. Source-scan + property-test enforced.
- **Synthesized nodes for normalized references** — every lens that joins through a foreign key synthesizes the target node (`tool` from `toolKey`; `cag_pack` from `cagPackId`; `inst_owner` from `ownerId`). One node per distinct value; the edge anchor is consistent. No need for the SoT row of the synthesized entity to be in scope.
- **Dangling-edge suppression** — when an edge's target is not in the cap-truncated node set, the edge is omitted (e.g. governance `has_step` when parent request is out of scope; runtime `parent_run` when parent run is out of scope). No dangling edges in any snapshot.
- **Column-level workspaceId fence when available** — 3 of 8 adapters (cag / rag / rac) apply BOTH region routing AND a column-level `workspaceId === params.workspaceId` filter (defense-in-depth). 5 of 8 (runtime / governance / mcp / graph_skill / institutional_memory) rely on region routing alone because their SoT tables are global or lack a `workspaceId` column.

**Standing-pattern menu after this arc:** (a) narrow empty; (b) T-A.16 ledger refresh (this row); T-F.70+ DB-backed boot wiring step that auto-installs all 8 real runners at server start (today each runner has its own envflag — boot composer would honor each independently while providing one-line activation); apply (l) steps (3)+(4) to other taxonomies (e.g. Phase 23 quality detection runtime, Phase 25 institutional/code/security/recommendation graphs); (c) larger new territory — T-D Quality Agent / T-F Bases MVP / T-G code-graph parser / T-G recommendation service.

| **#1266–#1271** | **T-F lens stack — boot wiring + 3-axis drill-in saturation arc on the lens browser** | **6** | Two paired sub-arcs that take the lens stack from "registered runners" to "operator-actionable production-ready surface". **Boot-wiring sub-arc (2 PRs)**: T-F.70 (#1266) `maybeInstallAllLensRunnersWithAsdb` composer — single-call helper iterating `GRAPH_LENS_KINDS` and dispatching to each per-kind installer (each retains its own envflag for independent enablement); the per-kind `Record<GraphLensKind, boolean>` literal is exhaustiveness-guarded so adding a new kind to the closed taxonomy fails tsc here. T-F.71 (#1267) **boot Step 3.36** in `server/agent-studio/boot.ts` immediately after Step 3.35's stub-stack install. Try-catch fail-soft swallows `LensRunnerAlreadyRegisteredForKindError` if an operator sets both the stub flag AND a real flag for the same kind. **Drill-in saturation sub-arc (4 PRs)** on `GraphLensBrowserPanel`: T-F.72 (#1268) **typeKey filter** — clickable rollup rows toggle a `string \| null` filter; (j₂) shape since no server change (filter applies to the already-fetched snapshot). T-F.73 (#1269) **visibility filter** — closed 3-mode union `"all" \| "visible_only" \| "hidden_only"` button group; composes with typeKey into a single `filteredNodes` predicate; "Hidden only" surfaces the permission-redacted set for audit. T-F.74 (#1270) **substring search** — case-insensitive match across `node.id` + `node.label`; empty trimmed query is a no-op. T-F.75 (#1271) **Clear-all-filters reset** per precedent (k); `clearAllFilters()` helper resets all 3 setters back-to-back; `selectLens()` delegates so the lens-switch reset and the manual reset stay in lockstep; top-level button gated on `hasAnyFilter` + inline button in the empty-state row. After this 4-PR drill-in sub-arc the lens browser mirrors the T-I.59-T-I.68 events-card saturation shape — 3 composing axes + filter-narrowed empty-state recovery. |

**Main head post-#1271**: `3d4fc41a` (T-F.75 squash-merge). **Lens-browser drill-in saturation arc COMPLETE per precedent (k).** Confirms (k) as a reusable closure shape across multi-axis drill-in arcs — second instance after T-I.68. The two-paired-sub-arc structure (boot wiring + UI saturation) is a new precedent: **(m) wire-and-saturate composite** — when a stack reaches taxonomy coverage (here 8/8 lens kinds) but lacks production boot wiring + operator drill-in ergonomics, ship a 6-PR composite: (1)+(2) boot composer + boot step, (3)+(4)+(5) three drill-in axes, (6) Clear-all reset close. Each sub-arc is its own bounded mini-arc but they pair naturally — the wiring is the prerequisite for the drill-in axes to surface real production data.

**Standing-pattern menu after T-F.75:** (a) narrow empty; (b) T-A.17 ledger refresh (this row); cross-lens linking (clicking on a `cag_pack` node in the rac lens jumps to the cag lens — uses the shared-typeKey discipline from lesson 43); actual graph visualization layer on the lens browser (force-directed / timeline layouts driven by `definition.layout`); apply (l) replication to Phase 23 quality detection / Phase 25 institutional/code/security/recommendation; per-row permission tightening on individual lens kinds; (c) larger new territory — T-D Quality Agent / T-F Bases MVP / T-G code-graph parser.

| **#1275–#1278** | **T-F lens browser — layout-specific renderers + dangling-edge gap closure** | **4** | Two-part mini-arc that takes the lens browser viz surface from "circular fallback only" to "layout-aware". **Layout-renderer sub-arc (3 PRs)**: T-F.78 (#1275) **TimelineGraphViz** — branches the graph-viz dispatch on `snapshot.layout === "timeline"`; x = index along the runner's deterministic node order, y banded by typeKey (alphabetical, each unique typeKey gets a horizontal row). T-F.79 (#1276) **MatrixGraphViz** for `snapshot.layout === "matrix"` (CAG Pack View) — N×N grid, every filtered node is both a row and column, cell filled iff edge source→target exists in the filtered edge set; O(1) cell lookup via pre-computed `edgeKeys: Set<string>`. T-F.80 (#1277) **TreeGraphViz** for `snapshot.layout === "tree"` (RAC + Graph Skill) — in-degree → roots → BFS depth assignment → orphan fallback at `maxDepth + 1`; y = depth × dy, x = sibling-index-within-depth slot-distributed. After this sub-arc, 3 of 5 layouts in the closed `GRAPH_LENS_LAYOUTS` taxonomy have dedicated renderers (only `force_directed` and `dependency_path` fall through to the circular `SnapshotGraphViz`, with `dependency_path` having no operator-visible caller yet in `install-default-lenses.ts`). **Dangling-edge gap closure (1 PR)**: T-F.81 (#1278) — the 3-axis drill-in filter narrowed the nodes table but the edges preview still rendered raw `snapshot.edges.slice(0, 15)`, including edges pointing at filtered-out nodes. Adds `filteredNodeIds = new Set(filteredNodes.map(n => n.id))` + `filteredEdges = snapshot.edges.filter(e => filteredNodeIds.has(e.sourceNodeId) && filteredNodeIds.has(e.targetNodeId))`, mirrors T-F.73's header copy convention, adds empty-state row + Clear-all reset reusing T-F.75's handler. Section-level gate stays on `snapshot.edges.length > 0` so a snapshot with zero edges total still doesn't render an empty section. |

**Main head post-#1278**: pending merge. **Layout-specific renderer arc + dangling-edge closure COMPLETE.** New precedent: **(n) taxonomy-driven renderer ladder** — when a closed taxonomy of `N` enum values (here 5 layouts) has a single layout-agnostic fallback, the operator-visible subset of `N` becomes a natural renderer ladder, one slice per value that has an actual caller. The ladder NATURALLY closes when the remaining enum values have no callers (here `dependency_path`) — the heuristic is "ship the renderer only when it surfaces an operator-visible lens", not "exhaust the enum mechanically". The dangling-edge gap closure is a different precedent shape: when a viz layer enforces an invariant the table layer doesn't, the table layer is the laggard — close the gap so the two stay in lockstep.

**Standing-pattern menu after T-F.81:** (a) narrow empty; (b) T-A.18 ledger refresh (this row); (c) larger new territory — T-F.4 Quality Lens / T-F.2 Bases MVP / T-D Quality Agent / T-G code-graph parser; per-row permission tightening; lens snapshot export to CSV/JSON; edge-typeKey filter as a 4th drill-in axis (the layout-renderer ladder already exhausted operator-visible portion).

| **#1280–#1283** | **T-F.4 Quality Lens opening — α/β/γ/δ slices (4 PRs)** | **4** | Opens **T-F.4 territory** (Quality Lens) per the post-T-F.81 menu's (c) item. The `agentStudio.graphQuality.*` tRPC stack shipped months ago (getStats / listFindings / dismissFinding / convertFindingToProposal / bulkDismissFindings) but had **zero client consumer**. This 4-PR arc closes the operator-surface gap end-to-end without any new server work — every slice consumes pre-existing tRPC procedures. **T-F.82 α-shell (#1280)** — `/agent-studio/graph-quality-findings` admin page; 3 Cards rendered from one `getStats` query (totals + findings-by-status + findings-by-severity); canonical 4-site shell wiring + sidebar Quality group + ShieldAlert icon + routes entry. **T-F.83 β-slice (#1281)** — recent-findings list Card consuming `listFindings` with `FINDINGS_LIST_LIMIT = 50`; 5-column table (id / class / severity / status / source); empty + loading + error states all testid-tagged; header copy mirrors lens-browser "first N of M" convention. **T-F.84 γ-slice (#1282)** — drill-in filters on severity + status axes; **server-side** narrowing (round-tripped through `listFindings` because the LIMIT cap would silently filter to zero rows under client-side); closed-taxonomy `as const` tuples sourced from the server's Zod enums; `hasAnyFilter` OR drives top-level Clear-all button + header copy + empty-state recovery message per precedent (k) saturation-completion shape. **T-F.85 δ-slice (#1283)** — first per-row mutation: Dismiss button on `status === "open"` rows; `isPending` resolved per-row via `mutation.variables?.findingId === f.id` so only the row being mutated disables + flips its label; `onSuccess` invalidates BOTH `listFindings` AND `getStats` so both the table and the by-status bucket card refresh; `onError` surfaces inline above the table; per-row testid `graph-quality-finding-dismiss-${f.id}`. |

**Main head post-#1283**: `7061f679`. **Quality Lens (T-F.4) opening arc COMPLETE — α/β/γ/δ slices live.** New precedent: **(o) opportunistic α-shell** — when an established server-side tRPC stack has no client consumer, the cheapest valuable slice is a single-fetch read-only admin page. β/γ/δ follow-ups can layer list / drill-in / per-row mutation respectively, each reusing previously-shipped server procedures without expanding server scope. This is the inverse of precedent (l): (l) ships server-first end-to-end; (o) consumes server-first slices that have aged without surface.

**Standing-pattern menu after T-F.85:** (a) narrow empty for Quality Lens (α-δ done); (b) T-A.19 ledger refresh (this row); (c) within-T-F.4 follow-ups — per-row convert-to-proposal / approve+apply via `convertFindingToProposal` + `applyApprovedProposal` (ε); bulk dismiss with selection model via `bulkDismissFindings` (ζ); dismiss-with-reason textarea (η); audit-trail per-finding via `getFindingAuditTrail` (θ); (d) larger new territory — T-F.2 Bases MVP / T-F.3 Impact Analysis / T-G code-graph parser; apply precedent (o) to other unsurfaced tRPC stacks (search `client/src/` for procedures with zero consumers).

| **#1285–#1287** | **T-F.4 Quality Lens within-T-F.4 saturation — θ + η + ε slices (3 PRs)** | **3** | Continues from the T-A.19 ledger refresh's standing-pattern menu (c) item by tackling 3 of the 4 named within-T-F.4 follow-ups. **T-F.86 θ-slice (#1285)** — per-row audit-trail expansion via `getFindingAuditTrail`; `expandedFindingId: number \| null` with gated tRPC query (`enabled: expandedFindingId !== null`); `Fragment` wrapping enables the adjacent `<tr>` pair (row + expansion) inside the same tbody; 3 sub-sections (finding / proposal / audit events) with 5 loading/error/unavailable/empty-proposal/empty-audit states all testid-tagged. **T-F.87 η-slice (#1286)** — dismiss-with-reason; refactors T-F.85's one-click Dismiss into a two-step inline editor (textarea max 2000 chars + char counter + Confirm/Cancel); trims the draft on confirm and spreads `reason: trimmed` only when non-empty so the server's optional field stays optional rather than getting submitted as `""`; mutual exclusion with the trail expansion via `openReasonEditor` clearing `expandedFindingId`. **T-F.88 ε-slice (#1287)** — per-row Triage / convert-to-proposal; mirrors the η editor shape with a rationale textarea routed through `convertFindingToProposal`; three-way mutual exclusion (Trail / Dismiss editor / Triage editor) — opening either editor closes the trail AND the other editor, so the operator's row state is always one-of-three. After this arc the Quality Lens findings page reaches genuine operator-functional saturation: stats summary, 50-row list, 2-axis drill-in, dismiss-with-reason, triage-with-rationale, per-row audit trail. The remaining ζ (bulk dismiss + selection model) is the only major within-T-F.4 follow-up left. |

**Main head post-#1287**: `e3d6392d`. **Quality Lens (T-F.4) operator-functional saturation COMPLETE.** Extends precedent (o) opportunistic α-shell: after the 4-slice α/β/γ/δ opening arc (#1280-#1283), the natural continuation is a θ/η/ε saturation burst that uses the SAME shell + list + filter scaffolding to wire in remaining per-row mutations one at a time. Each slice extends parallel state (expandedFindingId / reasonFindingId / triageFindingId) without touching the prior slices' state contracts; the mutual exclusion happens via the open* helpers, not via discriminated state. This keeps each slice naturally bounded (~60-100 LOC + ~7-9 source-scan asserts each).

**Standing-pattern menu after T-F.88:** (a) narrow — Quality Lens 6/7 within-T-F.4 done; ζ bulk dismiss remains; (b) T-A.20 ledger refresh (this row); (c) ζ bulk dismiss with selection model via `bulkDismissFindings` (needs `Set<number>` selection state + checkbox column + bulk confirm UX); (d) larger new territory — T-F.2 Bases MVP / T-F.3 Impact Analysis / T-G code-graph parser; apply precedent (o) to other unsurfaced tRPC stacks.

| **#1289–#1290** | **T-F.4 Quality Lens within-T-F.4 saturation completion — ζ bulk dismiss (2 PRs)** | **2** | Closes the (a) NARROW item from the T-F.88 standing-pattern menu by shipping the remaining within-T-F.4 follow-up — ζ bulk dismiss — in a clean 2-PR split. **T-F.89 ζ.1 (#1289)** — selection model + checkbox column: `selectedFindingIds: Set<number>` panel state with O(1) toggle; `toggleFindingSelection(id)` + `clearSelection()` helpers; leading checkbox column on each row (status-gated to `open` per lesson 54); header select-all checkbox that unions/diffs the visible-open slice (`pageAllSelected` derived from `selectableFindings.every`); bulk selection bar ("N selected" + Clear-selection) gated on `size > 0` and rendered above the table; `colSpan={6}` → `colSpan={7}` on the trail/dismiss/triage expansion rows. Selection is intentionally independent of the existing per-row editors — opening a trail/dismiss/triage editor does NOT clear the selection, since the operator may inspect or single-row mutate before bulk-acting on the rest. **T-F.90 ζ.2 (#1290)** — bulk-confirm UX + mutation: the selection bar gains an inline editor — `Dismiss N…` opens a shared-reason textarea (max 2000) + Confirm/Cancel + char counter, mirroring the η/ε editor shape; Confirm trims the draft and spreads `reason: trimmed` only when non-empty (lesson 57); mirrors the server-side `findingIds.max(500)` cap as `BULK_DISMISS_MAX` (over-limit selection shows a testid-tagged warning + disables Confirm); `onSuccess` invalidates BOTH listFindings + getStats so the by-status bucket card refreshes, clears the selection + editor, and captures the server's `{ dismissed, skipped }` payload into a result surface above the table ("dismissed N, skipped M (already-resolved / not-found)"). After this 2-PR arc the Quality Lens reaches **7/7 within-T-F.4 follow-ups closed**: stats summary, 50-row list, 2-axis drill-in, dismiss-with-reason, triage-with-rationale, audit trail, **bulk dismiss with shared-reason editor**. |

**Main head post-#1290**: `<TBD>`. **Quality Lens (T-F.4) FULL within-T-F.4 saturation COMPLETE.** Names two refinements: **precedent (o)-bulk** — extension of the 7-PR α/β/γ/δ/θ/η/ε template into an 8-PR-plus-bulk shape when the same tRPC stack also exposes a bulk-mutation procedure (split into ζ.1 selection model + ζ.2 confirm UX keeps the diff narrowly reviewable per the lesson-55 parallel-state-slices pattern). **lesson 60 — mirror server-side `.min/.max` caps as named constants on the client**: `BULK_DISMISS_MAX = 500` mirrors `router.ts:281 z.array(...).max(500)` so the UI warns and disables before the round-trip rather than catching a Zod rejection — same shape as the `FINDINGS_LIST_LIMIT = 50` mirroring β-slice; whenever a server-side `.max(N)` gates a list/array input that the client constructs, mirror N as a named constant and assert it in a source-scan test so the cap can't drift silently. **lesson 61 — bulk-mutation result surface is partial-success-first**: `bulkDismissFindings` returns `{ dismissed[], skipped[] }` because the per-row loop in `dismiss-finding.ts` catches `FindingAlreadyResolvedError` + `FindingNotFoundError` and continues. The UI's result surface MUST surface both counts (not just success) so the operator sees "dismissed N, skipped M" — claiming "dismissed N" when M were actually skipped is misleading; the partial-success shape is honest because the server explicitly chose partial-success over all-or-nothing. Pattern: when a bulk mutation returns a `{ succeeded[], skipped[]?, failed[]? }` shape, the result UI surface MUST surface every non-empty bucket.

**Standing-pattern menu after T-F.90:** (a) narrow — Quality Lens 7/7 within-T-F.4 follow-ups COMPLETE; no within-T-F.4 candidates remain; (b) T-A.21 ledger refresh (this row); (c) larger new territory — T-F.2 Bases MVP / T-F.3 Impact Analysis / T-G code-graph parser; apply precedent (o) to other unsurfaced tRPC stacks (search `client/src/` for procedures with zero consumers — `applyApprovedProposal` is the cleanest within-T-F.4 candidate but needs a separate `/graph-quality-proposals` page since proposals are a sibling surface to findings); (d) per-row permission tightening on findings (governance "approver only", runtime "triggered_by viewer only"); (e) Quality Lens snapshot export to CSV/JSON.

| **#1292–#1295** | **T-F.2 Bases MVP opening — α/β/γ/δ slices (4 PRs)** | **4** | Opens **T-F.2 Bases MVP territory** per the post-T-F.90 menu's (c) item. Per **precedent (o)-discriminator** (a refinement of precedent (o) opportunistic α-shell), Bases are persisted as `agsVaultSavedViews` rows with `viewKind="base"` discriminator instead of new `agsBases`/`agsBaseFilters`/`agsBaseColumns` tables (per the saved-view docblock that explicitly anticipates additional view kinds without DB churn). The 4-PR opening arc reuses the entire Phase-16 saved-view server stack (table, CRUD, sharing/visibility, version history) — **zero new server work**. **T-F.91 α-shell (#1292)** — first operator surface at `/agent-studio/bases`; new `ViewKindBlueprint` for `viewKind="base"` alongside the 5 existing canonical kinds; new BasesPage + BasesPanel with vault picker + totals card + 50-row recent-bases table consuming the existing `vault.listVisibleSavedViews({ vaultId, viewKind: "base", limit: 50 })` tRPC; canonical 4-site shell wiring (lazy import / path parser / sidebar / routes). **T-F.92 β-slice (#1293)** — per-row expand-to-detail; clicking a row toggles an inline detail row that renders the base's filters / sort / columns / metadata from already-fetched `SavedViewRow` data (no new tRPC query, no DB hit); mirrors the Quality Lens θ audit-trail shape but the gated-tRPC layer collapses to a pure prop render since data is in memory. **T-F.93 γ-slice (#1294)** — ownerScope (mine / all) drill-in filter routed server-side through the existing `listVisibleSavedViews` Zod input; per lesson 51 server-side narrowing is required when LIMIT cap is in play (bases list caps at 50 rows); closed-taxonomy `OWNER_SCOPE_VALUES` tuple sourced from the server's enum; spread `...(ownerScopeFilter !== "all" ? { ownerScope } : {})` preserves the server's optional field shape (lesson 57); header + empty-state both adapt to active filter. **T-F.94 δ-slice (#1295)** — first mutation (create base); "New base…" button opens inline name-only form (Confirm/Cancel + char counter); calls pre-existing `createSavedView` tRPC with `vaultId + trimmed name + viewKind:"base"` — **no client-fabricated optional fields**, filters/columns/sort default server-side; disable predicate `isPending || trimmed === "" || effectiveVaultId === null` catches three real failure modes before round-trip; `onSuccess` invalidates list + closes form. After this 4-PR arc the Bases page is operator-functional end-to-end: create → list (filtered) → inspect → use the data model. |

**Main head post-#1295**: `<TBD>`. **Bases MVP (T-F.2) opening arc COMPLETE.** Names a refinement: **precedent (o)-discriminator** — when an existing table has a free-form discriminator column (`agsVaultSavedViews.viewKind`) and the existing tRPC stack accepts that discriminator as a filter input, the cheapest valuable territory-opening slice is to ship a new operator surface that consumes the existing stack with the new discriminator value. This extends precedent (o) (consume an existing-but-unsurfaced tRPC stack) with the insight that adding a single closed-taxonomy entry (here a new `ViewKindBlueprint`) can open an entire territory zero-server-work. The 4-PR α/β/γ/δ shape is identical to Quality Lens; what differs is the cost — Quality Lens had a finished server stack to consume; Bases has a finished server stack PLUS a discriminator that turns one stack into many territories. **lesson 62 — discriminator columns are territory multipliers**: when a server-side table has a `kind: string` / `category: string` / `*_type` column with no enum constraint, every new canonical value opens a potential operator territory. Look for such columns when scanning for `precedent (o)` candidates; they are 10× cheaper than greenfielding a new table. **lesson 63 — write-path slice unblocks first-use**: δ (create-base) is the first slice where the page is meaningful on a fresh install. α/β/γ are read-only and look "complete" but the page is permanently empty without write-path. Always ship a write-path slice early in a new territory; otherwise the operator demo is "look at the empty list".

**Standing-pattern menu after T-F.94:** (a) narrow within-Bases — per-row Edit (`updateSavedView` for name / filters / sort / columns) ε / per-row Delete (`deleteSavedView`) η / per-row Share-flip (workspace_shared toggle, requires server add since `updateSavedView` doesn't currently accept visibility) θ; (b) T-A.22 ledger refresh (this row); (c) consume-the-base — apply-filter drill-through to vault notes preview ζ (first "use the base" operator path; consumes existing `vault.listNotes` or similar with the base's filters); (d) larger new territory — T-F.3 Impact Analysis (blocked on Phase 7.5 per the scout report) / T-G code-graph parser (also blocked on Phase 7.5) / apply precedent (o)-discriminator to other free-form columns elsewhere in the codebase.

---

## 7. Connection back to MVP 0–4 closure

This plan is the explicit "successor execution plan" named in:
- `docs/implementation/agent-studio-native-graph-workspace-execution-plan.md` §0 scope boundary
- `docs/implementation/agent-studio-native-graph-workspace-status-check-2026-05-13.md` "Known gaps" item 4 ("V1+ scope is Deferred by plan, not failed")
- `~/.claude/projects/-root/memory/feedback_native_graph_workspace_continuing_rule.md` (the standing rule)

When this plan ships its first PR, the predecessor closure-doc's "deferred by plan" line becomes "tracked in successor plan" — no longer a deferral, an active workstream.

---

## 8. Reference docs

- `docs/architecture/agent-studio-native-graph-workspace.md` — top-level overview
- `docs/architecture/agent-studio-canvas-strategy.md` — Phase 17 ADR (exists)
- `docs/architecture/agent-studio-extension-framework-strategy.md` — Phase 18 ADR (exists)
- `docs/architecture/agent-studio-multi-region.md` — Phase MR-1 ADR (exists)
- `docs/architecture/agent-studio-neo4j-aura-upgrade-path.md` — Track J upgrade path ADR (exists)
- `docs/architecture/agent-studio-realtime-collab-crdt.md` — Phase CRDT ADR (created with this plan)
- `docs/architecture/agent-studio-offline-local-first.md` — Phase OL-1 ADR (created with this plan)
- `docs/implementation/agent-studio-native-graph-workspace-execution-plan.md` — predecessor
- `AGENTS.md` — operating order
- `CLAUDE.md` — Non-Build List
