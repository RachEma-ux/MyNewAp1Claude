# Agent Studio Native Graph Workspace — V1 / V1.5 / V2 Successor Execution Plan

**Status:** Draft (2026-05-13). Approved scope; implementation tracks ready to start.
**Predecessor:** `agent-studio-native-graph-workspace-execution-plan.md` (MVP 0–4, closed)
**Predecessor closure evidence:** `agent-studio-native-graph-workspace-status-check-2026-05-13.md`
**Owner agents (per AGENTS.md):** Planner (this doc) → Builder / Reviewer / Tester / Governance per phase
**Scope boundary:** This plan covers everything the MVP 0–4 plan explicitly deferred to V1+ — including Phase 13.5 Agentic GraphRAG, the standalone scopes of Phase 15–19, Track J production hardening, multi-region, CRDT, and offline-first.

---

## 0. Why this plan exists

The MVP 0–4 plan capped its scope at G10 closure (Graph Agent Lite live, golden questions passing). Everything beyond that — adaptive planning, full collaborative editing, multi-region deployment, plugin/extension framework, production hardening — was **explicitly deferred** to a successor plan. PR #723's closure addendum, this plan's predecessor, classified those items as *Deferred by plan, not failed* and named this doc as the return path.

Each phase below has:
- **Status today** (in repo)
- **Acceptance criteria** (what closes the phase)
- **First implementation PR** (concrete, actionable)
- **Dependencies / order**
- **Governance / hard-rule constraints**

The same Planner → Builder → Reviewer → Tester → Governance order from AGENTS.md applies.

---

## 1. Critical path

```
V1.0 — Production hardening + Agentic GraphRAG slice
    │
    ├─ Phase J-1   Production hardening minimum: ops runbooks + alerts
    ├─ Phase 13.5  Agentic GraphRAG — planner contract + bounded loop
    └─ Phase 19-α  Sync/publish strategy implementation slice 1
    ↓
V1.5 — Extension surface + standalone editor scopes
    │
    ├─ Phase 15-α  Templates / attachments standalone management
    ├─ Phase 16-α  Saved views extended scope
    ├─ Phase 17-α  Canvas data model + minimal UI
    └─ Phase 18-α  Extension framework registry + governance
    ↓
V2.0 — Multi-region + real-time collaboration
    │
    ├─ Phase MR-1  Multi-region graph deployment
    ├─ Phase CRDT  Real-time collaborative editing
    └─ Phase OL-1  Offline-first / local-first mode
    ↓
V2.x — Track J production hardening full surface (Aura / CRDT / multi-region together)
```

The path **does not** require linear execution — V1 phases can ship in parallel; V1.5 phases can begin once their V1 prerequisites land; V2.0 phases gate on V1.5 + production telemetry.

---

## 2. Phase-by-phase plan

### Phase 13.5 — Agentic GraphRAG (V1.0) — **FULLY IMPLEMENTED on main** (2026-05-13)

**MVP 0–4 deferral rationale:** plan §9 — "deferred to V1. Phase 13 is 'Graph Agent Lite' intentionally — overbuilding adaptive planning in MVP risks the G9 boundary verification."

**Status (post-merge):**
- Graph Agent Lite (`server/agent-studio/services/graph-agent/`) ships both a fixed plan→retrieve→reason→answer pipeline AND a bounded adaptive loop via `runAgentic()`.
- Phase 13.5 trio merged: PR #731 (contract) + PR #732 `ffb4eba9` (engine wiring + `RoundRobinPlanner`) + PR #737 `a8f5c634` (`createModelDrivenPlanner` for LLM-emitted plans).
- 62/62 tests green on main (boundary 27 + model-planner 20 + engine-agentic 9 + engine 6).
- MCP dispatcher boundary + OpenRouter Model Access boundary + no-mutation property all source-scan tested under the agentic surface.
- See ADR `docs/architecture/agent-studio-agentic-graphrag.md` (Status: ACTIVE) for the truth-claim table.

**Acceptance criteria:**
1. Planner contract — explicit ADR + TS interface for `AgenticPlanner`:
   `plan(query, prevSteps) → { action: "retrieve" | "answer" | "stop", arguments: ... }`
2. Bounded adaptive retrieval loop — `services/graph-agent/agentic-loop.ts`:
   max-N-iterations bound (configurable; default 4), short-circuits when answer-confidence ≥ threshold or budget exhausted.
3. No direct tool execution from the planner — all tool calls still route through `dispatchMcpToolCall()`. Source-scan tested.
4. No direct graph mutation — same Phase 11.5 proposal/approval surface as Graph Agent Lite.
5. Full trace emission — every iteration writes a `graphAgentDecisionTrace` step row (stepKind: `agentic_iteration_N`).
6. Governance guardrails — `agentic_eligibility` flag on skill packs; only packs explicitly marked agentic-eligible can be used by the loop.
7. Property-based tests — extend `graph-permission-visibility-property.test.ts` to cover multi-iteration paths (no leak across iterations).
8. First PR ships only the planner contract + tests + ADR — no engine wiring yet. Engine integration follows in PR #2.

**First implementation PR (Phase 13.5 / PR #1):**
- New file: `docs/architecture/agent-studio-agentic-graphrag.md` (ADR with boundary table)
- New file: `server/agent-studio/services/graph-agent/agentic-planner-contract.ts`
- New file: `server/agent-studio/services/graph-agent/agentic-loop.ts` (no-op stub returning `action: "stop"` to keep boundaries verifiable)
- New file: `tests/agent-studio/agentic-planner-boundary.test.ts` — source-scan: no `neo4j-driver` import, no direct `dispatchMcpToolCall` call (must route through engine), no direct graph mutation, max-iterations guard exists.

**Dependencies:** MVP 4 G9 closure (done). Production-hardening Phase J-1 ops runbook is parallel.

**Governance:** Touch with Reviewer + Governance both. Adaptive loops historically slip past dispatcher/governance boundaries; the ADR + source-scan tests are non-negotiable.

---

### Phase 15 — Templates / Attachments standalone scope (V1.5)

**MVP 0–4 deferral rationale:** plan §9 — Phase 15 standalone scope was bundled into MVP 1 Phase 4 + 5; standalone management surfaces deferred.

**Status today:**
- Templates: `services/vault/markdown-import-export.ts` handles import/export. No dedicated template-management UI surface.
- Attachments: `services/vault/attachments.ts` handles attachment binding. No dedicated attachment library / quota / scrubbing surface.

**Acceptance criteria:**
1. Template management UI page — operator can create / edit / delete / browse vault templates (route `/agent-studio/templates`).
2. Template-instantiation tracking — `agsVaultTemplateInstantiations` table records which note was created from which template (for refactor-safety + audit).
3. Attachment library UI — operator can browse attachments across vault, filter by mime, drop unused.
4. Attachment quota enforcement — per-workspace MB cap; configurable; surfaced in `RetrofitPage` operator dashboard alongside retention crons.
5. Source-scan + unit tests for the new surfaces.

**First implementation PR (Phase 15-α):**
- New file: `client/src/modules/agent-studio/pages/TemplatesPage.tsx`
- New file: `client/src/modules/agent-studio/pages/AttachmentLibraryPage.tsx`
- New file: `drizzle/tables/agent-studio-vault-template-instantiations.ts`
- New file: `tests/agent-studio/vault-template-instantiation.test.ts`

**Dependencies:** MVP 1 (done).
**Governance:** Touch the Vault module manifest; ensure publication boundaries preserved.

---

### Phase 16 — Saved views extended scope (V1.5)

**Status today:**
- MVP minimal saved views exist (`services/vault/saved-views.ts`). Per-user, per-workspace, JSON definition.

**Acceptance criteria:**
1. Sharing model — saved views can be marked `workspace_shared` (visible to all members) vs `personal`.
2. Versioning — saved-view definitions are immutably versioned (same shape as `agsNoteVersions`).
3. UI surface — saved-views browser, edit / share / version-history.
4. Permission-aware materialization — shared saved views must respect viewer's permissions (re-runs the visibility filter per viewer, not snapshot of author's results).

**First implementation PR (Phase 16-α):**
- Extend `services/vault/saved-views.ts` with sharing + versioning columns
- Migration: `agsSavedViews` adds `visibility`, `version`, `parentSavedViewId`
- New file: `client/src/modules/agent-studio/components/SavedViewsBrowser.tsx`
- Property-based test: visibility filter is re-applied per viewer

**Dependencies:** MVP 1 (done). Permission filter pushdown (G8, done).

---

### Phase 17 — Canvas data model + minimal UI (V1.5)

**MVP 0–4 deferral rationale:** plan §9 — Phase 17 lands as ADR-only in MVP 0; full implementation deferred.

**Status today:** ADR exists at `docs/architecture/agent-studio-canvas-strategy.md`.

**Acceptance criteria:**
1. Canvas data model — `agsCanvases` + `agsCanvasNodes` + `agsCanvasEdges` tables (additive; no overlap with `agsGraphNodes`/`agsGraphEdges`).
2. Canvas → Vault projection — when a Canvas references a note, the relationship projects into the graph backend as a typed edge (`CANVAS_REFERENCES_NOTE`).
3. Minimal UI — operator can create / open / arrange a Canvas; nodes are notes + free-text + embedded queries.
4. No plugin framework yet — Canvas is a first-class module, not a plugin host. (Phase 18 wires plugins in.)
5. CAG/RAC awareness — Canvas content can flow into CAG blocks via the existing source-note reference path (Phase 10).

**First implementation PR (Phase 17-α):**
- New tables in `drizzle/tables/agent-studio-canvas.ts`
- New file: `server/agent-studio/services/canvas/manifest.ts` + `public-api.ts` + `router.ts`
- Frontend: `client/src/modules/agent-studio/pages/CanvasPage.tsx`
- Source-scan boundary tests: Canvas data goes through GraphRepository for relationship queries; no direct neo4j-driver imports.

**Dependencies:** Phase 7 (typed graph), Phase 12 (retrieval).

---

### Phase 18 — Extension framework (V1.5)

**MVP 0–4 deferral rationale:** plan §9 — Phase 18 lands as ADR-only in MVP 0; full implementation deferred.

**Status today:** ADR exists at `docs/architecture/agent-studio-extension-framework-strategy.md`.

**Acceptance criteria:**
1. Extension registry — `agsExtensions` table tracks installed extensions per workspace.
2. Extension manifest contract — TS interface in `services/extensions/contracts.ts` defining: identity, capability declarations (which RACT lanes can the extension hook), required permissions, signing key.
3. Governance — extensions must pass the same approval scaffolding as new agents (`agsApprovalSteps` reused).
4. Capability-pinned dispatch — extensions cannot bypass the MCP dispatcher. Each extension declares which tools it intends to invoke; runtime asserts the declared set matches actual calls.
5. Source-scan blockers — no extension code can `import { dispatchMcpToolCall }` directly; must use the wrapper `services/extensions/runtime.ts:invokeFromExtension()`.
6. UI — extension management page (browse, install, approve, disable).

**First implementation PR (Phase 18-α):**
- New file: `drizzle/tables/agent-studio-extensions.ts`
- New file: `server/agent-studio/services/extensions/manifest.ts` + `contracts.ts` + `runtime.ts`
- New file: `tests/agent-studio/extension-dispatcher-boundary.test.ts` — source-scan + unit
- Frontend: `client/src/modules/agent-studio/pages/ExtensionsPage.tsx`

**Dependencies:** MCP dispatcher chokepoint (G7, done). Approval scaffolding (Cycle-4, done).

---

### Phase 19 — Sync / publish strategy (V1.0)

**MVP 0–4 deferral rationale:** plan §9 — Phase 19 lands as ADR-only in MVP 0; full implementation deferred.

**Status today:** Existing publish surface in `services/promotion/` covers note-version-pinned bindings. No multi-target sync (publish to staging-env, publish to remote vault, publish to external KB).

**Acceptance criteria:**
1. Publish target registry — `agsPublishTargets` table: target id + type (`staging_env` | `remote_vault` | `external_kb`) + endpoint + credentials reference (via `withProviderCredential` — Plan v3 D1, no env reads).
2. Publish-request workflow — operator creates a publish request, governance signs off (existing scaffolding), worker pushes to target with retry/idempotency.
3. Telemetry — publish results land in `agsPublishResults` and the workspace-observability bundle.
4. Rollback — every published artifact carries the source `notePromotionId` so rollback walks the existing promotion-rollback path.

**First implementation PR (Phase 19-α):**
- New table: `agsPublishTargets`
- Extend `services/promotion/` (do not greenfield) with target binding
- Source-scan: no direct `process.env.*_API_KEY` reads (D1)
- Integration test: publish request → target push → ledger row

**Dependencies:** Promotion governance (G7, done). Provider connections (Plan v3 D1, done).

---

### Track J / Phase 27 — Production hardening (V1.0 partial; V2.x full)

**MVP 0–4 deferral rationale:** CLAUDE.md Non-Build List + plan §13 Q4 — out-of-scope until Neo4j CE limits actually bite in production.

**Status today:**
- Upgrade path documented at `docs/architecture/agent-studio-neo4j-aura-upgrade-path.md`.
- Reference architecture at `docs/architecture/agent-studio-neo4j-aura-agent-reference-architecture.md`.
- No operator-actionable runbook for the upgrade itself.

**V1.0 acceptance criteria (Phase J-1 — operator runbook):**
1. Operator-actionable runbook at `docs/runbooks/agent-studio-neo4j-ce-to-aura-migration-runbook.md` with:
   - Trigger conditions (when CE limits bite — query throughput, write contention, fan-out depth)
   - Pre-flight inventory (active graph backend, projection queue state, fixture freshness)
   - Migration steps (Aura provision, projection-queue drain, dual-write window, cutover)
   - Verification — Aura + ASDB row-count parity, projection-queue idle
   - Rollback — restore CE bolt URI, replay queue
   - Evidence path
2. Alerting hooks — Phase 14 trace-graph cron checks Neo4j health latency + writes a `runtime_alert` on threshold breach.

**V2.x acceptance criteria (Track J full):**
1. Aura adoption (operator pull-trigger).
2. Multi-region (see Phase MR-1).
3. Production SLO budget for the graph stack (extends `agent-studio-runtime-slo.md`).

**First implementation PR (Phase J-1):**
- New file: `docs/runbooks/agent-studio-neo4j-ce-to-aura-migration-runbook.md`
- New file: `server/agent-studio/services/graph/health-alert.ts` (writes runtime_alert on threshold breach)
- New file: `tests/agent-studio/graph-health-alert.test.ts`

**Dependencies:** Phase 14 runtime trace graph (done). Phase 27 ADR (done).

---

### Phase MR-1 — Multi-region graph deployment (V2.0)

**Status today:** ADR exists at `docs/architecture/agent-studio-multi-region.md` (forward-looking, single-region operational baseline).

**Acceptance criteria:**
1. Region registry — `agsRegions` table: region id + canonical Postgres URI + canonical Neo4j URI + active flag.
2. Routing model — workspace → region pinning; queries route to the workspace's home region; cross-region reads are denied unless workspace is replicated.
3. Replication model — Postgres logical replication (Aurora-style or pg_basebackup-based); Neo4j projection re-built per region from the local Postgres source-of-truth.
4. Failover runbook — `docs/runbooks/agent-studio-multi-region-failover-runbook.md`.
5. Governance — cross-region promotion / publish requires explicit governance step.

**First implementation PR (Phase MR-1):**
- New file: `docs/runbooks/agent-studio-multi-region-failover-runbook.md`
- New table: `agsRegions`
- Source-scan: any `import { db }` resolves via the region-routed connection helper, never the global pool.

**Dependencies:** Track J operator runbook (Phase J-1).
**Risk:** Highest in V2.0 — touches the most boundaries. Single phase, multiple PRs.

---

### Phase CRDT — Real-time collaborative editing (V2.0)

**Status today:** No CRDT support; vault notes use last-writer-wins. No existing ADR.

**Acceptance criteria:**
1. ADR at `docs/architecture/agent-studio-realtime-collab-crdt.md` (this PR creates it) — pinned CRDT choice (Yjs leading candidate; comparison vs Automerge), source-of-truth model (Postgres remains canonical; CRDT layer is presence + cursor + transient edits), governance boundaries.
2. Presence + cursor surface — `services/vault/presence.ts` tracks who is in which note in near-real-time (no persistence beyond session).
3. CRDT document layer — `services/vault/realtime-doc.ts` wraps Yjs document; sync with backend uses the existing Vault repository at save boundaries.
4. Conflict policy — concurrent edits merge via Yjs; on save, the Vault repository writes the new canonical version; `agsNoteVersions` row is created.
5. Audit — every save resulting from a multi-author session is tagged in `agsRuntimeRuns` so audit can reconstruct collaborator list.

**First implementation PR (Phase CRDT-α):**
- New file: `docs/architecture/agent-studio-realtime-collab-crdt.md` (lands with THIS V1+ plan PR as a stub ADR; full content in dedicated PR)
- New file: `server/agent-studio/services/vault/presence.ts`
- Source-scan: no concurrent-write bypass of `agsNoteVersions`.

**Dependencies:** Vault editor (MVP 1, done).
**Risk:** UI complexity, fork-merge edge cases. Plan many small PRs.

---

### Phase OL-1 — Offline / local-first mode (V2.0)

**Status today:** No offline support. No existing ADR.

**Acceptance criteria:**
1. ADR at `docs/architecture/agent-studio-offline-local-first.md` (this PR creates it) — source-of-truth rules (server remains canonical; offline cache is a derived view), sync conflict policy (CRDT-aware when CRDT phase lands; otherwise field-level merge), graph projection behavior (stale until reconnect), security model (no graph mutation in offline mode; offline writes queue for governance review on reconnect).
2. Service-worker scaffold — client-side cache of recent notes + linked-graph subset.
3. Sync queue — offline writes accumulate in IndexedDB; on reconnect, queue replays through normal `services/vault/*` write paths (and approval/governance as needed).

**First implementation PR (Phase OL-1):**
- New file: `docs/architecture/agent-studio-offline-local-first.md` (this PR creates ADR stub)
- New file: `client/src/modules/agent-studio/services/offline-cache.ts`

**Dependencies:** CRDT phase (in particular, the merge semantics if CRDT is in scope).
**Risk:** Security model — offline graph mutations are a leak surface. Governance enforcement on reconnect is the load-bearing piece.

---

## 3. Cross-cutting hard rules (V1+)

These extend the CLAUDE.md Non-Build List. They MUST be source-scan tested in every PR that touches the relevant surface:

| Rule | Applies to |
|---|---|
| Provider credentials flow through `withProviderCredential` (Plan v3 D1) — never `process.env.*_API_KEY` reads in script bodies | Phase 19-α (sync/publish), Phase 13.5 (agentic engine eventually) |
| Postgres = source of truth; CRDT layer + offline cache are derived | Phase CRDT, Phase OL-1 |
| Region-router boundary — no direct `db` import in region-scoped code | Phase MR-1 |
| Extension dispatcher boundary — no direct `dispatchMcpToolCall` import outside `services/extensions/runtime.ts` | Phase 18 |
| Agentic loop boundary — bounded max-iterations + governance flag | Phase 13.5 |
| Graph mutations only through Phase 11.5 proposal/approval | All V1+ phases that touch the graph |

---

## 4. Testing layers added in V1+

The V1+ plan adds two new test layers on top of the existing 1–9:

| Layer | What it tests | Lands in |
|---|---|---|
| **Layer 10 — adaptive loop boundary** | Agentic GraphRAG max-iterations, no-mutation, governance-flag respected | Phase 13.5 |
| **Layer 11 — cross-region routing** | Workspace → region pinning, cross-region denial, failover replay | Phase MR-1 |

Layer 4 — e2e browser tests — also lands in V1.0 (covered separately in PR-C of the closure bundle: Playwright smoke + ADR + first executable test).

---

## 5. Risk register (V1+)

| Risk | Likelihood | Impact | Mitigation |
|---|---|---|---|
| Agentic loop slips past dispatcher | Medium | Critical | Source-scan + property-based tests + Reviewer + Governance both review |
| CRDT introduces silent data loss on merge | Medium | High | Property-based tests on merge semantics; every save creates an `agsNoteVersions` row |
| Offline writes leak ungoverned mutations | High | Critical | On-reconnect approval gate (no offline write skips governance) |
| Multi-region split-brain | Low | Critical | Cross-region read-deny default; explicit replication-aware promotion |
| Extension framework grows into a parallel runtime | High | Critical | Capability-pinned dispatch + Phase 18 source-scan tests |
| V1+ scope creep delaying V2 | Medium | Medium | Each phase has a "first implementation PR" identified — ship slim |

---

## 6. First-slice PR ledger (2026-05-13)

| PR | Phase | Scope | Merge SHA |
|---|---|---|---|
| #748 (PR-V1-1) | Phase J-1 | Graph health-alert evaluator + scanner + `ags_runtime_alerts` table; threshold-breach taxonomy for the Neo4j CE → Aura migration trigger conditions | `23e47643` |
| #749 (PR-V1-2) | Phase 19-α | `ags_publish_targets` + `ags_publish_target_executions` ledger + `services/publish-targets/` (Plan v3 D2-clean — pusher owns credential acquisition) | `892519b0` |
| #750 (PR-V1-3) | Phase 15-α | `ags_vault_template_instantiations` ledger + `services/vault/template-instantiations.ts` (sha256 digest + record/list helpers) + router wiring | `71c6889d` |
| #751 (PR-V1-4) | Phase 16-α | Saved views: `visibility` + `version` + `parentSavedViewId` columns + `saved-views-visibility.ts` (per-viewer materialization filter; load-bearing #4 acceptance) | `a6959a6d` |
| #752 (PR-V1-5) | Phase 17-α | Canvas data model (3 tables) + `services/canvas/` (CRUD + snapshot loader + note-reference projection helper) | `9fc503de` |
| #753 (PR-V1-6) | Phase 18-α | Extension framework: `ags_extensions` + `ags_extension_invocations` + `evaluateCapability` (pure) + `invokeFromExtension()` (only path to MCP dispatcher) | `9f019393` |
| #754 (PR-V2-1) | Phase MR-1 | `ags_regions` + `services/region/` (pure router with cross-region deny) + multi-region failover runbook | `1bf8e509` |
| #755 (PR-V2-2) | Phase CRDT-α | `ags_vault_note_presence` (5-min idle TTL) + `services/vault/presence.ts` (enter / heartbeat / leave / list with eager eviction) | `0b36c820` |
| #756 (PR-V2-3) | Phase OL-1 | `client/src/modules/agent-studio/services/offline-cache.ts` — `OfflineQueue` + load-bearing deny-list for graph + agent + provider operations | `3e6bae92` |

All 9 first-slice PRs landed on main on 2026-05-13. Each carries:
- Closed-taxonomy type unions where applicable
- Source-scan boundary tests preserving CLAUDE.md / Plan v3 D2 invariants
- ASDB-unavailable test fall-through patterns
- No raw `process.env.*_API_KEY` reads, no `credential-resolver` imports outside model-access, no `dispatchMcpToolCall` imports outside the MCP chokepoint / `services/extensions/runtime.ts` wrapper

**Status:** V1.0 + V1.5 + V2.0 first slices are now **FULLY IMPLEMENTED** at the data-model + service + boundary-test layer. UI surfaces and lane-specific runtime hooks land in subsequent β/γ slices per each phase's "out of scope for the α slice" section.

### 6.1 Second-slice (β/γ) PR ledger — opened 2026-05-13

| PR | Phase | Scope | Merge SHA |
|---|---|---|---|
| #758 (PR-V1-7) | Phase J-1-β | Health-alert cron (`services/graph/health-alert-cron.ts`) wrapping PR-V1-1 evaluator via `makeRetentionCron` factory at `*/5 * * * *`; boot Step 3.25; admin tRPC `agentStudio.graphHealth.getAlertCronStatus` + `listOpen` | `660303f6` |
| #759 (PR-V1-8) | Phase 19-β | Shared `makeHttpPusher` factory + `registerDefaultPublishPushers()` wiring the 3 target types (`staging_env` POST /promote / `remote_vault` PUT /vault/notes / `external_kb` POST /ingest); boot Step 3.26 | `525c5f8b` |
| #760 (PR-V1-9) | Phase 15-β | Attachment library service (`services/vault/attachment-library.ts`): browseAttachmentLibrary (vault-wide + mime-class filter + sort) + findUnusedAttachments (LEFT JOIN to vault_notes for trash detection) + computeAttachmentQuota + closed 6-value mime-class taxonomy | `ad7f9829` |
| #761 (PR-V1-10) | Phase 17-β | Canvas → Note projection edge (`services/canvas/projection.ts`): buildCanvasReferenceProjection emits the 4-write bundle (Canvas + CanvasNode + CONTAINS_CANVAS_NODE + CANVAS_REFERENCES_NOTE); ProjectionSyncWorker handles `canvas.note_reference_changed` / `_removed` event kinds | `e608cffe` |
| #762 (PR-V1-11) | Phase OL-2 | OfflineQueue durability (`client/.../offline-cache-store.ts`): closed `OfflineQueueStore` contract + `InMemoryOfflineQueueStore` (test default + non-browser fall-through) + `IndexedDbOfflineQueueStore` (production; lazy-open db `agent-studio-offline` / store `offline_queue`) + `createOfflineQueueStore()` factory; OfflineQueue gains optional `persistence` adapter + `hydrate()` to restore on reload; no breaking change to OL-1 API | `c67fab23` |
| #763 (PR-V1-12) | Phase MR-2 | Region-routed connection helper (`services/region/connection-helper.ts`): `getDbForRegion(region, opts)` with per-regionKey Drizzle pool cache + `getDbForWorkspace(input, opts)` composing the MR-1 pure router + `getNeo4jUriForRegion(region)` + `listWarmRegionPoolKeys()` operator inspection. Caller migration deferred to a follow-up sub-arc | `af8b1667` |
| #764 (PR-V1-13) | Phase CRDT-β | Realtime document layer (`services/vault/realtime-doc.ts`): pinned `RealtimeDocBackend` interface (encodeSnapshot/applyUpdate/getText/setText/pendingUpdateCount) + `InMemoryRealtimeDocBackend` reference impl (zero deps; JSON-encoded snapshots; last-writer-wins) + `RealtimeDocSession` wrapper + process-local session registry + `getCanonicalContentForSave` save-boundary helper. Yjs adapter + WebSocket transport + browser IDB persistence land in CRDT-γ | `183f0909` |
| #765 (PR-V1-14) | Phase CRDT-γ | Yjs adapter (`services/vault/realtime-doc-backend-yjs.ts`) implementing the CRDT-β `RealtimeDocBackend` contract via a typed-only `YjsNamespace` DI seam — production passes `Y` from `import * as Y from "yjs"` (declared in package.json under `dependencies`); unit tests use a typed fake and run locally without the yjs install. WebSocket transport + browser IDB persistence land in CRDT-γ-2 | `2a534c51` |
| #766 (PR-V1-15) | Phase 15-γ | Attachment quota write-gate (`services/vault/attachment-quota-guard.ts`): `assertWithinQuota({ vaultId, sizeBytesAdded, bytesLimit })` pure function (calls `computeAttachmentQuota` + projects usage + throws `AttachmentQuotaExceededError`) + `resolveDefaultAttachmentBytesLimit()` env-based config resolver (`AGS_VAULT_ATTACHMENT_BYTES_LIMIT`). Caller integration into `createAttachment` deferred to a follow-up sub-arc | `61e0c600` |
| #767 (PR-V1-16) | Phase 16-β | `listVisibleSavedViewsForUser` (`services/vault/saved-views.ts`): composes `listSavedViews` + the pure `filterVisibleSavedViews` predicate from `saved-views-visibility.ts` so per-viewer filtering happens in one call. `options.listImpl` DI seam keeps the composition testable without a live ASDB. Existing `listSavedViews` (operator/admin path) unchanged | `0c04a3a5` |
| #768 (PR-V1-17) | Phase 18-β | Extension lane hook registry (`services/extensions/lane-hooks.ts`): process-local `Map<ExtensionCapabilityLane, LaneHookFn>` + `registerLaneHook` (rejects `lane="tool"` — dispatcher-only invariant) + `getLaneHook` + `listRegisteredLaneHookLanes` + `__resetExtensionLaneHooksForTests`. `invokeFromExtension` calls the registered hook for non-tool lanes (capability check + hook + ledger), captures throws as `{ succeeded: false, errorMessage }`, falls back to α "assert + ledger" when no hook is registered. `options.getDb` test seam added to runtime for ASDB-unavailable test path | `09c67999` |
| #769 (PR-V1-18) | Phase 16-γ | Immutable saved-view version history: new `ags_vault_saved_view_versions` table (savedViewId + version unique; name/viewKind/filters/sort/columns/visibility/capturedByUserId/capturedAt) + `updateSavedView` captures prior row snapshot BEFORE applying the patch (not-found check moves earlier to the snapshot read) + `listSavedViewVersions(savedViewId)` + `getSavedViewVersionById(versionId)` + `UpdateSavedViewOptions.capturedByUserId` for audit trail | `200d4665` |
| #770 (PR-V1-19) | Phase 16 wire-up | Vault router gains 3 protected tRPC procedures wiring the 16-β + 16-γ service surfaces: `listVisibleSavedViews` threads `ctx.user.id` as `viewerUserId` into the service; `listSavedViewVersions(savedViewId)` + `getSavedViewVersion(versionId)` expose the immutable history. 11-test source-scan procedure-mount integrity suite at `tests/agent-studio/vault-saved-views-router-procedures.test.ts` | `3db12f93` |
| #771 (PR-V1-20) | Phase 15-γ wire-up | `createAttachment` router mutation enforces the quota: calls `assertWithinQuota({ vaultId, sizeBytesAdded, bytesLimit: resolveDefaultAttachmentBytesLimit() })` BEFORE the insert; maps `AttachmentQuotaExceededError` to `TRPCError code="FORBIDDEN"` (not the default INTERNAL_SERVER_ERROR); 8-test source-scan order-of-operations + error-code-mapping suite at `tests/agent-studio/vault-attachment-quota-router-wireup.test.ts` | `fdf7ba36` |
| #772 (PR-V1-21) | Phase 19-γ | Publish executor governance gate (`services/publish-targets/`): closed `GOVERNANCE_DECISIONS` 3-value taxonomy (`approved`/`pending`/`rejected`) + `GovernanceGateFn` + `isGovernanceDecision` narrowing in `types.ts`; `executePublish` branches: rejected → ledger row `status=failed` + `errorMessage="governance_rejected"` + `details.governanceDecision="rejected"`, pending → `status=pending` + `details.governanceDecision="pending"`, both short-circuit before the pusher runs; new `targetLoader` test seam in `ExecutePublishInput` for ASDB-unavailable unit-test path. 11-test suite at `tests/agent-studio/publish-targets-governance-gate.test.ts`. The gate is operator-supplied (DI); real `agsApprovalSteps` wiring lands in a follow-up adapter | `123de148` |
| #773 (PR-V1-22) | Phase OL-3 | OfflineQueue client bootstrap (`client/.../offline-cache-bootstrap.ts`): `getOfflineQueueInstance(opts)` module-singleton lazy-init with `createOfflineQueueStore()` (picks IDB in browser, InMemory elsewhere) + `__resetOfflineQueueInstanceForTests`; `hydrateOfflineQueueOnLoad(opts)` rehydrates persisted entries with `beforeHydrate/afterHydrate` telemetry hooks; `installOfflineDrainListener({ onDrain, eventTarget?, afterDrain? })` registers a `window.addEventListener("online", drainNow)` handler and returns an `OfflineDrainListenerHandle { drainNow, uninstall }`. SSR/CLI no-op-installer fall-through path. 17-test suite at `tests/agent-studio/offline-cache-bootstrap.test.ts` | `d5806bf8` |
| #774 (PR-V1-23) | Phase CRDT-γ-2 | Realtime-doc WebSocket transport scaffold (`services/vault/realtime-doc-transport.ts`): `RealtimeDocConnection` narrow interface (send/on(message)/on(close)/close) + `RealtimeDocTransport.attachConnection(conn, opts)` lifecycle: sends initial snapshot, wires `on("message")` → `session.handleUpdate` + broadcast to peers (originator-skip + per-peer error tolerance), `on("close")` → `detachConnection` + drop session when last client leaves AND `backend.pendingUpdateCount() === 0`. `shouldBroadcast` gate. Module-singleton accessor + test reset seam. 14-test suite at `tests/agent-studio/vault-realtime-doc-transport.test.ts`. Out of scope: y-protocols message framing + auth/authz upgrade + reconnect (CRDT-γ-3) | `5c69ae7b` |
| #775 (PR-V1-24) | MR×19 integration | Cross-region publish governance composition (`services/publish-targets/cross-region-governance.ts`): `requiresCrossRegionGovernance({ sourceRegionKey, targetRegionKey })` pure predicate (true iff both known + differ; null on either side treated as same-region) + `wrapWithCrossRegionGovernance(baseGate, opts)` adapter that composes any `GovernanceGateFn` with the cross-region check (same-region passes through to the base gate; cross-region downgrades to `pending` by default or `rejected` mode). Closes V1+ plan §MR-1 acceptance criterion #5 ("Governance — cross-region promotion / publish requires explicit governance step"). 16-test suite at `tests/agent-studio/publish-targets-cross-region-governance.test.ts` | `d02e3ae6` |
| #776 (PR-V1-25) | AS-1 ApprovalSteps gate adapter | Concrete `agsApprovalSteps` adapter for the 19-γ governance gate (`services/publish-targets/governance-gate-approval-steps.ts`): `decideGovernanceFromApprovalSteps(steps)` pure rule (any rejected → "rejected"; non-empty all-approved → "approved"; else "pending") + `createApprovalStepsGovernanceGate(opts)` factory returning a `GovernanceGateFn`. DI seams: `resolvePublishRequestId(input) → number \| null`, `getDb` (defaults to `getAsDb`), and `listApprovalSteps(id)` (test seam bypasses ASDB entirely). Read-only against `agsApprovalSteps`; defensive `"pending"` when the resolver returns null or ASDB is unavailable (never silent approve). 22-test suite at `tests/agent-studio/publish-targets-governance-gate-approval-steps.test.ts` | `1798c1bc` |
| #777 (PR-V1-26) | OL-4 offline drain dispatcher registry | Module-scoped registry + dispatcher composer that bridges OL-3's `installOfflineDrainListener({ onDrain })` and the per-kind tRPC drain implementations (`client/src/modules/agent-studio/services/offline-drain-dispatcher.ts`). Mirrors the server-side `publish-targets/registry.ts` pattern: `registerOfflineDrainImpl({ kind, impl })` / `getOfflineDrainImpl(kind)` / `listRegisteredOfflineDrainKinds()` / `__resetOfflineDrainRegistryForTests()` + `OfflineDrainImplNotRegisteredError` raised on unknown kind (no silent drop — Phase OL-3 keeps the entry queued and bumps `attempts`). `createOfflineDrainDispatcher({ lookup? })` returns an `OfflineDrainOne`-compatible function. The dispatcher does NOT re-validate `OFFLINE_DENIED_OPERATION_KINDS` — `OfflineQueue.enqueue()` is the gate. 18-test suite at `tests/agent-studio/offline-drain-dispatcher.test.ts` | `a16cf4f8` |
| #778 (PR-V1-27) | OL-5 default offline drain impls factory | Mirror of `publish-targets/defaults.ts` for the OL-4 registry (`client/src/modules/agent-studio/services/offline-drain-defaults.ts`). `registerDefaultOfflineDrainImpls({ updateNote, createNote, deleteNote })` wires the three `OFFLINE_OPERATION_KINDS` into the dispatcher registry: each closure receives the raw entry payload and resolves to void (return values discarded). Idempotent — re-registration overwrites prior closures per kind. The factory is tRPC-type-free (no `AppRouter` import) so the offline-cache module stays buildable without a tRPC namespace dependency; callers (App.tsx bootstrap) supply the trpc closures at call time. Returns the kinds in deterministic alphabetical order. 13-test suite at `tests/agent-studio/offline-drain-defaults.test.ts` | `8fc11b49` |
| #779 (PR-V1-28) | OL-6 offline cache full bootstrap composer | Single-call composer (`client/src/modules/agent-studio/services/offline-cache-full-bootstrap.ts`) that wraps the locked OL-5→OL-4→OL-3 chain: `bootstrapOfflineCache({ updateNote, createNote, deleteNote, beforeHydrate?, afterHydrate?, afterDrain?, eventTarget? })` runs `registerDefaultOfflineDrainImpls` → `createOfflineDrainDispatcher()` → `await hydrateOfflineQueueOnLoad(...)` → `installOfflineDrainListener({ onDrain: dispatcher })` in order, returning `{ hydratedCount, handle }`. App.tsx / main.tsx call sites become one-liners. tRPC-type-free (no `AppRouter`/`@/lib/trpc` imports — closures injected). With this, the OL-1→OL-6 chain is end-to-end composed at the service layer; remaining work is the real App.tsx call site with live trpc mutations. 13-test suite covering composition, online-event drain through plain `EventTarget`, hydratedCount, telemetry beacons, uninstall semantics, and source-scan boundaries | `4d9abb01` |
| #780 (PR-V1-29) | NV-1 server-side `vault.note.delete` procedure | Closes the contract gap surfaced during OL-5: `OFFLINE_OPERATION_KINDS` declares `vault.note.delete` but the vault router had no `deleteNote` procedure. Adds `NoteDeleteInput` Zod (noteId + optional `expectedVersion` for online optimistic-lock; optional for offline-drain replays that may not know current state) + `VaultRepository.deleteNote(input, userId)` interface method + `VaultRepositoryStub` semantics (idempotent on already-deleted, conflict on version-mismatch, notFound on missing id) + `AsdbVaultRepository` soft-delete impl (`UPDATE ags_vault_notes SET deletedAt = NOW()` with `deletedAt IS NULL` precondition + `getNoteById` left unfiltered for audit-recovery; `listNotesInVault` already filters `deletedAt IS NULL` so deleted notes drop out of listings) + `agentStudio.vault.deleteNote` tRPC mutation (NOT_FOUND TRPCError on missing, alreadyDeleted:true on idempotent second call, conflict+latestVersion on optimistic-lock mismatch). 21-test suite at `tests/agent-studio/vault-delete-note.test.ts` (contract parsing, stub semantics, router source-scan, ASDB source-scan) | `f26c3829` |
| #781 (PR-V1-30) | OL-7 main.tsx wire-up to vanilla tRPC client | New `client/src/modules/agent-studio/services/offline-cache-app-wireup.ts` builds a vanilla `createTRPCClient<AppRouter>` (sibling to the React-hooks client) and wires its three `agentStudio.vault.*Note` mutations into `bootstrapOfflineCache` (OL-6). `main.tsx` imports and `void`-invokes `wireOfflineCacheToTrpc()` once at boot — fire-and-forget so a failed bootstrap never blocks render. Hard-rule clean (no `credential-resolver` / `dispatchMcpToolCall` / `*_API_KEY` / `neo4j-driver`). 11-test source-scan suite (`tests/agent-studio/offline-cache-app-wireup.test.ts`) covering tRPC client shape, mutation bindings, `.catch` failure path, hard-rule absences, + `main.tsx` single-call invariant. Closes the OL-1→OL-7 end-to-end offline-cache compose chain at the live App.tsx boundary | `75313ad2` |
| #782 (PR-V1-31) | AS-2 default governance-gate registry | Module-singleton registry (`server/agent-studio/services/publish-targets/default-governance-gate.ts`) consulted by `executePublish` when callers do not supply an explicit `governanceGate`. Exposes `installDefaultGovernanceGate` / `getDefaultGovernanceGate` / `hasDefaultGovernanceGate` / `__resetDefaultGovernanceGateForTests` (parallels the existing `registry.ts` pattern). Executor falls through via `input.governanceGate ?? getDefaultGovernanceGate()` — per-call gates still win. With this, AS-1's `createApprovalStepsGovernanceGate` can be installed once at boot rather than threaded through every `executePublish` caller. 16-test suite at `tests/agent-studio/publish-targets-default-governance-gate.test.ts` (registry lifecycle + executor fall-through + per-call precedence + barrel re-export + source-scan boundary). Real `installDefaultGovernanceGate(createApprovalStepsGovernanceGate(...))` boot call defers to a follow-up PR that owns the `resolvePublishRequestId` mapping | `b0e906e3` |
| #783 (PR-V1-32) | OL-8 offline-cache enqueue helper | Consumer-side surface for the OL-1..OL-7 compose chain (`client/src/modules/agent-studio/services/offline-cache-enqueue.ts`). Single helper `enqueueOfflineVaultMutation({ kind, payload, id?, generateId?, queue? })` that (1) routes through the OL-3 module-singleton queue, (2) generates a UUIDv4 id when none supplied (default via `crypto.randomUUID`; `generateId` test seam), and (3) delegates `assertOfflineQueueable(kind)` to `OfflineQueue.enqueue()` so the OL-1 deny-list is still the only gate. Re-exports `OfflineMutationDeniedError` for callers wanting to catch + degrade. tRPC-type-free (no `AppRouter` import) so the offline-cache module stays buildable without a tRPC namespace dependency — payloads stay `Record<string, unknown>` and the server re-runs Zod on drain. 15-test suite at `tests/agent-studio/offline-cache-enqueue.test.ts` (happy path, caller-supplied id, generateId seam, default crypto.randomUUID shape, deny-list rejection, unknown-kind rejection, allowlist coverage, source-scan boundary) | `192dc42b` |
| #784 (PR-V1-33) | AS-3 composeGovernanceGates helper | Pure generic composer (`server/agent-studio/services/publish-targets/compose-governance-gates.ts`): runs N `GovernanceGateFn`s in order; returns the strictest decision (rejected > pending > approved); short-circuits on rejected so an expensive gate (DB lookup, remote call) is skipped when a cheap pre-check has already rejected; defensive snapshot so caller mutation of the input array after composition does not change behavior; empty list returns "approved" (no constraint, preserves executor's "no gate ⇒ approved" semantics). With AS-1 (`createApprovalStepsGovernanceGate`) + AS-2 default-gate registry + this composer, operators can install `installDefaultGovernanceGate(composeGovernanceGates([approvalStepsGate, wrapWithCrossRegionGovernance(...)]))` at boot to enforce BOTH approval-steps AND cross-region governance without hand-rolling a wrapper. 17-test suite at `tests/agent-studio/publish-targets-compose-governance-gates.test.ts` (decision matrix + short-circuit + order preservation + defensive snapshot + public-api re-export + source-scan boundary including no `drizzle-orm` import — pure composer) | `a15583c5` |
| #785 (PR-V1-34) | OL-9 offline-or-live mutation router | One-liner for UI call sites that need to decide between calling the live tRPC mutation (online) and enqueueing offline (`client/src/modules/agent-studio/services/offline-cache-router.ts`). `routeVaultMutation({ kind, payload, callLive, isOnline?, id?, generateId?, queue? })` defaults `isOnline` to `navigator.onLine` (true when navigator is unavailable — non-browser env); online path returns `{ mode: "live", result }` with `callLive` invoked exactly once; offline path returns `{ mode: "queued", entry }` via OL-8 with `callLive` NOT invoked. Live-mode errors propagate unchanged (caller decides retry semantics — no implicit fall-through to enqueue, which would mask transient server errors). Pure function (no React/JSDOM coupling); future React-hook layer is a separate PR if needed. 13-test suite at `tests/agent-studio/offline-cache-router.test.ts` (online happy path, live-error propagation, offline enqueue, deny-list rejection on offline path, generateId seam, navigator.onLine default both directions, source-scan boundary — no react/react-dom/AppRouter imports) | `912a8490` |
| #786 (PR-V1-35) | AS-4 payload-based resolver + env-flag boot install | Closes the AS-2 named follow-up. Two new files: `payload-publish-request-id-resolver.ts` (`parsePublishRequestIdFromBody` pure parsing + `resolvePublishRequestIdFromPayload` async wrapper — pulls `payload.body.publishRequestId` as a positive integer; null on missing / non-integer / non-positive — defensive null is load-bearing, NEVER silent-approves when linkage is unclear) + `install-default-governance-gate.ts` (`maybeInstallDefaultGovernanceGate({ env?, install?, makeGate? })` env-flag-gated installer: composes AS-1 `createApprovalStepsGovernanceGate` with the AS-4 resolver and registers via AS-2 `installDefaultGovernanceGate` when `AGS_PUBLISH_GOVERNANCE_GATE=approval_steps_payload_resolver`; any other value or unset → no-op). Boot Step 3.27 in `server/agent-studio/boot.ts` calls the installer + logs the decision. Default OFF — preserves existing "no gate ⇒ approved" behavior on first install; operators opt-in once their callers wire `payload.body.publishRequestId`. 35-test suite at `tests/agent-studio/publish-targets-install-default-governance-gate.test.ts` (parsing matrix incl. NaN/Infinity/float/zero/negative rejection, env-flag strict-equality matrix incl. wrong casing, composed gate end-to-end via AS-1's listApprovalSteps seam, public-api re-exports, source-scan boundary including no `drizzle-orm` import in the resolver) | `439c5335` |
| #787 (PR-V1-36) | CRDT-γ-3-auth realtime-doc authorization rule | Pure authorization helper (`server/agent-studio/services/vault/realtime-doc-authorize.ts`) that the future WS-upgrade handler will call BEFORE `RealtimeDocTransport.attachConnection` (#774). `authorizeRealtimeDocConnection({ sessionKey, userId, getVaultIdsForUser })` returns the closed 3-value taxonomy: `{ allow: true }` when `sessionKey.vaultId` ∈ `getVaultIdsForUser(userId)`, else `{ allow: false, reason: "missing_user_id" \| "not_a_vault_member" \| "vault_membership_lookup_failed" }`. Defensive deny on DI throw — NEVER silent-approves on lookup failure (a duplex realtime channel cannot afford a silent-approve hole). Missing-userId short-circuits BEFORE the lookup. tRPC-type-free + repository-import-free — `getVaultIdsForUser` is a closure the caller wires from `VaultRepository.listVaultsForUser`. 16-test suite at `tests/agent-studio/vault-realtime-doc-authorize.test.ts` (closed taxonomy, allow path, 4 deny paths, lookup invocation count, source-scan boundary). Out of scope: WS-upgrade handler that calls this (separate PR), y-protocols message framing (CRDT-γ-3-framing), reconnect (CRDT-γ-3-reconnect) | `329526de` |
| #788 (PR-V1-37) | CRDT-γ-3-upgrade-handler — composes γ-3-auth + γ-2 transport | `handleRealtimeDocUpgrade({ conn, sessionKey, userId, transport, authorize? \| getVaultIdsForUser? })` is the entry-point the future WebSocket-upgrade pipeline calls (`server/agent-studio/services/vault/realtime-doc-upgrade-handler.ts`). Allow → `transport.attachConnection(conn, { sessionKey })` and returns `{ attached: true, disposer }`. Deny → closes the connection with a stable WebSocket close code in the private-use 4xxx range + the deny reason as message; NEVER attaches. Closed close-code taxonomy: `missing_user_id → 4401`, `not_a_vault_member → 4403`, `vault_membership_lookup_failed → 4500` (HTTP-ish for client UX). Tolerates `conn.close` throwing (race against client-side abort). Two DI surfaces: `authorize` for tests + framework wrappers, `getVaultIdsForUser` for production (builds the default authorizer via closure). Throws when neither is supplied (no silent default). 17-test suite at `tests/agent-studio/vault-realtime-doc-upgrade-handler.test.ts` (close-code taxonomy, allow→attach, parameterized 3-reason deny→close, conn.close race tolerance, getVaultIdsForUser default-wiring both directions, missing-DI throw, authorize-override-wins precedence, source-scan boundary). Still out of scope: WebSocket framework choice + actual upgrade pipeline (TBD), y-protocols framing (CRDT-γ-3-framing), reconnect (CRDT-γ-3-reconnect) | `388a575a` |
| #789 (PR-V1-38) | WebSocket upgrade pipeline ADR + first slice | ADR at `docs/architecture/agent-studio-websocket-upgrade-pipeline.md` pins **`ws` ^8.18.3 with `noServer: true`** as the framework; rejects uWebSockets.js / native http upgrade / socket.io / framework-bundled with rationale. Bridge at `server/agent-studio/services/vault/realtime-doc-websocket-bridge.ts` exports `adaptWsToRealtimeDocConnection(ws)` (wraps a `ws.WebSocket`-like in the `RealtimeDocConnection` narrow interface; routes `error` events to the close handler with `code=1011` + `reason="ws_error:..."` so no error is silently dropped) + `installRealtimeDocUpgradeOnServer(server, opts)` (env-flag-gated dispatcher that subscribes once to `server.on("upgrade")`; right path `/api/agent-studio/vault/realtime` + valid `vaultId`+`noteId` query → `wss.handleUpgrade` → adapter → `handleRealtimeDocUpgrade`; wrong path / malformed query → `socket.destroy()` — no half-open sockets). Env flag `AGS_REALTIME_DOC_WEBSOCKET_ENABLED=true` enables; default OFF preserves boot behavior. Boot install in `server/_core/index.ts` between `createServer(app)` and `server.listen(...)` — async-import + try/catch so an install error never crashes the process. tRPC-type-free, no `credential-resolver` / `dispatchMcpToolCall` / `*_API_KEY` / `neo4j-driver` / `openrouter` imports — source-scan tested. 43-test suite at `tests/agent-studio/vault-realtime-doc-websocket-bridge.test.ts` (adapter forwarding, error→close routing, parsePathname / parseSessionKey matrices, env-flag strict-equality matrix, wrong-path / malformed-query destroy paths, dispatch routing, deny path closes the ws, missing-userId 4401 path, source-scan boundary, boot-install integrity). Out of scope: production auth-cookie resolver, `getVaultIdsForUser` production wiring (DI seams for now), CRDT-γ-3-framing dispatch, live integration test against a running server | `a40ab696` |
| #790 (PR-V1-39) | CRDT-γ-3-framing message framing primitives + dispatcher | Pure framing layer (`server/agent-studio/services/vault/realtime-doc-framing.ts`) the future transport-side integration will compose with. Closed 3-value taxonomy `REALTIME_DOC_FRAME_TYPES = ["sync", "awareness", "unknown"]`; matches y-protocols wire format (leading byte 0 → sync, 1 → awareness, else → unknown). `parseRealtimeDocFrame(data, opts?)` returns `{ type, leadingByte, payload, totalBytes }`; payload is a `subarray` view (no copy). Throws `RealtimeDocFrameEmptyError` on empty buffer + `RealtimeDocFrameTooLargeError` on oversize (default cap `DEFAULT_MAX_REALTIME_DOC_FRAME_BYTES = 2 MiB`; tunable via `opts.maxBytes`). `dispatchRealtimeDocFrame({ data, handlers, ctx, maxBytes? })` composes parse + handler routing: sync → `handlers.onSync`, awareness → `handlers.onAwareness`, unknown → `handlers.onUnknown` (no-op when handler omitted); awaits async handlers + propagates their rejections + propagates parse errors so the caller (future transport message handler) decides close-vs-drop. tRPC-type-free, drizzle-free, y-protocols-free (DI seam — engine plugs in later via the handler closures). Hard-rule clean (no `credential-resolver` / `dispatchMcpToolCall` / `*_API_KEY` / `neo4j-driver` / `openrouter` / `GraphRepository` imports). 32-test suite at `tests/agent-studio/vault-realtime-doc-framing.test.ts` (closed taxonomy, parseRealtimeDocFrameType matrix incl. bytes 2..255 → unknown, parse happy paths with subarray-view assertion, empty-buffer + oversize errors at default + custom limits + at-limit no-throw, dispatcher routing for sync/awareness/unknown incl. missing-handler no-op, parse-error propagation through dispatch, async-handler rejection propagation, source-scan boundary). Out of scope: transport-side wire-up (separate PR; the framing primitives must land first so dispatch is testable in isolation), real y-protocols sync engine, real y-protocols awareness engine | `e99fcbf5` |
| #791 (PR-V1-40) | CRDT-γ-3-framing transport wire-up | Composes PR-V1-39's `parseRealtimeDocFrame` + `dispatchRealtimeDocFrame` into the CRDT-γ-2 transport's per-connection `onMessage` handler (`server/agent-studio/services/vault/realtime-doc-transport.ts`). Three new `AttachConnectionOptions` fields: `frameHandlers?: RealtimeDocFrameHandlers` (opt-in — when set, every inbound message routes through the dispatcher with the leading type byte stripped before handler invocation), `maxFrameBytes?: number` (frame-size cap override; ignored when frameHandlers unset), `closeOnFrameError?: boolean` (default `true` — empty / oversize / handler-throw closes the connection with a stable code). Closed 3-value close-code taxonomy `REALTIME_DOC_FRAME_CLOSE_CODES = { empty_frame: 4400, frame_too_large: 4413, handler_error: 4500 }` in the WebSocket private-use range. When `frameHandlers` is omitted, the existing CRDT-γ-2 behavior is preserved bit-for-bit (raw `data` → `session.handleUpdate`; raw frame broadcast to peers) — no breaking change to existing call sites. shouldBroadcast=false still suppresses both dispatch and broadcast (defense-in-depth). Tolerates `conn.close` throwing during error-handling (already-closed race). 15-test suite at `tests/agent-studio/vault-realtime-doc-transport-framing.test.ts` (default-behavior preservation, sync/awareness/unknown dispatch routing, payload-stripping assertion, peer-broadcast assertion, 3 close-code error paths, closeOnFrameError=false, maxFrameBytes override, shouldBroadcast=false interplay, taxonomy assertion, source-scan that transport now imports the framing dispatcher). Existing 14-test transport regression + 122-test full realtime-doc regression suite still green | `337753e6` |
| #792 (PR-V1-41) | 17-γ canvas → graph projection events sink + caller hook | ADR `docs/architecture/agent-studio-canvas-projection-events.md` accepts the **sink registry + caller hook** pattern (mirrors AS-2 default-governance-gate registry). Rejects: extending `agsRuntimeGraphEvents` (runtimeRunId notNull constraint), boot-installed `ProjectionSyncWorker` singleton (tight coupling), EventEmitter (process-local), tRPC-triggered worker (no operational benefit), cron polling (misses events). New `server/agent-studio/services/canvas/projection-events-sink.ts`: module-singleton sink (`registerCanvasProjectionEventSink` / `getCanvasProjectionEventSink` / `hasCanvasProjectionEventSink` / `__resetCanvasProjectionEventSinkForTests`) + `recordCanvasProjectionEvent(event)` convenience entry-point. Closed `CanvasProjectionEvent` union (`canvas.note_reference_changed` + `canvas.note_reference_removed`) declared structurally so the sink module stays isolated from `services/graph/projection/sync-worker.ts` (whose `GraphRepository` import would pull forbidden surfaces through a transitive scan). Default sink is no-op — installing this code does NOT change canvas-service behavior on first install. Sink errors caught + `console.warn` logged — mutation path never fails on sink failure. Caller wired in `createCanvasNode`: when `node.referencedNoteId != null`, emits `canvas.note_reference_changed`. 21-test suite at `tests/agent-studio/canvas-projection-events-sink.test.ts` (registry semantics, no-op default both kinds, sink forwarding both kinds, async-sink await, sync + async sink-throw catch, public-api re-export of 5 names + 2 types, source-scan boundary including no-ProjectionSyncWorker import in either sink or canvas-service). Out of scope: persistence sink + new `ags_canvas_projection_events` table (schema migration its own PR), drain wiring boot step, update/delete caller-side hooks (canvas-service has no updateCanvasNode/deleteCanvasNode yet), cross-region propagation via MR-2 | `0540c826` |
| #793 (PR-V1-42) | 18-γ-contracts extension lane hook contracts (typed surface) | ADR `docs/architecture/agent-studio-extension-lane-hook-contracts.md` pins the **typed per-lane contract surface** every concrete hook must satisfy. Closed 3-value `CONTRACTED_EXTENSION_LANES = ["retrieve", "assemble", "compose"]` — `tool` lane is excluded by construction (dispatcher-only invariant). Per-lane outcome types in `server/agent-studio/services/extensions/lane-hook-contracts.ts`: `ContractedRetrieveOutcome` (`supplementalPlanItems?` + `supplementalChunks?` with required `provenance.sourceType` + `sourceId` so the existing `retrieval-filter.ts` governance + permission filter applies without special-casing), `ContractedAssembleOutcome` (`supplementalChunks?` + `tokensAttributed?` — `RAC_MAX_RETRIEVAL_EVIDENCE_TOKENS` cap enforced at the assembler), `ContractedComposeOutcome` (`supplementalBlocks?` in CAG `BuildPackInput.userBlocks` shape — hash invariant preserved; a different contribution produces a different pack id). Discriminated `ContractedLaneHookFn<Lane>` that the compiler uses to enforce per-lane outcome shape at registrant boundary. **Hard rules (load-bearing per CLAUDE.md):** no RAC bypass (all contributions are INPUTS to existing pipeline; planner/filter/assembler still run); no raw context injection (contributions flow through the same filter); citation + freshness preserved (provenance required at type level); CAG hash invariant preserved; tool lane stays dispatcher-only (excluded from contract surface). Type-only imports of RAC + CAG canonical types — runtime boundary preserved; source-scan tests catch a future contributor accidentally promoting a type-only import to a value import. 18-test suite at `tests/agent-studio/extension-lane-hook-contracts.test.ts` (closed taxonomy, retrieve / assemble / compose outcome shape construction, contract→registry composition assertion for all 3 lanes via `ContractedLaneHookFn` up-cast to `LaneHookFn`, discriminated union test, public-api re-export of 9 names, source-scan boundary + type-only-import boundary). Out of scope: concrete retrieve / assemble / compose hook implementations (each its own PR with RAC + CAG pipeline integration), extension-install UI | `0d8bb0fc` |
| #794 (PR-V1-43) | MR-3 getAsDb caller inventory + first batch migration | Inventory doc `docs/implementation/agent-studio-mr-3-getasdb-inventory.md` classifies all 151 `getAsDb()` call sites across 44 files into 5 categories: A (should migrate now — workspaceId in scope), B (requires plumbing), C (already acceptable — infrastructure / boot / seed), D (tRPC routers — dedicated sub-arc), E (requires ADR — multi-workspace iterators). New Phase-1 shim `getAsDbForWorkspace(workspaceId)` in `server/agent-studio/db/connection.ts` accepts `workspaceId` and delegates to `getAsDb()` — preserves single-region operational baseline bit-for-bit; Phase-2 follow-up wires real region routing via the existing `getDbForWorkspace` helper without caller changes. First-batch migration: `extensions/manifest.ts::installExtension(input)` → `getAsDbForWorkspace(input.workspaceId)`; `extensions/manifest.ts::listExtensionsByWorkspace(workspaceId, ...)` → `getAsDbForWorkspace(workspaceId)`. The other 3 functions in the same file (`approveExtension` / `setExtensionStatus` / `getExtensionById`) are Category B (need workspaceId plumbed through callers) — left on `getAsDb()` intentionally; source-scan tests assert this. 9-test suite at `tests/agent-studio/mr-3-getasdb-for-workspace-shim.test.ts` (shim cache-identity check, function-body source-scan with comment-stripping, asserts both migrated functions call the shim, asserts the 3 unmigrated functions still call `getAsDb()`, inventory-doc presence check). Out of scope: Phase-2 of the shim (real region routing), Category B migrations (one per caller-chain), Category D router sub-arc, Category E ADRs | `d7297ad6` |
| #795 (PR-V1-44) | 16-δ SavedViewVersionHistoryPanel first UI surface | First Phase G UI slice: small focused component (`client/src/modules/agent-studio/components/SavedViewVersionHistoryPanel.tsx`) backed by the existing `agentStudio.vault.listSavedViewVersions` tRPC procedure (#770). Renders 4 states (loading / error / empty / populated) with one `<li>` per version showing `v{N} — {name}`, relative capturedAt, and optional `Captured by user #{id}` line. Single-responsibility extracted component pattern per the playbook's "Avoid large RetrofitPage-style growth"; matches the retention-panel arc reference. 4-test JSDOM-environment suite at `SavedViewVersionHistoryPanel.test.tsx` covering loading branch, error branch with message echo, empty branch, populated branch with version/name/user-id rendering + null-userId omits the "Captured by" line. Out of scope: route/page that embeds the panel, edit/restore actions (those need additional tRPC procedures), saved-view detail page composition | `370308dd` |
| #796 (PR-V1-45) | 15-δ AttachmentListPanel second focused UI surface | Sibling to PR-V1-44's pattern. Component (`client/src/modules/agent-studio/components/AttachmentListPanel.tsx`) backed by the existing `agentStudio.vault.listAttachments` tRPC procedure. Accepts `vaultId` and/or `noteId` (router enforces "one of"; panel renders a guard-error card when both are missing rather than firing an invalid query). 5 states (input-guard / loading / error / empty / populated). Populated rows render filename, mimeType, formatted size via local `formatBytes` (B / KiB / MiB / GiB), relative `createdAt`, and an amber "source artifact" badge when `isSourceArtifact=true`. 6-test JSDOM-environment suite at `AttachmentListPanel.test.tsx` (input-guard branch, 3 query-state branches, populated multi-row rendering with size formatting + badge selector assertion, sub-1KiB byte-formatting branch). Out of scope: route/page composition, attachment-quota dashboard, attachment-library deletion / link / source-artifact-toggle UI | `81fbe761` |
| #797 (PR-V1-46) | MR-3 second batch — `vault/repository-asdb.ts::createVault` | Second migration batch off the MR-3 Phase-1 shim (#794). Widens `getAsDbForWorkspace`'s signature from `(workspaceId: number)` to `(workspaceId: number \| null \| undefined)` to accommodate optional / nullable workspaceId at call sites — Phase-1 shim still delegates to `getAsDb()` regardless, so single-region behavior is preserved bit-for-bit; Phase-2 will treat null/undefined as "use the default region" rather than throwing. `createVault(input, createdByUserId)` migrates to `getAsDbForWorkspace(input.workspaceId)` because `VaultCreateInput.workspaceId` is on the input schema (optional). The other 8 methods in the same file (`addMember` / `listVaultsForUser` / `createNote` / `getNoteById` / `getNoteVersion` / `updateNote` / `listNotesInVault` / `deleteNote`) remain on `getAsDb()` — they're inventory Category B (require workspaceId plumbed through callers via a separate per-method sub-arc). 12-test source-scan suite at `tests/agent-studio/mr-3-second-batch-vault-repository.test.ts` (shim regression for `undefined` / `null`, import-shape check, `createVault` body asserts `getAsDbForWorkspace(input.workspaceId)` + no `getAsDb()`, parameterized assertion that the 8 unmigrated methods still call `getAsDb()`). Comment-stripping method-body extractor reused from #794. Out of scope: Phase-2 of the shim (real region routing), Category B migrations (one per caller-chain), Category D router sub-arc | `c6839045` |
| #798 (PR-V1-47) | MR-3 third batch — `ingestion/ingestion-job-service.ts::startJob` | Third migration batch off the MR-3 Phase-1 shim (#794). `startJob(input)` in `server/agent-studio/services/ingestion/ingestion-job-service.ts` migrates to `getAsDbForWorkspace(input.workspaceId)` (StartJobInput.workspaceId is required `number`). The other 2 functions in the same file (`completeJob` / `getJob`) remain on `getAsDb()` — they take only `jobId` and would need workspaceId plumbed through callers (Category B). 4-test source-scan suite at `tests/agent-studio/mr-3-third-batch-ingestion-job-service.test.ts` (import-shape check, `startJob` body assertion, parameterized assertion that the 2 unmigrated functions still call `getAsDb()`). Function-body extractor variant of #794's method extractor — handles top-level `export async function` shape instead of class methods. Out of scope: Phase-2 of the shim, Category B migrations for `completeJob` / `getJob`, the other 3 ingestion files still on `getAsDb()` (separate per-file sub-arcs) | `176befbb` |
| #799 (PR-V1-48) | MR-3 fourth batch — `ingestion/knowledge-unit-service.ts::insertUnit` | Fourth migration batch off the MR-3 Phase-1 shim (#794). `insertUnit(input)` in `server/agent-studio/services/ingestion/knowledge-unit-service.ts` migrates to `getAsDbForWorkspace(input.workspaceId)` (`InsertUnitInput.workspaceId` is required via `NormalizedKnowledgeUnitInput`). The other function in the same file (`archiveUnit(unitId)`) remains on `getAsDb()` — it takes only `unitId` and would need workspaceId plumbed through callers (Category B). 3-test source-scan suite at `tests/agent-studio/mr-3-fourth-batch-knowledge-unit-service.test.ts` (import-shape check, `insertUnit` body assertion, `archiveUnit` Category-B assertion). Out of scope: Phase-2 of the shim, Category B migration for `archiveUnit`, the other 2 ingestion files still on `getAsDb()` (`provenance-service.ts`, `universal-ingestion-service.ts` — each a separate sub-arc) | `688ad711` |
| #800 (PR-V1-49) | MR-3 fifth batch — `ingestion/provenance-service.ts::recordProvenance` | Fifth migration batch off the MR-3 Phase-1 shim (#794). `recordProvenance(input)` in `server/agent-studio/services/ingestion/provenance-service.ts` migrates to `getAsDbForWorkspace(input.workspaceId)` (`RecordProvenanceInput.workspaceId` is required `number`). The other function in the same file (`getProvenance(provenanceId)`) remains on `getAsDb()` — it takes only `provenanceId` and would need workspaceId plumbed through callers (Category B). 3-test source-scan suite at `tests/agent-studio/mr-3-fifth-batch-provenance-service.test.ts` (import-shape check, `recordProvenance` body assertion, `getProvenance` Category-B assertion). Out of scope: Phase-2 of the shim, Category B migration for `getProvenance`, the last remaining ingestion file (`universal-ingestion-service.ts`, sixth batch) | `bb1a1dc0` |
| #801 (PR-V1-50) | MR-3 sixth batch — `ingestion/universal-ingestion-service.ts` (FULL file migration) | Sixth and final ingestion migration off the MR-3 Phase-1 shim (#794). Both call sites in this file have `workspaceId` in scope (Category A): the Layer-4 extractor loop in `runIngestion(request, ...)` uses `request.workspaceId`; the `persistArtifact(input)` helper uses `input.workspaceId`. Both migrate to `getAsDbForWorkspace(workspaceId)`. Because there are **no remaining `getAsDb()` call sites** in this file, the import line drops `getAsDb` and only imports `getAsDbForWorkspace`. This is the FIRST ingestion file in MR-3 to be 100% migrated; closes the 4-of-4 ingestion sub-arc (with #798 / #799 / #800 covering the other three). 6-test source-scan suite at `tests/agent-studio/mr-3-sixth-batch-universal-ingestion-service.test.ts` (import-shape check, no-getAsDb-import assertion, no-getAsDb-call-sites assertion, ≥2 getAsDbForWorkspace call-sites count, both Layer-4 and persistArtifact body assertions). Out of scope: Phase-2 of the shim, Category B migrations across other files | `6dc03385` |
| #802 (PR-V1-51) | CRDT-γ-3 production `getVaultIdsForUser` wiring | Closes the V1+ named follow-up surfaced by PR-V1-37 (#787) / PR-V1-38 (#789): the realtime-doc WebSocket bridge accepts `installRealtimeDocUpgradeOnServer(server, { getVaultIdsForUser })` but the first slice shipped with no production default — every upgrade landed `not_a_vault_member` (4403). New `server/agent-studio/services/vault/realtime-doc-default-getvaultids.ts` exports `createDefaultGetVaultIdsForUser({ getRepo? })` — returns a `GetVaultIdsForUserFn` closure that consults `VaultRepository.listVaultsForUser(userId)` and projects row ids. Production lazy-imports `AsdbVaultRepository`; test seam injects a stub. Repo cached after first invocation (one construction per closure). Lookup throw propagates unchanged so the upgrade handler can map to `vault_membership_lookup_failed` → close code 4500 (defensive deny). Boot at `server/_core/index.ts` now passes `{ getVaultIdsForUser: createDefaultGetVaultIdsForUser() }` to the existing install call — env-flag gating unchanged (`AGS_REALTIME_DOC_WEBSOCKET_ENABLED=true`). 11-test JSDOM-environment suite at `tests/agent-studio/vault-realtime-doc-default-getvaultids.test.ts` (happy path with multi-row projection, empty rows → empty array, repo invoked with userId, repo cached across invocations, throw propagation, source-scan boundaries with comment-stripped code). Out of scope: production auth-cookie resolver for `getUserIdFromUpgradeRequest` (separate PR; still defaults to null) | `e91423f1` |
| #803 (PR-V1-52) | CRDT-γ-3 production auth-cookie resolver | Closes the second V1+ named follow-up from PR-V1-38 (#789): the realtime-doc WebSocket bridge accepts `installRealtimeDocUpgradeOnServer(server, { getUserIdFromUpgradeRequest })` but the first slice shipped a null-returning default — every upgrade landed `missing_user_id` (4401). The bridge's signature for `getUserIdFromUpgradeRequest` is widened from `(req) => number \| null` to `(req) => number \| null \| Promise<number \| null>` so async DB-backed resolvers fit (the dispatcher now awaits the return). New `server/agent-studio/services/vault/realtime-doc-default-getuserid.ts` exports `createDefaultGetUserIdFromUpgradeRequest({ authenticate? })`: returns a closure that calls the OAuth SDK's `authenticateRequest(req)` (cast-safe because the SDK only reads `req.headers.cookie`, which `IncomingMessage` provides), and returns `user.id` on success. ANY throw from the authenticator (verification failure, DB unavailability) is caught and translated to `null` — defensive deny is load-bearing because a duplex realtime channel must NEVER silently allow on auth error. DI seam (`authenticate`) lets unit tests bypass the SDK entirely. Boot wires the production closure alongside `getVaultIdsForUser` (#802). 11-test JSDOM-environment suite at `tests/agent-studio/vault-realtime-doc-default-getuserid.test.ts` covering authenticated path, null-from-authenticator, async + sync throw both → null, return-type widening assertion, source-scan boundaries. Out of scope: CRDT-γ-3-reconnect (server session-resume), y-protocols sync/awareness engines | `6a466d5c` |
| #804 (PR-V1-53) | 17-γ `ags_canvas_projection_events` table + seed wiring | First half of the 17-γ persistence sink: the schema migration. New table `agsCanvasProjectionEvents` declared in `drizzle/tables/agent-studio-canvas.ts` (alongside the existing canvas tables from #752) — columns: `id`, `kind` (closed taxonomy mirroring `CanvasProjectionEvent` in `projection-events-sink.ts`), `workspaceId` (for retention + multi-region routing), `canvasId`, `canvasNodeId`, `referencedNoteId`, `metadata`, `createdAt`. Indexes on `(workspaceId, createdAt)`, `canvasId`, `canvasNodeId`, and `kind`. Append-only by sink-contract — no UPDATE/DELETE paths declared. Seed script (`server/agent-studio/db/seed.ts`) now imports the canvas schema barrel and spreads it into `combinedSchema` so the introspection loop auto-provisions the new table (and the existing 3 canvas tables that were previously missed by the seed — recovered as a side-benefit). 18-test source-scan suite at `tests/agent-studio/canvas-projection-events-table.test.ts` (table declared, column shape, index names, type exports, seed-script import + spread, kind taxonomy lockstep with sink, hard-rule boundaries). Out of scope: ASDB-backed sink implementation that writes to this table (follow-up PR composes on this schema); drain wiring boot step; update/delete caller-side hooks; cross-region propagation via MR-2 | `1dc655c0` |
| #805 (PR-V1-54) | 17-γ ASDB-backed canvas projection events sink | Second half of the 17-γ persistence sink: the writer. New `server/agent-studio/services/canvas/projection-events-sink-asdb.ts` exports `createAsdbCanvasProjectionEventSink({ getDb?, resolveWorkspaceId? })` returning a `CanvasProjectionEventSink` that writes one row per event to `agsCanvasProjectionEvents`. Default `resolveWorkspaceId` queries `agsCanvases.vaultId` → `agsVaults.workspaceId` for the event's `canvasId`. The `CanvasProjectionEvent.note_reference_removed` payload is extended (additively) to carry `canvasId` + `priorReferencedNoteId` so persistence has the same shape on both kinds — no existing caller emits `_removed` yet (canvas-service has no `updateCanvasNode`/`deleteCanvasNode`), so the extension is safe. Defensive posture: null-workspaceId → WARN + drop (`agsCanvasProjectionEvents.workspaceId` is notNull, can't fabricate); resolver throw → WARN + drop; `getDb()` returns null → WARN + drop. Append-only — `.update`/`.delete` on the events table are source-scan-forbidden. 13-test JSDOM suite at `tests/agent-studio/canvas-projection-events-sink-asdb.test.ts` (happy path both kinds, resolver invocation, 3 defensive paths, source-scan boundaries including no-GraphRepository import + write-only assertion). Out of scope: boot install (next PR registers the sink via `registerCanvasProjectionEventSink`); update/delete caller hooks (canvas-service surface not yet present); drain consumer worker (separate sub-arc) | `8faaf57c` |
| #806 (PR-V1-55) | 17-γ env-flag-gated default canvas projection events sink boot install | Closes the 17-γ persistence sink wire-up trio: schema (#804) + writer (#805) + this install. New `server/agent-studio/services/canvas/install-default-projection-events-sink.ts` exports `maybeInstallDefaultCanvasProjectionEventsSink({ env?, install?, makeSink? })` — mirrors AS-4 (`install-default-governance-gate.ts` from #786) one-for-one. Env flag `AGS_CANVAS_PROJECTION_EVENTS_SINK=asdb` → installs the ASDB-backed sink via `registerCanvasProjectionEventSink(createAsdbCanvasProjectionEventSink())`. Any other value / unset → no-op (preserves the silent-swallow default sink). Boot Step 3.28 in `server/agent-studio/boot.ts` calls the installer + logs the decision. Default OFF — preserves existing no-op behavior on first install so operators can flip the flag once `ags_canvas_projection_events` is confirmed provisioned. 19-test JSDOM suite at `tests/agent-studio/canvas-install-default-projection-events-sink.test.ts` (env-flag strict-equality matrix incl. uppercase/whitespace/sentinel rejection, install+makeSink seam composition, process.env default, source-scan boundaries, boot.ts wiring assertion for Step 3.28). Out of scope: drain consumer worker (separate sub-arc); update/delete caller hooks; cross-region propagation via MR-2 | `e92cc517` |
| #807 (PR-V1-56) | 17-γ canvas projection events reader (drain helper) | Pure read helper for the future drain consumer (`server/agent-studio/services/canvas/projection-events-reader.ts`). `readCanvasProjectionEventsAfter({ workspaceId, lastSeenId, limit }, { getDb? })` returns up to `limit` rows from `ags_canvas_projection_events` where `id > lastSeenId`, scoped to `workspaceId`, ordered by `id` ASC (keyset pagination — strict ordering, no `createdAt`-tie issues). Defensive clamps: `limit` is truncated and capped at `READ_CANVAS_PROJECTION_EVENTS_HARD_LIMIT = 1000`; floor of 1. ASDB-unavailable → `[]` (no throw, so the future cron-style consumer can treat it as "no progress; retry next tick"). DI seam (`getDb`) for unit testing. 14-test JSDOM suite at `tests/agent-studio/canvas-projection-events-reader.test.ts` (happy-path returns, limit application, orderBy ASC count, hard-limit clamping, below-1 clamping, float truncation, ASDB-unavailable safety, source-scan boundaries including write-only-forbidden assertion). Out of scope: cron/queue/worker loop that calls this reader, cursor persistence (separate sub-arc — operational shape TBD: process-local cursor vs. dedicated table vs. piggyback on existing workspace observability) | `f05ccd7b` |
| #808 (PR-V1-57) | 17-γ canvas projection events drain orchestrator | Stateless per-workspace drain (`server/agent-studio/services/canvas/projection-events-drain.ts`). `drainCanvasProjectionEventsForWorkspace({ workspaceId, lastSeenId, forward, limit?, maxBatches? }, { getDb? })` composes #807's reader with a caller-supplied `forward(event)` closure. Multi-batch loop: keeps reading until either the reader returns fewer than `limit` rows (genuine catch-up) OR `maxBatches` fires (defensive cap so a runaway workspace can't monopolise a cron tick). Per-event forward throw → halts the drain immediately, cursor stays at the LAST SUCCESSFUL row id (next tick retries). Returns `{ workspaceId, nextLastSeenId, processed, failed, batchesRun, hitMaxBatches }` so the caller can wire telemetry + cursor persistence (kept out of scope intentionally — the orchestrator is stateless and operator-supplied storage decides cron / queue / table / observability piggyback). Defaults: `limit=100`, `maxBatches=10`. 12-test JSDOM suite at `tests/agent-studio/canvas-projection-events-drain.test.ts` (single-batch catch-up, empty-page no-op, multi-batch full drain, maxBatches ceiling fires, forwarder throw halts at the failing row + cursor stays + WARN logged, default-limit assertion, sane DEFAULT_DRAIN_MAX_BATCHES, source-scan boundaries including no-write assertion). Out of scope: cron scheduler that invokes this drain, cursor persistence, multi-workspace fan-out | `8309e292` |
| #809 (PR-V1-58) | 17-γ canvas projection events drain fan-out (multi-workspace) | Multi-workspace orchestrator (`server/agent-studio/services/canvas/projection-events-drain-fanout.ts`). `drainCanvasProjectionEventsAllWorkspaces({ cursorMap, forward, getWorkspaceIds?, limit?, maxBatchesPerWorkspace? }, { getDb? })` iterates workspaces and calls #808's per-workspace drain for each. `cursorMap: Map<workspaceId, lastSeenId>` is mutated in place (caller retains the reference across ticks; process-local persistence). Workspace discovery: caller-supplied `getWorkspaceIds()` overrides the default; the default `defaultListActiveProjectionWorkspaces` queries `SELECT DISTINCT workspaceId FROM agsCanvasProjectionEvents` so new workspaces show up automatically. Per-workspace isolation: a workspace drain throwing does NOT block others — the cursor map stays unchanged for the failing workspace (next tick retries from the same point). Workspace-discovery failure → no-op tick + WARN (operator's cron stays alive across ASDB blips). Aggregate result exposes `{ workspacesScanned, totalProcessed, totalFailed, anyHitMaxBatches, perWorkspace }`. 11-test JSDOM suite at `tests/agent-studio/canvas-projection-events-drain-fanout.test.ts` (multi-workspace happy path with cursor mutation, empty-workspace-list result, existing-cursor as starting point, missing-key default to 0, per-workspace failure isolation, workspace discovery throw → no-op, source-scan boundaries). Out of scope: cron scheduler tick wiring (next PR), cursor persistence across process restarts | `a7131036` |
| #810 (PR-V1-59) | 17-γ canvas projection events drain — module-singleton tick wrapper | Module-local cursor + forwarder install wrapping #809's fan-out so a cron / setInterval / admin trigger can invoke a nullary `tickCanvasProjectionEventsDrain()` (`server/agent-studio/services/canvas/projection-events-drain-tick.ts`). Exports: `installCanvasProjectionEventsDrainForwarder(forwarder)`, `hasCanvasProjectionEventsDrainForwarder()`, `tickCanvasProjectionEventsDrain(options?)`, `getCanvasProjectionEventsDrainLastTickResult()` (observability — ticksRun, lastTickResult, lastTickError, cursorMapSize), `__resetCanvasProjectionEventsDrainForTests()`. Cursor scope is process-local Map — survives ticks within the same process, resets on boot. First tick after boot scans from `lastSeenId=0` per workspace; forwarder MUST be idempotent (call out documented in module docstring). Tick before install → no-op + WARN. Unexpected fan-out throw → captured to `lastTickError` + WARN; module-singleton state preserved. 11-test JSDOM suite at `tests/agent-studio/canvas-projection-events-drain-tick.test.ts` (no-forwarder no-op, hasForwarder reflects install, invoke-after-install delivers events, cursor persists across ticks, observability state on success + error capture path, source-scan boundaries). Out of scope: actual setInterval-based scheduler that calls the tick (separate PR composes with `setInterval` or `makeRetentionCron`-style envelope) | `60fc0429` |
| #811 (PR-V1-60) | 17-γ canvas projection events drain — env-flag-gated setInterval scheduler installer | Closes the 17-γ persistence sink END-TO-END loop: schema (#804) → writer (#805) → boot-install of writer (#806) → drain helpers (#807, #808, #809) → tick wrapper (#810) → this scheduler. New `server/agent-studio/services/canvas/install-default-projection-events-drain-scheduler.ts` exports `maybeInstallDefaultCanvasProjectionEventsDrainScheduler({ env?, setInterval?, tick? })`. Env flags: `AGS_CANVAS_PROJECTION_EVENTS_DRAIN=interval` → installs setInterval that calls #810's `tickCanvasProjectionEventsDrain()` every `intervalMs`. `AGS_CANVAS_PROJECTION_EVENTS_DRAIN_INTERVAL_MS=<ms>` overrides the default 60_000ms. Invalid override → falls back to default. Default OFF — operator opt-in. Forwarder is OPERATOR-supplied (not installed by this scheduler) so the integration boundary stays explicit: scheduler decides WHEN to drain, operator decides WHERE events go. Fire-and-forget tick callback never throws out of `setInterval`; rejections logged via WARN; the tick wrapper already captures errors to `lastTickError`. Best-effort `handle.unref()` so the interval doesn't block process shutdown when the handle exposes it. Returns `{ mode, installed, intervalMs, handle }` for boot-step logging + shutdown via `clearInterval(handle)`. 17-test JSDOM suite at `tests/agent-studio/canvas-install-default-projection-events-drain-scheduler.test.ts` (env strict-equality matrix, default interval, valid override, invalid override fallback, interval-callback tick invocation, fire-and-forget rejection safety + WARN, unref invocation, numeric-handle tolerance, source-scan boundaries). Out of scope: boot.ts wire-up (next PR) | `3020862e` |
| #812 (PR-V1-61) | 17-γ canvas projection events drain — boot.ts wire-up Step 3.29 | Closes the operator-facing surface: `server/agent-studio/boot.ts` now calls `maybeInstallDefaultCanvasProjectionEventsDrainScheduler()` in a new Step 3.29 immediately after the #806 sink-install (Step 3.28). Logs the installed-mode case with both `mode` and `intervalMs` so operators can verify the cadence the scheduler picked; logs the skipped case when `AGS_CANVAS_PROJECTION_EVENTS_DRAIN` is unset. Wrapped in try/catch so a malformed env / install throw never crashes boot. 6-test source-scan suite at `tests/agent-studio/canvas-projection-events-drain-scheduler-boot.test.ts` (import name, file-path reference, installed-mode log shape, skipped-mode log, Step 3.29 label, try/catch structural assertion). With this, the 17-γ persistence sink loop is **operator-ready** end-to-end: schema is provisioned (#804 seed), the writer auto-installs on `AGS_CANVAS_PROJECTION_EVENTS_SINK=asdb` (#806), the drain auto-installs on `AGS_CANVAS_PROJECTION_EVENTS_DRAIN=interval`; the operator's only remaining task is to install a forwarder (e.g. into `ProjectionSyncWorker` or an external queue) | `57d7fdf6` |
| #813 (PR-V1-62) | 17-γ canvas projection events drain — admin tRPC status router | Operator observability surface for the 17-γ drain scheduler. New `server/agent-studio/services/canvas/projection-events-drain-router.ts` exports `canvasProjectionEventsDrainRouter` with one `adminProcedure` `getDrainStatus` that returns the module-singleton state from #810 (`{ ticksRun, lastTickResult, lastTickError, cursorMapSize }`). Mounted at `agentStudio.canvasProjectionEventsDrain.*` (mirrors the J-1-β `agentStudio.graphHealth.*` shape). Operator questions answered: is the scheduler running (`ticksRun > 0`)? Did the last tick succeed (`lastTickError == null`)? How many workspaces are tracked (`cursorMapSize`)? Per-workspace aggregates from the last tick (`lastTickResult.perWorkspace`). Read-only — no `.mutation` procedures by source-scan. 11-test source-scan suite at `tests/agent-studio/canvas-projection-events-drain-router.test.ts` (router shape, adminProcedure declaration, delegates to #810's getter, import from tick module, main router imports + mounts, source-scan boundaries including no-mutation assertion). Out of scope: client-side panel that consumes this procedure | `46384931` |
| #814 (PR-V1-63) | 17-γ `CanvasProjectionEventsDrainStatusPanel` operator UI | Client-side panel consuming the #813 `getDrainStatus` admin procedure (`client/src/modules/agent-studio/components/CanvasProjectionEventsDrainStatusPanel.tsx`). Renders 5 branches: loading / error (with `error.message` echo) / not-yet-ticked (`ticksRun=0` advisory with cursorMapSize) / populated (ticksRun, workspaces tracked, last-tick processed/failed counts with singular-vs-plural pluralisation, optional last-tick-error in destructive style, optional hit-max-batches amber advisory) / null-status fallback. Reuses `formatRelative` for the lastTickError timestamp. Pattern matches #795 / #796 single-responsibility extracted-component shape; mounts into an admin dashboard via the next composition PR. 9-test JSDOM-environment suite at `CanvasProjectionEventsDrainStatusPanel.test.tsx` (5 branch states + singular pluralization + failure-suffix + lastTickError row + hit-max-batches advisory). Hard-rule clean (no credential-resolver / dispatchMcpToolCall / *_API_KEY / neo4j-driver / openrouter / GraphRepository imports — verified by the existing client-side boundary tests on the modules/agent-studio path). Out of scope: route/page that embeds the panel | `5e6d4d32` |
| #815 (PR-V1-64) | 15-δ vault `getAttachmentQuota` tRPC procedure | New `agentStudio.vault.getAttachmentQuota` protectedProcedure (`server/agent-studio/services/vault/router.ts`) that wraps `computeAttachmentQuota` from #760's attachment-library service. Takes `{ vaultId }`; threads `resolveDefaultAttachmentBytesLimit()` from #771's quota-guard so the panel's displayed "X% used" matches the actual `createAttachment` rejection threshold bit-for-bit (single source of truth for the limit). Error path routes through the existing `throwTrpcAndCapture` observability hook used by all vault procedures (#491/#492/#493 pattern). 6-test source-scan suite at `tests/agent-studio/vault-quota-router-procedure.test.ts` (computeAttachmentQuota import, resolveDefaultAttachmentBytesLimit import, protectedProcedure + vaultId input shape, delegation assertion, bytesLimit threading assertion, throwTrpcAndCapture in the error path). Unblocks the next slice — a Phase G UI panel that calls this procedure. Out of scope: the UI panel itself; mutation procedures for adjusting the quota (operator action via env var) | merged @ 02790061 |
| #816 (PR-V1-65) | 15-δ AttachmentQuotaPanel Phase G UI surface | New `client/src/modules/agent-studio/components/AttachmentQuotaPanel.tsx` consuming `trpc.agentStudio.vault.getAttachmentQuota.useQuery({vaultId})` from #815. Mirrors AttachmentListPanel (#796) and CanvasProjectionEventsDrainStatusPanel (#814): single-responsibility extracted component with explicit query-state branches. Six rendering branches: loading, error, null-data fallback, null-bytesLimit ("No quota limit configured"), overQuota=true (destructive style, "new uploads will be rejected"), utilization > 0.8 (amber "Approaching quota" warning), utilization ≤ 0.8 (standard "Used: X/Y (Z%)"). Singular/plural pluralization on `attachmentCount`. Byte formatting reuses the AttachmentListPanel helper shape (B/KiB/MiB/GiB). 9-test JSDOM suite at `client/src/modules/agent-studio/components/AttachmentQuotaPanel.test.tsx` with `vi.hoisted`+`vi.mock("@/lib/trpc")` covering all branches plus singular/plural variants. Closes the 15-δ vault attachment quota slice end-to-end (service `computeAttachmentQuota` → router `getAttachmentQuota` → panel `AttachmentQuotaPanel`). Out of scope: the operator route/page that composes AttachmentListPanel + AttachmentQuotaPanel into a single Vault Attachments admin surface (next PR). | pending |
| #817 (PR-V1-66) | 15-δ VaultAttachmentsAdminSurface composition slice | New `client/src/modules/agent-studio/components/VaultAttachmentsAdminSurface.tsx` composing the two atomic 15-δ panels (`AttachmentQuotaPanel` #816 on top, `AttachmentListPanel` #796 below) into a single per-vault admin surface. Single-responsibility composition shell — no business logic, no extra fetches, no provider knowledge: each child panel owns its own tRPC query. Renders the surface inside a `data-testid="vault-attachments-admin-surface"` wrapper with a `data-vault-id` attribute for test/operator visibility, separated by a Tailwind `space-y-4` stack. 5-test JSDOM suite at `VaultAttachmentsAdminSurface.test.tsx` mocking both useQuery hooks via `vi.hoisted` + `vi.fn` (with `beforeEach` mockReset so call-history assertions are per-test): wrapper render, both child section labels appearing, vaultId forwarding to the quota useQuery, vaultId + listLimit forwarding to the list useQuery, and combined over-quota + empty-list rendering. Drops the eventual route/page into a single `<VaultAttachmentsAdminSurface vaultId={…} />` call. Out of scope: AgentStudioShell view wiring + URL routing (next PR); mutation/upload UX. | pending |
| #818 (PR-V1-67) | 17-γ drain row→event projector + projecting forwarder | Closes the operator-facing integration boundary for the 17-γ persistence sink loop. The #808-#811 drain hands rows from `ags_canvas_projection_events` to a caller-supplied forwarder; this PR provides the reusable row→event reconstruction so operators don't re-implement DB-row de-serialization at each downstream binding. New `server/agent-studio/services/canvas/projection-events-drain-row-projector.ts` exports `projectAgsCanvasProjectionEventRow(row): CanvasProjectionEvent \| null` — pure function with closed `CANVAS_PROJECTION_EVENT_ROW_KINDS` taxonomy (`note_reference_changed` / `note_reference_removed`); maps the row-kind suffix back to the canonical canvas-service event kind (`canvas.note_reference_changed` / `canvas.note_reference_removed`); reads `referencedNoteId` as `priorReferencedNoteId` for the removed kind (matches #805's writer convention). Defensive null on unknown kinds + null `referencedNoteId` (sink writer guarantees non-null; defensive drop on schema drift). New `server/agent-studio/services/canvas/projection-events-drain-projecting-forwarder.ts` exports `createProjectingCanvasProjectionEventForwarder({dispatch})` returning a `CanvasProjectionEventForwarder` that projects then awaits the operator-supplied `dispatch(event)` closure. Unprojectable rows → WARN + advance cursor (do not block the drain). `dispatch` throws propagate so #808's "halt at failing row, cursor stays at last successful" semantics still apply. Strict hard-rule boundary: no `ProjectionSyncWorker` / `GraphRepository` / `sync-worker` imports — operators bind their worker at boot by passing the `dispatch` closure (preserves the `services/graph/repository/**` import locality rule per CLAUDE.md). 14-test JSDOM suite at `tests/agent-studio/canvas-projection-events-drain-row-projector.test.ts` (closed taxonomy assertion, 2 happy-path projections, 3 defensive null branches, 2 forwarder dispatch-shape assertions, 2 unprojectable-row no-dispatch branches with WARN spy, dispatch throw propagation, async dispatch await, comment-stripped source-scan asserting `ProjectionSyncWorker` + `sync-worker` + `GraphRepository` imports stay out). Out of scope: a default `installProjectingCanvasProjectionEventDrainForwarder` boot step (separate PR — operator's worker reference must be available at install time); a separate ASDB-direct sink that bypasses the drain (already shipped in #805, this PR is for downstream consumers of the drain). | pending |
| #819 (PR-V1-68) | MR-3 seventh batch — `services/cag/events.ts` | Seventh migration batch off the MR-3 Phase-1 shim (#794). Two Category A functions migrate to `getAsDbForWorkspace`: `appendPackEvent(input)` → `getAsDbForWorkspace(input.workspaceId)` (AppendEventInput.workspaceId is required); `listEventsByPack(workspaceId, packId, limit)` → `getAsDbForWorkspace(workspaceId)` (workspaceId is the first parameter). The third function `listPackEvents(agentDraftId, limit)` remains on `getAsDb()` — Category B (agentDraftId-scoped; workspaceId would need to be plumbed through callers via a separate sub-arc). 4-test source-scan suite at `tests/agent-studio/mr-3-seventh-batch-cag-events.test.ts` (import-shape check; `appendPackEvent` + `listEventsByPack` body assertions; `listPackEvents` Category B assertion). Comment-stripping function-body extractor reused from #797 / #800. Out of scope: Phase-2 of the shim (real region routing), Category B migration for `listPackEvents`, remaining `services/cag/store.ts` migrations (separate sub-arc — 8 call sites). | pending |

---

## 7. Connection back to MVP 0–4 closure

This plan is the explicit "successor execution plan" named in:
- `docs/implementation/agent-studio-native-graph-workspace-execution-plan.md` §0 scope boundary
- `docs/implementation/agent-studio-native-graph-workspace-status-check-2026-05-13.md` "Known gaps" item 4 ("V1+ scope is Deferred by plan, not failed")
- `~/.claude/projects/-root/memory/feedback_native_graph_workspace_continuing_rule.md` (the standing rule)

When this plan ships its first PR, the predecessor closure-doc's "deferred by plan" line becomes "tracked in successor plan" — no longer a deferral, an active workstream.

---

## 8. Reference docs

- `docs/architecture/agent-studio-native-graph-workspace.md` — top-level overview
- `docs/architecture/agent-studio-canvas-strategy.md` — Phase 17 ADR (exists)
- `docs/architecture/agent-studio-extension-framework-strategy.md` — Phase 18 ADR (exists)
- `docs/architecture/agent-studio-multi-region.md` — Phase MR-1 ADR (exists)
- `docs/architecture/agent-studio-neo4j-aura-upgrade-path.md` — Track J upgrade path ADR (exists)
- `docs/architecture/agent-studio-realtime-collab-crdt.md` — Phase CRDT ADR (created with this plan)
- `docs/architecture/agent-studio-offline-local-first.md` — Phase OL-1 ADR (created with this plan)
- `docs/implementation/agent-studio-native-graph-workspace-execution-plan.md` — predecessor
- `AGENTS.md` — operating order
- `CLAUDE.md` — Non-Build List
