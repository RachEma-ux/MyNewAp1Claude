// Provider domain components
// Components used by 2+ pages within the providers feature domain
//
// Candidates from inventory:
// - ProviderCard (shared card layout with type icon + status)
// - ProviderTypeIcon (icon + color mapping for provider types)
