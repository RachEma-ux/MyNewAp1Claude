// Model domain components
// Components used by 2+ pages within the models feature domain
//
// Candidates from inventory:
// - ModelCard (shared card layout with status + download progress)
// - ModelStatusBadge (ready, downloading, converting, error)
